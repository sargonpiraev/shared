name: Setup Node + npm ci
description: Node 24, npm cache, install dependencies

runs:
  using: composite
  steps:
    - uses: actions/setup-node@v6
      with:
        node-version: "24"
        cache: npm

    - shell: bash
      run: npm ci
