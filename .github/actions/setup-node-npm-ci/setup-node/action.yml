name: Setup Node
description: Node 24 + npm ci with cache

runs:
  using: composite
  steps:
    - uses: actions/setup-node@v6
      with:
        node-version: "24"
        cache: npm

    - shell: bash
      run: npm ci
