name: publish-seodit

on:
  push:
    branches: [main]

permissions:
  contents: write
  issues: write
  pull-requests: write
  id-token: write

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: package-lock.json
          registry-url: https://registry.npmjs.org

      - run: npm ci

      - run: npm run build
        working-directory: packages/seodit

      - run: npm test
        working-directory: packages/seodit

      - run: npm run check-types
        working-directory: packages/seodit

      - name: Sync seodit release tag with npm
        working-directory: packages/seodit
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          PKG_VERSION=$(node -p "require('./package.json').version")
          TAG="seodit-v${PKG_VERSION}"
          if git rev-parse "$TAG" >/dev/null 2>&1; then
            echo "Tag $TAG already exists"
            exit 0
          fi
          NPM_VERSION=$(npm view @sargonpiraev/seodit version 2>/dev/null || true)
          if [ -z "$NPM_VERSION" ] || [ "$NPM_VERSION" != "$PKG_VERSION" ]; then
            echo "Skip tag sync: npm=$NPM_VERSION package.json=$PKG_VERSION"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git tag "$TAG"
          git push origin "$TAG"

      - run: npx semantic-release
        working-directory: packages/seodit
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
