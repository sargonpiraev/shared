{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Project/shared Nx project.json",
  "description": "Required test targets for project/shared project.json. Checks use the test:* namespace; build is separate. Use test:eslint (never test:lint). test:fslint is required. Playwright lane targets test:functional / test:seo / test:analytics / test:cwv are optional in schema (declare when the project has webapp/docapp/extapp narrow-integration harness; inventory requires test:cwv script when webapp/docapp or flat-root PW exists). test:pulumi is always required (index-clusters + mocks; never noop). Orchestration targets such as git:status are optional (not a governance gate). Shipped by @sargonpiraev/eslint-config.",
  "type": "object",
  "required": ["name", "tags", "targets"],
  "properties": {
    "name": {
      "type": "string",
      "minLength": 1
    },
    "tags": {
      "type": "array",
      "items": { "type": "string" },
      "contains": {
        "enum": ["project", "shared"]
      },
      "minContains": 1
    },
    "targets": {
      "type": "object",
      "required": [
        "test",
        "test:unit",
        "test:eslint",
        "test:fslint",
        "test:check-types",
        "test:spec",
        "test:visual",
        "test:format",
        "test:pulumi",
        "build"
      ],
      "properties": {
        "test": { "type": "object" },
        "test:unit": { "type": "object" },
        "test:eslint": { "type": "object" },
        "test:lint": false,
        "test:alint": false,
        "test:fslint": { "type": "object" },
        "test:check-types": { "type": "object" },
        "test:spec": { "type": "object" },
        "test:functional": { "type": "object" },
        "test:seo": { "type": "object" },
        "test:analytics": { "type": "object" },
        "test:visual": { "type": "object" },
        "test:cwv": { "type": "object" },
        "test:format": { "type": "object" },
        "build": { "type": "object" },
        "git:status": { "type": "object" },
        "test:pulumi": {
          "type": "object",
          "description": "Always required. index-clusters + Pulumi mocks; never noop."
        }
      },
      "additionalProperties": true
    }
  },

  "additionalProperties": true
}
