import { McpServer } from '@modelcontextprotocol/sdk/server/mcp.js'
import { z } from 'zod'
import axios, { AxiosInstance } from 'axios'
import dotenv from 'dotenv'
import { CallToolResult } from '@modelcontextprotocol/sdk/types.js'

dotenv.config()

export const envSchema = z.object({
  GITHUB_API_KEY: z.string(),
})

export const mcpServer = new McpServer(
  {
    name: '@sargonpiraev/github-mcp-server',
    version: '1.1.4',
  },
  {
    instructions: ``,
    capabilities: {
      tools: {},
      logging: {},
    },
  }
)

export const env = envSchema.parse(process.env)

export const apiClient: AxiosInstance = axios.create({
  baseURL: 'https://api.github.com',
  headers: {
    Accept: 'application/json',
  },
  timeout: 30000,
})

apiClient.interceptors.request.use(
  (config) => {
    if (env.GITHUB_API_KEY) {
      config.headers['Authorization'] = env.GITHUB_API_KEY
    }

    return config
  },
  (error) => {
    return Promise.reject(error)
  }
)

function handleResult(data: unknown): CallToolResult {
  return {
    content: [
      {
        type: 'text',
        text: JSON.stringify(data, null, 2),
      },
    ],
  }
}

function handleError(error: unknown): CallToolResult {
  console.error(error)

  if (axios.isAxiosError(error)) {
    const message = error.response?.data?.message || error.message
    return {
      isError: true,
      content: [{ type: 'text', text: `API Error: ${message}` }],
    } as CallToolResult
  }

  return {
    isError: true,
    content: [{ type: 'text', text: `Error: ${error}` }],
  } as CallToolResult
}

// Register tools
mcpServer.tool('meta/root', `GitHub API Root`, {}, async (args, extra) => {
  try {
    const otherParams = args

    // Map camelCase to original parameter names for API request
    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    const mappedParams: any = { ...otherParams }

    // Extract authorization token from HTTP request headers
    const authorization = extra?.requestInfo?.headers?.authorization as string
    const bearer = authorization?.replace('Bearer ', '')

    const response = await apiClient.request({
      headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
      method: 'GET',
      url: '/',
      params: mappedParams,
    })

    return handleResult(response.data)
  } catch (error) {
    return handleError(error)
  }
})

mcpServer.tool(
  'security-advisories/list-global-advisories',
  `List global security advisories`,
  {
    ghsaId: z.string().optional(),
    type: z.string().optional(),
    cveId: z.string().optional(),
    ecosystem: z.string().optional(),
    severity: z.string().optional(),
    cwes: z.string().optional(),
    isWithdrawn: z.string().optional(),
    affects: z.string().optional(),
    published: z.string().optional(),
    updated: z.string().optional(),
    modified: z.string().optional(),
    epssPercentage: z.string().optional(),
    epssPercentile: z.string().optional(),
    perPage: z.string().optional(),
    sort: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const otherParams = args

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('ghsaId' in mappedParams) {
        mappedParams['ghsa_id'] = mappedParams['ghsaId']
        delete mappedParams['ghsaId']
      }
      if ('cveId' in mappedParams) {
        mappedParams['cve_id'] = mappedParams['cveId']
        delete mappedParams['cveId']
      }
      if ('isWithdrawn' in mappedParams) {
        mappedParams['is_withdrawn'] = mappedParams['isWithdrawn']
        delete mappedParams['isWithdrawn']
      }
      if ('epssPercentage' in mappedParams) {
        mappedParams['epss_percentage'] = mappedParams['epssPercentage']
        delete mappedParams['epssPercentage']
      }
      if ('epssPercentile' in mappedParams) {
        mappedParams['epss_percentile'] = mappedParams['epssPercentile']
        delete mappedParams['epssPercentile']
      }
      if ('perPage' in mappedParams) {
        mappedParams['per_page'] = mappedParams['perPage']
        delete mappedParams['perPage']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: '/advisories',
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'security-advisories/get-global-advisory',
  `Get a global security advisory`,
  {
    ghsaId: z.string(),
  },
  async (args, extra) => {
    try {
      const { ghsaId, ...otherParams } = args
      const url = `/advisories/${ghsaId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('ghsaId' in mappedParams) {
        mappedParams['ghsa_id'] = mappedParams['ghsaId']
        delete mappedParams['ghsaId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool('apps/get-authenticated', `Get the authenticated app`, {}, async (args, extra) => {
  try {
    const otherParams = args

    // Map camelCase to original parameter names for API request
    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    const mappedParams: any = { ...otherParams }

    // Extract authorization token from HTTP request headers
    const authorization = extra?.requestInfo?.headers?.authorization as string
    const bearer = authorization?.replace('Bearer ', '')

    const response = await apiClient.request({
      headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
      method: 'GET',
      url: '/app',
      params: mappedParams,
    })

    return handleResult(response.data)
  } catch (error) {
    return handleError(error)
  }
})

mcpServer.tool(
  'apps/create-from-manifest',
  `Create a GitHub App from a manifest`,
  {
    code: z.string(),
  },
  async (args, extra) => {
    try {
      const { code, ...otherParams } = args
      const url = `/app-manifests/${code}/conversions`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'apps/get-webhook-config-for-app',
  `Get a webhook configuration for an app`,
  {},
  async (args, extra) => {
    try {
      const otherParams = args

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: '/app/hook/config',
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'apps/update-webhook-config-for-app',
  `Update a webhook configuration for an app`,
  {
    url: z.any().optional(),
    contentType: z.any().optional(),
    secret: z.any().optional(),
    insecureSsl: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const otherParams = args

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('contentType' in mappedParams) {
        mappedParams['content_type'] = mappedParams['contentType']
        delete mappedParams['contentType']
      }
      if ('insecureSsl' in mappedParams) {
        mappedParams['insecure_ssl'] = mappedParams['insecureSsl']
        delete mappedParams['insecureSsl']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PATCH',
        url: '/app/hook/config',
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'apps/list-webhook-deliveries',
  `List deliveries for an app webhook`,
  {},
  async (args, extra) => {
    try {
      const otherParams = args

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: '/app/hook/deliveries',
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'apps/get-webhook-delivery',
  `Get a delivery for an app webhook`,
  {
    deliveryId: z.string(),
  },
  async (args, extra) => {
    try {
      const { deliveryId, ...otherParams } = args
      const url = `/app/hook/deliveries/${deliveryId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('deliveryId' in mappedParams) {
        mappedParams['delivery_id'] = mappedParams['deliveryId']
        delete mappedParams['deliveryId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'apps/redeliver-webhook-delivery',
  `Redeliver a delivery for an app webhook`,
  {
    deliveryId: z.string(),
  },
  async (args, extra) => {
    try {
      const { deliveryId, ...otherParams } = args
      const url = `/app/hook/deliveries/${deliveryId}/attempts`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('deliveryId' in mappedParams) {
        mappedParams['delivery_id'] = mappedParams['deliveryId']
        delete mappedParams['deliveryId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'apps/list-installation-requests-for-authenticated-app',
  `List installation requests for the authenticated app`,
  {},
  async (args, extra) => {
    try {
      const otherParams = args

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: '/app/installation-requests',
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'apps/list-installations',
  `List installations for the authenticated app`,
  {
    outdated: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const otherParams = args

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: '/app/installations',
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'apps/get-installation',
  `Get an installation for the authenticated app`,
  {
    installationId: z.string(),
  },
  async (args, extra) => {
    try {
      const { installationId, ...otherParams } = args
      const url = `/app/installations/${installationId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('installationId' in mappedParams) {
        mappedParams['installation_id'] = mappedParams['installationId']
        delete mappedParams['installationId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'apps/delete-installation',
  `Delete an installation for the authenticated app`,
  {
    installationId: z.string(),
  },
  async (args, extra) => {
    try {
      const { installationId, ...otherParams } = args
      const url = `/app/installations/${installationId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('installationId' in mappedParams) {
        mappedParams['installation_id'] = mappedParams['installationId']
        delete mappedParams['installationId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'apps/create-installation-access-token',
  `Create an installation access token for an app`,
  {
    installationId: z.string(),
    repositories: z.any().optional(),
    repositoryIds: z.any().optional(),
    permissions: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { installationId, ...otherParams } = args
      const url = `/app/installations/${installationId}/access_tokens`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('installationId' in mappedParams) {
        mappedParams['installation_id'] = mappedParams['installationId']
        delete mappedParams['installationId']
      }
      if ('repositoryIds' in mappedParams) {
        mappedParams['repository_ids'] = mappedParams['repositoryIds']
        delete mappedParams['repositoryIds']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'apps/suspend-installation',
  `Suspend an app installation`,
  {
    installationId: z.string(),
  },
  async (args, extra) => {
    try {
      const { installationId, ...otherParams } = args
      const url = `/app/installations/${installationId}/suspended`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('installationId' in mappedParams) {
        mappedParams['installation_id'] = mappedParams['installationId']
        delete mappedParams['installationId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'apps/unsuspend-installation',
  `Unsuspend an app installation`,
  {
    installationId: z.string(),
  },
  async (args, extra) => {
    try {
      const { installationId, ...otherParams } = args
      const url = `/app/installations/${installationId}/suspended`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('installationId' in mappedParams) {
        mappedParams['installation_id'] = mappedParams['installationId']
        delete mappedParams['installationId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'apps/delete-authorization',
  `Delete an app authorization`,
  {
    clientId: z.string(),
    accessToken: z.any(),
  },
  async (args, extra) => {
    try {
      const { clientId, ...otherParams } = args
      const url = `/applications/${clientId}/grant`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('clientId' in mappedParams) {
        mappedParams['client_id'] = mappedParams['clientId']
        delete mappedParams['clientId']
      }
      if ('accessToken' in mappedParams) {
        mappedParams['access_token'] = mappedParams['accessToken']
        delete mappedParams['accessToken']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'apps/check-token',
  `Check a token`,
  {
    clientId: z.string(),
    accessToken: z.any(),
  },
  async (args, extra) => {
    try {
      const { clientId, ...otherParams } = args
      const url = `/applications/${clientId}/token`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('clientId' in mappedParams) {
        mappedParams['client_id'] = mappedParams['clientId']
        delete mappedParams['clientId']
      }
      if ('accessToken' in mappedParams) {
        mappedParams['access_token'] = mappedParams['accessToken']
        delete mappedParams['accessToken']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'apps/reset-token',
  `Reset a token`,
  {
    clientId: z.string(),
    accessToken: z.any(),
  },
  async (args, extra) => {
    try {
      const { clientId, ...otherParams } = args
      const url = `/applications/${clientId}/token`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('clientId' in mappedParams) {
        mappedParams['client_id'] = mappedParams['clientId']
        delete mappedParams['clientId']
      }
      if ('accessToken' in mappedParams) {
        mappedParams['access_token'] = mappedParams['accessToken']
        delete mappedParams['accessToken']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PATCH',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'apps/delete-token',
  `Delete an app token`,
  {
    clientId: z.string(),
    accessToken: z.any(),
  },
  async (args, extra) => {
    try {
      const { clientId, ...otherParams } = args
      const url = `/applications/${clientId}/token`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('clientId' in mappedParams) {
        mappedParams['client_id'] = mappedParams['clientId']
        delete mappedParams['clientId']
      }
      if ('accessToken' in mappedParams) {
        mappedParams['access_token'] = mappedParams['accessToken']
        delete mappedParams['accessToken']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'apps/scope-token',
  `Create a scoped access token`,
  {
    clientId: z.string(),
    accessToken: z.any(),
    target: z.any().optional(),
    targetId: z.any().optional(),
    repositories: z.any().optional(),
    repositoryIds: z.any().optional(),
    permissions: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { clientId, ...otherParams } = args
      const url = `/applications/${clientId}/token/scoped`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('clientId' in mappedParams) {
        mappedParams['client_id'] = mappedParams['clientId']
        delete mappedParams['clientId']
      }
      if ('accessToken' in mappedParams) {
        mappedParams['access_token'] = mappedParams['accessToken']
        delete mappedParams['accessToken']
      }
      if ('targetId' in mappedParams) {
        mappedParams['target_id'] = mappedParams['targetId']
        delete mappedParams['targetId']
      }
      if ('repositoryIds' in mappedParams) {
        mappedParams['repository_ids'] = mappedParams['repositoryIds']
        delete mappedParams['repositoryIds']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'apps/get-by-slug',
  `Get an app`,
  {
    appSlug: z.string(),
  },
  async (args, extra) => {
    try {
      const { appSlug, ...otherParams } = args
      const url = `/apps/${appSlug}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('appSlug' in mappedParams) {
        mappedParams['app_slug'] = mappedParams['appSlug']
        delete mappedParams['appSlug']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'classroom/get-an-assignment',
  `Get an assignment`,
  {
    assignmentId: z.string(),
  },
  async (args, extra) => {
    try {
      const { assignmentId, ...otherParams } = args
      const url = `/assignments/${assignmentId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('assignmentId' in mappedParams) {
        mappedParams['assignment_id'] = mappedParams['assignmentId']
        delete mappedParams['assignmentId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'classroom/list-accepted-assignments-for-an-assignment',
  `List accepted assignments for an assignment`,
  {
    assignmentId: z.string(),
  },
  async (args, extra) => {
    try {
      const { assignmentId, ...otherParams } = args
      const url = `/assignments/${assignmentId}/accepted_assignments`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('assignmentId' in mappedParams) {
        mappedParams['assignment_id'] = mappedParams['assignmentId']
        delete mappedParams['assignmentId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'classroom/get-assignment-grades',
  `Get assignment grades`,
  {
    assignmentId: z.string(),
  },
  async (args, extra) => {
    try {
      const { assignmentId, ...otherParams } = args
      const url = `/assignments/${assignmentId}/grades`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('assignmentId' in mappedParams) {
        mappedParams['assignment_id'] = mappedParams['assignmentId']
        delete mappedParams['assignmentId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool('classroom/list-classrooms', `List classrooms`, {}, async (args, extra) => {
  try {
    const otherParams = args

    // Map camelCase to original parameter names for API request
    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    const mappedParams: any = { ...otherParams }

    // Extract authorization token from HTTP request headers
    const authorization = extra?.requestInfo?.headers?.authorization as string
    const bearer = authorization?.replace('Bearer ', '')

    const response = await apiClient.request({
      headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
      method: 'GET',
      url: '/classrooms',
      params: mappedParams,
    })

    return handleResult(response.data)
  } catch (error) {
    return handleError(error)
  }
})

mcpServer.tool(
  'classroom/get-a-classroom',
  `Get a classroom`,
  {
    classroomId: z.string(),
  },
  async (args, extra) => {
    try {
      const { classroomId, ...otherParams } = args
      const url = `/classrooms/${classroomId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('classroomId' in mappedParams) {
        mappedParams['classroom_id'] = mappedParams['classroomId']
        delete mappedParams['classroomId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'classroom/list-assignments-for-a-classroom',
  `List assignments for a classroom`,
  {
    classroomId: z.string(),
  },
  async (args, extra) => {
    try {
      const { classroomId, ...otherParams } = args
      const url = `/classrooms/${classroomId}/assignments`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('classroomId' in mappedParams) {
        mappedParams['classroom_id'] = mappedParams['classroomId']
        delete mappedParams['classroomId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'codes-of-conduct/get-all-codes-of-conduct',
  `Get all codes of conduct`,
  {},
  async (args, extra) => {
    try {
      const otherParams = args

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: '/codes_of_conduct',
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'codes-of-conduct/get-conduct-code',
  `Get a code of conduct`,
  {
    key: z.string(),
  },
  async (args, extra) => {
    try {
      const { key, ...otherParams } = args
      const url = `/codes_of_conduct/${key}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'credentials/revoke',
  `Revoke a list of credentials`,
  {
    credentials: z.any(),
  },
  async (args, extra) => {
    try {
      const otherParams = args

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: '/credentials/revoke',
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool('emojis/get', `Get emojis`, {}, async (args, extra) => {
  try {
    const otherParams = args

    // Map camelCase to original parameter names for API request
    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    const mappedParams: any = { ...otherParams }

    // Extract authorization token from HTTP request headers
    const authorization = extra?.requestInfo?.headers?.authorization as string
    const bearer = authorization?.replace('Bearer ', '')

    const response = await apiClient.request({
      headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
      method: 'GET',
      url: '/emojis',
      params: mappedParams,
    })

    return handleResult(response.data)
  } catch (error) {
    return handleError(error)
  }
})

mcpServer.tool(
  'code-security/get-configurations-for-enterprise',
  `Get code security configurations for an enterprise`,
  {
    enterprise: z.string(),
    perPage: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const { enterprise, ...otherParams } = args
      const url = `/enterprises/${enterprise}/code-security/configurations`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('perPage' in mappedParams) {
        mappedParams['per_page'] = mappedParams['perPage']
        delete mappedParams['perPage']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'code-security/create-configuration-for-enterprise',
  `Create a code security configuration for an enterprise`,
  {
    enterprise: z.string(),
    name: z.any(),
    description: z.any(),
    advancedSecurity: z.any().optional(),
    codeSecurity: z.any().optional(),
    dependencyGraph: z.any().optional(),
    dependencyGraphAutosubmitAction: z.any().optional(),
    dependencyGraphAutosubmitActionOptions: z.any().optional(),
    dependabotAlerts: z.any().optional(),
    dependabotSecurityUpdates: z.any().optional(),
    codeScanningOptions: z.any().optional(),
    codeScanningDefaultSetup: z.any().optional(),
    codeScanningDefaultSetupOptions: z.any().optional(),
    codeScanningDelegatedAlertDismissal: z.any().optional(),
    secretProtection: z.any().optional(),
    secretScanning: z.any().optional(),
    secretScanningPushProtection: z.any().optional(),
    secretScanningValidityChecks: z.any().optional(),
    secretScanningNonProviderPatterns: z.any().optional(),
    secretScanningGenericSecrets: z.any().optional(),
    secretScanningDelegatedAlertDismissal: z.any().optional(),
    privateVulnerabilityReporting: z.any().optional(),
    enforcement: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { enterprise, ...otherParams } = args
      const url = `/enterprises/${enterprise}/code-security/configurations`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('advancedSecurity' in mappedParams) {
        mappedParams['advanced_security'] = mappedParams['advancedSecurity']
        delete mappedParams['advancedSecurity']
      }
      if ('codeSecurity' in mappedParams) {
        mappedParams['code_security'] = mappedParams['codeSecurity']
        delete mappedParams['codeSecurity']
      }
      if ('dependencyGraph' in mappedParams) {
        mappedParams['dependency_graph'] = mappedParams['dependencyGraph']
        delete mappedParams['dependencyGraph']
      }
      if ('dependencyGraphAutosubmitAction' in mappedParams) {
        mappedParams['dependency_graph_autosubmit_action'] =
          mappedParams['dependencyGraphAutosubmitAction']
        delete mappedParams['dependencyGraphAutosubmitAction']
      }
      if ('dependencyGraphAutosubmitActionOptions' in mappedParams) {
        mappedParams['dependency_graph_autosubmit_action_options'] =
          mappedParams['dependencyGraphAutosubmitActionOptions']
        delete mappedParams['dependencyGraphAutosubmitActionOptions']
      }
      if ('dependabotAlerts' in mappedParams) {
        mappedParams['dependabot_alerts'] = mappedParams['dependabotAlerts']
        delete mappedParams['dependabotAlerts']
      }
      if ('dependabotSecurityUpdates' in mappedParams) {
        mappedParams['dependabot_security_updates'] = mappedParams['dependabotSecurityUpdates']
        delete mappedParams['dependabotSecurityUpdates']
      }
      if ('codeScanningOptions' in mappedParams) {
        mappedParams['code_scanning_options'] = mappedParams['codeScanningOptions']
        delete mappedParams['codeScanningOptions']
      }
      if ('codeScanningDefaultSetup' in mappedParams) {
        mappedParams['code_scanning_default_setup'] = mappedParams['codeScanningDefaultSetup']
        delete mappedParams['codeScanningDefaultSetup']
      }
      if ('codeScanningDefaultSetupOptions' in mappedParams) {
        mappedParams['code_scanning_default_setup_options'] =
          mappedParams['codeScanningDefaultSetupOptions']
        delete mappedParams['codeScanningDefaultSetupOptions']
      }
      if ('codeScanningDelegatedAlertDismissal' in mappedParams) {
        mappedParams['code_scanning_delegated_alert_dismissal'] =
          mappedParams['codeScanningDelegatedAlertDismissal']
        delete mappedParams['codeScanningDelegatedAlertDismissal']
      }
      if ('secretProtection' in mappedParams) {
        mappedParams['secret_protection'] = mappedParams['secretProtection']
        delete mappedParams['secretProtection']
      }
      if ('secretScanning' in mappedParams) {
        mappedParams['secret_scanning'] = mappedParams['secretScanning']
        delete mappedParams['secretScanning']
      }
      if ('secretScanningPushProtection' in mappedParams) {
        mappedParams['secret_scanning_push_protection'] =
          mappedParams['secretScanningPushProtection']
        delete mappedParams['secretScanningPushProtection']
      }
      if ('secretScanningValidityChecks' in mappedParams) {
        mappedParams['secret_scanning_validity_checks'] =
          mappedParams['secretScanningValidityChecks']
        delete mappedParams['secretScanningValidityChecks']
      }
      if ('secretScanningNonProviderPatterns' in mappedParams) {
        mappedParams['secret_scanning_non_provider_patterns'] =
          mappedParams['secretScanningNonProviderPatterns']
        delete mappedParams['secretScanningNonProviderPatterns']
      }
      if ('secretScanningGenericSecrets' in mappedParams) {
        mappedParams['secret_scanning_generic_secrets'] =
          mappedParams['secretScanningGenericSecrets']
        delete mappedParams['secretScanningGenericSecrets']
      }
      if ('secretScanningDelegatedAlertDismissal' in mappedParams) {
        mappedParams['secret_scanning_delegated_alert_dismissal'] =
          mappedParams['secretScanningDelegatedAlertDismissal']
        delete mappedParams['secretScanningDelegatedAlertDismissal']
      }
      if ('privateVulnerabilityReporting' in mappedParams) {
        mappedParams['private_vulnerability_reporting'] =
          mappedParams['privateVulnerabilityReporting']
        delete mappedParams['privateVulnerabilityReporting']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'code-security/get-default-configurations-for-enterprise',
  `Get default code security configurations for an enterprise`,
  {
    enterprise: z.string(),
  },
  async (args, extra) => {
    try {
      const { enterprise, ...otherParams } = args
      const url = `/enterprises/${enterprise}/code-security/configurations/defaults`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'code-security/get-single-configuration-for-enterprise',
  `Retrieve a code security configuration of an enterprise`,
  {
    enterprise: z.string(),
    configurationId: z.string(),
  },
  async (args, extra) => {
    try {
      const { enterprise, configurationId, ...otherParams } = args
      const url = `/enterprises/${enterprise}/code-security/configurations/${configurationId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('configurationId' in mappedParams) {
        mappedParams['configuration_id'] = mappedParams['configurationId']
        delete mappedParams['configurationId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'code-security/update-enterprise-configuration',
  `Update a custom code security configuration for an enterprise`,
  {
    enterprise: z.string(),
    configurationId: z.string(),
    name: z.any().optional(),
    description: z.any().optional(),
    advancedSecurity: z.any().optional(),
    codeSecurity: z.any().optional(),
    dependencyGraph: z.any().optional(),
    dependencyGraphAutosubmitAction: z.any().optional(),
    dependencyGraphAutosubmitActionOptions: z.any().optional(),
    dependabotAlerts: z.any().optional(),
    dependabotSecurityUpdates: z.any().optional(),
    codeScanningDefaultSetup: z.any().optional(),
    codeScanningDefaultSetupOptions: z.any().optional(),
    codeScanningDelegatedAlertDismissal: z.any().optional(),
    secretProtection: z.any().optional(),
    secretScanning: z.any().optional(),
    secretScanningPushProtection: z.any().optional(),
    secretScanningValidityChecks: z.any().optional(),
    secretScanningNonProviderPatterns: z.any().optional(),
    secretScanningGenericSecrets: z.any().optional(),
    secretScanningDelegatedAlertDismissal: z.any().optional(),
    privateVulnerabilityReporting: z.any().optional(),
    enforcement: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { enterprise, configurationId, ...otherParams } = args
      const url = `/enterprises/${enterprise}/code-security/configurations/${configurationId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('configurationId' in mappedParams) {
        mappedParams['configuration_id'] = mappedParams['configurationId']
        delete mappedParams['configurationId']
      }
      if ('advancedSecurity' in mappedParams) {
        mappedParams['advanced_security'] = mappedParams['advancedSecurity']
        delete mappedParams['advancedSecurity']
      }
      if ('codeSecurity' in mappedParams) {
        mappedParams['code_security'] = mappedParams['codeSecurity']
        delete mappedParams['codeSecurity']
      }
      if ('dependencyGraph' in mappedParams) {
        mappedParams['dependency_graph'] = mappedParams['dependencyGraph']
        delete mappedParams['dependencyGraph']
      }
      if ('dependencyGraphAutosubmitAction' in mappedParams) {
        mappedParams['dependency_graph_autosubmit_action'] =
          mappedParams['dependencyGraphAutosubmitAction']
        delete mappedParams['dependencyGraphAutosubmitAction']
      }
      if ('dependencyGraphAutosubmitActionOptions' in mappedParams) {
        mappedParams['dependency_graph_autosubmit_action_options'] =
          mappedParams['dependencyGraphAutosubmitActionOptions']
        delete mappedParams['dependencyGraphAutosubmitActionOptions']
      }
      if ('dependabotAlerts' in mappedParams) {
        mappedParams['dependabot_alerts'] = mappedParams['dependabotAlerts']
        delete mappedParams['dependabotAlerts']
      }
      if ('dependabotSecurityUpdates' in mappedParams) {
        mappedParams['dependabot_security_updates'] = mappedParams['dependabotSecurityUpdates']
        delete mappedParams['dependabotSecurityUpdates']
      }
      if ('codeScanningDefaultSetup' in mappedParams) {
        mappedParams['code_scanning_default_setup'] = mappedParams['codeScanningDefaultSetup']
        delete mappedParams['codeScanningDefaultSetup']
      }
      if ('codeScanningDefaultSetupOptions' in mappedParams) {
        mappedParams['code_scanning_default_setup_options'] =
          mappedParams['codeScanningDefaultSetupOptions']
        delete mappedParams['codeScanningDefaultSetupOptions']
      }
      if ('codeScanningDelegatedAlertDismissal' in mappedParams) {
        mappedParams['code_scanning_delegated_alert_dismissal'] =
          mappedParams['codeScanningDelegatedAlertDismissal']
        delete mappedParams['codeScanningDelegatedAlertDismissal']
      }
      if ('secretProtection' in mappedParams) {
        mappedParams['secret_protection'] = mappedParams['secretProtection']
        delete mappedParams['secretProtection']
      }
      if ('secretScanning' in mappedParams) {
        mappedParams['secret_scanning'] = mappedParams['secretScanning']
        delete mappedParams['secretScanning']
      }
      if ('secretScanningPushProtection' in mappedParams) {
        mappedParams['secret_scanning_push_protection'] =
          mappedParams['secretScanningPushProtection']
        delete mappedParams['secretScanningPushProtection']
      }
      if ('secretScanningValidityChecks' in mappedParams) {
        mappedParams['secret_scanning_validity_checks'] =
          mappedParams['secretScanningValidityChecks']
        delete mappedParams['secretScanningValidityChecks']
      }
      if ('secretScanningNonProviderPatterns' in mappedParams) {
        mappedParams['secret_scanning_non_provider_patterns'] =
          mappedParams['secretScanningNonProviderPatterns']
        delete mappedParams['secretScanningNonProviderPatterns']
      }
      if ('secretScanningGenericSecrets' in mappedParams) {
        mappedParams['secret_scanning_generic_secrets'] =
          mappedParams['secretScanningGenericSecrets']
        delete mappedParams['secretScanningGenericSecrets']
      }
      if ('secretScanningDelegatedAlertDismissal' in mappedParams) {
        mappedParams['secret_scanning_delegated_alert_dismissal'] =
          mappedParams['secretScanningDelegatedAlertDismissal']
        delete mappedParams['secretScanningDelegatedAlertDismissal']
      }
      if ('privateVulnerabilityReporting' in mappedParams) {
        mappedParams['private_vulnerability_reporting'] =
          mappedParams['privateVulnerabilityReporting']
        delete mappedParams['privateVulnerabilityReporting']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PATCH',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'code-security/delete-configuration-for-enterprise',
  `Delete a code security configuration for an enterprise`,
  {
    enterprise: z.string(),
    configurationId: z.string(),
  },
  async (args, extra) => {
    try {
      const { enterprise, configurationId, ...otherParams } = args
      const url = `/enterprises/${enterprise}/code-security/configurations/${configurationId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('configurationId' in mappedParams) {
        mappedParams['configuration_id'] = mappedParams['configurationId']
        delete mappedParams['configurationId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'code-security/attach-enterprise-configuration',
  `Attach an enterprise configuration to repositories`,
  {
    enterprise: z.string(),
    configurationId: z.string(),
    scope: z.any(),
  },
  async (args, extra) => {
    try {
      const { enterprise, configurationId, ...otherParams } = args
      const url = `/enterprises/${enterprise}/code-security/configurations/${configurationId}/attach`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('configurationId' in mappedParams) {
        mappedParams['configuration_id'] = mappedParams['configurationId']
        delete mappedParams['configurationId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'code-security/set-configuration-as-default-for-enterprise',
  `Set a code security configuration as a default for an enterprise`,
  {
    enterprise: z.string(),
    configurationId: z.string(),
    defaultForNewRepos: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { enterprise, configurationId, ...otherParams } = args
      const url = `/enterprises/${enterprise}/code-security/configurations/${configurationId}/defaults`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('configurationId' in mappedParams) {
        mappedParams['configuration_id'] = mappedParams['configurationId']
        delete mappedParams['configurationId']
      }
      if ('defaultForNewRepos' in mappedParams) {
        mappedParams['default_for_new_repos'] = mappedParams['defaultForNewRepos']
        delete mappedParams['defaultForNewRepos']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'code-security/get-repositories-for-enterprise-configuration',
  `Get repositories associated with an enterprise code security configuration`,
  {
    enterprise: z.string(),
    configurationId: z.string(),
    perPage: z.string().optional(),
    status: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const { enterprise, configurationId, ...otherParams } = args
      const url = `/enterprises/${enterprise}/code-security/configurations/${configurationId}/repositories`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('configurationId' in mappedParams) {
        mappedParams['configuration_id'] = mappedParams['configurationId']
        delete mappedParams['configurationId']
      }
      if ('perPage' in mappedParams) {
        mappedParams['per_page'] = mappedParams['perPage']
        delete mappedParams['perPage']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'dependabot/list-alerts-for-enterprise',
  `List Dependabot alerts for an enterprise`,
  {
    enterprise: z.string(),
  },
  async (args, extra) => {
    try {
      const { enterprise, ...otherParams } = args
      const url = `/enterprises/${enterprise}/dependabot/alerts`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'secret-scanning/list-alerts-for-enterprise',
  `List secret scanning alerts for an enterprise`,
  {
    enterprise: z.string(),
  },
  async (args, extra) => {
    try {
      const { enterprise, ...otherParams } = args
      const url = `/enterprises/${enterprise}/secret-scanning/alerts`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool('activity/list-public-events', `List public events`, {}, async (args, extra) => {
  try {
    const otherParams = args

    // Map camelCase to original parameter names for API request
    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    const mappedParams: any = { ...otherParams }

    // Extract authorization token from HTTP request headers
    const authorization = extra?.requestInfo?.headers?.authorization as string
    const bearer = authorization?.replace('Bearer ', '')

    const response = await apiClient.request({
      headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
      method: 'GET',
      url: '/events',
      params: mappedParams,
    })

    return handleResult(response.data)
  } catch (error) {
    return handleError(error)
  }
})

mcpServer.tool('activity/get-feeds', `Get feeds`, {}, async (args, extra) => {
  try {
    const otherParams = args

    // Map camelCase to original parameter names for API request
    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    const mappedParams: any = { ...otherParams }

    // Extract authorization token from HTTP request headers
    const authorization = extra?.requestInfo?.headers?.authorization as string
    const bearer = authorization?.replace('Bearer ', '')

    const response = await apiClient.request({
      headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
      method: 'GET',
      url: '/feeds',
      params: mappedParams,
    })

    return handleResult(response.data)
  } catch (error) {
    return handleError(error)
  }
})

mcpServer.tool('gists/list', `List gists for the authenticated user`, {}, async (args, extra) => {
  try {
    const otherParams = args

    // Map camelCase to original parameter names for API request
    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    const mappedParams: any = { ...otherParams }

    // Extract authorization token from HTTP request headers
    const authorization = extra?.requestInfo?.headers?.authorization as string
    const bearer = authorization?.replace('Bearer ', '')

    const response = await apiClient.request({
      headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
      method: 'GET',
      url: '/gists',
      params: mappedParams,
    })

    return handleResult(response.data)
  } catch (error) {
    return handleError(error)
  }
})

mcpServer.tool(
  'gists/create',
  `Create a gist`,
  {
    description: z.any().optional(),
    files: z.any(),
    public: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const otherParams = args

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: '/gists',
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool('gists/list-public', `List public gists`, {}, async (args, extra) => {
  try {
    const otherParams = args

    // Map camelCase to original parameter names for API request
    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    const mappedParams: any = { ...otherParams }

    // Extract authorization token from HTTP request headers
    const authorization = extra?.requestInfo?.headers?.authorization as string
    const bearer = authorization?.replace('Bearer ', '')

    const response = await apiClient.request({
      headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
      method: 'GET',
      url: '/gists/public',
      params: mappedParams,
    })

    return handleResult(response.data)
  } catch (error) {
    return handleError(error)
  }
})

mcpServer.tool('gists/list-starred', `List starred gists`, {}, async (args, extra) => {
  try {
    const otherParams = args

    // Map camelCase to original parameter names for API request
    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    const mappedParams: any = { ...otherParams }

    // Extract authorization token from HTTP request headers
    const authorization = extra?.requestInfo?.headers?.authorization as string
    const bearer = authorization?.replace('Bearer ', '')

    const response = await apiClient.request({
      headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
      method: 'GET',
      url: '/gists/starred',
      params: mappedParams,
    })

    return handleResult(response.data)
  } catch (error) {
    return handleError(error)
  }
})

mcpServer.tool(
  'gists/get',
  `Get a gist`,
  {
    gistId: z.string(),
  },
  async (args, extra) => {
    try {
      const { gistId, ...otherParams } = args
      const url = `/gists/${gistId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('gistId' in mappedParams) {
        mappedParams['gist_id'] = mappedParams['gistId']
        delete mappedParams['gistId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'gists/update',
  `Update a gist`,
  {
    gistId: z.string(),
    description: z.any().optional(),
    files: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { gistId, ...otherParams } = args
      const url = `/gists/${gistId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('gistId' in mappedParams) {
        mappedParams['gist_id'] = mappedParams['gistId']
        delete mappedParams['gistId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PATCH',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'gists/delete',
  `Delete a gist`,
  {
    gistId: z.string(),
  },
  async (args, extra) => {
    try {
      const { gistId, ...otherParams } = args
      const url = `/gists/${gistId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('gistId' in mappedParams) {
        mappedParams['gist_id'] = mappedParams['gistId']
        delete mappedParams['gistId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'gists/list-comments',
  `List gist comments`,
  {
    gistId: z.string(),
  },
  async (args, extra) => {
    try {
      const { gistId, ...otherParams } = args
      const url = `/gists/${gistId}/comments`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('gistId' in mappedParams) {
        mappedParams['gist_id'] = mappedParams['gistId']
        delete mappedParams['gistId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'gists/create-comment',
  `Create a gist comment`,
  {
    gistId: z.string(),
    body: z.any(),
  },
  async (args, extra) => {
    try {
      const { gistId, ...otherParams } = args
      const url = `/gists/${gistId}/comments`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('gistId' in mappedParams) {
        mappedParams['gist_id'] = mappedParams['gistId']
        delete mappedParams['gistId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'gists/get-comment',
  `Get a gist comment`,
  {
    gistId: z.string(),
    commentId: z.string(),
  },
  async (args, extra) => {
    try {
      const { gistId, commentId, ...otherParams } = args
      const url = `/gists/${gistId}/comments/${commentId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('gistId' in mappedParams) {
        mappedParams['gist_id'] = mappedParams['gistId']
        delete mappedParams['gistId']
      }
      if ('commentId' in mappedParams) {
        mappedParams['comment_id'] = mappedParams['commentId']
        delete mappedParams['commentId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'gists/update-comment',
  `Update a gist comment`,
  {
    gistId: z.string(),
    commentId: z.string(),
    body: z.any(),
  },
  async (args, extra) => {
    try {
      const { gistId, commentId, ...otherParams } = args
      const url = `/gists/${gistId}/comments/${commentId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('gistId' in mappedParams) {
        mappedParams['gist_id'] = mappedParams['gistId']
        delete mappedParams['gistId']
      }
      if ('commentId' in mappedParams) {
        mappedParams['comment_id'] = mappedParams['commentId']
        delete mappedParams['commentId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PATCH',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'gists/delete-comment',
  `Delete a gist comment`,
  {
    gistId: z.string(),
    commentId: z.string(),
  },
  async (args, extra) => {
    try {
      const { gistId, commentId, ...otherParams } = args
      const url = `/gists/${gistId}/comments/${commentId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('gistId' in mappedParams) {
        mappedParams['gist_id'] = mappedParams['gistId']
        delete mappedParams['gistId']
      }
      if ('commentId' in mappedParams) {
        mappedParams['comment_id'] = mappedParams['commentId']
        delete mappedParams['commentId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'gists/list-commits',
  `List gist commits`,
  {
    gistId: z.string(),
  },
  async (args, extra) => {
    try {
      const { gistId, ...otherParams } = args
      const url = `/gists/${gistId}/commits`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('gistId' in mappedParams) {
        mappedParams['gist_id'] = mappedParams['gistId']
        delete mappedParams['gistId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'gists/list-forks',
  `List gist forks`,
  {
    gistId: z.string(),
  },
  async (args, extra) => {
    try {
      const { gistId, ...otherParams } = args
      const url = `/gists/${gistId}/forks`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('gistId' in mappedParams) {
        mappedParams['gist_id'] = mappedParams['gistId']
        delete mappedParams['gistId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'gists/fork',
  `Fork a gist`,
  {
    gistId: z.string(),
  },
  async (args, extra) => {
    try {
      const { gistId, ...otherParams } = args
      const url = `/gists/${gistId}/forks`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('gistId' in mappedParams) {
        mappedParams['gist_id'] = mappedParams['gistId']
        delete mappedParams['gistId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'gists/check-is-starred',
  `Check if a gist is starred`,
  {
    gistId: z.string(),
  },
  async (args, extra) => {
    try {
      const { gistId, ...otherParams } = args
      const url = `/gists/${gistId}/star`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('gistId' in mappedParams) {
        mappedParams['gist_id'] = mappedParams['gistId']
        delete mappedParams['gistId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'gists/star',
  `Star a gist`,
  {
    gistId: z.string(),
  },
  async (args, extra) => {
    try {
      const { gistId, ...otherParams } = args
      const url = `/gists/${gistId}/star`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('gistId' in mappedParams) {
        mappedParams['gist_id'] = mappedParams['gistId']
        delete mappedParams['gistId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'gists/unstar',
  `Unstar a gist`,
  {
    gistId: z.string(),
  },
  async (args, extra) => {
    try {
      const { gistId, ...otherParams } = args
      const url = `/gists/${gistId}/star`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('gistId' in mappedParams) {
        mappedParams['gist_id'] = mappedParams['gistId']
        delete mappedParams['gistId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'gists/get-revision',
  `Get a gist revision`,
  {
    gistId: z.string(),
    sha: z.string(),
  },
  async (args, extra) => {
    try {
      const { gistId, sha, ...otherParams } = args
      const url = `/gists/${gistId}/${sha}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('gistId' in mappedParams) {
        mappedParams['gist_id'] = mappedParams['gistId']
        delete mappedParams['gistId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'gitignore/get-all-templates',
  `Get all gitignore templates`,
  {},
  async (args, extra) => {
    try {
      const otherParams = args

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: '/gitignore/templates',
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'gitignore/get-template',
  `Get a gitignore template`,
  {
    name: z.string(),
  },
  async (args, extra) => {
    try {
      const { name, ...otherParams } = args
      const url = `/gitignore/templates/${name}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'apps/list-repos-accessible-to-installation',
  `List repositories accessible to the app installation`,
  {},
  async (args, extra) => {
    try {
      const otherParams = args

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: '/installation/repositories',
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'apps/revoke-installation-access-token',
  `Revoke an installation access token`,
  {},
  async (args, extra) => {
    try {
      const otherParams = args

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: '/installation/token',
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'issues/list',
  `List issues assigned to the authenticated user`,
  {
    filter: z.string().optional(),
    state: z.string().optional(),
    sort: z.string().optional(),
    collab: z.string().optional(),
    orgs: z.string().optional(),
    owned: z.string().optional(),
    pulls: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const otherParams = args

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: '/issues',
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'licenses/get-all-commonly-used',
  `Get all commonly used licenses`,
  {
    featured: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const otherParams = args

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: '/licenses',
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'licenses/get',
  `Get a license`,
  {
    license: z.string(),
  },
  async (args, extra) => {
    try {
      const { license, ...otherParams } = args
      const url = `/licenses/${license}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'markdown/render',
  `Render a Markdown document`,
  {
    text: z.any(),
    mode: z.any().optional(),
    context: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const otherParams = args

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: '/markdown',
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'markdown/render-raw',
  `Render a Markdown document in raw mode`,
  {},
  async (args, extra) => {
    try {
      const otherParams = args

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: '/markdown/raw',
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'apps/get-subscription-plan-for-account',
  `Get a subscription plan for an account`,
  {
    accountId: z.string(),
  },
  async (args, extra) => {
    try {
      const { accountId, ...otherParams } = args
      const url = `/marketplace_listing/accounts/${accountId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('accountId' in mappedParams) {
        mappedParams['account_id'] = mappedParams['accountId']
        delete mappedParams['accountId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool('apps/list-plans', `List plans`, {}, async (args, extra) => {
  try {
    const otherParams = args

    // Map camelCase to original parameter names for API request
    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    const mappedParams: any = { ...otherParams }

    // Extract authorization token from HTTP request headers
    const authorization = extra?.requestInfo?.headers?.authorization as string
    const bearer = authorization?.replace('Bearer ', '')

    const response = await apiClient.request({
      headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
      method: 'GET',
      url: '/marketplace_listing/plans',
      params: mappedParams,
    })

    return handleResult(response.data)
  } catch (error) {
    return handleError(error)
  }
})

mcpServer.tool(
  'apps/list-accounts-for-plan',
  `List accounts for a plan`,
  {
    planId: z.string(),
    direction: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const { planId, ...otherParams } = args
      const url = `/marketplace_listing/plans/${planId}/accounts`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('planId' in mappedParams) {
        mappedParams['plan_id'] = mappedParams['planId']
        delete mappedParams['planId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'apps/get-subscription-plan-for-account-stubbed',
  `Get a subscription plan for an account (stubbed)`,
  {
    accountId: z.string(),
  },
  async (args, extra) => {
    try {
      const { accountId, ...otherParams } = args
      const url = `/marketplace_listing/stubbed/accounts/${accountId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('accountId' in mappedParams) {
        mappedParams['account_id'] = mappedParams['accountId']
        delete mappedParams['accountId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool('apps/list-plans-stubbed', `List plans (stubbed)`, {}, async (args, extra) => {
  try {
    const otherParams = args

    // Map camelCase to original parameter names for API request
    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    const mappedParams: any = { ...otherParams }

    // Extract authorization token from HTTP request headers
    const authorization = extra?.requestInfo?.headers?.authorization as string
    const bearer = authorization?.replace('Bearer ', '')

    const response = await apiClient.request({
      headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
      method: 'GET',
      url: '/marketplace_listing/stubbed/plans',
      params: mappedParams,
    })

    return handleResult(response.data)
  } catch (error) {
    return handleError(error)
  }
})

mcpServer.tool(
  'apps/list-accounts-for-plan-stubbed',
  `List accounts for a plan (stubbed)`,
  {
    planId: z.string(),
    direction: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const { planId, ...otherParams } = args
      const url = `/marketplace_listing/stubbed/plans/${planId}/accounts`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('planId' in mappedParams) {
        mappedParams['plan_id'] = mappedParams['planId']
        delete mappedParams['planId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool('meta/get', `Get GitHub meta information`, {}, async (args, extra) => {
  try {
    const otherParams = args

    // Map camelCase to original parameter names for API request
    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    const mappedParams: any = { ...otherParams }

    // Extract authorization token from HTTP request headers
    const authorization = extra?.requestInfo?.headers?.authorization as string
    const bearer = authorization?.replace('Bearer ', '')

    const response = await apiClient.request({
      headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
      method: 'GET',
      url: '/meta',
      params: mappedParams,
    })

    return handleResult(response.data)
  } catch (error) {
    return handleError(error)
  }
})

mcpServer.tool(
  'activity/list-public-events-for-repo-network',
  `List public events for a network of repositories`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/networks/${owner}/${repo}/events`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'activity/list-notifications-for-authenticated-user',
  `List notifications for the authenticated user`,
  {
    perPage: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const otherParams = args

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('perPage' in mappedParams) {
        mappedParams['per_page'] = mappedParams['perPage']
        delete mappedParams['perPage']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: '/notifications',
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'activity/mark-notifications-as-read',
  `Mark notifications as read`,
  {
    lastReadAt: z.any().optional(),
    read: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const otherParams = args

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('lastReadAt' in mappedParams) {
        mappedParams['last_read_at'] = mappedParams['lastReadAt']
        delete mappedParams['lastReadAt']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: '/notifications',
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'activity/get-thread',
  `Get a thread`,
  {
    threadId: z.string(),
  },
  async (args, extra) => {
    try {
      const { threadId, ...otherParams } = args
      const url = `/notifications/threads/${threadId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('threadId' in mappedParams) {
        mappedParams['thread_id'] = mappedParams['threadId']
        delete mappedParams['threadId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'activity/mark-thread-as-read',
  `Mark a thread as read`,
  {
    threadId: z.string(),
  },
  async (args, extra) => {
    try {
      const { threadId, ...otherParams } = args
      const url = `/notifications/threads/${threadId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('threadId' in mappedParams) {
        mappedParams['thread_id'] = mappedParams['threadId']
        delete mappedParams['threadId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PATCH',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'activity/mark-thread-as-done',
  `Mark a thread as done`,
  {
    threadId: z.string(),
  },
  async (args, extra) => {
    try {
      const { threadId, ...otherParams } = args
      const url = `/notifications/threads/${threadId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('threadId' in mappedParams) {
        mappedParams['thread_id'] = mappedParams['threadId']
        delete mappedParams['threadId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'activity/get-thread-subscription-for-authenticated-user',
  `Get a thread subscription for the authenticated user`,
  {
    threadId: z.string(),
  },
  async (args, extra) => {
    try {
      const { threadId, ...otherParams } = args
      const url = `/notifications/threads/${threadId}/subscription`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('threadId' in mappedParams) {
        mappedParams['thread_id'] = mappedParams['threadId']
        delete mappedParams['threadId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'activity/set-thread-subscription',
  `Set a thread subscription`,
  {
    threadId: z.string(),
    ignored: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { threadId, ...otherParams } = args
      const url = `/notifications/threads/${threadId}/subscription`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('threadId' in mappedParams) {
        mappedParams['thread_id'] = mappedParams['threadId']
        delete mappedParams['threadId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'activity/delete-thread-subscription',
  `Delete a thread subscription`,
  {
    threadId: z.string(),
  },
  async (args, extra) => {
    try {
      const { threadId, ...otherParams } = args
      const url = `/notifications/threads/${threadId}/subscription`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('threadId' in mappedParams) {
        mappedParams['thread_id'] = mappedParams['threadId']
        delete mappedParams['threadId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'meta/get-octocat',
  `Get Octocat`,
  {
    s: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const otherParams = args

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: '/octocat',
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool('orgs/list', `List organizations`, {}, async (args, extra) => {
  try {
    const otherParams = args

    // Map camelCase to original parameter names for API request
    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    const mappedParams: any = { ...otherParams }

    // Extract authorization token from HTTP request headers
    const authorization = extra?.requestInfo?.headers?.authorization as string
    const bearer = authorization?.replace('Bearer ', '')

    const response = await apiClient.request({
      headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
      method: 'GET',
      url: '/organizations',
      params: mappedParams,
    })

    return handleResult(response.data)
  } catch (error) {
    return handleError(error)
  }
})

mcpServer.tool(
  'dependabot/repository-access-for-org',
  `Lists the repositories Dependabot can access in an organization`,
  {
    org: z.string(),
    page: z.string().optional(),
    perPage: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/organizations/${org}/dependabot/repository-access`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('perPage' in mappedParams) {
        mappedParams['per_page'] = mappedParams['perPage']
        delete mappedParams['perPage']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'dependabot/update-repository-access-for-org',
  `Updates Dependabot&#x27;s repository access list for an organization`,
  {
    org: z.string(),
    repositoryIdsToAdd: z.any().optional(),
    repositoryIdsToRemove: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/organizations/${org}/dependabot/repository-access`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('repositoryIdsToAdd' in mappedParams) {
        mappedParams['repository_ids_to_add'] = mappedParams['repositoryIdsToAdd']
        delete mappedParams['repositoryIdsToAdd']
      }
      if ('repositoryIdsToRemove' in mappedParams) {
        mappedParams['repository_ids_to_remove'] = mappedParams['repositoryIdsToRemove']
        delete mappedParams['repositoryIdsToRemove']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PATCH',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'dependabot/set-repository-access-default-level',
  `Set the default repository access level for Dependabot`,
  {
    org: z.string(),
    defaultLevel: z.any(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/organizations/${org}/dependabot/repository-access/default-level`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('defaultLevel' in mappedParams) {
        mappedParams['default_level'] = mappedParams['defaultLevel']
        delete mappedParams['defaultLevel']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'billing/get-github-billing-usage-report-org',
  `Get billing usage report for an organization`,
  {
    org: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/organizations/${org}/settings/billing/usage`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'orgs/get',
  `Get an organization`,
  {
    org: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'orgs/update',
  `Update an organization`,
  {
    org: z.string(),
    billingEmail: z.any().optional(),
    company: z.any().optional(),
    email: z.any().optional(),
    twitterUsername: z.any().optional(),
    location: z.any().optional(),
    name: z.any().optional(),
    description: z.any().optional(),
    hasOrganizationProjects: z.any().optional(),
    hasRepositoryProjects: z.any().optional(),
    defaultRepositoryPermission: z.any().optional(),
    membersCanCreateRepositories: z.any().optional(),
    membersCanCreateInternalRepositories: z.any().optional(),
    membersCanCreatePrivateRepositories: z.any().optional(),
    membersCanCreatePublicRepositories: z.any().optional(),
    membersAllowedRepositoryCreationType: z.any().optional(),
    membersCanCreatePages: z.any().optional(),
    membersCanCreatePublicPages: z.any().optional(),
    membersCanCreatePrivatePages: z.any().optional(),
    membersCanForkPrivateRepositories: z.any().optional(),
    webCommitSignoffRequired: z.any().optional(),
    blog: z.any().optional(),
    advancedSecurityEnabledForNewRepositories: z.any().optional(),
    dependabotAlertsEnabledForNewRepositories: z.any().optional(),
    dependabotSecurityUpdatesEnabledForNewRepositories: z.any().optional(),
    dependencyGraphEnabledForNewRepositories: z.any().optional(),
    secretScanningEnabledForNewRepositories: z.any().optional(),
    secretScanningPushProtectionEnabledForNewRepositories: z.any().optional(),
    secretScanningPushProtectionCustomLinkEnabled: z.any().optional(),
    secretScanningPushProtectionCustomLink: z.any().optional(),
    deployKeysEnabledForRepositories: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('billingEmail' in mappedParams) {
        mappedParams['billing_email'] = mappedParams['billingEmail']
        delete mappedParams['billingEmail']
      }
      if ('twitterUsername' in mappedParams) {
        mappedParams['twitter_username'] = mappedParams['twitterUsername']
        delete mappedParams['twitterUsername']
      }
      if ('hasOrganizationProjects' in mappedParams) {
        mappedParams['has_organization_projects'] = mappedParams['hasOrganizationProjects']
        delete mappedParams['hasOrganizationProjects']
      }
      if ('hasRepositoryProjects' in mappedParams) {
        mappedParams['has_repository_projects'] = mappedParams['hasRepositoryProjects']
        delete mappedParams['hasRepositoryProjects']
      }
      if ('defaultRepositoryPermission' in mappedParams) {
        mappedParams['default_repository_permission'] = mappedParams['defaultRepositoryPermission']
        delete mappedParams['defaultRepositoryPermission']
      }
      if ('membersCanCreateRepositories' in mappedParams) {
        mappedParams['members_can_create_repositories'] =
          mappedParams['membersCanCreateRepositories']
        delete mappedParams['membersCanCreateRepositories']
      }
      if ('membersCanCreateInternalRepositories' in mappedParams) {
        mappedParams['members_can_create_internal_repositories'] =
          mappedParams['membersCanCreateInternalRepositories']
        delete mappedParams['membersCanCreateInternalRepositories']
      }
      if ('membersCanCreatePrivateRepositories' in mappedParams) {
        mappedParams['members_can_create_private_repositories'] =
          mappedParams['membersCanCreatePrivateRepositories']
        delete mappedParams['membersCanCreatePrivateRepositories']
      }
      if ('membersCanCreatePublicRepositories' in mappedParams) {
        mappedParams['members_can_create_public_repositories'] =
          mappedParams['membersCanCreatePublicRepositories']
        delete mappedParams['membersCanCreatePublicRepositories']
      }
      if ('membersAllowedRepositoryCreationType' in mappedParams) {
        mappedParams['members_allowed_repository_creation_type'] =
          mappedParams['membersAllowedRepositoryCreationType']
        delete mappedParams['membersAllowedRepositoryCreationType']
      }
      if ('membersCanCreatePages' in mappedParams) {
        mappedParams['members_can_create_pages'] = mappedParams['membersCanCreatePages']
        delete mappedParams['membersCanCreatePages']
      }
      if ('membersCanCreatePublicPages' in mappedParams) {
        mappedParams['members_can_create_public_pages'] =
          mappedParams['membersCanCreatePublicPages']
        delete mappedParams['membersCanCreatePublicPages']
      }
      if ('membersCanCreatePrivatePages' in mappedParams) {
        mappedParams['members_can_create_private_pages'] =
          mappedParams['membersCanCreatePrivatePages']
        delete mappedParams['membersCanCreatePrivatePages']
      }
      if ('membersCanForkPrivateRepositories' in mappedParams) {
        mappedParams['members_can_fork_private_repositories'] =
          mappedParams['membersCanForkPrivateRepositories']
        delete mappedParams['membersCanForkPrivateRepositories']
      }
      if ('webCommitSignoffRequired' in mappedParams) {
        mappedParams['web_commit_signoff_required'] = mappedParams['webCommitSignoffRequired']
        delete mappedParams['webCommitSignoffRequired']
      }
      if ('advancedSecurityEnabledForNewRepositories' in mappedParams) {
        mappedParams['advanced_security_enabled_for_new_repositories'] =
          mappedParams['advancedSecurityEnabledForNewRepositories']
        delete mappedParams['advancedSecurityEnabledForNewRepositories']
      }
      if ('dependabotAlertsEnabledForNewRepositories' in mappedParams) {
        mappedParams['dependabot_alerts_enabled_for_new_repositories'] =
          mappedParams['dependabotAlertsEnabledForNewRepositories']
        delete mappedParams['dependabotAlertsEnabledForNewRepositories']
      }
      if ('dependabotSecurityUpdatesEnabledForNewRepositories' in mappedParams) {
        mappedParams['dependabot_security_updates_enabled_for_new_repositories'] =
          mappedParams['dependabotSecurityUpdatesEnabledForNewRepositories']
        delete mappedParams['dependabotSecurityUpdatesEnabledForNewRepositories']
      }
      if ('dependencyGraphEnabledForNewRepositories' in mappedParams) {
        mappedParams['dependency_graph_enabled_for_new_repositories'] =
          mappedParams['dependencyGraphEnabledForNewRepositories']
        delete mappedParams['dependencyGraphEnabledForNewRepositories']
      }
      if ('secretScanningEnabledForNewRepositories' in mappedParams) {
        mappedParams['secret_scanning_enabled_for_new_repositories'] =
          mappedParams['secretScanningEnabledForNewRepositories']
        delete mappedParams['secretScanningEnabledForNewRepositories']
      }
      if ('secretScanningPushProtectionEnabledForNewRepositories' in mappedParams) {
        mappedParams['secret_scanning_push_protection_enabled_for_new_repositories'] =
          mappedParams['secretScanningPushProtectionEnabledForNewRepositories']
        delete mappedParams['secretScanningPushProtectionEnabledForNewRepositories']
      }
      if ('secretScanningPushProtectionCustomLinkEnabled' in mappedParams) {
        mappedParams['secret_scanning_push_protection_custom_link_enabled'] =
          mappedParams['secretScanningPushProtectionCustomLinkEnabled']
        delete mappedParams['secretScanningPushProtectionCustomLinkEnabled']
      }
      if ('secretScanningPushProtectionCustomLink' in mappedParams) {
        mappedParams['secret_scanning_push_protection_custom_link'] =
          mappedParams['secretScanningPushProtectionCustomLink']
        delete mappedParams['secretScanningPushProtectionCustomLink']
      }
      if ('deployKeysEnabledForRepositories' in mappedParams) {
        mappedParams['deploy_keys_enabled_for_repositories'] =
          mappedParams['deployKeysEnabledForRepositories']
        delete mappedParams['deployKeysEnabledForRepositories']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PATCH',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'orgs/delete',
  `Delete an organization`,
  {
    org: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/get-actions-cache-usage-for-org',
  `Get GitHub Actions cache usage for an organization`,
  {
    org: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/actions/cache/usage`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/get-actions-cache-usage-by-repo-for-org',
  `List repositories with GitHub Actions cache usage for an organization`,
  {
    org: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/actions/cache/usage-by-repository`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/list-hosted-runners-for-org',
  `List GitHub-hosted runners for an organization`,
  {
    org: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/actions/hosted-runners`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/create-hosted-runner-for-org',
  `Create a GitHub-hosted runner for an organization`,
  {
    org: z.string(),
    name: z.any(),
    image: z.any(),
    size: z.any(),
    runnerGroupId: z.any(),
    maximumRunners: z.any().optional(),
    enableStaticIp: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/actions/hosted-runners`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('runnerGroupId' in mappedParams) {
        mappedParams['runner_group_id'] = mappedParams['runnerGroupId']
        delete mappedParams['runnerGroupId']
      }
      if ('maximumRunners' in mappedParams) {
        mappedParams['maximum_runners'] = mappedParams['maximumRunners']
        delete mappedParams['maximumRunners']
      }
      if ('enableStaticIp' in mappedParams) {
        mappedParams['enable_static_ip'] = mappedParams['enableStaticIp']
        delete mappedParams['enableStaticIp']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/get-hosted-runners-github-owned-images-for-org',
  `Get GitHub-owned images for GitHub-hosted runners in an organization`,
  {
    org: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/actions/hosted-runners/images/github-owned`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/get-hosted-runners-partner-images-for-org',
  `Get partner images for GitHub-hosted runners in an organization`,
  {
    org: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/actions/hosted-runners/images/partner`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/get-hosted-runners-limits-for-org',
  `Get limits on GitHub-hosted runners for an organization`,
  {
    org: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/actions/hosted-runners/limits`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/get-hosted-runners-machine-specs-for-org',
  `Get GitHub-hosted runners machine specs for an organization`,
  {
    org: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/actions/hosted-runners/machine-sizes`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/get-hosted-runners-platforms-for-org',
  `Get platforms for GitHub-hosted runners in an organization`,
  {
    org: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/actions/hosted-runners/platforms`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/get-hosted-runner-for-org',
  `Get a GitHub-hosted runner for an organization`,
  {
    org: z.string(),
    hostedRunnerId: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, hostedRunnerId, ...otherParams } = args
      const url = `/orgs/${org}/actions/hosted-runners/${hostedRunnerId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('hostedRunnerId' in mappedParams) {
        mappedParams['hosted_runner_id'] = mappedParams['hostedRunnerId']
        delete mappedParams['hostedRunnerId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/update-hosted-runner-for-org',
  `Update a GitHub-hosted runner for an organization`,
  {
    org: z.string(),
    hostedRunnerId: z.string(),
    name: z.any().optional(),
    runnerGroupId: z.any().optional(),
    maximumRunners: z.any().optional(),
    enableStaticIp: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { org, hostedRunnerId, ...otherParams } = args
      const url = `/orgs/${org}/actions/hosted-runners/${hostedRunnerId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('hostedRunnerId' in mappedParams) {
        mappedParams['hosted_runner_id'] = mappedParams['hostedRunnerId']
        delete mappedParams['hostedRunnerId']
      }
      if ('runnerGroupId' in mappedParams) {
        mappedParams['runner_group_id'] = mappedParams['runnerGroupId']
        delete mappedParams['runnerGroupId']
      }
      if ('maximumRunners' in mappedParams) {
        mappedParams['maximum_runners'] = mappedParams['maximumRunners']
        delete mappedParams['maximumRunners']
      }
      if ('enableStaticIp' in mappedParams) {
        mappedParams['enable_static_ip'] = mappedParams['enableStaticIp']
        delete mappedParams['enableStaticIp']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PATCH',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/delete-hosted-runner-for-org',
  `Delete a GitHub-hosted runner for an organization`,
  {
    org: z.string(),
    hostedRunnerId: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, hostedRunnerId, ...otherParams } = args
      const url = `/orgs/${org}/actions/hosted-runners/${hostedRunnerId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('hostedRunnerId' in mappedParams) {
        mappedParams['hosted_runner_id'] = mappedParams['hostedRunnerId']
        delete mappedParams['hostedRunnerId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'oidc/get-oidc-custom-sub-template-for-org',
  `Get the customization template for an OIDC subject claim for an organization`,
  {
    org: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/actions/oidc/customization/sub`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'oidc/update-oidc-custom-sub-template-for-org',
  `Set the customization template for an OIDC subject claim for an organization`,
  {
    org: z.string(),
    requestData: z.record(z.any()).optional(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/actions/oidc/customization/sub`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/get-github-actions-permissions-organization',
  `Get GitHub Actions permissions for an organization`,
  {
    org: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/actions/permissions`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/set-github-actions-permissions-organization',
  `Set GitHub Actions permissions for an organization`,
  {
    org: z.string(),
    enabledRepositories: z.any(),
    allowedActions: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/actions/permissions`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('enabledRepositories' in mappedParams) {
        mappedParams['enabled_repositories'] = mappedParams['enabledRepositories']
        delete mappedParams['enabledRepositories']
      }
      if ('allowedActions' in mappedParams) {
        mappedParams['allowed_actions'] = mappedParams['allowedActions']
        delete mappedParams['allowedActions']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/list-selected-repositories-enabled-github-actions-organization',
  `List selected repositories enabled for GitHub Actions in an organization`,
  {
    org: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/actions/permissions/repositories`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/set-selected-repositories-enabled-github-actions-organization',
  `Set selected repositories enabled for GitHub Actions in an organization`,
  {
    org: z.string(),
    selectedRepositoryIds: z.any(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/actions/permissions/repositories`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('selectedRepositoryIds' in mappedParams) {
        mappedParams['selected_repository_ids'] = mappedParams['selectedRepositoryIds']
        delete mappedParams['selectedRepositoryIds']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/enable-selected-repository-github-actions-organization',
  `Enable a selected repository for GitHub Actions in an organization`,
  {
    org: z.string(),
    repositoryId: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, repositoryId, ...otherParams } = args
      const url = `/orgs/${org}/actions/permissions/repositories/${repositoryId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('repositoryId' in mappedParams) {
        mappedParams['repository_id'] = mappedParams['repositoryId']
        delete mappedParams['repositoryId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/disable-selected-repository-github-actions-organization',
  `Disable a selected repository for GitHub Actions in an organization`,
  {
    org: z.string(),
    repositoryId: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, repositoryId, ...otherParams } = args
      const url = `/orgs/${org}/actions/permissions/repositories/${repositoryId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('repositoryId' in mappedParams) {
        mappedParams['repository_id'] = mappedParams['repositoryId']
        delete mappedParams['repositoryId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/get-allowed-actions-organization',
  `Get allowed actions and reusable workflows for an organization`,
  {
    org: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/actions/permissions/selected-actions`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/set-allowed-actions-organization',
  `Set allowed actions and reusable workflows for an organization`,
  {
    org: z.string(),
    requestData: z.record(z.any()).optional(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/actions/permissions/selected-actions`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/get-github-actions-default-workflow-permissions-organization',
  `Get default workflow permissions for an organization`,
  {
    org: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/actions/permissions/workflow`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/set-github-actions-default-workflow-permissions-organization',
  `Set default workflow permissions for an organization`,
  {
    org: z.string(),
    requestData: z.record(z.any()).optional(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/actions/permissions/workflow`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/list-self-hosted-runner-groups-for-org',
  `List self-hosted runner groups for an organization`,
  {
    org: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/actions/runner-groups`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/create-self-hosted-runner-group-for-org',
  `Create a self-hosted runner group for an organization`,
  {
    org: z.string(),
    name: z.any(),
    visibility: z.any().optional(),
    selectedRepositoryIds: z.any().optional(),
    runners: z.any().optional(),
    allowsPublicRepositories: z.any().optional(),
    restrictedToWorkflows: z.any().optional(),
    selectedWorkflows: z.any().optional(),
    networkConfigurationId: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/actions/runner-groups`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('selectedRepositoryIds' in mappedParams) {
        mappedParams['selected_repository_ids'] = mappedParams['selectedRepositoryIds']
        delete mappedParams['selectedRepositoryIds']
      }
      if ('allowsPublicRepositories' in mappedParams) {
        mappedParams['allows_public_repositories'] = mappedParams['allowsPublicRepositories']
        delete mappedParams['allowsPublicRepositories']
      }
      if ('restrictedToWorkflows' in mappedParams) {
        mappedParams['restricted_to_workflows'] = mappedParams['restrictedToWorkflows']
        delete mappedParams['restrictedToWorkflows']
      }
      if ('selectedWorkflows' in mappedParams) {
        mappedParams['selected_workflows'] = mappedParams['selectedWorkflows']
        delete mappedParams['selectedWorkflows']
      }
      if ('networkConfigurationId' in mappedParams) {
        mappedParams['network_configuration_id'] = mappedParams['networkConfigurationId']
        delete mappedParams['networkConfigurationId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/get-self-hosted-runner-group-for-org',
  `Get a self-hosted runner group for an organization`,
  {
    org: z.string(),
    runnerGroupId: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, runnerGroupId, ...otherParams } = args
      const url = `/orgs/${org}/actions/runner-groups/${runnerGroupId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('runnerGroupId' in mappedParams) {
        mappedParams['runner_group_id'] = mappedParams['runnerGroupId']
        delete mappedParams['runnerGroupId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/update-self-hosted-runner-group-for-org',
  `Update a self-hosted runner group for an organization`,
  {
    org: z.string(),
    runnerGroupId: z.string(),
    name: z.any(),
    visibility: z.any().optional(),
    allowsPublicRepositories: z.any().optional(),
    restrictedToWorkflows: z.any().optional(),
    selectedWorkflows: z.any().optional(),
    networkConfigurationId: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { org, runnerGroupId, ...otherParams } = args
      const url = `/orgs/${org}/actions/runner-groups/${runnerGroupId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('runnerGroupId' in mappedParams) {
        mappedParams['runner_group_id'] = mappedParams['runnerGroupId']
        delete mappedParams['runnerGroupId']
      }
      if ('allowsPublicRepositories' in mappedParams) {
        mappedParams['allows_public_repositories'] = mappedParams['allowsPublicRepositories']
        delete mappedParams['allowsPublicRepositories']
      }
      if ('restrictedToWorkflows' in mappedParams) {
        mappedParams['restricted_to_workflows'] = mappedParams['restrictedToWorkflows']
        delete mappedParams['restrictedToWorkflows']
      }
      if ('selectedWorkflows' in mappedParams) {
        mappedParams['selected_workflows'] = mappedParams['selectedWorkflows']
        delete mappedParams['selectedWorkflows']
      }
      if ('networkConfigurationId' in mappedParams) {
        mappedParams['network_configuration_id'] = mappedParams['networkConfigurationId']
        delete mappedParams['networkConfigurationId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PATCH',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/delete-self-hosted-runner-group-from-org',
  `Delete a self-hosted runner group from an organization`,
  {
    org: z.string(),
    runnerGroupId: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, runnerGroupId, ...otherParams } = args
      const url = `/orgs/${org}/actions/runner-groups/${runnerGroupId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('runnerGroupId' in mappedParams) {
        mappedParams['runner_group_id'] = mappedParams['runnerGroupId']
        delete mappedParams['runnerGroupId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/list-github-hosted-runners-in-group-for-org',
  `List GitHub-hosted runners in a group for an organization`,
  {
    org: z.string(),
    runnerGroupId: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, runnerGroupId, ...otherParams } = args
      const url = `/orgs/${org}/actions/runner-groups/${runnerGroupId}/hosted-runners`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('runnerGroupId' in mappedParams) {
        mappedParams['runner_group_id'] = mappedParams['runnerGroupId']
        delete mappedParams['runnerGroupId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/list-repo-access-to-self-hosted-runner-group-in-org',
  `List repository access to a self-hosted runner group in an organization`,
  {
    org: z.string(),
    runnerGroupId: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, runnerGroupId, ...otherParams } = args
      const url = `/orgs/${org}/actions/runner-groups/${runnerGroupId}/repositories`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('runnerGroupId' in mappedParams) {
        mappedParams['runner_group_id'] = mappedParams['runnerGroupId']
        delete mappedParams['runnerGroupId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/set-repo-access-to-self-hosted-runner-group-in-org',
  `Set repository access for a self-hosted runner group in an organization`,
  {
    org: z.string(),
    runnerGroupId: z.string(),
    selectedRepositoryIds: z.any(),
  },
  async (args, extra) => {
    try {
      const { org, runnerGroupId, ...otherParams } = args
      const url = `/orgs/${org}/actions/runner-groups/${runnerGroupId}/repositories`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('runnerGroupId' in mappedParams) {
        mappedParams['runner_group_id'] = mappedParams['runnerGroupId']
        delete mappedParams['runnerGroupId']
      }
      if ('selectedRepositoryIds' in mappedParams) {
        mappedParams['selected_repository_ids'] = mappedParams['selectedRepositoryIds']
        delete mappedParams['selectedRepositoryIds']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/add-repo-access-to-self-hosted-runner-group-in-org',
  `Add repository access to a self-hosted runner group in an organization`,
  {
    org: z.string(),
    runnerGroupId: z.string(),
    repositoryId: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, runnerGroupId, repositoryId, ...otherParams } = args
      const url = `/orgs/${org}/actions/runner-groups/${runnerGroupId}/repositories/${repositoryId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('runnerGroupId' in mappedParams) {
        mappedParams['runner_group_id'] = mappedParams['runnerGroupId']
        delete mappedParams['runnerGroupId']
      }
      if ('repositoryId' in mappedParams) {
        mappedParams['repository_id'] = mappedParams['repositoryId']
        delete mappedParams['repositoryId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/remove-repo-access-to-self-hosted-runner-group-in-org',
  `Remove repository access to a self-hosted runner group in an organization`,
  {
    org: z.string(),
    runnerGroupId: z.string(),
    repositoryId: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, runnerGroupId, repositoryId, ...otherParams } = args
      const url = `/orgs/${org}/actions/runner-groups/${runnerGroupId}/repositories/${repositoryId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('runnerGroupId' in mappedParams) {
        mappedParams['runner_group_id'] = mappedParams['runnerGroupId']
        delete mappedParams['runnerGroupId']
      }
      if ('repositoryId' in mappedParams) {
        mappedParams['repository_id'] = mappedParams['repositoryId']
        delete mappedParams['repositoryId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/list-self-hosted-runners-in-group-for-org',
  `List self-hosted runners in a group for an organization`,
  {
    org: z.string(),
    runnerGroupId: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, runnerGroupId, ...otherParams } = args
      const url = `/orgs/${org}/actions/runner-groups/${runnerGroupId}/runners`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('runnerGroupId' in mappedParams) {
        mappedParams['runner_group_id'] = mappedParams['runnerGroupId']
        delete mappedParams['runnerGroupId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/set-self-hosted-runners-in-group-for-org',
  `Set self-hosted runners in a group for an organization`,
  {
    org: z.string(),
    runnerGroupId: z.string(),
    runners: z.any(),
  },
  async (args, extra) => {
    try {
      const { org, runnerGroupId, ...otherParams } = args
      const url = `/orgs/${org}/actions/runner-groups/${runnerGroupId}/runners`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('runnerGroupId' in mappedParams) {
        mappedParams['runner_group_id'] = mappedParams['runnerGroupId']
        delete mappedParams['runnerGroupId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/add-self-hosted-runner-to-group-for-org',
  `Add a self-hosted runner to a group for an organization`,
  {
    org: z.string(),
    runnerGroupId: z.string(),
    runnerId: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, runnerGroupId, runnerId, ...otherParams } = args
      const url = `/orgs/${org}/actions/runner-groups/${runnerGroupId}/runners/${runnerId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('runnerGroupId' in mappedParams) {
        mappedParams['runner_group_id'] = mappedParams['runnerGroupId']
        delete mappedParams['runnerGroupId']
      }
      if ('runnerId' in mappedParams) {
        mappedParams['runner_id'] = mappedParams['runnerId']
        delete mappedParams['runnerId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/remove-self-hosted-runner-from-group-for-org',
  `Remove a self-hosted runner from a group for an organization`,
  {
    org: z.string(),
    runnerGroupId: z.string(),
    runnerId: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, runnerGroupId, runnerId, ...otherParams } = args
      const url = `/orgs/${org}/actions/runner-groups/${runnerGroupId}/runners/${runnerId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('runnerGroupId' in mappedParams) {
        mappedParams['runner_group_id'] = mappedParams['runnerGroupId']
        delete mappedParams['runnerGroupId']
      }
      if ('runnerId' in mappedParams) {
        mappedParams['runner_id'] = mappedParams['runnerId']
        delete mappedParams['runnerId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/list-self-hosted-runners-for-org',
  `List self-hosted runners for an organization`,
  {
    org: z.string(),
    name: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/actions/runners`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/list-runner-applications-for-org',
  `List runner applications for an organization`,
  {
    org: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/actions/runners/downloads`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/generate-runner-jitconfig-for-org',
  `Create configuration for a just-in-time runner for an organization`,
  {
    org: z.string(),
    name: z.any(),
    runnerGroupId: z.any(),
    labels: z.any(),
    workFolder: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/actions/runners/generate-jitconfig`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('runnerGroupId' in mappedParams) {
        mappedParams['runner_group_id'] = mappedParams['runnerGroupId']
        delete mappedParams['runnerGroupId']
      }
      if ('workFolder' in mappedParams) {
        mappedParams['work_folder'] = mappedParams['workFolder']
        delete mappedParams['workFolder']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/create-registration-token-for-org',
  `Create a registration token for an organization`,
  {
    org: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/actions/runners/registration-token`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/create-remove-token-for-org',
  `Create a remove token for an organization`,
  {
    org: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/actions/runners/remove-token`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/get-self-hosted-runner-for-org',
  `Get a self-hosted runner for an organization`,
  {
    org: z.string(),
    runnerId: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, runnerId, ...otherParams } = args
      const url = `/orgs/${org}/actions/runners/${runnerId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('runnerId' in mappedParams) {
        mappedParams['runner_id'] = mappedParams['runnerId']
        delete mappedParams['runnerId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/delete-self-hosted-runner-from-org',
  `Delete a self-hosted runner from an organization`,
  {
    org: z.string(),
    runnerId: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, runnerId, ...otherParams } = args
      const url = `/orgs/${org}/actions/runners/${runnerId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('runnerId' in mappedParams) {
        mappedParams['runner_id'] = mappedParams['runnerId']
        delete mappedParams['runnerId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/list-labels-for-self-hosted-runner-for-org',
  `List labels for a self-hosted runner for an organization`,
  {
    org: z.string(),
    runnerId: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, runnerId, ...otherParams } = args
      const url = `/orgs/${org}/actions/runners/${runnerId}/labels`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('runnerId' in mappedParams) {
        mappedParams['runner_id'] = mappedParams['runnerId']
        delete mappedParams['runnerId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/add-custom-labels-to-self-hosted-runner-for-org',
  `Add custom labels to a self-hosted runner for an organization`,
  {
    org: z.string(),
    runnerId: z.string(),
    labels: z.any(),
  },
  async (args, extra) => {
    try {
      const { org, runnerId, ...otherParams } = args
      const url = `/orgs/${org}/actions/runners/${runnerId}/labels`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('runnerId' in mappedParams) {
        mappedParams['runner_id'] = mappedParams['runnerId']
        delete mappedParams['runnerId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/set-custom-labels-for-self-hosted-runner-for-org',
  `Set custom labels for a self-hosted runner for an organization`,
  {
    org: z.string(),
    runnerId: z.string(),
    labels: z.any(),
  },
  async (args, extra) => {
    try {
      const { org, runnerId, ...otherParams } = args
      const url = `/orgs/${org}/actions/runners/${runnerId}/labels`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('runnerId' in mappedParams) {
        mappedParams['runner_id'] = mappedParams['runnerId']
        delete mappedParams['runnerId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/remove-all-custom-labels-from-self-hosted-runner-for-org',
  `Remove all custom labels from a self-hosted runner for an organization`,
  {
    org: z.string(),
    runnerId: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, runnerId, ...otherParams } = args
      const url = `/orgs/${org}/actions/runners/${runnerId}/labels`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('runnerId' in mappedParams) {
        mappedParams['runner_id'] = mappedParams['runnerId']
        delete mappedParams['runnerId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/remove-custom-label-from-self-hosted-runner-for-org',
  `Remove a custom label from a self-hosted runner for an organization`,
  {
    org: z.string(),
    runnerId: z.string(),
    name: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, runnerId, name, ...otherParams } = args
      const url = `/orgs/${org}/actions/runners/${runnerId}/labels/${name}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('runnerId' in mappedParams) {
        mappedParams['runner_id'] = mappedParams['runnerId']
        delete mappedParams['runnerId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/list-org-secrets',
  `List organization secrets`,
  {
    org: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/actions/secrets`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/get-org-public-key',
  `Get an organization public key`,
  {
    org: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/actions/secrets/public-key`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/get-org-secret',
  `Get an organization secret`,
  {
    org: z.string(),
    secretName: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, secretName, ...otherParams } = args
      const url = `/orgs/${org}/actions/secrets/${secretName}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('secretName' in mappedParams) {
        mappedParams['secret_name'] = mappedParams['secretName']
        delete mappedParams['secretName']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/create-or-update-org-secret',
  `Create or update an organization secret`,
  {
    org: z.string(),
    secretName: z.string(),
    encryptedValue: z.any(),
    keyId: z.any(),
    visibility: z.any(),
    selectedRepositoryIds: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { org, secretName, ...otherParams } = args
      const url = `/orgs/${org}/actions/secrets/${secretName}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('secretName' in mappedParams) {
        mappedParams['secret_name'] = mappedParams['secretName']
        delete mappedParams['secretName']
      }
      if ('encryptedValue' in mappedParams) {
        mappedParams['encrypted_value'] = mappedParams['encryptedValue']
        delete mappedParams['encryptedValue']
      }
      if ('keyId' in mappedParams) {
        mappedParams['key_id'] = mappedParams['keyId']
        delete mappedParams['keyId']
      }
      if ('selectedRepositoryIds' in mappedParams) {
        mappedParams['selected_repository_ids'] = mappedParams['selectedRepositoryIds']
        delete mappedParams['selectedRepositoryIds']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/delete-org-secret',
  `Delete an organization secret`,
  {
    org: z.string(),
    secretName: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, secretName, ...otherParams } = args
      const url = `/orgs/${org}/actions/secrets/${secretName}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('secretName' in mappedParams) {
        mappedParams['secret_name'] = mappedParams['secretName']
        delete mappedParams['secretName']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/list-selected-repos-for-org-secret',
  `List selected repositories for an organization secret`,
  {
    org: z.string(),
    secretName: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, secretName, ...otherParams } = args
      const url = `/orgs/${org}/actions/secrets/${secretName}/repositories`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('secretName' in mappedParams) {
        mappedParams['secret_name'] = mappedParams['secretName']
        delete mappedParams['secretName']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/set-selected-repos-for-org-secret',
  `Set selected repositories for an organization secret`,
  {
    org: z.string(),
    secretName: z.string(),
    selectedRepositoryIds: z.any(),
  },
  async (args, extra) => {
    try {
      const { org, secretName, ...otherParams } = args
      const url = `/orgs/${org}/actions/secrets/${secretName}/repositories`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('secretName' in mappedParams) {
        mappedParams['secret_name'] = mappedParams['secretName']
        delete mappedParams['secretName']
      }
      if ('selectedRepositoryIds' in mappedParams) {
        mappedParams['selected_repository_ids'] = mappedParams['selectedRepositoryIds']
        delete mappedParams['selectedRepositoryIds']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/add-selected-repo-to-org-secret',
  `Add selected repository to an organization secret`,
  {
    org: z.string(),
    secretName: z.string(),
    repositoryId: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, secretName, repositoryId, ...otherParams } = args
      const url = `/orgs/${org}/actions/secrets/${secretName}/repositories/${repositoryId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('secretName' in mappedParams) {
        mappedParams['secret_name'] = mappedParams['secretName']
        delete mappedParams['secretName']
      }
      if ('repositoryId' in mappedParams) {
        mappedParams['repository_id'] = mappedParams['repositoryId']
        delete mappedParams['repositoryId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/remove-selected-repo-from-org-secret',
  `Remove selected repository from an organization secret`,
  {
    org: z.string(),
    secretName: z.string(),
    repositoryId: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, secretName, repositoryId, ...otherParams } = args
      const url = `/orgs/${org}/actions/secrets/${secretName}/repositories/${repositoryId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('secretName' in mappedParams) {
        mappedParams['secret_name'] = mappedParams['secretName']
        delete mappedParams['secretName']
      }
      if ('repositoryId' in mappedParams) {
        mappedParams['repository_id'] = mappedParams['repositoryId']
        delete mappedParams['repositoryId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/list-org-variables',
  `List organization variables`,
  {
    org: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/actions/variables`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/create-org-variable',
  `Create an organization variable`,
  {
    org: z.string(),
    name: z.any(),
    value: z.any(),
    visibility: z.any(),
    selectedRepositoryIds: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/actions/variables`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('selectedRepositoryIds' in mappedParams) {
        mappedParams['selected_repository_ids'] = mappedParams['selectedRepositoryIds']
        delete mappedParams['selectedRepositoryIds']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/get-org-variable',
  `Get an organization variable`,
  {
    org: z.string(),
    name: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, name, ...otherParams } = args
      const url = `/orgs/${org}/actions/variables/${name}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/update-org-variable',
  `Update an organization variable`,
  {
    org: z.string(),
    name: z.string(),
    value: z.any().optional(),
    visibility: z.any().optional(),
    selectedRepositoryIds: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { org, name, ...otherParams } = args
      const url = `/orgs/${org}/actions/variables/${name}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('selectedRepositoryIds' in mappedParams) {
        mappedParams['selected_repository_ids'] = mappedParams['selectedRepositoryIds']
        delete mappedParams['selectedRepositoryIds']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PATCH',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/delete-org-variable',
  `Delete an organization variable`,
  {
    org: z.string(),
    name: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, name, ...otherParams } = args
      const url = `/orgs/${org}/actions/variables/${name}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/list-selected-repos-for-org-variable',
  `List selected repositories for an organization variable`,
  {
    org: z.string(),
    name: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, name, ...otherParams } = args
      const url = `/orgs/${org}/actions/variables/${name}/repositories`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/set-selected-repos-for-org-variable',
  `Set selected repositories for an organization variable`,
  {
    org: z.string(),
    name: z.string(),
    selectedRepositoryIds: z.any(),
  },
  async (args, extra) => {
    try {
      const { org, name, ...otherParams } = args
      const url = `/orgs/${org}/actions/variables/${name}/repositories`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('selectedRepositoryIds' in mappedParams) {
        mappedParams['selected_repository_ids'] = mappedParams['selectedRepositoryIds']
        delete mappedParams['selectedRepositoryIds']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/add-selected-repo-to-org-variable',
  `Add selected repository to an organization variable`,
  {
    org: z.string(),
    name: z.string(),
    repositoryId: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, name, repositoryId, ...otherParams } = args
      const url = `/orgs/${org}/actions/variables/${name}/repositories/${repositoryId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('repositoryId' in mappedParams) {
        mappedParams['repository_id'] = mappedParams['repositoryId']
        delete mappedParams['repositoryId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/remove-selected-repo-from-org-variable',
  `Remove selected repository from an organization variable`,
  {
    org: z.string(),
    name: z.string(),
    repositoryId: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, name, repositoryId, ...otherParams } = args
      const url = `/orgs/${org}/actions/variables/${name}/repositories/${repositoryId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('repositoryId' in mappedParams) {
        mappedParams['repository_id'] = mappedParams['repositoryId']
        delete mappedParams['repositoryId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'orgs/list-attestations-bulk',
  `List attestations by bulk subject digests`,
  {
    org: z.string(),
    subjectDigests: z.any(),
    predicateType: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/attestations/bulk-list`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('subjectDigests' in mappedParams) {
        mappedParams['subject_digests'] = mappedParams['subjectDigests']
        delete mappedParams['subjectDigests']
      }
      if ('predicateType' in mappedParams) {
        mappedParams['predicate_type'] = mappedParams['predicateType']
        delete mappedParams['predicateType']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'orgs/delete-attestations-bulk',
  `Delete attestations in bulk`,
  {
    org: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/attestations/delete-request`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'orgs/delete-attestations-by-subject-digest',
  `Delete attestations by subject digest`,
  {
    org: z.string(),
    subjectDigest: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, subjectDigest, ...otherParams } = args
      const url = `/orgs/${org}/attestations/digest/${subjectDigest}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('subjectDigest' in mappedParams) {
        mappedParams['subject_digest'] = mappedParams['subjectDigest']
        delete mappedParams['subjectDigest']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'orgs/delete-attestations-by-id',
  `Delete attestations by ID`,
  {
    org: z.string(),
    attestationId: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, attestationId, ...otherParams } = args
      const url = `/orgs/${org}/attestations/${attestationId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('attestationId' in mappedParams) {
        mappedParams['attestation_id'] = mappedParams['attestationId']
        delete mappedParams['attestationId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'orgs/list-attestations',
  `List attestations`,
  {
    org: z.string(),
    subjectDigest: z.string(),
    predicateType: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const { org, subjectDigest, ...otherParams } = args
      const url = `/orgs/${org}/attestations/${subjectDigest}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('subjectDigest' in mappedParams) {
        mappedParams['subject_digest'] = mappedParams['subjectDigest']
        delete mappedParams['subjectDigest']
      }
      if ('predicateType' in mappedParams) {
        mappedParams['predicate_type'] = mappedParams['predicateType']
        delete mappedParams['predicateType']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'orgs/list-blocked-users',
  `List users blocked by an organization`,
  {
    org: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/blocks`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'orgs/check-blocked-user',
  `Check if a user is blocked by an organization`,
  {
    org: z.string(),
    username: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, username, ...otherParams } = args
      const url = `/orgs/${org}/blocks/${username}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'orgs/block-user',
  `Block a user from an organization`,
  {
    org: z.string(),
    username: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, username, ...otherParams } = args
      const url = `/orgs/${org}/blocks/${username}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'orgs/unblock-user',
  `Unblock a user from an organization`,
  {
    org: z.string(),
    username: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, username, ...otherParams } = args
      const url = `/orgs/${org}/blocks/${username}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'campaigns/list-org-campaigns',
  `List campaigns for an organization`,
  {
    org: z.string(),
    state: z.string().optional(),
    sort: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/campaigns`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'campaigns/create-campaign',
  `Create a campaign for an organization`,
  {
    org: z.string(),
    name: z.any(),
    description: z.any(),
    managers: z.any().optional(),
    teamManagers: z.any().optional(),
    endsAt: z.any(),
    contactLink: z.any().optional(),
    codeScanningAlerts: z.any(),
    generateIssues: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/campaigns`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('teamManagers' in mappedParams) {
        mappedParams['team_managers'] = mappedParams['teamManagers']
        delete mappedParams['teamManagers']
      }
      if ('endsAt' in mappedParams) {
        mappedParams['ends_at'] = mappedParams['endsAt']
        delete mappedParams['endsAt']
      }
      if ('contactLink' in mappedParams) {
        mappedParams['contact_link'] = mappedParams['contactLink']
        delete mappedParams['contactLink']
      }
      if ('codeScanningAlerts' in mappedParams) {
        mappedParams['code_scanning_alerts'] = mappedParams['codeScanningAlerts']
        delete mappedParams['codeScanningAlerts']
      }
      if ('generateIssues' in mappedParams) {
        mappedParams['generate_issues'] = mappedParams['generateIssues']
        delete mappedParams['generateIssues']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'campaigns/get-campaign-summary',
  `Get a campaign for an organization`,
  {
    org: z.string(),
    campaignNumber: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, campaignNumber, ...otherParams } = args
      const url = `/orgs/${org}/campaigns/${campaignNumber}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('campaignNumber' in mappedParams) {
        mappedParams['campaign_number'] = mappedParams['campaignNumber']
        delete mappedParams['campaignNumber']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'campaigns/update-campaign',
  `Update a campaign`,
  {
    org: z.string(),
    campaignNumber: z.string(),
    name: z.any().optional(),
    description: z.any().optional(),
    managers: z.any().optional(),
    teamManagers: z.any().optional(),
    endsAt: z.any().optional(),
    contactLink: z.any().optional(),
    state: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { org, campaignNumber, ...otherParams } = args
      const url = `/orgs/${org}/campaigns/${campaignNumber}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('campaignNumber' in mappedParams) {
        mappedParams['campaign_number'] = mappedParams['campaignNumber']
        delete mappedParams['campaignNumber']
      }
      if ('teamManagers' in mappedParams) {
        mappedParams['team_managers'] = mappedParams['teamManagers']
        delete mappedParams['teamManagers']
      }
      if ('endsAt' in mappedParams) {
        mappedParams['ends_at'] = mappedParams['endsAt']
        delete mappedParams['endsAt']
      }
      if ('contactLink' in mappedParams) {
        mappedParams['contact_link'] = mappedParams['contactLink']
        delete mappedParams['contactLink']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PATCH',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'campaigns/delete-campaign',
  `Delete a campaign for an organization`,
  {
    org: z.string(),
    campaignNumber: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, campaignNumber, ...otherParams } = args
      const url = `/orgs/${org}/campaigns/${campaignNumber}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('campaignNumber' in mappedParams) {
        mappedParams['campaign_number'] = mappedParams['campaignNumber']
        delete mappedParams['campaignNumber']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'code-scanning/list-alerts-for-org',
  `List code scanning alerts for an organization`,
  {
    org: z.string(),
    state: z.string().optional(),
    sort: z.string().optional(),
    severity: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/code-scanning/alerts`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'code-security/get-configurations-for-org',
  `Get code security configurations for an organization`,
  {
    org: z.string(),
    targetType: z.string().optional(),
    perPage: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/code-security/configurations`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('targetType' in mappedParams) {
        mappedParams['target_type'] = mappedParams['targetType']
        delete mappedParams['targetType']
      }
      if ('perPage' in mappedParams) {
        mappedParams['per_page'] = mappedParams['perPage']
        delete mappedParams['perPage']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'code-security/create-configuration',
  `Create a code security configuration`,
  {
    org: z.string(),
    name: z.any(),
    description: z.any(),
    advancedSecurity: z.any().optional(),
    codeSecurity: z.any().optional(),
    dependencyGraph: z.any().optional(),
    dependencyGraphAutosubmitAction: z.any().optional(),
    dependencyGraphAutosubmitActionOptions: z.any().optional(),
    dependabotAlerts: z.any().optional(),
    dependabotSecurityUpdates: z.any().optional(),
    codeScanningOptions: z.any().optional(),
    codeScanningDefaultSetup: z.any().optional(),
    codeScanningDefaultSetupOptions: z.any().optional(),
    codeScanningDelegatedAlertDismissal: z.any().optional(),
    secretProtection: z.any().optional(),
    secretScanning: z.any().optional(),
    secretScanningPushProtection: z.any().optional(),
    secretScanningDelegatedBypass: z.any().optional(),
    secretScanningDelegatedBypassOptions: z.any().optional(),
    secretScanningValidityChecks: z.any().optional(),
    secretScanningNonProviderPatterns: z.any().optional(),
    secretScanningGenericSecrets: z.any().optional(),
    secretScanningDelegatedAlertDismissal: z.any().optional(),
    privateVulnerabilityReporting: z.any().optional(),
    enforcement: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/code-security/configurations`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('advancedSecurity' in mappedParams) {
        mappedParams['advanced_security'] = mappedParams['advancedSecurity']
        delete mappedParams['advancedSecurity']
      }
      if ('codeSecurity' in mappedParams) {
        mappedParams['code_security'] = mappedParams['codeSecurity']
        delete mappedParams['codeSecurity']
      }
      if ('dependencyGraph' in mappedParams) {
        mappedParams['dependency_graph'] = mappedParams['dependencyGraph']
        delete mappedParams['dependencyGraph']
      }
      if ('dependencyGraphAutosubmitAction' in mappedParams) {
        mappedParams['dependency_graph_autosubmit_action'] =
          mappedParams['dependencyGraphAutosubmitAction']
        delete mappedParams['dependencyGraphAutosubmitAction']
      }
      if ('dependencyGraphAutosubmitActionOptions' in mappedParams) {
        mappedParams['dependency_graph_autosubmit_action_options'] =
          mappedParams['dependencyGraphAutosubmitActionOptions']
        delete mappedParams['dependencyGraphAutosubmitActionOptions']
      }
      if ('dependabotAlerts' in mappedParams) {
        mappedParams['dependabot_alerts'] = mappedParams['dependabotAlerts']
        delete mappedParams['dependabotAlerts']
      }
      if ('dependabotSecurityUpdates' in mappedParams) {
        mappedParams['dependabot_security_updates'] = mappedParams['dependabotSecurityUpdates']
        delete mappedParams['dependabotSecurityUpdates']
      }
      if ('codeScanningOptions' in mappedParams) {
        mappedParams['code_scanning_options'] = mappedParams['codeScanningOptions']
        delete mappedParams['codeScanningOptions']
      }
      if ('codeScanningDefaultSetup' in mappedParams) {
        mappedParams['code_scanning_default_setup'] = mappedParams['codeScanningDefaultSetup']
        delete mappedParams['codeScanningDefaultSetup']
      }
      if ('codeScanningDefaultSetupOptions' in mappedParams) {
        mappedParams['code_scanning_default_setup_options'] =
          mappedParams['codeScanningDefaultSetupOptions']
        delete mappedParams['codeScanningDefaultSetupOptions']
      }
      if ('codeScanningDelegatedAlertDismissal' in mappedParams) {
        mappedParams['code_scanning_delegated_alert_dismissal'] =
          mappedParams['codeScanningDelegatedAlertDismissal']
        delete mappedParams['codeScanningDelegatedAlertDismissal']
      }
      if ('secretProtection' in mappedParams) {
        mappedParams['secret_protection'] = mappedParams['secretProtection']
        delete mappedParams['secretProtection']
      }
      if ('secretScanning' in mappedParams) {
        mappedParams['secret_scanning'] = mappedParams['secretScanning']
        delete mappedParams['secretScanning']
      }
      if ('secretScanningPushProtection' in mappedParams) {
        mappedParams['secret_scanning_push_protection'] =
          mappedParams['secretScanningPushProtection']
        delete mappedParams['secretScanningPushProtection']
      }
      if ('secretScanningDelegatedBypass' in mappedParams) {
        mappedParams['secret_scanning_delegated_bypass'] =
          mappedParams['secretScanningDelegatedBypass']
        delete mappedParams['secretScanningDelegatedBypass']
      }
      if ('secretScanningDelegatedBypassOptions' in mappedParams) {
        mappedParams['secret_scanning_delegated_bypass_options'] =
          mappedParams['secretScanningDelegatedBypassOptions']
        delete mappedParams['secretScanningDelegatedBypassOptions']
      }
      if ('secretScanningValidityChecks' in mappedParams) {
        mappedParams['secret_scanning_validity_checks'] =
          mappedParams['secretScanningValidityChecks']
        delete mappedParams['secretScanningValidityChecks']
      }
      if ('secretScanningNonProviderPatterns' in mappedParams) {
        mappedParams['secret_scanning_non_provider_patterns'] =
          mappedParams['secretScanningNonProviderPatterns']
        delete mappedParams['secretScanningNonProviderPatterns']
      }
      if ('secretScanningGenericSecrets' in mappedParams) {
        mappedParams['secret_scanning_generic_secrets'] =
          mappedParams['secretScanningGenericSecrets']
        delete mappedParams['secretScanningGenericSecrets']
      }
      if ('secretScanningDelegatedAlertDismissal' in mappedParams) {
        mappedParams['secret_scanning_delegated_alert_dismissal'] =
          mappedParams['secretScanningDelegatedAlertDismissal']
        delete mappedParams['secretScanningDelegatedAlertDismissal']
      }
      if ('privateVulnerabilityReporting' in mappedParams) {
        mappedParams['private_vulnerability_reporting'] =
          mappedParams['privateVulnerabilityReporting']
        delete mappedParams['privateVulnerabilityReporting']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'code-security/get-default-configurations',
  `Get default code security configurations`,
  {
    org: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/code-security/configurations/defaults`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'code-security/detach-configuration',
  `Detach configurations from repositories`,
  {
    org: z.string(),
    selectedRepositoryIds: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/code-security/configurations/detach`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('selectedRepositoryIds' in mappedParams) {
        mappedParams['selected_repository_ids'] = mappedParams['selectedRepositoryIds']
        delete mappedParams['selectedRepositoryIds']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'code-security/get-configuration',
  `Get a code security configuration`,
  {
    org: z.string(),
    configurationId: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, configurationId, ...otherParams } = args
      const url = `/orgs/${org}/code-security/configurations/${configurationId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('configurationId' in mappedParams) {
        mappedParams['configuration_id'] = mappedParams['configurationId']
        delete mappedParams['configurationId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'code-security/update-configuration',
  `Update a code security configuration`,
  {
    org: z.string(),
    configurationId: z.string(),
    name: z.any().optional(),
    description: z.any().optional(),
    advancedSecurity: z.any().optional(),
    codeSecurity: z.any().optional(),
    dependencyGraph: z.any().optional(),
    dependencyGraphAutosubmitAction: z.any().optional(),
    dependencyGraphAutosubmitActionOptions: z.any().optional(),
    dependabotAlerts: z.any().optional(),
    dependabotSecurityUpdates: z.any().optional(),
    codeScanningDefaultSetup: z.any().optional(),
    codeScanningDefaultSetupOptions: z.any().optional(),
    codeScanningDelegatedAlertDismissal: z.any().optional(),
    secretProtection: z.any().optional(),
    secretScanning: z.any().optional(),
    secretScanningPushProtection: z.any().optional(),
    secretScanningDelegatedBypass: z.any().optional(),
    secretScanningDelegatedBypassOptions: z.any().optional(),
    secretScanningValidityChecks: z.any().optional(),
    secretScanningNonProviderPatterns: z.any().optional(),
    secretScanningGenericSecrets: z.any().optional(),
    secretScanningDelegatedAlertDismissal: z.any().optional(),
    privateVulnerabilityReporting: z.any().optional(),
    enforcement: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { org, configurationId, ...otherParams } = args
      const url = `/orgs/${org}/code-security/configurations/${configurationId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('configurationId' in mappedParams) {
        mappedParams['configuration_id'] = mappedParams['configurationId']
        delete mappedParams['configurationId']
      }
      if ('advancedSecurity' in mappedParams) {
        mappedParams['advanced_security'] = mappedParams['advancedSecurity']
        delete mappedParams['advancedSecurity']
      }
      if ('codeSecurity' in mappedParams) {
        mappedParams['code_security'] = mappedParams['codeSecurity']
        delete mappedParams['codeSecurity']
      }
      if ('dependencyGraph' in mappedParams) {
        mappedParams['dependency_graph'] = mappedParams['dependencyGraph']
        delete mappedParams['dependencyGraph']
      }
      if ('dependencyGraphAutosubmitAction' in mappedParams) {
        mappedParams['dependency_graph_autosubmit_action'] =
          mappedParams['dependencyGraphAutosubmitAction']
        delete mappedParams['dependencyGraphAutosubmitAction']
      }
      if ('dependencyGraphAutosubmitActionOptions' in mappedParams) {
        mappedParams['dependency_graph_autosubmit_action_options'] =
          mappedParams['dependencyGraphAutosubmitActionOptions']
        delete mappedParams['dependencyGraphAutosubmitActionOptions']
      }
      if ('dependabotAlerts' in mappedParams) {
        mappedParams['dependabot_alerts'] = mappedParams['dependabotAlerts']
        delete mappedParams['dependabotAlerts']
      }
      if ('dependabotSecurityUpdates' in mappedParams) {
        mappedParams['dependabot_security_updates'] = mappedParams['dependabotSecurityUpdates']
        delete mappedParams['dependabotSecurityUpdates']
      }
      if ('codeScanningDefaultSetup' in mappedParams) {
        mappedParams['code_scanning_default_setup'] = mappedParams['codeScanningDefaultSetup']
        delete mappedParams['codeScanningDefaultSetup']
      }
      if ('codeScanningDefaultSetupOptions' in mappedParams) {
        mappedParams['code_scanning_default_setup_options'] =
          mappedParams['codeScanningDefaultSetupOptions']
        delete mappedParams['codeScanningDefaultSetupOptions']
      }
      if ('codeScanningDelegatedAlertDismissal' in mappedParams) {
        mappedParams['code_scanning_delegated_alert_dismissal'] =
          mappedParams['codeScanningDelegatedAlertDismissal']
        delete mappedParams['codeScanningDelegatedAlertDismissal']
      }
      if ('secretProtection' in mappedParams) {
        mappedParams['secret_protection'] = mappedParams['secretProtection']
        delete mappedParams['secretProtection']
      }
      if ('secretScanning' in mappedParams) {
        mappedParams['secret_scanning'] = mappedParams['secretScanning']
        delete mappedParams['secretScanning']
      }
      if ('secretScanningPushProtection' in mappedParams) {
        mappedParams['secret_scanning_push_protection'] =
          mappedParams['secretScanningPushProtection']
        delete mappedParams['secretScanningPushProtection']
      }
      if ('secretScanningDelegatedBypass' in mappedParams) {
        mappedParams['secret_scanning_delegated_bypass'] =
          mappedParams['secretScanningDelegatedBypass']
        delete mappedParams['secretScanningDelegatedBypass']
      }
      if ('secretScanningDelegatedBypassOptions' in mappedParams) {
        mappedParams['secret_scanning_delegated_bypass_options'] =
          mappedParams['secretScanningDelegatedBypassOptions']
        delete mappedParams['secretScanningDelegatedBypassOptions']
      }
      if ('secretScanningValidityChecks' in mappedParams) {
        mappedParams['secret_scanning_validity_checks'] =
          mappedParams['secretScanningValidityChecks']
        delete mappedParams['secretScanningValidityChecks']
      }
      if ('secretScanningNonProviderPatterns' in mappedParams) {
        mappedParams['secret_scanning_non_provider_patterns'] =
          mappedParams['secretScanningNonProviderPatterns']
        delete mappedParams['secretScanningNonProviderPatterns']
      }
      if ('secretScanningGenericSecrets' in mappedParams) {
        mappedParams['secret_scanning_generic_secrets'] =
          mappedParams['secretScanningGenericSecrets']
        delete mappedParams['secretScanningGenericSecrets']
      }
      if ('secretScanningDelegatedAlertDismissal' in mappedParams) {
        mappedParams['secret_scanning_delegated_alert_dismissal'] =
          mappedParams['secretScanningDelegatedAlertDismissal']
        delete mappedParams['secretScanningDelegatedAlertDismissal']
      }
      if ('privateVulnerabilityReporting' in mappedParams) {
        mappedParams['private_vulnerability_reporting'] =
          mappedParams['privateVulnerabilityReporting']
        delete mappedParams['privateVulnerabilityReporting']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PATCH',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'code-security/delete-configuration',
  `Delete a code security configuration`,
  {
    org: z.string(),
    configurationId: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, configurationId, ...otherParams } = args
      const url = `/orgs/${org}/code-security/configurations/${configurationId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('configurationId' in mappedParams) {
        mappedParams['configuration_id'] = mappedParams['configurationId']
        delete mappedParams['configurationId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'code-security/attach-configuration',
  `Attach a configuration to repositories`,
  {
    org: z.string(),
    configurationId: z.string(),
    scope: z.any(),
    selectedRepositoryIds: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { org, configurationId, ...otherParams } = args
      const url = `/orgs/${org}/code-security/configurations/${configurationId}/attach`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('configurationId' in mappedParams) {
        mappedParams['configuration_id'] = mappedParams['configurationId']
        delete mappedParams['configurationId']
      }
      if ('selectedRepositoryIds' in mappedParams) {
        mappedParams['selected_repository_ids'] = mappedParams['selectedRepositoryIds']
        delete mappedParams['selectedRepositoryIds']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'code-security/set-configuration-as-default',
  `Set a code security configuration as a default for an organization`,
  {
    org: z.string(),
    configurationId: z.string(),
    defaultForNewRepos: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { org, configurationId, ...otherParams } = args
      const url = `/orgs/${org}/code-security/configurations/${configurationId}/defaults`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('configurationId' in mappedParams) {
        mappedParams['configuration_id'] = mappedParams['configurationId']
        delete mappedParams['configurationId']
      }
      if ('defaultForNewRepos' in mappedParams) {
        mappedParams['default_for_new_repos'] = mappedParams['defaultForNewRepos']
        delete mappedParams['defaultForNewRepos']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'code-security/get-repositories-for-configuration',
  `Get repositories associated with a code security configuration`,
  {
    org: z.string(),
    configurationId: z.string(),
    perPage: z.string().optional(),
    status: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const { org, configurationId, ...otherParams } = args
      const url = `/orgs/${org}/code-security/configurations/${configurationId}/repositories`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('configurationId' in mappedParams) {
        mappedParams['configuration_id'] = mappedParams['configurationId']
        delete mappedParams['configurationId']
      }
      if ('perPage' in mappedParams) {
        mappedParams['per_page'] = mappedParams['perPage']
        delete mappedParams['perPage']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'codespaces/list-in-organization',
  `List codespaces for the organization`,
  {
    org: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/codespaces`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'codespaces/set-codespaces-access',
  `Manage access control for organization codespaces`,
  {
    org: z.string(),
    visibility: z.any(),
    selectedUsernames: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/codespaces/access`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('selectedUsernames' in mappedParams) {
        mappedParams['selected_usernames'] = mappedParams['selectedUsernames']
        delete mappedParams['selectedUsernames']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'codespaces/set-codespaces-access-users',
  `Add users to Codespaces access for an organization`,
  {
    org: z.string(),
    selectedUsernames: z.any(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/codespaces/access/selected_users`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('selectedUsernames' in mappedParams) {
        mappedParams['selected_usernames'] = mappedParams['selectedUsernames']
        delete mappedParams['selectedUsernames']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'codespaces/delete-codespaces-access-users',
  `Remove users from Codespaces access for an organization`,
  {
    org: z.string(),
    selectedUsernames: z.any(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/codespaces/access/selected_users`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('selectedUsernames' in mappedParams) {
        mappedParams['selected_usernames'] = mappedParams['selectedUsernames']
        delete mappedParams['selectedUsernames']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'codespaces/list-org-secrets',
  `List organization secrets`,
  {
    org: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/codespaces/secrets`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'codespaces/get-org-public-key',
  `Get an organization public key`,
  {
    org: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/codespaces/secrets/public-key`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'codespaces/get-org-secret',
  `Get an organization secret`,
  {
    org: z.string(),
    secretName: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, secretName, ...otherParams } = args
      const url = `/orgs/${org}/codespaces/secrets/${secretName}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('secretName' in mappedParams) {
        mappedParams['secret_name'] = mappedParams['secretName']
        delete mappedParams['secretName']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'codespaces/create-or-update-org-secret',
  `Create or update an organization secret`,
  {
    org: z.string(),
    secretName: z.string(),
    encryptedValue: z.any().optional(),
    keyId: z.any().optional(),
    visibility: z.any(),
    selectedRepositoryIds: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { org, secretName, ...otherParams } = args
      const url = `/orgs/${org}/codespaces/secrets/${secretName}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('secretName' in mappedParams) {
        mappedParams['secret_name'] = mappedParams['secretName']
        delete mappedParams['secretName']
      }
      if ('encryptedValue' in mappedParams) {
        mappedParams['encrypted_value'] = mappedParams['encryptedValue']
        delete mappedParams['encryptedValue']
      }
      if ('keyId' in mappedParams) {
        mappedParams['key_id'] = mappedParams['keyId']
        delete mappedParams['keyId']
      }
      if ('selectedRepositoryIds' in mappedParams) {
        mappedParams['selected_repository_ids'] = mappedParams['selectedRepositoryIds']
        delete mappedParams['selectedRepositoryIds']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'codespaces/delete-org-secret',
  `Delete an organization secret`,
  {
    org: z.string(),
    secretName: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, secretName, ...otherParams } = args
      const url = `/orgs/${org}/codespaces/secrets/${secretName}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('secretName' in mappedParams) {
        mappedParams['secret_name'] = mappedParams['secretName']
        delete mappedParams['secretName']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'codespaces/list-selected-repos-for-org-secret',
  `List selected repositories for an organization secret`,
  {
    org: z.string(),
    secretName: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, secretName, ...otherParams } = args
      const url = `/orgs/${org}/codespaces/secrets/${secretName}/repositories`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('secretName' in mappedParams) {
        mappedParams['secret_name'] = mappedParams['secretName']
        delete mappedParams['secretName']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'codespaces/set-selected-repos-for-org-secret',
  `Set selected repositories for an organization secret`,
  {
    org: z.string(),
    secretName: z.string(),
    selectedRepositoryIds: z.any(),
  },
  async (args, extra) => {
    try {
      const { org, secretName, ...otherParams } = args
      const url = `/orgs/${org}/codespaces/secrets/${secretName}/repositories`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('secretName' in mappedParams) {
        mappedParams['secret_name'] = mappedParams['secretName']
        delete mappedParams['secretName']
      }
      if ('selectedRepositoryIds' in mappedParams) {
        mappedParams['selected_repository_ids'] = mappedParams['selectedRepositoryIds']
        delete mappedParams['selectedRepositoryIds']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'codespaces/add-selected-repo-to-org-secret',
  `Add selected repository to an organization secret`,
  {
    org: z.string(),
    secretName: z.string(),
    repositoryId: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, secretName, repositoryId, ...otherParams } = args
      const url = `/orgs/${org}/codespaces/secrets/${secretName}/repositories/${repositoryId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('secretName' in mappedParams) {
        mappedParams['secret_name'] = mappedParams['secretName']
        delete mappedParams['secretName']
      }
      if ('repositoryId' in mappedParams) {
        mappedParams['repository_id'] = mappedParams['repositoryId']
        delete mappedParams['repositoryId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'codespaces/remove-selected-repo-from-org-secret',
  `Remove selected repository from an organization secret`,
  {
    org: z.string(),
    secretName: z.string(),
    repositoryId: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, secretName, repositoryId, ...otherParams } = args
      const url = `/orgs/${org}/codespaces/secrets/${secretName}/repositories/${repositoryId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('secretName' in mappedParams) {
        mappedParams['secret_name'] = mappedParams['secretName']
        delete mappedParams['secretName']
      }
      if ('repositoryId' in mappedParams) {
        mappedParams['repository_id'] = mappedParams['repositoryId']
        delete mappedParams['repositoryId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'copilot/get-copilot-organization-details',
  `Get Copilot seat information and settings for an organization`,
  {
    org: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/copilot/billing`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'copilot/list-copilot-seats',
  `List all Copilot seat assignments for an organization`,
  {
    org: z.string(),
    perPage: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/copilot/billing/seats`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('perPage' in mappedParams) {
        mappedParams['per_page'] = mappedParams['perPage']
        delete mappedParams['perPage']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'copilot/add-copilot-seats-for-teams',
  `Add teams to the Copilot subscription for an organization`,
  {
    org: z.string(),
    selectedTeams: z.any(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/copilot/billing/selected_teams`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('selectedTeams' in mappedParams) {
        mappedParams['selected_teams'] = mappedParams['selectedTeams']
        delete mappedParams['selectedTeams']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'copilot/cancel-copilot-seat-assignment-for-teams',
  `Remove teams from the Copilot subscription for an organization`,
  {
    org: z.string(),
    selectedTeams: z.any(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/copilot/billing/selected_teams`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('selectedTeams' in mappedParams) {
        mappedParams['selected_teams'] = mappedParams['selectedTeams']
        delete mappedParams['selectedTeams']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'copilot/add-copilot-seats-for-users',
  `Add users to the Copilot subscription for an organization`,
  {
    org: z.string(),
    selectedUsernames: z.any(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/copilot/billing/selected_users`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('selectedUsernames' in mappedParams) {
        mappedParams['selected_usernames'] = mappedParams['selectedUsernames']
        delete mappedParams['selectedUsernames']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'copilot/cancel-copilot-seat-assignment-for-users',
  `Remove users from the Copilot subscription for an organization`,
  {
    org: z.string(),
    selectedUsernames: z.any(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/copilot/billing/selected_users`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('selectedUsernames' in mappedParams) {
        mappedParams['selected_usernames'] = mappedParams['selectedUsernames']
        delete mappedParams['selectedUsernames']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'copilot/copilot-metrics-for-organization',
  `Get Copilot metrics for an organization`,
  {
    org: z.string(),
    since: z.string().optional(),
    until: z.string().optional(),
    perPage: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/copilot/metrics`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('perPage' in mappedParams) {
        mappedParams['per_page'] = mappedParams['perPage']
        delete mappedParams['perPage']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'dependabot/list-alerts-for-org',
  `List Dependabot alerts for an organization`,
  {
    org: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/dependabot/alerts`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'dependabot/list-org-secrets',
  `List organization secrets`,
  {
    org: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/dependabot/secrets`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'dependabot/get-org-public-key',
  `Get an organization public key`,
  {
    org: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/dependabot/secrets/public-key`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'dependabot/get-org-secret',
  `Get an organization secret`,
  {
    org: z.string(),
    secretName: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, secretName, ...otherParams } = args
      const url = `/orgs/${org}/dependabot/secrets/${secretName}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('secretName' in mappedParams) {
        mappedParams['secret_name'] = mappedParams['secretName']
        delete mappedParams['secretName']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'dependabot/create-or-update-org-secret',
  `Create or update an organization secret`,
  {
    org: z.string(),
    secretName: z.string(),
    encryptedValue: z.any().optional(),
    keyId: z.any().optional(),
    visibility: z.any(),
    selectedRepositoryIds: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { org, secretName, ...otherParams } = args
      const url = `/orgs/${org}/dependabot/secrets/${secretName}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('secretName' in mappedParams) {
        mappedParams['secret_name'] = mappedParams['secretName']
        delete mappedParams['secretName']
      }
      if ('encryptedValue' in mappedParams) {
        mappedParams['encrypted_value'] = mappedParams['encryptedValue']
        delete mappedParams['encryptedValue']
      }
      if ('keyId' in mappedParams) {
        mappedParams['key_id'] = mappedParams['keyId']
        delete mappedParams['keyId']
      }
      if ('selectedRepositoryIds' in mappedParams) {
        mappedParams['selected_repository_ids'] = mappedParams['selectedRepositoryIds']
        delete mappedParams['selectedRepositoryIds']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'dependabot/delete-org-secret',
  `Delete an organization secret`,
  {
    org: z.string(),
    secretName: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, secretName, ...otherParams } = args
      const url = `/orgs/${org}/dependabot/secrets/${secretName}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('secretName' in mappedParams) {
        mappedParams['secret_name'] = mappedParams['secretName']
        delete mappedParams['secretName']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'dependabot/list-selected-repos-for-org-secret',
  `List selected repositories for an organization secret`,
  {
    org: z.string(),
    secretName: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, secretName, ...otherParams } = args
      const url = `/orgs/${org}/dependabot/secrets/${secretName}/repositories`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('secretName' in mappedParams) {
        mappedParams['secret_name'] = mappedParams['secretName']
        delete mappedParams['secretName']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'dependabot/set-selected-repos-for-org-secret',
  `Set selected repositories for an organization secret`,
  {
    org: z.string(),
    secretName: z.string(),
    selectedRepositoryIds: z.any(),
  },
  async (args, extra) => {
    try {
      const { org, secretName, ...otherParams } = args
      const url = `/orgs/${org}/dependabot/secrets/${secretName}/repositories`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('secretName' in mappedParams) {
        mappedParams['secret_name'] = mappedParams['secretName']
        delete mappedParams['secretName']
      }
      if ('selectedRepositoryIds' in mappedParams) {
        mappedParams['selected_repository_ids'] = mappedParams['selectedRepositoryIds']
        delete mappedParams['selectedRepositoryIds']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'dependabot/add-selected-repo-to-org-secret',
  `Add selected repository to an organization secret`,
  {
    org: z.string(),
    secretName: z.string(),
    repositoryId: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, secretName, repositoryId, ...otherParams } = args
      const url = `/orgs/${org}/dependabot/secrets/${secretName}/repositories/${repositoryId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('secretName' in mappedParams) {
        mappedParams['secret_name'] = mappedParams['secretName']
        delete mappedParams['secretName']
      }
      if ('repositoryId' in mappedParams) {
        mappedParams['repository_id'] = mappedParams['repositoryId']
        delete mappedParams['repositoryId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'dependabot/remove-selected-repo-from-org-secret',
  `Remove selected repository from an organization secret`,
  {
    org: z.string(),
    secretName: z.string(),
    repositoryId: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, secretName, repositoryId, ...otherParams } = args
      const url = `/orgs/${org}/dependabot/secrets/${secretName}/repositories/${repositoryId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('secretName' in mappedParams) {
        mappedParams['secret_name'] = mappedParams['secretName']
        delete mappedParams['secretName']
      }
      if ('repositoryId' in mappedParams) {
        mappedParams['repository_id'] = mappedParams['repositoryId']
        delete mappedParams['repositoryId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'packages/list-docker-migration-conflicting-packages-for-organization',
  `Get list of conflicting packages during Docker migration for organization`,
  {
    org: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/docker/conflicts`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'activity/list-public-org-events',
  `List public organization events`,
  {
    org: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/events`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'orgs/list-failed-invitations',
  `List failed organization invitations`,
  {
    org: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/failed_invitations`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'orgs/list-webhooks',
  `List organization webhooks`,
  {
    org: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/hooks`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'orgs/create-webhook',
  `Create an organization webhook`,
  {
    org: z.string(),
    name: z.any(),
    config: z.any(),
    events: z.any().optional(),
    active: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/hooks`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'orgs/get-webhook',
  `Get an organization webhook`,
  {
    org: z.string(),
    hookId: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, hookId, ...otherParams } = args
      const url = `/orgs/${org}/hooks/${hookId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('hookId' in mappedParams) {
        mappedParams['hook_id'] = mappedParams['hookId']
        delete mappedParams['hookId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'orgs/update-webhook',
  `Update an organization webhook`,
  {
    org: z.string(),
    hookId: z.string(),
    config: z.any().optional(),
    events: z.any().optional(),
    active: z.any().optional(),
    name: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { org, hookId, ...otherParams } = args
      const url = `/orgs/${org}/hooks/${hookId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('hookId' in mappedParams) {
        mappedParams['hook_id'] = mappedParams['hookId']
        delete mappedParams['hookId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PATCH',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'orgs/delete-webhook',
  `Delete an organization webhook`,
  {
    org: z.string(),
    hookId: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, hookId, ...otherParams } = args
      const url = `/orgs/${org}/hooks/${hookId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('hookId' in mappedParams) {
        mappedParams['hook_id'] = mappedParams['hookId']
        delete mappedParams['hookId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'orgs/get-webhook-config-for-org',
  `Get a webhook configuration for an organization`,
  {
    org: z.string(),
    hookId: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, hookId, ...otherParams } = args
      const url = `/orgs/${org}/hooks/${hookId}/config`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('hookId' in mappedParams) {
        mappedParams['hook_id'] = mappedParams['hookId']
        delete mappedParams['hookId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'orgs/update-webhook-config-for-org',
  `Update a webhook configuration for an organization`,
  {
    org: z.string(),
    hookId: z.string(),
    url: z.any().optional(),
    contentType: z.any().optional(),
    secret: z.any().optional(),
    insecureSsl: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { org, hookId, ...otherParams } = args
      const url = `/orgs/${org}/hooks/${hookId}/config`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('hookId' in mappedParams) {
        mappedParams['hook_id'] = mappedParams['hookId']
        delete mappedParams['hookId']
      }
      if ('contentType' in mappedParams) {
        mappedParams['content_type'] = mappedParams['contentType']
        delete mappedParams['contentType']
      }
      if ('insecureSsl' in mappedParams) {
        mappedParams['insecure_ssl'] = mappedParams['insecureSsl']
        delete mappedParams['insecureSsl']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PATCH',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'orgs/list-webhook-deliveries',
  `List deliveries for an organization webhook`,
  {
    org: z.string(),
    hookId: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, hookId, ...otherParams } = args
      const url = `/orgs/${org}/hooks/${hookId}/deliveries`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('hookId' in mappedParams) {
        mappedParams['hook_id'] = mappedParams['hookId']
        delete mappedParams['hookId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'orgs/get-webhook-delivery',
  `Get a webhook delivery for an organization webhook`,
  {
    org: z.string(),
    hookId: z.string(),
    deliveryId: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, hookId, deliveryId, ...otherParams } = args
      const url = `/orgs/${org}/hooks/${hookId}/deliveries/${deliveryId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('hookId' in mappedParams) {
        mappedParams['hook_id'] = mappedParams['hookId']
        delete mappedParams['hookId']
      }
      if ('deliveryId' in mappedParams) {
        mappedParams['delivery_id'] = mappedParams['deliveryId']
        delete mappedParams['deliveryId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'orgs/redeliver-webhook-delivery',
  `Redeliver a delivery for an organization webhook`,
  {
    org: z.string(),
    hookId: z.string(),
    deliveryId: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, hookId, deliveryId, ...otherParams } = args
      const url = `/orgs/${org}/hooks/${hookId}/deliveries/${deliveryId}/attempts`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('hookId' in mappedParams) {
        mappedParams['hook_id'] = mappedParams['hookId']
        delete mappedParams['hookId']
      }
      if ('deliveryId' in mappedParams) {
        mappedParams['delivery_id'] = mappedParams['deliveryId']
        delete mappedParams['deliveryId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'orgs/ping-webhook',
  `Ping an organization webhook`,
  {
    org: z.string(),
    hookId: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, hookId, ...otherParams } = args
      const url = `/orgs/${org}/hooks/${hookId}/pings`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('hookId' in mappedParams) {
        mappedParams['hook_id'] = mappedParams['hookId']
        delete mappedParams['hookId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'api-insights/get-route-stats-by-actor',
  `Get route stats by actor`,
  {
    org: z.string(),
    actorType: z.string(),
    actorId: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, actorType, actorId, ...otherParams } = args
      const url = `/orgs/${org}/insights/api/route-stats/${actorType}/${actorId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('actorType' in mappedParams) {
        mappedParams['actor_type'] = mappedParams['actorType']
        delete mappedParams['actorType']
      }
      if ('actorId' in mappedParams) {
        mappedParams['actor_id'] = mappedParams['actorId']
        delete mappedParams['actorId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'api-insights/get-subject-stats',
  `Get subject stats`,
  {
    org: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/insights/api/subject-stats`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'api-insights/get-summary-stats',
  `Get summary stats`,
  {
    org: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/insights/api/summary-stats`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'api-insights/get-summary-stats-by-user',
  `Get summary stats by user`,
  {
    org: z.string(),
    userId: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, userId, ...otherParams } = args
      const url = `/orgs/${org}/insights/api/summary-stats/users/${userId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('userId' in mappedParams) {
        mappedParams['user_id'] = mappedParams['userId']
        delete mappedParams['userId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'api-insights/get-summary-stats-by-actor',
  `Get summary stats by actor`,
  {
    org: z.string(),
    actorType: z.string(),
    actorId: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, actorType, actorId, ...otherParams } = args
      const url = `/orgs/${org}/insights/api/summary-stats/${actorType}/${actorId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('actorType' in mappedParams) {
        mappedParams['actor_type'] = mappedParams['actorType']
        delete mappedParams['actorType']
      }
      if ('actorId' in mappedParams) {
        mappedParams['actor_id'] = mappedParams['actorId']
        delete mappedParams['actorId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'api-insights/get-time-stats',
  `Get time stats`,
  {
    org: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/insights/api/time-stats`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'api-insights/get-time-stats-by-user',
  `Get time stats by user`,
  {
    org: z.string(),
    userId: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, userId, ...otherParams } = args
      const url = `/orgs/${org}/insights/api/time-stats/users/${userId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('userId' in mappedParams) {
        mappedParams['user_id'] = mappedParams['userId']
        delete mappedParams['userId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'api-insights/get-time-stats-by-actor',
  `Get time stats by actor`,
  {
    org: z.string(),
    actorType: z.string(),
    actorId: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, actorType, actorId, ...otherParams } = args
      const url = `/orgs/${org}/insights/api/time-stats/${actorType}/${actorId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('actorType' in mappedParams) {
        mappedParams['actor_type'] = mappedParams['actorType']
        delete mappedParams['actorType']
      }
      if ('actorId' in mappedParams) {
        mappedParams['actor_id'] = mappedParams['actorId']
        delete mappedParams['actorId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'api-insights/get-user-stats',
  `Get user stats`,
  {
    org: z.string(),
    userId: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, userId, ...otherParams } = args
      const url = `/orgs/${org}/insights/api/user-stats/${userId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('userId' in mappedParams) {
        mappedParams['user_id'] = mappedParams['userId']
        delete mappedParams['userId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'apps/get-org-installation',
  `Get an organization installation for the authenticated app`,
  {
    org: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/installation`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'orgs/list-app-installations',
  `List app installations for an organization`,
  {
    org: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/installations`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'interactions/get-restrictions-for-org',
  `Get interaction restrictions for an organization`,
  {
    org: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/interaction-limits`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'interactions/set-restrictions-for-org',
  `Set interaction restrictions for an organization`,
  {
    org: z.string(),
    requestData: z.record(z.any()).optional(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/interaction-limits`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'interactions/remove-restrictions-for-org',
  `Remove interaction restrictions for an organization`,
  {
    org: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/interaction-limits`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'orgs/list-pending-invitations',
  `List pending organization invitations`,
  {
    org: z.string(),
    role: z.string().optional(),
    invitationSource: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/invitations`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('invitationSource' in mappedParams) {
        mappedParams['invitation_source'] = mappedParams['invitationSource']
        delete mappedParams['invitationSource']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'orgs/create-invitation',
  `Create an organization invitation`,
  {
    org: z.string(),
    inviteeId: z.any().optional(),
    email: z.any().optional(),
    role: z.any().optional(),
    teamIds: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/invitations`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('inviteeId' in mappedParams) {
        mappedParams['invitee_id'] = mappedParams['inviteeId']
        delete mappedParams['inviteeId']
      }
      if ('teamIds' in mappedParams) {
        mappedParams['team_ids'] = mappedParams['teamIds']
        delete mappedParams['teamIds']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'orgs/cancel-invitation',
  `Cancel an organization invitation`,
  {
    org: z.string(),
    invitationId: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, invitationId, ...otherParams } = args
      const url = `/orgs/${org}/invitations/${invitationId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('invitationId' in mappedParams) {
        mappedParams['invitation_id'] = mappedParams['invitationId']
        delete mappedParams['invitationId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'orgs/list-invitation-teams',
  `List organization invitation teams`,
  {
    org: z.string(),
    invitationId: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, invitationId, ...otherParams } = args
      const url = `/orgs/${org}/invitations/${invitationId}/teams`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('invitationId' in mappedParams) {
        mappedParams['invitation_id'] = mappedParams['invitationId']
        delete mappedParams['invitationId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'orgs/list-issue-types',
  `List issue types for an organization`,
  {
    org: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/issue-types`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'orgs/create-issue-type',
  `Create issue type for an organization`,
  {
    org: z.string(),
    requestData: z.record(z.any()).optional(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/issue-types`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'orgs/update-issue-type',
  `Update issue type for an organization`,
  {
    org: z.string(),
    issueTypeId: z.string(),
    requestData: z.record(z.any()).optional(),
  },
  async (args, extra) => {
    try {
      const { org, issueTypeId, ...otherParams } = args
      const url = `/orgs/${org}/issue-types/${issueTypeId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('issueTypeId' in mappedParams) {
        mappedParams['issue_type_id'] = mappedParams['issueTypeId']
        delete mappedParams['issueTypeId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'orgs/delete-issue-type',
  `Delete issue type for an organization`,
  {
    org: z.string(),
    issueTypeId: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, issueTypeId, ...otherParams } = args
      const url = `/orgs/${org}/issue-types/${issueTypeId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('issueTypeId' in mappedParams) {
        mappedParams['issue_type_id'] = mappedParams['issueTypeId']
        delete mappedParams['issueTypeId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'issues/list-for-org',
  `List organization issues assigned to the authenticated user`,
  {
    org: z.string(),
    filter: z.string().optional(),
    state: z.string().optional(),
    type: z.string().optional(),
    sort: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/issues`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'orgs/list-members',
  `List organization members`,
  {
    org: z.string(),
    filter: z.string().optional(),
    role: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/members`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'orgs/check-membership-for-user',
  `Check organization membership for a user`,
  {
    org: z.string(),
    username: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, username, ...otherParams } = args
      const url = `/orgs/${org}/members/${username}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'orgs/remove-member',
  `Remove an organization member`,
  {
    org: z.string(),
    username: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, username, ...otherParams } = args
      const url = `/orgs/${org}/members/${username}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'codespaces/get-codespaces-for-user-in-org',
  `List codespaces for a user in organization`,
  {
    org: z.string(),
    username: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, username, ...otherParams } = args
      const url = `/orgs/${org}/members/${username}/codespaces`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'codespaces/delete-from-organization',
  `Delete a codespace from the organization`,
  {
    org: z.string(),
    username: z.string(),
    codespaceName: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, username, codespaceName, ...otherParams } = args
      const url = `/orgs/${org}/members/${username}/codespaces/${codespaceName}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('codespaceName' in mappedParams) {
        mappedParams['codespace_name'] = mappedParams['codespaceName']
        delete mappedParams['codespaceName']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'codespaces/stop-in-organization',
  `Stop a codespace for an organization user`,
  {
    org: z.string(),
    username: z.string(),
    codespaceName: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, username, codespaceName, ...otherParams } = args
      const url = `/orgs/${org}/members/${username}/codespaces/${codespaceName}/stop`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('codespaceName' in mappedParams) {
        mappedParams['codespace_name'] = mappedParams['codespaceName']
        delete mappedParams['codespaceName']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'copilot/get-copilot-seat-details-for-user',
  `Get Copilot seat assignment details for a user`,
  {
    org: z.string(),
    username: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, username, ...otherParams } = args
      const url = `/orgs/${org}/members/${username}/copilot`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'orgs/get-membership-for-user',
  `Get organization membership for a user`,
  {
    org: z.string(),
    username: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, username, ...otherParams } = args
      const url = `/orgs/${org}/memberships/${username}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'orgs/set-membership-for-user',
  `Set organization membership for a user`,
  {
    org: z.string(),
    username: z.string(),
    role: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { org, username, ...otherParams } = args
      const url = `/orgs/${org}/memberships/${username}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'orgs/remove-membership-for-user',
  `Remove organization membership for a user`,
  {
    org: z.string(),
    username: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, username, ...otherParams } = args
      const url = `/orgs/${org}/memberships/${username}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'migrations/list-for-org',
  `List organization migrations`,
  {
    org: z.string(),
    exclude: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/migrations`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'migrations/start-for-org',
  `Start an organization migration`,
  {
    org: z.string(),
    repositories: z.any(),
    lockRepositories: z.any().optional(),
    excludeMetadata: z.any().optional(),
    excludeGitData: z.any().optional(),
    excludeAttachments: z.any().optional(),
    excludeReleases: z.any().optional(),
    excludeOwnerProjects: z.any().optional(),
    orgMetadataOnly: z.any().optional(),
    exclude: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/migrations`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('lockRepositories' in mappedParams) {
        mappedParams['lock_repositories'] = mappedParams['lockRepositories']
        delete mappedParams['lockRepositories']
      }
      if ('excludeMetadata' in mappedParams) {
        mappedParams['exclude_metadata'] = mappedParams['excludeMetadata']
        delete mappedParams['excludeMetadata']
      }
      if ('excludeGitData' in mappedParams) {
        mappedParams['exclude_git_data'] = mappedParams['excludeGitData']
        delete mappedParams['excludeGitData']
      }
      if ('excludeAttachments' in mappedParams) {
        mappedParams['exclude_attachments'] = mappedParams['excludeAttachments']
        delete mappedParams['excludeAttachments']
      }
      if ('excludeReleases' in mappedParams) {
        mappedParams['exclude_releases'] = mappedParams['excludeReleases']
        delete mappedParams['excludeReleases']
      }
      if ('excludeOwnerProjects' in mappedParams) {
        mappedParams['exclude_owner_projects'] = mappedParams['excludeOwnerProjects']
        delete mappedParams['excludeOwnerProjects']
      }
      if ('orgMetadataOnly' in mappedParams) {
        mappedParams['org_metadata_only'] = mappedParams['orgMetadataOnly']
        delete mappedParams['orgMetadataOnly']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'migrations/get-status-for-org',
  `Get an organization migration status`,
  {
    org: z.string(),
    migrationId: z.string(),
    exclude: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const { org, migrationId, ...otherParams } = args
      const url = `/orgs/${org}/migrations/${migrationId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('migrationId' in mappedParams) {
        mappedParams['migration_id'] = mappedParams['migrationId']
        delete mappedParams['migrationId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'migrations/download-archive-for-org',
  `Download an organization migration archive`,
  {
    org: z.string(),
    migrationId: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, migrationId, ...otherParams } = args
      const url = `/orgs/${org}/migrations/${migrationId}/archive`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('migrationId' in mappedParams) {
        mappedParams['migration_id'] = mappedParams['migrationId']
        delete mappedParams['migrationId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'migrations/delete-archive-for-org',
  `Delete an organization migration archive`,
  {
    org: z.string(),
    migrationId: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, migrationId, ...otherParams } = args
      const url = `/orgs/${org}/migrations/${migrationId}/archive`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('migrationId' in mappedParams) {
        mappedParams['migration_id'] = mappedParams['migrationId']
        delete mappedParams['migrationId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'migrations/unlock-repo-for-org',
  `Unlock an organization repository`,
  {
    org: z.string(),
    migrationId: z.string(),
    repoName: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, migrationId, repoName, ...otherParams } = args
      const url = `/orgs/${org}/migrations/${migrationId}/repos/${repoName}/lock`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('migrationId' in mappedParams) {
        mappedParams['migration_id'] = mappedParams['migrationId']
        delete mappedParams['migrationId']
      }
      if ('repoName' in mappedParams) {
        mappedParams['repo_name'] = mappedParams['repoName']
        delete mappedParams['repoName']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'migrations/list-repos-for-org',
  `List repositories in an organization migration`,
  {
    org: z.string(),
    migrationId: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, migrationId, ...otherParams } = args
      const url = `/orgs/${org}/migrations/${migrationId}/repositories`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('migrationId' in mappedParams) {
        mappedParams['migration_id'] = mappedParams['migrationId']
        delete mappedParams['migrationId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'orgs/list-org-roles',
  `Get all organization roles for an organization`,
  {
    org: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/organization-roles`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'orgs/revoke-all-org-roles-team',
  `Remove all organization roles for a team`,
  {
    org: z.string(),
    teamSlug: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, teamSlug, ...otherParams } = args
      const url = `/orgs/${org}/organization-roles/teams/${teamSlug}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('teamSlug' in mappedParams) {
        mappedParams['team_slug'] = mappedParams['teamSlug']
        delete mappedParams['teamSlug']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'orgs/assign-team-to-org-role',
  `Assign an organization role to a team`,
  {
    org: z.string(),
    teamSlug: z.string(),
    roleId: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, teamSlug, roleId, ...otherParams } = args
      const url = `/orgs/${org}/organization-roles/teams/${teamSlug}/${roleId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('teamSlug' in mappedParams) {
        mappedParams['team_slug'] = mappedParams['teamSlug']
        delete mappedParams['teamSlug']
      }
      if ('roleId' in mappedParams) {
        mappedParams['role_id'] = mappedParams['roleId']
        delete mappedParams['roleId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'orgs/revoke-org-role-team',
  `Remove an organization role from a team`,
  {
    org: z.string(),
    teamSlug: z.string(),
    roleId: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, teamSlug, roleId, ...otherParams } = args
      const url = `/orgs/${org}/organization-roles/teams/${teamSlug}/${roleId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('teamSlug' in mappedParams) {
        mappedParams['team_slug'] = mappedParams['teamSlug']
        delete mappedParams['teamSlug']
      }
      if ('roleId' in mappedParams) {
        mappedParams['role_id'] = mappedParams['roleId']
        delete mappedParams['roleId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'orgs/revoke-all-org-roles-user',
  `Remove all organization roles for a user`,
  {
    org: z.string(),
    username: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, username, ...otherParams } = args
      const url = `/orgs/${org}/organization-roles/users/${username}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'orgs/assign-user-to-org-role',
  `Assign an organization role to a user`,
  {
    org: z.string(),
    username: z.string(),
    roleId: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, username, roleId, ...otherParams } = args
      const url = `/orgs/${org}/organization-roles/users/${username}/${roleId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('roleId' in mappedParams) {
        mappedParams['role_id'] = mappedParams['roleId']
        delete mappedParams['roleId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'orgs/revoke-org-role-user',
  `Remove an organization role from a user`,
  {
    org: z.string(),
    username: z.string(),
    roleId: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, username, roleId, ...otherParams } = args
      const url = `/orgs/${org}/organization-roles/users/${username}/${roleId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('roleId' in mappedParams) {
        mappedParams['role_id'] = mappedParams['roleId']
        delete mappedParams['roleId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'orgs/get-org-role',
  `Get an organization role`,
  {
    org: z.string(),
    roleId: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, roleId, ...otherParams } = args
      const url = `/orgs/${org}/organization-roles/${roleId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('roleId' in mappedParams) {
        mappedParams['role_id'] = mappedParams['roleId']
        delete mappedParams['roleId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'orgs/list-org-role-teams',
  `List teams that are assigned to an organization role`,
  {
    org: z.string(),
    roleId: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, roleId, ...otherParams } = args
      const url = `/orgs/${org}/organization-roles/${roleId}/teams`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('roleId' in mappedParams) {
        mappedParams['role_id'] = mappedParams['roleId']
        delete mappedParams['roleId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'orgs/list-org-role-users',
  `List users that are assigned to an organization role`,
  {
    org: z.string(),
    roleId: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, roleId, ...otherParams } = args
      const url = `/orgs/${org}/organization-roles/${roleId}/users`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('roleId' in mappedParams) {
        mappedParams['role_id'] = mappedParams['roleId']
        delete mappedParams['roleId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'orgs/list-outside-collaborators',
  `List outside collaborators for an organization`,
  {
    org: z.string(),
    filter: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/outside_collaborators`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'orgs/convert-member-to-outside-collaborator',
  `Convert an organization member to outside collaborator`,
  {
    org: z.string(),
    username: z.string(),
    async: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { org, username, ...otherParams } = args
      const url = `/orgs/${org}/outside_collaborators/${username}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'orgs/remove-outside-collaborator',
  `Remove outside collaborator from an organization`,
  {
    org: z.string(),
    username: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, username, ...otherParams } = args
      const url = `/orgs/${org}/outside_collaborators/${username}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'packages/list-packages-for-organization',
  `List packages for an organization`,
  {
    org: z.string(),
    packageType: z.string(),
    page: z.string().optional(),
    perPage: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/packages`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('packageType' in mappedParams) {
        mappedParams['package_type'] = mappedParams['packageType']
        delete mappedParams['packageType']
      }
      if ('perPage' in mappedParams) {
        mappedParams['per_page'] = mappedParams['perPage']
        delete mappedParams['perPage']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'packages/get-package-for-organization',
  `Get a package for an organization`,
  {
    org: z.string(),
    packageType: z.string(),
    packageName: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, packageType, packageName, ...otherParams } = args
      const url = `/orgs/${org}/packages/${packageType}/${packageName}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('packageType' in mappedParams) {
        mappedParams['package_type'] = mappedParams['packageType']
        delete mappedParams['packageType']
      }
      if ('packageName' in mappedParams) {
        mappedParams['package_name'] = mappedParams['packageName']
        delete mappedParams['packageName']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'packages/delete-package-for-org',
  `Delete a package for an organization`,
  {
    org: z.string(),
    packageType: z.string(),
    packageName: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, packageType, packageName, ...otherParams } = args
      const url = `/orgs/${org}/packages/${packageType}/${packageName}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('packageType' in mappedParams) {
        mappedParams['package_type'] = mappedParams['packageType']
        delete mappedParams['packageType']
      }
      if ('packageName' in mappedParams) {
        mappedParams['package_name'] = mappedParams['packageName']
        delete mappedParams['packageName']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'packages/restore-package-for-org',
  `Restore a package for an organization`,
  {
    org: z.string(),
    packageType: z.string(),
    packageName: z.string(),
    token: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const { org, packageType, packageName, ...otherParams } = args
      const url = `/orgs/${org}/packages/${packageType}/${packageName}/restore`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('packageType' in mappedParams) {
        mappedParams['package_type'] = mappedParams['packageType']
        delete mappedParams['packageType']
      }
      if ('packageName' in mappedParams) {
        mappedParams['package_name'] = mappedParams['packageName']
        delete mappedParams['packageName']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'packages/get-all-package-versions-for-package-owned-by-org',
  `List package versions for a package owned by an organization`,
  {
    org: z.string(),
    packageType: z.string(),
    packageName: z.string(),
    state: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const { org, packageType, packageName, ...otherParams } = args
      const url = `/orgs/${org}/packages/${packageType}/${packageName}/versions`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('packageType' in mappedParams) {
        mappedParams['package_type'] = mappedParams['packageType']
        delete mappedParams['packageType']
      }
      if ('packageName' in mappedParams) {
        mappedParams['package_name'] = mappedParams['packageName']
        delete mappedParams['packageName']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'packages/get-package-version-for-organization',
  `Get a package version for an organization`,
  {
    org: z.string(),
    packageType: z.string(),
    packageName: z.string(),
    packageVersionId: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, packageType, packageName, packageVersionId, ...otherParams } = args
      const url = `/orgs/${org}/packages/${packageType}/${packageName}/versions/${packageVersionId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('packageType' in mappedParams) {
        mappedParams['package_type'] = mappedParams['packageType']
        delete mappedParams['packageType']
      }
      if ('packageName' in mappedParams) {
        mappedParams['package_name'] = mappedParams['packageName']
        delete mappedParams['packageName']
      }
      if ('packageVersionId' in mappedParams) {
        mappedParams['package_version_id'] = mappedParams['packageVersionId']
        delete mappedParams['packageVersionId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'packages/delete-package-version-for-org',
  `Delete package version for an organization`,
  {
    org: z.string(),
    packageType: z.string(),
    packageName: z.string(),
    packageVersionId: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, packageType, packageName, packageVersionId, ...otherParams } = args
      const url = `/orgs/${org}/packages/${packageType}/${packageName}/versions/${packageVersionId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('packageType' in mappedParams) {
        mappedParams['package_type'] = mappedParams['packageType']
        delete mappedParams['packageType']
      }
      if ('packageName' in mappedParams) {
        mappedParams['package_name'] = mappedParams['packageName']
        delete mappedParams['packageName']
      }
      if ('packageVersionId' in mappedParams) {
        mappedParams['package_version_id'] = mappedParams['packageVersionId']
        delete mappedParams['packageVersionId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'packages/restore-package-version-for-org',
  `Restore package version for an organization`,
  {
    org: z.string(),
    packageType: z.string(),
    packageName: z.string(),
    packageVersionId: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, packageType, packageName, packageVersionId, ...otherParams } = args
      const url = `/orgs/${org}/packages/${packageType}/${packageName}/versions/${packageVersionId}/restore`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('packageType' in mappedParams) {
        mappedParams['package_type'] = mappedParams['packageType']
        delete mappedParams['packageType']
      }
      if ('packageName' in mappedParams) {
        mappedParams['package_name'] = mappedParams['packageName']
        delete mappedParams['packageName']
      }
      if ('packageVersionId' in mappedParams) {
        mappedParams['package_version_id'] = mappedParams['packageVersionId']
        delete mappedParams['packageVersionId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'orgs/list-pat-grant-requests',
  `List requests to access organization resources with fine-grained personal access tokens`,
  {
    org: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/personal-access-token-requests`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'orgs/review-pat-grant-requests-in-bulk',
  `Review requests to access organization resources with fine-grained personal access tokens`,
  {
    org: z.string(),
    patRequestIds: z.any().optional(),
    action: z.any(),
    reason: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/personal-access-token-requests`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('patRequestIds' in mappedParams) {
        mappedParams['pat_request_ids'] = mappedParams['patRequestIds']
        delete mappedParams['patRequestIds']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'orgs/review-pat-grant-request',
  `Review a request to access organization resources with a fine-grained personal access token`,
  {
    org: z.string(),
    patRequestId: z.string(),
    action: z.any(),
    reason: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { org, patRequestId, ...otherParams } = args
      const url = `/orgs/${org}/personal-access-token-requests/${patRequestId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('patRequestId' in mappedParams) {
        mappedParams['pat_request_id'] = mappedParams['patRequestId']
        delete mappedParams['patRequestId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'orgs/list-pat-grant-request-repositories',
  `List repositories requested to be accessed by a fine-grained personal access token`,
  {
    org: z.string(),
    patRequestId: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, patRequestId, ...otherParams } = args
      const url = `/orgs/${org}/personal-access-token-requests/${patRequestId}/repositories`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('patRequestId' in mappedParams) {
        mappedParams['pat_request_id'] = mappedParams['patRequestId']
        delete mappedParams['patRequestId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'orgs/list-pat-grants',
  `List fine-grained personal access tokens with access to organization resources`,
  {
    org: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/personal-access-tokens`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'orgs/update-pat-accesses',
  `Update the access to organization resources via fine-grained personal access tokens`,
  {
    org: z.string(),
    action: z.any(),
    patIds: z.any(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/personal-access-tokens`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('patIds' in mappedParams) {
        mappedParams['pat_ids'] = mappedParams['patIds']
        delete mappedParams['patIds']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'orgs/update-pat-access',
  `Update the access a fine-grained personal access token has to organization resources`,
  {
    org: z.string(),
    patId: z.string(),
    action: z.any(),
  },
  async (args, extra) => {
    try {
      const { org, patId, ...otherParams } = args
      const url = `/orgs/${org}/personal-access-tokens/${patId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('patId' in mappedParams) {
        mappedParams['pat_id'] = mappedParams['patId']
        delete mappedParams['patId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'orgs/list-pat-grant-repositories',
  `List repositories a fine-grained personal access token has access to`,
  {
    org: z.string(),
    patId: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, patId, ...otherParams } = args
      const url = `/orgs/${org}/personal-access-tokens/${patId}/repositories`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('patId' in mappedParams) {
        mappedParams['pat_id'] = mappedParams['patId']
        delete mappedParams['patId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'private-registries/list-org-private-registries',
  `List private registries for an organization`,
  {
    org: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/private-registries`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'private-registries/create-org-private-registry',
  `Create a private registry for an organization`,
  {
    org: z.string(),
    registryType: z.any(),
    url: z.any(),
    username: z.any().optional(),
    encryptedValue: z.any(),
    keyId: z.any(),
    visibility: z.any(),
    selectedRepositoryIds: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/private-registries`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('registryType' in mappedParams) {
        mappedParams['registry_type'] = mappedParams['registryType']
        delete mappedParams['registryType']
      }
      if ('encryptedValue' in mappedParams) {
        mappedParams['encrypted_value'] = mappedParams['encryptedValue']
        delete mappedParams['encryptedValue']
      }
      if ('keyId' in mappedParams) {
        mappedParams['key_id'] = mappedParams['keyId']
        delete mappedParams['keyId']
      }
      if ('selectedRepositoryIds' in mappedParams) {
        mappedParams['selected_repository_ids'] = mappedParams['selectedRepositoryIds']
        delete mappedParams['selectedRepositoryIds']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'private-registries/get-org-public-key',
  `Get private registries public key for an organization`,
  {
    org: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/private-registries/public-key`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'private-registries/get-org-private-registry',
  `Get a private registry for an organization`,
  {
    org: z.string(),
    secretName: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, secretName, ...otherParams } = args
      const url = `/orgs/${org}/private-registries/${secretName}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('secretName' in mappedParams) {
        mappedParams['secret_name'] = mappedParams['secretName']
        delete mappedParams['secretName']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'private-registries/update-org-private-registry',
  `Update a private registry for an organization`,
  {
    org: z.string(),
    secretName: z.string(),
    registryType: z.any().optional(),
    url: z.any().optional(),
    username: z.any().optional(),
    encryptedValue: z.any().optional(),
    keyId: z.any().optional(),
    visibility: z.any().optional(),
    selectedRepositoryIds: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { org, secretName, ...otherParams } = args
      const url = `/orgs/${org}/private-registries/${secretName}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('secretName' in mappedParams) {
        mappedParams['secret_name'] = mappedParams['secretName']
        delete mappedParams['secretName']
      }
      if ('registryType' in mappedParams) {
        mappedParams['registry_type'] = mappedParams['registryType']
        delete mappedParams['registryType']
      }
      if ('encryptedValue' in mappedParams) {
        mappedParams['encrypted_value'] = mappedParams['encryptedValue']
        delete mappedParams['encryptedValue']
      }
      if ('keyId' in mappedParams) {
        mappedParams['key_id'] = mappedParams['keyId']
        delete mappedParams['keyId']
      }
      if ('selectedRepositoryIds' in mappedParams) {
        mappedParams['selected_repository_ids'] = mappedParams['selectedRepositoryIds']
        delete mappedParams['selectedRepositoryIds']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PATCH',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'private-registries/delete-org-private-registry',
  `Delete a private registry for an organization`,
  {
    org: z.string(),
    secretName: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, secretName, ...otherParams } = args
      const url = `/orgs/${org}/private-registries/${secretName}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('secretName' in mappedParams) {
        mappedParams['secret_name'] = mappedParams['secretName']
        delete mappedParams['secretName']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'projects-classic/list-for-org',
  `List organization projects`,
  {
    org: z.string(),
    state: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/projects`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'projects-classic/create-for-org',
  `Create an organization project`,
  {
    org: z.string(),
    name: z.any(),
    body: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/projects`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'orgs/get-all-custom-properties',
  `Get all custom properties for an organization`,
  {
    org: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/properties/schema`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'orgs/create-or-update-custom-properties',
  `Create or update custom properties for an organization`,
  {
    org: z.string(),
    properties: z.any(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/properties/schema`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PATCH',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'orgs/get-custom-property',
  `Get a custom property for an organization`,
  {
    org: z.string(),
    customPropertyName: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, customPropertyName, ...otherParams } = args
      const url = `/orgs/${org}/properties/schema/${customPropertyName}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('customPropertyName' in mappedParams) {
        mappedParams['custom_property_name'] = mappedParams['customPropertyName']
        delete mappedParams['customPropertyName']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'orgs/create-or-update-custom-property',
  `Create or update a custom property for an organization`,
  {
    org: z.string(),
    customPropertyName: z.string(),
    requestData: z.record(z.any()).optional(),
  },
  async (args, extra) => {
    try {
      const { org, customPropertyName, ...otherParams } = args
      const url = `/orgs/${org}/properties/schema/${customPropertyName}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('customPropertyName' in mappedParams) {
        mappedParams['custom_property_name'] = mappedParams['customPropertyName']
        delete mappedParams['customPropertyName']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'orgs/remove-custom-property',
  `Remove a custom property for an organization`,
  {
    org: z.string(),
    customPropertyName: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, customPropertyName, ...otherParams } = args
      const url = `/orgs/${org}/properties/schema/${customPropertyName}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('customPropertyName' in mappedParams) {
        mappedParams['custom_property_name'] = mappedParams['customPropertyName']
        delete mappedParams['customPropertyName']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'orgs/list-custom-properties-values-for-repos',
  `List custom property values for organization repositories`,
  {
    org: z.string(),
    repositoryQuery: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/properties/values`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('repositoryQuery' in mappedParams) {
        mappedParams['repository_query'] = mappedParams['repositoryQuery']
        delete mappedParams['repositoryQuery']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'orgs/create-or-update-custom-properties-values-for-repos',
  `Create or update custom property values for organization repositories`,
  {
    org: z.string(),
    repositoryNames: z.any(),
    properties: z.any(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/properties/values`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('repositoryNames' in mappedParams) {
        mappedParams['repository_names'] = mappedParams['repositoryNames']
        delete mappedParams['repositoryNames']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PATCH',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'orgs/list-public-members',
  `List public organization members`,
  {
    org: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/public_members`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'orgs/check-public-membership-for-user',
  `Check public organization membership for a user`,
  {
    org: z.string(),
    username: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, username, ...otherParams } = args
      const url = `/orgs/${org}/public_members/${username}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'orgs/set-public-membership-for-authenticated-user',
  `Set public organization membership for the authenticated user`,
  {
    org: z.string(),
    username: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, username, ...otherParams } = args
      const url = `/orgs/${org}/public_members/${username}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'orgs/remove-public-membership-for-authenticated-user',
  `Remove public organization membership for the authenticated user`,
  {
    org: z.string(),
    username: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, username, ...otherParams } = args
      const url = `/orgs/${org}/public_members/${username}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/list-for-org',
  `List organization repositories`,
  {
    org: z.string(),
    type: z.string().optional(),
    sort: z.string().optional(),
    direction: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/repos`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/create-in-org',
  `Create an organization repository`,
  {
    org: z.string(),
    name: z.any(),
    description: z.any().optional(),
    homepage: z.any().optional(),
    private: z.any().optional(),
    visibility: z.any().optional(),
    hasIssues: z.any().optional(),
    hasProjects: z.any().optional(),
    hasWiki: z.any().optional(),
    hasDownloads: z.any().optional(),
    isTemplate: z.any().optional(),
    teamId: z.any().optional(),
    autoInit: z.any().optional(),
    gitignoreTemplate: z.any().optional(),
    licenseTemplate: z.any().optional(),
    allowSquashMerge: z.any().optional(),
    allowMergeCommit: z.any().optional(),
    allowRebaseMerge: z.any().optional(),
    allowAutoMerge: z.any().optional(),
    deleteBranchOnMerge: z.any().optional(),
    useSquashPrTitleAsDefault: z.any().optional(),
    squashMergeCommitTitle: z.any().optional(),
    squashMergeCommitMessage: z.any().optional(),
    mergeCommitTitle: z.any().optional(),
    mergeCommitMessage: z.any().optional(),
    customProperties: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/repos`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('hasIssues' in mappedParams) {
        mappedParams['has_issues'] = mappedParams['hasIssues']
        delete mappedParams['hasIssues']
      }
      if ('hasProjects' in mappedParams) {
        mappedParams['has_projects'] = mappedParams['hasProjects']
        delete mappedParams['hasProjects']
      }
      if ('hasWiki' in mappedParams) {
        mappedParams['has_wiki'] = mappedParams['hasWiki']
        delete mappedParams['hasWiki']
      }
      if ('hasDownloads' in mappedParams) {
        mappedParams['has_downloads'] = mappedParams['hasDownloads']
        delete mappedParams['hasDownloads']
      }
      if ('isTemplate' in mappedParams) {
        mappedParams['is_template'] = mappedParams['isTemplate']
        delete mappedParams['isTemplate']
      }
      if ('teamId' in mappedParams) {
        mappedParams['team_id'] = mappedParams['teamId']
        delete mappedParams['teamId']
      }
      if ('autoInit' in mappedParams) {
        mappedParams['auto_init'] = mappedParams['autoInit']
        delete mappedParams['autoInit']
      }
      if ('gitignoreTemplate' in mappedParams) {
        mappedParams['gitignore_template'] = mappedParams['gitignoreTemplate']
        delete mappedParams['gitignoreTemplate']
      }
      if ('licenseTemplate' in mappedParams) {
        mappedParams['license_template'] = mappedParams['licenseTemplate']
        delete mappedParams['licenseTemplate']
      }
      if ('allowSquashMerge' in mappedParams) {
        mappedParams['allow_squash_merge'] = mappedParams['allowSquashMerge']
        delete mappedParams['allowSquashMerge']
      }
      if ('allowMergeCommit' in mappedParams) {
        mappedParams['allow_merge_commit'] = mappedParams['allowMergeCommit']
        delete mappedParams['allowMergeCommit']
      }
      if ('allowRebaseMerge' in mappedParams) {
        mappedParams['allow_rebase_merge'] = mappedParams['allowRebaseMerge']
        delete mappedParams['allowRebaseMerge']
      }
      if ('allowAutoMerge' in mappedParams) {
        mappedParams['allow_auto_merge'] = mappedParams['allowAutoMerge']
        delete mappedParams['allowAutoMerge']
      }
      if ('deleteBranchOnMerge' in mappedParams) {
        mappedParams['delete_branch_on_merge'] = mappedParams['deleteBranchOnMerge']
        delete mappedParams['deleteBranchOnMerge']
      }
      if ('useSquashPrTitleAsDefault' in mappedParams) {
        mappedParams['use_squash_pr_title_as_default'] = mappedParams['useSquashPrTitleAsDefault']
        delete mappedParams['useSquashPrTitleAsDefault']
      }
      if ('squashMergeCommitTitle' in mappedParams) {
        mappedParams['squash_merge_commit_title'] = mappedParams['squashMergeCommitTitle']
        delete mappedParams['squashMergeCommitTitle']
      }
      if ('squashMergeCommitMessage' in mappedParams) {
        mappedParams['squash_merge_commit_message'] = mappedParams['squashMergeCommitMessage']
        delete mappedParams['squashMergeCommitMessage']
      }
      if ('mergeCommitTitle' in mappedParams) {
        mappedParams['merge_commit_title'] = mappedParams['mergeCommitTitle']
        delete mappedParams['mergeCommitTitle']
      }
      if ('mergeCommitMessage' in mappedParams) {
        mappedParams['merge_commit_message'] = mappedParams['mergeCommitMessage']
        delete mappedParams['mergeCommitMessage']
      }
      if ('customProperties' in mappedParams) {
        mappedParams['custom_properties'] = mappedParams['customProperties']
        delete mappedParams['customProperties']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/get-org-rulesets',
  `Get all organization repository rulesets`,
  {
    org: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/rulesets`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/create-org-ruleset',
  `Create an organization repository ruleset`,
  {
    org: z.string(),
    name: z.any(),
    target: z.any().optional(),
    enforcement: z.any(),
    bypassActors: z.any().optional(),
    conditions: z.any().optional(),
    rules: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/rulesets`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('bypassActors' in mappedParams) {
        mappedParams['bypass_actors'] = mappedParams['bypassActors']
        delete mappedParams['bypassActors']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/get-org-rule-suites',
  `List organization rule suites`,
  {
    org: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/rulesets/rule-suites`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/get-org-rule-suite',
  `Get an organization rule suite`,
  {
    org: z.string(),
    ruleSuiteId: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, ruleSuiteId, ...otherParams } = args
      const url = `/orgs/${org}/rulesets/rule-suites/${ruleSuiteId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('ruleSuiteId' in mappedParams) {
        mappedParams['rule_suite_id'] = mappedParams['ruleSuiteId']
        delete mappedParams['ruleSuiteId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/get-org-ruleset',
  `Get an organization repository ruleset`,
  {
    org: z.string(),
    rulesetId: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, rulesetId, ...otherParams } = args
      const url = `/orgs/${org}/rulesets/${rulesetId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('rulesetId' in mappedParams) {
        mappedParams['ruleset_id'] = mappedParams['rulesetId']
        delete mappedParams['rulesetId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/update-org-ruleset',
  `Update an organization repository ruleset`,
  {
    org: z.string(),
    rulesetId: z.string(),
    name: z.any().optional(),
    target: z.any().optional(),
    enforcement: z.any().optional(),
    bypassActors: z.any().optional(),
    conditions: z.any().optional(),
    rules: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { org, rulesetId, ...otherParams } = args
      const url = `/orgs/${org}/rulesets/${rulesetId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('rulesetId' in mappedParams) {
        mappedParams['ruleset_id'] = mappedParams['rulesetId']
        delete mappedParams['rulesetId']
      }
      if ('bypassActors' in mappedParams) {
        mappedParams['bypass_actors'] = mappedParams['bypassActors']
        delete mappedParams['bypassActors']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/delete-org-ruleset',
  `Delete an organization repository ruleset`,
  {
    org: z.string(),
    rulesetId: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, rulesetId, ...otherParams } = args
      const url = `/orgs/${org}/rulesets/${rulesetId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('rulesetId' in mappedParams) {
        mappedParams['ruleset_id'] = mappedParams['rulesetId']
        delete mappedParams['rulesetId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'orgs/get-org-ruleset-history',
  `Get organization ruleset history`,
  {
    org: z.string(),
    rulesetId: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, rulesetId, ...otherParams } = args
      const url = `/orgs/${org}/rulesets/${rulesetId}/history`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('rulesetId' in mappedParams) {
        mappedParams['ruleset_id'] = mappedParams['rulesetId']
        delete mappedParams['rulesetId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'orgs/get-org-ruleset-version',
  `Get organization ruleset version`,
  {
    org: z.string(),
    rulesetId: z.string(),
    versionId: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, rulesetId, versionId, ...otherParams } = args
      const url = `/orgs/${org}/rulesets/${rulesetId}/history/${versionId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('rulesetId' in mappedParams) {
        mappedParams['ruleset_id'] = mappedParams['rulesetId']
        delete mappedParams['rulesetId']
      }
      if ('versionId' in mappedParams) {
        mappedParams['version_id'] = mappedParams['versionId']
        delete mappedParams['versionId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'secret-scanning/list-alerts-for-org',
  `List secret scanning alerts for an organization`,
  {
    org: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/secret-scanning/alerts`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'security-advisories/list-org-repository-advisories',
  `List repository security advisories for an organization`,
  {
    org: z.string(),
    sort: z.string().optional(),
    perPage: z.string().optional(),
    state: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/security-advisories`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('perPage' in mappedParams) {
        mappedParams['per_page'] = mappedParams['perPage']
        delete mappedParams['perPage']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'orgs/list-security-manager-teams',
  `List security manager teams`,
  {
    org: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/security-managers`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'orgs/add-security-manager-team',
  `Add a security manager team`,
  {
    org: z.string(),
    teamSlug: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, teamSlug, ...otherParams } = args
      const url = `/orgs/${org}/security-managers/teams/${teamSlug}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('teamSlug' in mappedParams) {
        mappedParams['team_slug'] = mappedParams['teamSlug']
        delete mappedParams['teamSlug']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'orgs/remove-security-manager-team',
  `Remove a security manager team`,
  {
    org: z.string(),
    teamSlug: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, teamSlug, ...otherParams } = args
      const url = `/orgs/${org}/security-managers/teams/${teamSlug}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('teamSlug' in mappedParams) {
        mappedParams['team_slug'] = mappedParams['teamSlug']
        delete mappedParams['teamSlug']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'billing/get-github-actions-billing-org',
  `Get GitHub Actions billing for an organization`,
  {
    org: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/settings/billing/actions`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'billing/get-github-packages-billing-org',
  `Get GitHub Packages billing for an organization`,
  {
    org: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/settings/billing/packages`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'billing/get-shared-storage-billing-org',
  `Get shared storage billing for an organization`,
  {
    org: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/settings/billing/shared-storage`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'hosted-compute/list-network-configurations-for-org',
  `List hosted compute network configurations for an organization`,
  {
    org: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/settings/network-configurations`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'hosted-compute/create-network-configuration-for-org',
  `Create a hosted compute network configuration for an organization`,
  {
    org: z.string(),
    name: z.any(),
    computeService: z.any().optional(),
    networkSettingsIds: z.any(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/settings/network-configurations`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('computeService' in mappedParams) {
        mappedParams['compute_service'] = mappedParams['computeService']
        delete mappedParams['computeService']
      }
      if ('networkSettingsIds' in mappedParams) {
        mappedParams['network_settings_ids'] = mappedParams['networkSettingsIds']
        delete mappedParams['networkSettingsIds']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'hosted-compute/get-network-configuration-for-org',
  `Get a hosted compute network configuration for an organization`,
  {
    org: z.string(),
    networkConfigurationId: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, networkConfigurationId, ...otherParams } = args
      const url = `/orgs/${org}/settings/network-configurations/${networkConfigurationId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('networkConfigurationId' in mappedParams) {
        mappedParams['network_configuration_id'] = mappedParams['networkConfigurationId']
        delete mappedParams['networkConfigurationId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'hosted-compute/update-network-configuration-for-org',
  `Update a hosted compute network configuration for an organization`,
  {
    org: z.string(),
    networkConfigurationId: z.string(),
    name: z.any().optional(),
    computeService: z.any().optional(),
    networkSettingsIds: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { org, networkConfigurationId, ...otherParams } = args
      const url = `/orgs/${org}/settings/network-configurations/${networkConfigurationId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('networkConfigurationId' in mappedParams) {
        mappedParams['network_configuration_id'] = mappedParams['networkConfigurationId']
        delete mappedParams['networkConfigurationId']
      }
      if ('computeService' in mappedParams) {
        mappedParams['compute_service'] = mappedParams['computeService']
        delete mappedParams['computeService']
      }
      if ('networkSettingsIds' in mappedParams) {
        mappedParams['network_settings_ids'] = mappedParams['networkSettingsIds']
        delete mappedParams['networkSettingsIds']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PATCH',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'hosted-compute/delete-network-configuration-from-org',
  `Delete a hosted compute network configuration from an organization`,
  {
    org: z.string(),
    networkConfigurationId: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, networkConfigurationId, ...otherParams } = args
      const url = `/orgs/${org}/settings/network-configurations/${networkConfigurationId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('networkConfigurationId' in mappedParams) {
        mappedParams['network_configuration_id'] = mappedParams['networkConfigurationId']
        delete mappedParams['networkConfigurationId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'hosted-compute/get-network-settings-for-org',
  `Get a hosted compute network settings resource for an organization`,
  {
    org: z.string(),
    networkSettingsId: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, networkSettingsId, ...otherParams } = args
      const url = `/orgs/${org}/settings/network-settings/${networkSettingsId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('networkSettingsId' in mappedParams) {
        mappedParams['network_settings_id'] = mappedParams['networkSettingsId']
        delete mappedParams['networkSettingsId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'copilot/copilot-metrics-for-team',
  `Get Copilot metrics for a team`,
  {
    org: z.string(),
    teamSlug: z.string(),
    since: z.string().optional(),
    until: z.string().optional(),
    perPage: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const { org, teamSlug, ...otherParams } = args
      const url = `/orgs/${org}/team/${teamSlug}/copilot/metrics`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('teamSlug' in mappedParams) {
        mappedParams['team_slug'] = mappedParams['teamSlug']
        delete mappedParams['teamSlug']
      }
      if ('perPage' in mappedParams) {
        mappedParams['per_page'] = mappedParams['perPage']
        delete mappedParams['perPage']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'teams/list',
  `List teams`,
  {
    org: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/teams`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'teams/create',
  `Create a team`,
  {
    org: z.string(),
    name: z.any(),
    description: z.any().optional(),
    maintainers: z.any().optional(),
    repoNames: z.any().optional(),
    privacy: z.any().optional(),
    notificationSetting: z.any().optional(),
    permission: z.any().optional(),
    parentTeamId: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/orgs/${org}/teams`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('repoNames' in mappedParams) {
        mappedParams['repo_names'] = mappedParams['repoNames']
        delete mappedParams['repoNames']
      }
      if ('notificationSetting' in mappedParams) {
        mappedParams['notification_setting'] = mappedParams['notificationSetting']
        delete mappedParams['notificationSetting']
      }
      if ('parentTeamId' in mappedParams) {
        mappedParams['parent_team_id'] = mappedParams['parentTeamId']
        delete mappedParams['parentTeamId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'teams/get-by-name',
  `Get a team by name`,
  {
    org: z.string(),
    teamSlug: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, teamSlug, ...otherParams } = args
      const url = `/orgs/${org}/teams/${teamSlug}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('teamSlug' in mappedParams) {
        mappedParams['team_slug'] = mappedParams['teamSlug']
        delete mappedParams['teamSlug']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'teams/update-in-org',
  `Update a team`,
  {
    org: z.string(),
    teamSlug: z.string(),
    name: z.any().optional(),
    description: z.any().optional(),
    privacy: z.any().optional(),
    notificationSetting: z.any().optional(),
    permission: z.any().optional(),
    parentTeamId: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { org, teamSlug, ...otherParams } = args
      const url = `/orgs/${org}/teams/${teamSlug}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('teamSlug' in mappedParams) {
        mappedParams['team_slug'] = mappedParams['teamSlug']
        delete mappedParams['teamSlug']
      }
      if ('notificationSetting' in mappedParams) {
        mappedParams['notification_setting'] = mappedParams['notificationSetting']
        delete mappedParams['notificationSetting']
      }
      if ('parentTeamId' in mappedParams) {
        mappedParams['parent_team_id'] = mappedParams['parentTeamId']
        delete mappedParams['parentTeamId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PATCH',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'teams/delete-in-org',
  `Delete a team`,
  {
    org: z.string(),
    teamSlug: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, teamSlug, ...otherParams } = args
      const url = `/orgs/${org}/teams/${teamSlug}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('teamSlug' in mappedParams) {
        mappedParams['team_slug'] = mappedParams['teamSlug']
        delete mappedParams['teamSlug']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'teams/list-discussions-in-org',
  `List discussions`,
  {
    org: z.string(),
    teamSlug: z.string(),
    pinned: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const { org, teamSlug, ...otherParams } = args
      const url = `/orgs/${org}/teams/${teamSlug}/discussions`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('teamSlug' in mappedParams) {
        mappedParams['team_slug'] = mappedParams['teamSlug']
        delete mappedParams['teamSlug']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'teams/create-discussion-in-org',
  `Create a discussion`,
  {
    org: z.string(),
    teamSlug: z.string(),
    title: z.any(),
    body: z.any(),
    private: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { org, teamSlug, ...otherParams } = args
      const url = `/orgs/${org}/teams/${teamSlug}/discussions`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('teamSlug' in mappedParams) {
        mappedParams['team_slug'] = mappedParams['teamSlug']
        delete mappedParams['teamSlug']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'teams/get-discussion-in-org',
  `Get a discussion`,
  {
    org: z.string(),
    teamSlug: z.string(),
    discussionNumber: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, teamSlug, discussionNumber, ...otherParams } = args
      const url = `/orgs/${org}/teams/${teamSlug}/discussions/${discussionNumber}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('teamSlug' in mappedParams) {
        mappedParams['team_slug'] = mappedParams['teamSlug']
        delete mappedParams['teamSlug']
      }
      if ('discussionNumber' in mappedParams) {
        mappedParams['discussion_number'] = mappedParams['discussionNumber']
        delete mappedParams['discussionNumber']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'teams/update-discussion-in-org',
  `Update a discussion`,
  {
    org: z.string(),
    teamSlug: z.string(),
    discussionNumber: z.string(),
    title: z.any().optional(),
    body: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { org, teamSlug, discussionNumber, ...otherParams } = args
      const url = `/orgs/${org}/teams/${teamSlug}/discussions/${discussionNumber}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('teamSlug' in mappedParams) {
        mappedParams['team_slug'] = mappedParams['teamSlug']
        delete mappedParams['teamSlug']
      }
      if ('discussionNumber' in mappedParams) {
        mappedParams['discussion_number'] = mappedParams['discussionNumber']
        delete mappedParams['discussionNumber']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PATCH',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'teams/delete-discussion-in-org',
  `Delete a discussion`,
  {
    org: z.string(),
    teamSlug: z.string(),
    discussionNumber: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, teamSlug, discussionNumber, ...otherParams } = args
      const url = `/orgs/${org}/teams/${teamSlug}/discussions/${discussionNumber}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('teamSlug' in mappedParams) {
        mappedParams['team_slug'] = mappedParams['teamSlug']
        delete mappedParams['teamSlug']
      }
      if ('discussionNumber' in mappedParams) {
        mappedParams['discussion_number'] = mappedParams['discussionNumber']
        delete mappedParams['discussionNumber']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'teams/list-discussion-comments-in-org',
  `List discussion comments`,
  {
    org: z.string(),
    teamSlug: z.string(),
    discussionNumber: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, teamSlug, discussionNumber, ...otherParams } = args
      const url = `/orgs/${org}/teams/${teamSlug}/discussions/${discussionNumber}/comments`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('teamSlug' in mappedParams) {
        mappedParams['team_slug'] = mappedParams['teamSlug']
        delete mappedParams['teamSlug']
      }
      if ('discussionNumber' in mappedParams) {
        mappedParams['discussion_number'] = mappedParams['discussionNumber']
        delete mappedParams['discussionNumber']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'teams/create-discussion-comment-in-org',
  `Create a discussion comment`,
  {
    org: z.string(),
    teamSlug: z.string(),
    discussionNumber: z.string(),
    body: z.any(),
  },
  async (args, extra) => {
    try {
      const { org, teamSlug, discussionNumber, ...otherParams } = args
      const url = `/orgs/${org}/teams/${teamSlug}/discussions/${discussionNumber}/comments`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('teamSlug' in mappedParams) {
        mappedParams['team_slug'] = mappedParams['teamSlug']
        delete mappedParams['teamSlug']
      }
      if ('discussionNumber' in mappedParams) {
        mappedParams['discussion_number'] = mappedParams['discussionNumber']
        delete mappedParams['discussionNumber']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'teams/get-discussion-comment-in-org',
  `Get a discussion comment`,
  {
    org: z.string(),
    teamSlug: z.string(),
    discussionNumber: z.string(),
    commentNumber: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, teamSlug, discussionNumber, commentNumber, ...otherParams } = args
      const url = `/orgs/${org}/teams/${teamSlug}/discussions/${discussionNumber}/comments/${commentNumber}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('teamSlug' in mappedParams) {
        mappedParams['team_slug'] = mappedParams['teamSlug']
        delete mappedParams['teamSlug']
      }
      if ('discussionNumber' in mappedParams) {
        mappedParams['discussion_number'] = mappedParams['discussionNumber']
        delete mappedParams['discussionNumber']
      }
      if ('commentNumber' in mappedParams) {
        mappedParams['comment_number'] = mappedParams['commentNumber']
        delete mappedParams['commentNumber']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'teams/update-discussion-comment-in-org',
  `Update a discussion comment`,
  {
    org: z.string(),
    teamSlug: z.string(),
    discussionNumber: z.string(),
    commentNumber: z.string(),
    body: z.any(),
  },
  async (args, extra) => {
    try {
      const { org, teamSlug, discussionNumber, commentNumber, ...otherParams } = args
      const url = `/orgs/${org}/teams/${teamSlug}/discussions/${discussionNumber}/comments/${commentNumber}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('teamSlug' in mappedParams) {
        mappedParams['team_slug'] = mappedParams['teamSlug']
        delete mappedParams['teamSlug']
      }
      if ('discussionNumber' in mappedParams) {
        mappedParams['discussion_number'] = mappedParams['discussionNumber']
        delete mappedParams['discussionNumber']
      }
      if ('commentNumber' in mappedParams) {
        mappedParams['comment_number'] = mappedParams['commentNumber']
        delete mappedParams['commentNumber']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PATCH',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'teams/delete-discussion-comment-in-org',
  `Delete a discussion comment`,
  {
    org: z.string(),
    teamSlug: z.string(),
    discussionNumber: z.string(),
    commentNumber: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, teamSlug, discussionNumber, commentNumber, ...otherParams } = args
      const url = `/orgs/${org}/teams/${teamSlug}/discussions/${discussionNumber}/comments/${commentNumber}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('teamSlug' in mappedParams) {
        mappedParams['team_slug'] = mappedParams['teamSlug']
        delete mappedParams['teamSlug']
      }
      if ('discussionNumber' in mappedParams) {
        mappedParams['discussion_number'] = mappedParams['discussionNumber']
        delete mappedParams['discussionNumber']
      }
      if ('commentNumber' in mappedParams) {
        mappedParams['comment_number'] = mappedParams['commentNumber']
        delete mappedParams['commentNumber']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'reactions/list-for-team-discussion-comment-in-org',
  `List reactions for a team discussion comment`,
  {
    org: z.string(),
    teamSlug: z.string(),
    discussionNumber: z.string(),
    commentNumber: z.string(),
    content: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const { org, teamSlug, discussionNumber, commentNumber, ...otherParams } = args
      const url = `/orgs/${org}/teams/${teamSlug}/discussions/${discussionNumber}/comments/${commentNumber}/reactions`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('teamSlug' in mappedParams) {
        mappedParams['team_slug'] = mappedParams['teamSlug']
        delete mappedParams['teamSlug']
      }
      if ('discussionNumber' in mappedParams) {
        mappedParams['discussion_number'] = mappedParams['discussionNumber']
        delete mappedParams['discussionNumber']
      }
      if ('commentNumber' in mappedParams) {
        mappedParams['comment_number'] = mappedParams['commentNumber']
        delete mappedParams['commentNumber']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'reactions/create-for-team-discussion-comment-in-org',
  `Create reaction for a team discussion comment`,
  {
    org: z.string(),
    teamSlug: z.string(),
    discussionNumber: z.string(),
    commentNumber: z.string(),
    content: z.any(),
  },
  async (args, extra) => {
    try {
      const { org, teamSlug, discussionNumber, commentNumber, ...otherParams } = args
      const url = `/orgs/${org}/teams/${teamSlug}/discussions/${discussionNumber}/comments/${commentNumber}/reactions`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('teamSlug' in mappedParams) {
        mappedParams['team_slug'] = mappedParams['teamSlug']
        delete mappedParams['teamSlug']
      }
      if ('discussionNumber' in mappedParams) {
        mappedParams['discussion_number'] = mappedParams['discussionNumber']
        delete mappedParams['discussionNumber']
      }
      if ('commentNumber' in mappedParams) {
        mappedParams['comment_number'] = mappedParams['commentNumber']
        delete mappedParams['commentNumber']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'reactions/delete-for-team-discussion-comment',
  `Delete team discussion comment reaction`,
  {
    org: z.string(),
    teamSlug: z.string(),
    discussionNumber: z.string(),
    commentNumber: z.string(),
    reactionId: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, teamSlug, discussionNumber, commentNumber, reactionId, ...otherParams } = args
      const url = `/orgs/${org}/teams/${teamSlug}/discussions/${discussionNumber}/comments/${commentNumber}/reactions/${reactionId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('teamSlug' in mappedParams) {
        mappedParams['team_slug'] = mappedParams['teamSlug']
        delete mappedParams['teamSlug']
      }
      if ('discussionNumber' in mappedParams) {
        mappedParams['discussion_number'] = mappedParams['discussionNumber']
        delete mappedParams['discussionNumber']
      }
      if ('commentNumber' in mappedParams) {
        mappedParams['comment_number'] = mappedParams['commentNumber']
        delete mappedParams['commentNumber']
      }
      if ('reactionId' in mappedParams) {
        mappedParams['reaction_id'] = mappedParams['reactionId']
        delete mappedParams['reactionId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'reactions/list-for-team-discussion-in-org',
  `List reactions for a team discussion`,
  {
    org: z.string(),
    teamSlug: z.string(),
    discussionNumber: z.string(),
    content: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const { org, teamSlug, discussionNumber, ...otherParams } = args
      const url = `/orgs/${org}/teams/${teamSlug}/discussions/${discussionNumber}/reactions`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('teamSlug' in mappedParams) {
        mappedParams['team_slug'] = mappedParams['teamSlug']
        delete mappedParams['teamSlug']
      }
      if ('discussionNumber' in mappedParams) {
        mappedParams['discussion_number'] = mappedParams['discussionNumber']
        delete mappedParams['discussionNumber']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'reactions/create-for-team-discussion-in-org',
  `Create reaction for a team discussion`,
  {
    org: z.string(),
    teamSlug: z.string(),
    discussionNumber: z.string(),
    content: z.any(),
  },
  async (args, extra) => {
    try {
      const { org, teamSlug, discussionNumber, ...otherParams } = args
      const url = `/orgs/${org}/teams/${teamSlug}/discussions/${discussionNumber}/reactions`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('teamSlug' in mappedParams) {
        mappedParams['team_slug'] = mappedParams['teamSlug']
        delete mappedParams['teamSlug']
      }
      if ('discussionNumber' in mappedParams) {
        mappedParams['discussion_number'] = mappedParams['discussionNumber']
        delete mappedParams['discussionNumber']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'reactions/delete-for-team-discussion',
  `Delete team discussion reaction`,
  {
    org: z.string(),
    teamSlug: z.string(),
    discussionNumber: z.string(),
    reactionId: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, teamSlug, discussionNumber, reactionId, ...otherParams } = args
      const url = `/orgs/${org}/teams/${teamSlug}/discussions/${discussionNumber}/reactions/${reactionId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('teamSlug' in mappedParams) {
        mappedParams['team_slug'] = mappedParams['teamSlug']
        delete mappedParams['teamSlug']
      }
      if ('discussionNumber' in mappedParams) {
        mappedParams['discussion_number'] = mappedParams['discussionNumber']
        delete mappedParams['discussionNumber']
      }
      if ('reactionId' in mappedParams) {
        mappedParams['reaction_id'] = mappedParams['reactionId']
        delete mappedParams['reactionId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'teams/list-pending-invitations-in-org',
  `List pending team invitations`,
  {
    org: z.string(),
    teamSlug: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, teamSlug, ...otherParams } = args
      const url = `/orgs/${org}/teams/${teamSlug}/invitations`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('teamSlug' in mappedParams) {
        mappedParams['team_slug'] = mappedParams['teamSlug']
        delete mappedParams['teamSlug']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'teams/list-members-in-org',
  `List team members`,
  {
    org: z.string(),
    teamSlug: z.string(),
    role: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const { org, teamSlug, ...otherParams } = args
      const url = `/orgs/${org}/teams/${teamSlug}/members`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('teamSlug' in mappedParams) {
        mappedParams['team_slug'] = mappedParams['teamSlug']
        delete mappedParams['teamSlug']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'teams/get-membership-for-user-in-org',
  `Get team membership for a user`,
  {
    org: z.string(),
    teamSlug: z.string(),
    username: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, teamSlug, username, ...otherParams } = args
      const url = `/orgs/${org}/teams/${teamSlug}/memberships/${username}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('teamSlug' in mappedParams) {
        mappedParams['team_slug'] = mappedParams['teamSlug']
        delete mappedParams['teamSlug']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'teams/add-or-update-membership-for-user-in-org',
  `Add or update team membership for a user`,
  {
    org: z.string(),
    teamSlug: z.string(),
    username: z.string(),
    role: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { org, teamSlug, username, ...otherParams } = args
      const url = `/orgs/${org}/teams/${teamSlug}/memberships/${username}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('teamSlug' in mappedParams) {
        mappedParams['team_slug'] = mappedParams['teamSlug']
        delete mappedParams['teamSlug']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'teams/remove-membership-for-user-in-org',
  `Remove team membership for a user`,
  {
    org: z.string(),
    teamSlug: z.string(),
    username: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, teamSlug, username, ...otherParams } = args
      const url = `/orgs/${org}/teams/${teamSlug}/memberships/${username}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('teamSlug' in mappedParams) {
        mappedParams['team_slug'] = mappedParams['teamSlug']
        delete mappedParams['teamSlug']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'teams/list-projects-in-org',
  `List team projects`,
  {
    org: z.string(),
    teamSlug: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, teamSlug, ...otherParams } = args
      const url = `/orgs/${org}/teams/${teamSlug}/projects`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('teamSlug' in mappedParams) {
        mappedParams['team_slug'] = mappedParams['teamSlug']
        delete mappedParams['teamSlug']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'teams/check-permissions-for-project-in-org',
  `Check team permissions for a project`,
  {
    org: z.string(),
    teamSlug: z.string(),
    projectId: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, teamSlug, projectId, ...otherParams } = args
      const url = `/orgs/${org}/teams/${teamSlug}/projects/${projectId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('teamSlug' in mappedParams) {
        mappedParams['team_slug'] = mappedParams['teamSlug']
        delete mappedParams['teamSlug']
      }
      if ('projectId' in mappedParams) {
        mappedParams['project_id'] = mappedParams['projectId']
        delete mappedParams['projectId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'teams/add-or-update-project-permissions-in-org',
  `Add or update team project permissions`,
  {
    org: z.string(),
    teamSlug: z.string(),
    projectId: z.string(),
    permission: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { org, teamSlug, projectId, ...otherParams } = args
      const url = `/orgs/${org}/teams/${teamSlug}/projects/${projectId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('teamSlug' in mappedParams) {
        mappedParams['team_slug'] = mappedParams['teamSlug']
        delete mappedParams['teamSlug']
      }
      if ('projectId' in mappedParams) {
        mappedParams['project_id'] = mappedParams['projectId']
        delete mappedParams['projectId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'teams/remove-project-in-org',
  `Remove a project from a team`,
  {
    org: z.string(),
    teamSlug: z.string(),
    projectId: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, teamSlug, projectId, ...otherParams } = args
      const url = `/orgs/${org}/teams/${teamSlug}/projects/${projectId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('teamSlug' in mappedParams) {
        mappedParams['team_slug'] = mappedParams['teamSlug']
        delete mappedParams['teamSlug']
      }
      if ('projectId' in mappedParams) {
        mappedParams['project_id'] = mappedParams['projectId']
        delete mappedParams['projectId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'teams/list-repos-in-org',
  `List team repositories`,
  {
    org: z.string(),
    teamSlug: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, teamSlug, ...otherParams } = args
      const url = `/orgs/${org}/teams/${teamSlug}/repos`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('teamSlug' in mappedParams) {
        mappedParams['team_slug'] = mappedParams['teamSlug']
        delete mappedParams['teamSlug']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'teams/check-permissions-for-repo-in-org',
  `Check team permissions for a repository`,
  {
    org: z.string(),
    teamSlug: z.string(),
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, teamSlug, owner, repo, ...otherParams } = args
      const url = `/orgs/${org}/teams/${teamSlug}/repos/${owner}/${repo}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('teamSlug' in mappedParams) {
        mappedParams['team_slug'] = mappedParams['teamSlug']
        delete mappedParams['teamSlug']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'teams/add-or-update-repo-permissions-in-org',
  `Add or update team repository permissions`,
  {
    org: z.string(),
    teamSlug: z.string(),
    owner: z.string(),
    repo: z.string(),
    permission: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { org, teamSlug, owner, repo, ...otherParams } = args
      const url = `/orgs/${org}/teams/${teamSlug}/repos/${owner}/${repo}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('teamSlug' in mappedParams) {
        mappedParams['team_slug'] = mappedParams['teamSlug']
        delete mappedParams['teamSlug']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'teams/remove-repo-in-org',
  `Remove a repository from a team`,
  {
    org: z.string(),
    teamSlug: z.string(),
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, teamSlug, owner, repo, ...otherParams } = args
      const url = `/orgs/${org}/teams/${teamSlug}/repos/${owner}/${repo}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('teamSlug' in mappedParams) {
        mappedParams['team_slug'] = mappedParams['teamSlug']
        delete mappedParams['teamSlug']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'teams/list-child-in-org',
  `List child teams`,
  {
    org: z.string(),
    teamSlug: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, teamSlug, ...otherParams } = args
      const url = `/orgs/${org}/teams/${teamSlug}/teams`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('teamSlug' in mappedParams) {
        mappedParams['team_slug'] = mappedParams['teamSlug']
        delete mappedParams['teamSlug']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'orgs/enable-or-disable-security-product-on-all-org-repos',
  `Enable or disable a security feature for an organization`,
  {
    org: z.string(),
    securityProduct: z.string(),
    enablement: z.string(),
    querySuite: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { org, securityProduct, enablement, ...otherParams } = args
      const url = `/orgs/${org}/${securityProduct}/${enablement}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('securityProduct' in mappedParams) {
        mappedParams['security_product'] = mappedParams['securityProduct']
        delete mappedParams['securityProduct']
      }
      if ('querySuite' in mappedParams) {
        mappedParams['query_suite'] = mappedParams['querySuite']
        delete mappedParams['querySuite']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'projects-classic/get-card',
  `Get a project card`,
  {
    cardId: z.string(),
  },
  async (args, extra) => {
    try {
      const { cardId, ...otherParams } = args
      const url = `/projects/columns/cards/${cardId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('cardId' in mappedParams) {
        mappedParams['card_id'] = mappedParams['cardId']
        delete mappedParams['cardId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'projects-classic/update-card',
  `Update an existing project card`,
  {
    cardId: z.string(),
    note: z.any().optional(),
    archived: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { cardId, ...otherParams } = args
      const url = `/projects/columns/cards/${cardId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('cardId' in mappedParams) {
        mappedParams['card_id'] = mappedParams['cardId']
        delete mappedParams['cardId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PATCH',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'projects-classic/delete-card',
  `Delete a project card`,
  {
    cardId: z.string(),
  },
  async (args, extra) => {
    try {
      const { cardId, ...otherParams } = args
      const url = `/projects/columns/cards/${cardId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('cardId' in mappedParams) {
        mappedParams['card_id'] = mappedParams['cardId']
        delete mappedParams['cardId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'projects-classic/move-card',
  `Move a project card`,
  {
    cardId: z.string(),
    position: z.any(),
    columnId: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { cardId, ...otherParams } = args
      const url = `/projects/columns/cards/${cardId}/moves`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('cardId' in mappedParams) {
        mappedParams['card_id'] = mappedParams['cardId']
        delete mappedParams['cardId']
      }
      if ('columnId' in mappedParams) {
        mappedParams['column_id'] = mappedParams['columnId']
        delete mappedParams['columnId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'projects-classic/get-column',
  `Get a project column`,
  {
    columnId: z.string(),
  },
  async (args, extra) => {
    try {
      const { columnId, ...otherParams } = args
      const url = `/projects/columns/${columnId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('columnId' in mappedParams) {
        mappedParams['column_id'] = mappedParams['columnId']
        delete mappedParams['columnId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'projects-classic/update-column',
  `Update an existing project column`,
  {
    columnId: z.string(),
    name: z.any(),
  },
  async (args, extra) => {
    try {
      const { columnId, ...otherParams } = args
      const url = `/projects/columns/${columnId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('columnId' in mappedParams) {
        mappedParams['column_id'] = mappedParams['columnId']
        delete mappedParams['columnId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PATCH',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'projects-classic/delete-column',
  `Delete a project column`,
  {
    columnId: z.string(),
  },
  async (args, extra) => {
    try {
      const { columnId, ...otherParams } = args
      const url = `/projects/columns/${columnId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('columnId' in mappedParams) {
        mappedParams['column_id'] = mappedParams['columnId']
        delete mappedParams['columnId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'projects-classic/list-cards',
  `List project cards`,
  {
    columnId: z.string(),
    archivedState: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const { columnId, ...otherParams } = args
      const url = `/projects/columns/${columnId}/cards`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('columnId' in mappedParams) {
        mappedParams['column_id'] = mappedParams['columnId']
        delete mappedParams['columnId']
      }
      if ('archivedState' in mappedParams) {
        mappedParams['archived_state'] = mappedParams['archivedState']
        delete mappedParams['archivedState']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'projects-classic/create-card',
  `Create a project card`,
  {
    columnId: z.string(),
  },
  async (args, extra) => {
    try {
      const { columnId, ...otherParams } = args
      const url = `/projects/columns/${columnId}/cards`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('columnId' in mappedParams) {
        mappedParams['column_id'] = mappedParams['columnId']
        delete mappedParams['columnId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'projects-classic/move-column',
  `Move a project column`,
  {
    columnId: z.string(),
    position: z.any(),
  },
  async (args, extra) => {
    try {
      const { columnId, ...otherParams } = args
      const url = `/projects/columns/${columnId}/moves`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('columnId' in mappedParams) {
        mappedParams['column_id'] = mappedParams['columnId']
        delete mappedParams['columnId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'projects-classic/get',
  `Get a project`,
  {
    projectId: z.string(),
  },
  async (args, extra) => {
    try {
      const { projectId, ...otherParams } = args
      const url = `/projects/${projectId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('projectId' in mappedParams) {
        mappedParams['project_id'] = mappedParams['projectId']
        delete mappedParams['projectId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'projects-classic/update',
  `Update a project`,
  {
    projectId: z.string(),
    name: z.any().optional(),
    body: z.any().optional(),
    state: z.any().optional(),
    organizationPermission: z.any().optional(),
    private: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { projectId, ...otherParams } = args
      const url = `/projects/${projectId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('projectId' in mappedParams) {
        mappedParams['project_id'] = mappedParams['projectId']
        delete mappedParams['projectId']
      }
      if ('organizationPermission' in mappedParams) {
        mappedParams['organization_permission'] = mappedParams['organizationPermission']
        delete mappedParams['organizationPermission']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PATCH',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'projects-classic/delete',
  `Delete a project`,
  {
    projectId: z.string(),
  },
  async (args, extra) => {
    try {
      const { projectId, ...otherParams } = args
      const url = `/projects/${projectId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('projectId' in mappedParams) {
        mappedParams['project_id'] = mappedParams['projectId']
        delete mappedParams['projectId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'projects-classic/list-collaborators',
  `List project collaborators`,
  {
    projectId: z.string(),
    affiliation: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const { projectId, ...otherParams } = args
      const url = `/projects/${projectId}/collaborators`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('projectId' in mappedParams) {
        mappedParams['project_id'] = mappedParams['projectId']
        delete mappedParams['projectId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'projects-classic/add-collaborator',
  `Add project collaborator`,
  {
    projectId: z.string(),
    username: z.string(),
    permission: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { projectId, username, ...otherParams } = args
      const url = `/projects/${projectId}/collaborators/${username}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('projectId' in mappedParams) {
        mappedParams['project_id'] = mappedParams['projectId']
        delete mappedParams['projectId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'projects-classic/remove-collaborator',
  `Remove user as a collaborator`,
  {
    projectId: z.string(),
    username: z.string(),
  },
  async (args, extra) => {
    try {
      const { projectId, username, ...otherParams } = args
      const url = `/projects/${projectId}/collaborators/${username}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('projectId' in mappedParams) {
        mappedParams['project_id'] = mappedParams['projectId']
        delete mappedParams['projectId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'projects-classic/get-permission-for-user',
  `Get project permission for a user`,
  {
    projectId: z.string(),
    username: z.string(),
  },
  async (args, extra) => {
    try {
      const { projectId, username, ...otherParams } = args
      const url = `/projects/${projectId}/collaborators/${username}/permission`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('projectId' in mappedParams) {
        mappedParams['project_id'] = mappedParams['projectId']
        delete mappedParams['projectId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'projects-classic/list-columns',
  `List project columns`,
  {
    projectId: z.string(),
  },
  async (args, extra) => {
    try {
      const { projectId, ...otherParams } = args
      const url = `/projects/${projectId}/columns`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('projectId' in mappedParams) {
        mappedParams['project_id'] = mappedParams['projectId']
        delete mappedParams['projectId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'projects-classic/create-column',
  `Create a project column`,
  {
    projectId: z.string(),
    name: z.any(),
  },
  async (args, extra) => {
    try {
      const { projectId, ...otherParams } = args
      const url = `/projects/${projectId}/columns`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('projectId' in mappedParams) {
        mappedParams['project_id'] = mappedParams['projectId']
        delete mappedParams['projectId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'rate-limit/get',
  `Get rate limit status for the authenticated user`,
  {},
  async (args, extra) => {
    try {
      const otherParams = args

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: '/rate_limit',
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/get',
  `Get a repository`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/update',
  `Update a repository`,
  {
    owner: z.string(),
    repo: z.string(),
    name: z.any().optional(),
    description: z.any().optional(),
    homepage: z.any().optional(),
    private: z.any().optional(),
    visibility: z.any().optional(),
    securityAndAnalysis: z.any().optional(),
    hasIssues: z.any().optional(),
    hasProjects: z.any().optional(),
    hasWiki: z.any().optional(),
    isTemplate: z.any().optional(),
    defaultBranch: z.any().optional(),
    allowSquashMerge: z.any().optional(),
    allowMergeCommit: z.any().optional(),
    allowRebaseMerge: z.any().optional(),
    allowAutoMerge: z.any().optional(),
    deleteBranchOnMerge: z.any().optional(),
    allowUpdateBranch: z.any().optional(),
    useSquashPrTitleAsDefault: z.any().optional(),
    squashMergeCommitTitle: z.any().optional(),
    squashMergeCommitMessage: z.any().optional(),
    mergeCommitTitle: z.any().optional(),
    mergeCommitMessage: z.any().optional(),
    archived: z.any().optional(),
    allowForking: z.any().optional(),
    webCommitSignoffRequired: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('securityAndAnalysis' in mappedParams) {
        mappedParams['security_and_analysis'] = mappedParams['securityAndAnalysis']
        delete mappedParams['securityAndAnalysis']
      }
      if ('hasIssues' in mappedParams) {
        mappedParams['has_issues'] = mappedParams['hasIssues']
        delete mappedParams['hasIssues']
      }
      if ('hasProjects' in mappedParams) {
        mappedParams['has_projects'] = mappedParams['hasProjects']
        delete mappedParams['hasProjects']
      }
      if ('hasWiki' in mappedParams) {
        mappedParams['has_wiki'] = mappedParams['hasWiki']
        delete mappedParams['hasWiki']
      }
      if ('isTemplate' in mappedParams) {
        mappedParams['is_template'] = mappedParams['isTemplate']
        delete mappedParams['isTemplate']
      }
      if ('defaultBranch' in mappedParams) {
        mappedParams['default_branch'] = mappedParams['defaultBranch']
        delete mappedParams['defaultBranch']
      }
      if ('allowSquashMerge' in mappedParams) {
        mappedParams['allow_squash_merge'] = mappedParams['allowSquashMerge']
        delete mappedParams['allowSquashMerge']
      }
      if ('allowMergeCommit' in mappedParams) {
        mappedParams['allow_merge_commit'] = mappedParams['allowMergeCommit']
        delete mappedParams['allowMergeCommit']
      }
      if ('allowRebaseMerge' in mappedParams) {
        mappedParams['allow_rebase_merge'] = mappedParams['allowRebaseMerge']
        delete mappedParams['allowRebaseMerge']
      }
      if ('allowAutoMerge' in mappedParams) {
        mappedParams['allow_auto_merge'] = mappedParams['allowAutoMerge']
        delete mappedParams['allowAutoMerge']
      }
      if ('deleteBranchOnMerge' in mappedParams) {
        mappedParams['delete_branch_on_merge'] = mappedParams['deleteBranchOnMerge']
        delete mappedParams['deleteBranchOnMerge']
      }
      if ('allowUpdateBranch' in mappedParams) {
        mappedParams['allow_update_branch'] = mappedParams['allowUpdateBranch']
        delete mappedParams['allowUpdateBranch']
      }
      if ('useSquashPrTitleAsDefault' in mappedParams) {
        mappedParams['use_squash_pr_title_as_default'] = mappedParams['useSquashPrTitleAsDefault']
        delete mappedParams['useSquashPrTitleAsDefault']
      }
      if ('squashMergeCommitTitle' in mappedParams) {
        mappedParams['squash_merge_commit_title'] = mappedParams['squashMergeCommitTitle']
        delete mappedParams['squashMergeCommitTitle']
      }
      if ('squashMergeCommitMessage' in mappedParams) {
        mappedParams['squash_merge_commit_message'] = mappedParams['squashMergeCommitMessage']
        delete mappedParams['squashMergeCommitMessage']
      }
      if ('mergeCommitTitle' in mappedParams) {
        mappedParams['merge_commit_title'] = mappedParams['mergeCommitTitle']
        delete mappedParams['mergeCommitTitle']
      }
      if ('mergeCommitMessage' in mappedParams) {
        mappedParams['merge_commit_message'] = mappedParams['mergeCommitMessage']
        delete mappedParams['mergeCommitMessage']
      }
      if ('allowForking' in mappedParams) {
        mappedParams['allow_forking'] = mappedParams['allowForking']
        delete mappedParams['allowForking']
      }
      if ('webCommitSignoffRequired' in mappedParams) {
        mappedParams['web_commit_signoff_required'] = mappedParams['webCommitSignoffRequired']
        delete mappedParams['webCommitSignoffRequired']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PATCH',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/delete',
  `Delete a repository`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/list-artifacts-for-repo',
  `List artifacts for a repository`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/actions/artifacts`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/get-artifact',
  `Get an artifact`,
  {
    owner: z.string(),
    repo: z.string(),
    artifactId: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, artifactId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/actions/artifacts/${artifactId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('artifactId' in mappedParams) {
        mappedParams['artifact_id'] = mappedParams['artifactId']
        delete mappedParams['artifactId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/delete-artifact',
  `Delete an artifact`,
  {
    owner: z.string(),
    repo: z.string(),
    artifactId: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, artifactId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/actions/artifacts/${artifactId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('artifactId' in mappedParams) {
        mappedParams['artifact_id'] = mappedParams['artifactId']
        delete mappedParams['artifactId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/download-artifact',
  `Download an artifact`,
  {
    owner: z.string(),
    repo: z.string(),
    artifactId: z.string(),
    archiveFormat: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, artifactId, archiveFormat, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/actions/artifacts/${artifactId}/${archiveFormat}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('artifactId' in mappedParams) {
        mappedParams['artifact_id'] = mappedParams['artifactId']
        delete mappedParams['artifactId']
      }
      if ('archiveFormat' in mappedParams) {
        mappedParams['archive_format'] = mappedParams['archiveFormat']
        delete mappedParams['archiveFormat']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/get-actions-cache-usage',
  `Get GitHub Actions cache usage for a repository`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/actions/cache/usage`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/get-actions-cache-list',
  `List GitHub Actions caches for a repository`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/actions/caches`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/delete-actions-cache-by-key',
  `Delete GitHub Actions caches for a repository (using a cache key)`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/actions/caches`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/delete-actions-cache-by-id',
  `Delete a GitHub Actions cache for a repository (using a cache ID)`,
  {
    owner: z.string(),
    repo: z.string(),
    cacheId: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, cacheId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/actions/caches/${cacheId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('cacheId' in mappedParams) {
        mappedParams['cache_id'] = mappedParams['cacheId']
        delete mappedParams['cacheId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/get-job-for-workflow-run',
  `Get a job for a workflow run`,
  {
    owner: z.string(),
    repo: z.string(),
    jobId: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, jobId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/actions/jobs/${jobId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('jobId' in mappedParams) {
        mappedParams['job_id'] = mappedParams['jobId']
        delete mappedParams['jobId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/download-job-logs-for-workflow-run',
  `Download job logs for a workflow run`,
  {
    owner: z.string(),
    repo: z.string(),
    jobId: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, jobId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/actions/jobs/${jobId}/logs`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('jobId' in mappedParams) {
        mappedParams['job_id'] = mappedParams['jobId']
        delete mappedParams['jobId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/re-run-job-for-workflow-run',
  `Re-run a job from a workflow run`,
  {
    owner: z.string(),
    repo: z.string(),
    jobId: z.string(),
    enableDebugLogging: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, jobId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/actions/jobs/${jobId}/rerun`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('jobId' in mappedParams) {
        mappedParams['job_id'] = mappedParams['jobId']
        delete mappedParams['jobId']
      }
      if ('enableDebugLogging' in mappedParams) {
        mappedParams['enable_debug_logging'] = mappedParams['enableDebugLogging']
        delete mappedParams['enableDebugLogging']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/get-custom-oidc-sub-claim-for-repo',
  `Get the customization template for an OIDC subject claim for a repository`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/actions/oidc/customization/sub`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/set-custom-oidc-sub-claim-for-repo',
  `Set the customization template for an OIDC subject claim for a repository`,
  {
    owner: z.string(),
    repo: z.string(),
    useDefault: z.any(),
    includeClaimKeys: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/actions/oidc/customization/sub`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('useDefault' in mappedParams) {
        mappedParams['use_default'] = mappedParams['useDefault']
        delete mappedParams['useDefault']
      }
      if ('includeClaimKeys' in mappedParams) {
        mappedParams['include_claim_keys'] = mappedParams['includeClaimKeys']
        delete mappedParams['includeClaimKeys']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/list-repo-organization-secrets',
  `List repository organization secrets`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/actions/organization-secrets`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/list-repo-organization-variables',
  `List repository organization variables`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/actions/organization-variables`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/get-github-actions-permissions-repository',
  `Get GitHub Actions permissions for a repository`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/actions/permissions`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/set-github-actions-permissions-repository',
  `Set GitHub Actions permissions for a repository`,
  {
    owner: z.string(),
    repo: z.string(),
    enabled: z.any(),
    allowedActions: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/actions/permissions`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('allowedActions' in mappedParams) {
        mappedParams['allowed_actions'] = mappedParams['allowedActions']
        delete mappedParams['allowedActions']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/get-workflow-access-to-repository',
  `Get the level of access for workflows outside of the repository`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/actions/permissions/access`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/set-workflow-access-to-repository',
  `Set the level of access for workflows outside of the repository`,
  {
    owner: z.string(),
    repo: z.string(),
    requestData: z.record(z.any()).optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/actions/permissions/access`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/get-allowed-actions-repository',
  `Get allowed actions and reusable workflows for a repository`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/actions/permissions/selected-actions`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/set-allowed-actions-repository',
  `Set allowed actions and reusable workflows for a repository`,
  {
    owner: z.string(),
    repo: z.string(),
    requestData: z.record(z.any()).optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/actions/permissions/selected-actions`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/get-github-actions-default-workflow-permissions-repository',
  `Get default workflow permissions for a repository`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/actions/permissions/workflow`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/set-github-actions-default-workflow-permissions-repository',
  `Set default workflow permissions for a repository`,
  {
    owner: z.string(),
    repo: z.string(),
    requestData: z.record(z.any()).optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/actions/permissions/workflow`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/list-self-hosted-runners-for-repo',
  `List self-hosted runners for a repository`,
  {
    owner: z.string(),
    repo: z.string(),
    name: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/actions/runners`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/list-runner-applications-for-repo',
  `List runner applications for a repository`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/actions/runners/downloads`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/generate-runner-jitconfig-for-repo',
  `Create configuration for a just-in-time runner for a repository`,
  {
    owner: z.string(),
    repo: z.string(),
    name: z.any(),
    runnerGroupId: z.any(),
    labels: z.any(),
    workFolder: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/actions/runners/generate-jitconfig`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('runnerGroupId' in mappedParams) {
        mappedParams['runner_group_id'] = mappedParams['runnerGroupId']
        delete mappedParams['runnerGroupId']
      }
      if ('workFolder' in mappedParams) {
        mappedParams['work_folder'] = mappedParams['workFolder']
        delete mappedParams['workFolder']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/create-registration-token-for-repo',
  `Create a registration token for a repository`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/actions/runners/registration-token`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/create-remove-token-for-repo',
  `Create a remove token for a repository`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/actions/runners/remove-token`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/get-self-hosted-runner-for-repo',
  `Get a self-hosted runner for a repository`,
  {
    owner: z.string(),
    repo: z.string(),
    runnerId: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, runnerId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/actions/runners/${runnerId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('runnerId' in mappedParams) {
        mappedParams['runner_id'] = mappedParams['runnerId']
        delete mappedParams['runnerId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/delete-self-hosted-runner-from-repo',
  `Delete a self-hosted runner from a repository`,
  {
    owner: z.string(),
    repo: z.string(),
    runnerId: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, runnerId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/actions/runners/${runnerId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('runnerId' in mappedParams) {
        mappedParams['runner_id'] = mappedParams['runnerId']
        delete mappedParams['runnerId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/list-labels-for-self-hosted-runner-for-repo',
  `List labels for a self-hosted runner for a repository`,
  {
    owner: z.string(),
    repo: z.string(),
    runnerId: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, runnerId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/actions/runners/${runnerId}/labels`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('runnerId' in mappedParams) {
        mappedParams['runner_id'] = mappedParams['runnerId']
        delete mappedParams['runnerId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/add-custom-labels-to-self-hosted-runner-for-repo',
  `Add custom labels to a self-hosted runner for a repository`,
  {
    owner: z.string(),
    repo: z.string(),
    runnerId: z.string(),
    labels: z.any(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, runnerId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/actions/runners/${runnerId}/labels`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('runnerId' in mappedParams) {
        mappedParams['runner_id'] = mappedParams['runnerId']
        delete mappedParams['runnerId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/set-custom-labels-for-self-hosted-runner-for-repo',
  `Set custom labels for a self-hosted runner for a repository`,
  {
    owner: z.string(),
    repo: z.string(),
    runnerId: z.string(),
    labels: z.any(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, runnerId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/actions/runners/${runnerId}/labels`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('runnerId' in mappedParams) {
        mappedParams['runner_id'] = mappedParams['runnerId']
        delete mappedParams['runnerId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/remove-all-custom-labels-from-self-hosted-runner-for-repo',
  `Remove all custom labels from a self-hosted runner for a repository`,
  {
    owner: z.string(),
    repo: z.string(),
    runnerId: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, runnerId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/actions/runners/${runnerId}/labels`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('runnerId' in mappedParams) {
        mappedParams['runner_id'] = mappedParams['runnerId']
        delete mappedParams['runnerId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/remove-custom-label-from-self-hosted-runner-for-repo',
  `Remove a custom label from a self-hosted runner for a repository`,
  {
    owner: z.string(),
    repo: z.string(),
    runnerId: z.string(),
    name: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, runnerId, name, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/actions/runners/${runnerId}/labels/${name}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('runnerId' in mappedParams) {
        mappedParams['runner_id'] = mappedParams['runnerId']
        delete mappedParams['runnerId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/list-workflow-runs-for-repo',
  `List workflow runs for a repository`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/actions/runs`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/get-workflow-run',
  `Get a workflow run`,
  {
    owner: z.string(),
    repo: z.string(),
    runId: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, runId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/actions/runs/${runId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('runId' in mappedParams) {
        mappedParams['run_id'] = mappedParams['runId']
        delete mappedParams['runId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/delete-workflow-run',
  `Delete a workflow run`,
  {
    owner: z.string(),
    repo: z.string(),
    runId: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, runId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/actions/runs/${runId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('runId' in mappedParams) {
        mappedParams['run_id'] = mappedParams['runId']
        delete mappedParams['runId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/get-reviews-for-run',
  `Get the review history for a workflow run`,
  {
    owner: z.string(),
    repo: z.string(),
    runId: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, runId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/actions/runs/${runId}/approvals`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('runId' in mappedParams) {
        mappedParams['run_id'] = mappedParams['runId']
        delete mappedParams['runId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/approve-workflow-run',
  `Approve a workflow run for a fork pull request`,
  {
    owner: z.string(),
    repo: z.string(),
    runId: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, runId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/actions/runs/${runId}/approve`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('runId' in mappedParams) {
        mappedParams['run_id'] = mappedParams['runId']
        delete mappedParams['runId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/list-workflow-run-artifacts',
  `List workflow run artifacts`,
  {
    owner: z.string(),
    repo: z.string(),
    runId: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, runId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/actions/runs/${runId}/artifacts`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('runId' in mappedParams) {
        mappedParams['run_id'] = mappedParams['runId']
        delete mappedParams['runId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/get-workflow-run-attempt',
  `Get a workflow run attempt`,
  {
    owner: z.string(),
    repo: z.string(),
    runId: z.string(),
    attemptNumber: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, runId, attemptNumber, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/actions/runs/${runId}/attempts/${attemptNumber}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('runId' in mappedParams) {
        mappedParams['run_id'] = mappedParams['runId']
        delete mappedParams['runId']
      }
      if ('attemptNumber' in mappedParams) {
        mappedParams['attempt_number'] = mappedParams['attemptNumber']
        delete mappedParams['attemptNumber']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/list-jobs-for-workflow-run-attempt',
  `List jobs for a workflow run attempt`,
  {
    owner: z.string(),
    repo: z.string(),
    runId: z.string(),
    attemptNumber: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, runId, attemptNumber, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/actions/runs/${runId}/attempts/${attemptNumber}/jobs`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('runId' in mappedParams) {
        mappedParams['run_id'] = mappedParams['runId']
        delete mappedParams['runId']
      }
      if ('attemptNumber' in mappedParams) {
        mappedParams['attempt_number'] = mappedParams['attemptNumber']
        delete mappedParams['attemptNumber']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/download-workflow-run-attempt-logs',
  `Download workflow run attempt logs`,
  {
    owner: z.string(),
    repo: z.string(),
    runId: z.string(),
    attemptNumber: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, runId, attemptNumber, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/actions/runs/${runId}/attempts/${attemptNumber}/logs`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('runId' in mappedParams) {
        mappedParams['run_id'] = mappedParams['runId']
        delete mappedParams['runId']
      }
      if ('attemptNumber' in mappedParams) {
        mappedParams['attempt_number'] = mappedParams['attemptNumber']
        delete mappedParams['attemptNumber']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/cancel-workflow-run',
  `Cancel a workflow run`,
  {
    owner: z.string(),
    repo: z.string(),
    runId: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, runId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/actions/runs/${runId}/cancel`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('runId' in mappedParams) {
        mappedParams['run_id'] = mappedParams['runId']
        delete mappedParams['runId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/review-custom-gates-for-run',
  `Review custom deployment protection rules for a workflow run`,
  {
    owner: z.string(),
    repo: z.string(),
    runId: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, runId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/actions/runs/${runId}/deployment_protection_rule`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('runId' in mappedParams) {
        mappedParams['run_id'] = mappedParams['runId']
        delete mappedParams['runId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/force-cancel-workflow-run',
  `Force cancel a workflow run`,
  {
    owner: z.string(),
    repo: z.string(),
    runId: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, runId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/actions/runs/${runId}/force-cancel`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('runId' in mappedParams) {
        mappedParams['run_id'] = mappedParams['runId']
        delete mappedParams['runId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/list-jobs-for-workflow-run',
  `List jobs for a workflow run`,
  {
    owner: z.string(),
    repo: z.string(),
    runId: z.string(),
    filter: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, runId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/actions/runs/${runId}/jobs`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('runId' in mappedParams) {
        mappedParams['run_id'] = mappedParams['runId']
        delete mappedParams['runId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/download-workflow-run-logs',
  `Download workflow run logs`,
  {
    owner: z.string(),
    repo: z.string(),
    runId: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, runId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/actions/runs/${runId}/logs`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('runId' in mappedParams) {
        mappedParams['run_id'] = mappedParams['runId']
        delete mappedParams['runId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/delete-workflow-run-logs',
  `Delete workflow run logs`,
  {
    owner: z.string(),
    repo: z.string(),
    runId: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, runId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/actions/runs/${runId}/logs`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('runId' in mappedParams) {
        mappedParams['run_id'] = mappedParams['runId']
        delete mappedParams['runId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/get-pending-deployments-for-run',
  `Get pending deployments for a workflow run`,
  {
    owner: z.string(),
    repo: z.string(),
    runId: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, runId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/actions/runs/${runId}/pending_deployments`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('runId' in mappedParams) {
        mappedParams['run_id'] = mappedParams['runId']
        delete mappedParams['runId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/review-pending-deployments-for-run',
  `Review pending deployments for a workflow run`,
  {
    owner: z.string(),
    repo: z.string(),
    runId: z.string(),
    environmentIds: z.any(),
    state: z.any(),
    comment: z.any(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, runId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/actions/runs/${runId}/pending_deployments`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('runId' in mappedParams) {
        mappedParams['run_id'] = mappedParams['runId']
        delete mappedParams['runId']
      }
      if ('environmentIds' in mappedParams) {
        mappedParams['environment_ids'] = mappedParams['environmentIds']
        delete mappedParams['environmentIds']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/re-run-workflow',
  `Re-run a workflow`,
  {
    owner: z.string(),
    repo: z.string(),
    runId: z.string(),
    enableDebugLogging: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, runId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/actions/runs/${runId}/rerun`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('runId' in mappedParams) {
        mappedParams['run_id'] = mappedParams['runId']
        delete mappedParams['runId']
      }
      if ('enableDebugLogging' in mappedParams) {
        mappedParams['enable_debug_logging'] = mappedParams['enableDebugLogging']
        delete mappedParams['enableDebugLogging']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/re-run-workflow-failed-jobs',
  `Re-run failed jobs from a workflow run`,
  {
    owner: z.string(),
    repo: z.string(),
    runId: z.string(),
    enableDebugLogging: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, runId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/actions/runs/${runId}/rerun-failed-jobs`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('runId' in mappedParams) {
        mappedParams['run_id'] = mappedParams['runId']
        delete mappedParams['runId']
      }
      if ('enableDebugLogging' in mappedParams) {
        mappedParams['enable_debug_logging'] = mappedParams['enableDebugLogging']
        delete mappedParams['enableDebugLogging']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/get-workflow-run-usage',
  `Get workflow run usage`,
  {
    owner: z.string(),
    repo: z.string(),
    runId: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, runId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/actions/runs/${runId}/timing`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('runId' in mappedParams) {
        mappedParams['run_id'] = mappedParams['runId']
        delete mappedParams['runId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/list-repo-secrets',
  `List repository secrets`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/actions/secrets`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/get-repo-public-key',
  `Get a repository public key`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/actions/secrets/public-key`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/get-repo-secret',
  `Get a repository secret`,
  {
    owner: z.string(),
    repo: z.string(),
    secretName: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, secretName, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/actions/secrets/${secretName}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('secretName' in mappedParams) {
        mappedParams['secret_name'] = mappedParams['secretName']
        delete mappedParams['secretName']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/create-or-update-repo-secret',
  `Create or update a repository secret`,
  {
    owner: z.string(),
    repo: z.string(),
    secretName: z.string(),
    encryptedValue: z.any(),
    keyId: z.any(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, secretName, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/actions/secrets/${secretName}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('secretName' in mappedParams) {
        mappedParams['secret_name'] = mappedParams['secretName']
        delete mappedParams['secretName']
      }
      if ('encryptedValue' in mappedParams) {
        mappedParams['encrypted_value'] = mappedParams['encryptedValue']
        delete mappedParams['encryptedValue']
      }
      if ('keyId' in mappedParams) {
        mappedParams['key_id'] = mappedParams['keyId']
        delete mappedParams['keyId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/delete-repo-secret',
  `Delete a repository secret`,
  {
    owner: z.string(),
    repo: z.string(),
    secretName: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, secretName, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/actions/secrets/${secretName}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('secretName' in mappedParams) {
        mappedParams['secret_name'] = mappedParams['secretName']
        delete mappedParams['secretName']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/list-repo-variables',
  `List repository variables`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/actions/variables`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/create-repo-variable',
  `Create a repository variable`,
  {
    owner: z.string(),
    repo: z.string(),
    name: z.any(),
    value: z.any(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/actions/variables`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/get-repo-variable',
  `Get a repository variable`,
  {
    owner: z.string(),
    repo: z.string(),
    name: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, name, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/actions/variables/${name}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/update-repo-variable',
  `Update a repository variable`,
  {
    owner: z.string(),
    repo: z.string(),
    name: z.string(),
    value: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, name, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/actions/variables/${name}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PATCH',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/delete-repo-variable',
  `Delete a repository variable`,
  {
    owner: z.string(),
    repo: z.string(),
    name: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, name, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/actions/variables/${name}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/list-repo-workflows',
  `List repository workflows`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/actions/workflows`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/get-workflow',
  `Get a workflow`,
  {
    owner: z.string(),
    repo: z.string(),
    workflowId: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, workflowId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/actions/workflows/${workflowId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('workflowId' in mappedParams) {
        mappedParams['workflow_id'] = mappedParams['workflowId']
        delete mappedParams['workflowId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/disable-workflow',
  `Disable a workflow`,
  {
    owner: z.string(),
    repo: z.string(),
    workflowId: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, workflowId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/actions/workflows/${workflowId}/disable`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('workflowId' in mappedParams) {
        mappedParams['workflow_id'] = mappedParams['workflowId']
        delete mappedParams['workflowId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/create-workflow-dispatch',
  `Create a workflow dispatch event`,
  {
    owner: z.string(),
    repo: z.string(),
    workflowId: z.string(),
    ref: z.any(),
    inputs: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, workflowId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/actions/workflows/${workflowId}/dispatches`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('workflowId' in mappedParams) {
        mappedParams['workflow_id'] = mappedParams['workflowId']
        delete mappedParams['workflowId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/enable-workflow',
  `Enable a workflow`,
  {
    owner: z.string(),
    repo: z.string(),
    workflowId: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, workflowId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/actions/workflows/${workflowId}/enable`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('workflowId' in mappedParams) {
        mappedParams['workflow_id'] = mappedParams['workflowId']
        delete mappedParams['workflowId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/list-workflow-runs',
  `List workflow runs for a workflow`,
  {
    owner: z.string(),
    repo: z.string(),
    workflowId: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, workflowId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/actions/workflows/${workflowId}/runs`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('workflowId' in mappedParams) {
        mappedParams['workflow_id'] = mappedParams['workflowId']
        delete mappedParams['workflowId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/get-workflow-usage',
  `Get workflow usage`,
  {
    owner: z.string(),
    repo: z.string(),
    workflowId: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, workflowId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/actions/workflows/${workflowId}/timing`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('workflowId' in mappedParams) {
        mappedParams['workflow_id'] = mappedParams['workflowId']
        delete mappedParams['workflowId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/list-activities',
  `List repository activities`,
  {
    owner: z.string(),
    repo: z.string(),
    ref: z.string().optional(),
    actor: z.string().optional(),
    timePeriod: z.string().optional(),
    activityType: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/activity`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('timePeriod' in mappedParams) {
        mappedParams['time_period'] = mappedParams['timePeriod']
        delete mappedParams['timePeriod']
      }
      if ('activityType' in mappedParams) {
        mappedParams['activity_type'] = mappedParams['activityType']
        delete mappedParams['activityType']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'issues/list-assignees',
  `List assignees`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/assignees`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'issues/check-user-can-be-assigned',
  `Check if a user can be assigned`,
  {
    owner: z.string(),
    repo: z.string(),
    assignee: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, assignee, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/assignees/${assignee}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/create-attestation',
  `Create an attestation`,
  {
    owner: z.string(),
    repo: z.string(),
    bundle: z.any(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/attestations`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/list-attestations',
  `List attestations`,
  {
    owner: z.string(),
    repo: z.string(),
    subjectDigest: z.string(),
    predicateType: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, subjectDigest, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/attestations/${subjectDigest}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('subjectDigest' in mappedParams) {
        mappedParams['subject_digest'] = mappedParams['subjectDigest']
        delete mappedParams['subjectDigest']
      }
      if ('predicateType' in mappedParams) {
        mappedParams['predicate_type'] = mappedParams['predicateType']
        delete mappedParams['predicateType']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/list-autolinks',
  `Get all autolinks of a repository`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/autolinks`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/create-autolink',
  `Create an autolink reference for a repository`,
  {
    owner: z.string(),
    repo: z.string(),
    keyPrefix: z.any(),
    urlTemplate: z.any(),
    isAlphanumeric: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/autolinks`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('keyPrefix' in mappedParams) {
        mappedParams['key_prefix'] = mappedParams['keyPrefix']
        delete mappedParams['keyPrefix']
      }
      if ('urlTemplate' in mappedParams) {
        mappedParams['url_template'] = mappedParams['urlTemplate']
        delete mappedParams['urlTemplate']
      }
      if ('isAlphanumeric' in mappedParams) {
        mappedParams['is_alphanumeric'] = mappedParams['isAlphanumeric']
        delete mappedParams['isAlphanumeric']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/get-autolink',
  `Get an autolink reference of a repository`,
  {
    owner: z.string(),
    repo: z.string(),
    autolinkId: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, autolinkId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/autolinks/${autolinkId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('autolinkId' in mappedParams) {
        mappedParams['autolink_id'] = mappedParams['autolinkId']
        delete mappedParams['autolinkId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/delete-autolink',
  `Delete an autolink reference from a repository`,
  {
    owner: z.string(),
    repo: z.string(),
    autolinkId: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, autolinkId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/autolinks/${autolinkId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('autolinkId' in mappedParams) {
        mappedParams['autolink_id'] = mappedParams['autolinkId']
        delete mappedParams['autolinkId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/check-automated-security-fixes',
  `Check if Dependabot security updates are enabled for a repository`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/automated-security-fixes`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/enable-automated-security-fixes',
  `Enable Dependabot security updates`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/automated-security-fixes`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/disable-automated-security-fixes',
  `Disable Dependabot security updates`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/automated-security-fixes`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/list-branches',
  `List branches`,
  {
    owner: z.string(),
    repo: z.string(),
    protected: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/branches`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/get-branch',
  `Get a branch`,
  {
    owner: z.string(),
    repo: z.string(),
    branch: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, branch, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/branches/${branch}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/get-branch-protection',
  `Get branch protection`,
  {
    owner: z.string(),
    repo: z.string(),
    branch: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, branch, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/branches/${branch}/protection`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/update-branch-protection',
  `Update branch protection`,
  {
    owner: z.string(),
    repo: z.string(),
    branch: z.string(),
    requiredStatusChecks: z.any(),
    enforceAdmins: z.any(),
    requiredPullRequestReviews: z.any(),
    restrictions: z.any(),
    requiredLinearHistory: z.any().optional(),
    allowForcePushes: z.any().optional(),
    allowDeletions: z.any().optional(),
    blockCreations: z.any().optional(),
    requiredConversationResolution: z.any().optional(),
    lockBranch: z.any().optional(),
    allowForkSyncing: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, branch, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/branches/${branch}/protection`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('requiredStatusChecks' in mappedParams) {
        mappedParams['required_status_checks'] = mappedParams['requiredStatusChecks']
        delete mappedParams['requiredStatusChecks']
      }
      if ('enforceAdmins' in mappedParams) {
        mappedParams['enforce_admins'] = mappedParams['enforceAdmins']
        delete mappedParams['enforceAdmins']
      }
      if ('requiredPullRequestReviews' in mappedParams) {
        mappedParams['required_pull_request_reviews'] = mappedParams['requiredPullRequestReviews']
        delete mappedParams['requiredPullRequestReviews']
      }
      if ('requiredLinearHistory' in mappedParams) {
        mappedParams['required_linear_history'] = mappedParams['requiredLinearHistory']
        delete mappedParams['requiredLinearHistory']
      }
      if ('allowForcePushes' in mappedParams) {
        mappedParams['allow_force_pushes'] = mappedParams['allowForcePushes']
        delete mappedParams['allowForcePushes']
      }
      if ('allowDeletions' in mappedParams) {
        mappedParams['allow_deletions'] = mappedParams['allowDeletions']
        delete mappedParams['allowDeletions']
      }
      if ('blockCreations' in mappedParams) {
        mappedParams['block_creations'] = mappedParams['blockCreations']
        delete mappedParams['blockCreations']
      }
      if ('requiredConversationResolution' in mappedParams) {
        mappedParams['required_conversation_resolution'] =
          mappedParams['requiredConversationResolution']
        delete mappedParams['requiredConversationResolution']
      }
      if ('lockBranch' in mappedParams) {
        mappedParams['lock_branch'] = mappedParams['lockBranch']
        delete mappedParams['lockBranch']
      }
      if ('allowForkSyncing' in mappedParams) {
        mappedParams['allow_fork_syncing'] = mappedParams['allowForkSyncing']
        delete mappedParams['allowForkSyncing']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/delete-branch-protection',
  `Delete branch protection`,
  {
    owner: z.string(),
    repo: z.string(),
    branch: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, branch, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/branches/${branch}/protection`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/get-admin-branch-protection',
  `Get admin branch protection`,
  {
    owner: z.string(),
    repo: z.string(),
    branch: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, branch, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/branches/${branch}/protection/enforce_admins`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/set-admin-branch-protection',
  `Set admin branch protection`,
  {
    owner: z.string(),
    repo: z.string(),
    branch: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, branch, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/branches/${branch}/protection/enforce_admins`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/delete-admin-branch-protection',
  `Delete admin branch protection`,
  {
    owner: z.string(),
    repo: z.string(),
    branch: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, branch, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/branches/${branch}/protection/enforce_admins`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/get-pull-request-review-protection',
  `Get pull request review protection`,
  {
    owner: z.string(),
    repo: z.string(),
    branch: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, branch, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/branches/${branch}/protection/required_pull_request_reviews`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/update-pull-request-review-protection',
  `Update pull request review protection`,
  {
    owner: z.string(),
    repo: z.string(),
    branch: z.string(),
    dismissalRestrictions: z.any().optional(),
    dismissStaleReviews: z.any().optional(),
    requireCodeOwnerReviews: z.any().optional(),
    requiredApprovingReviewCount: z.any().optional(),
    requireLastPushApproval: z.any().optional(),
    bypassPullRequestAllowances: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, branch, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/branches/${branch}/protection/required_pull_request_reviews`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('dismissalRestrictions' in mappedParams) {
        mappedParams['dismissal_restrictions'] = mappedParams['dismissalRestrictions']
        delete mappedParams['dismissalRestrictions']
      }
      if ('dismissStaleReviews' in mappedParams) {
        mappedParams['dismiss_stale_reviews'] = mappedParams['dismissStaleReviews']
        delete mappedParams['dismissStaleReviews']
      }
      if ('requireCodeOwnerReviews' in mappedParams) {
        mappedParams['require_code_owner_reviews'] = mappedParams['requireCodeOwnerReviews']
        delete mappedParams['requireCodeOwnerReviews']
      }
      if ('requiredApprovingReviewCount' in mappedParams) {
        mappedParams['required_approving_review_count'] =
          mappedParams['requiredApprovingReviewCount']
        delete mappedParams['requiredApprovingReviewCount']
      }
      if ('requireLastPushApproval' in mappedParams) {
        mappedParams['require_last_push_approval'] = mappedParams['requireLastPushApproval']
        delete mappedParams['requireLastPushApproval']
      }
      if ('bypassPullRequestAllowances' in mappedParams) {
        mappedParams['bypass_pull_request_allowances'] = mappedParams['bypassPullRequestAllowances']
        delete mappedParams['bypassPullRequestAllowances']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PATCH',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/delete-pull-request-review-protection',
  `Delete pull request review protection`,
  {
    owner: z.string(),
    repo: z.string(),
    branch: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, branch, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/branches/${branch}/protection/required_pull_request_reviews`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/get-commit-signature-protection',
  `Get commit signature protection`,
  {
    owner: z.string(),
    repo: z.string(),
    branch: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, branch, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/branches/${branch}/protection/required_signatures`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/create-commit-signature-protection',
  `Create commit signature protection`,
  {
    owner: z.string(),
    repo: z.string(),
    branch: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, branch, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/branches/${branch}/protection/required_signatures`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/delete-commit-signature-protection',
  `Delete commit signature protection`,
  {
    owner: z.string(),
    repo: z.string(),
    branch: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, branch, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/branches/${branch}/protection/required_signatures`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/get-status-checks-protection',
  `Get status checks protection`,
  {
    owner: z.string(),
    repo: z.string(),
    branch: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, branch, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/branches/${branch}/protection/required_status_checks`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/update-status-check-protection',
  `Update status check protection`,
  {
    owner: z.string(),
    repo: z.string(),
    branch: z.string(),
    strict: z.any().optional(),
    contexts: z.any().optional(),
    checks: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, branch, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/branches/${branch}/protection/required_status_checks`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PATCH',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/remove-status-check-protection',
  `Remove status check protection`,
  {
    owner: z.string(),
    repo: z.string(),
    branch: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, branch, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/branches/${branch}/protection/required_status_checks`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/get-all-status-check-contexts',
  `Get all status check contexts`,
  {
    owner: z.string(),
    repo: z.string(),
    branch: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, branch, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/branches/${branch}/protection/required_status_checks/contexts`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/add-status-check-contexts',
  `Add status check contexts`,
  {
    owner: z.string(),
    repo: z.string(),
    branch: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, branch, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/branches/${branch}/protection/required_status_checks/contexts`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/set-status-check-contexts',
  `Set status check contexts`,
  {
    owner: z.string(),
    repo: z.string(),
    branch: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, branch, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/branches/${branch}/protection/required_status_checks/contexts`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/remove-status-check-contexts',
  `Remove status check contexts`,
  {
    owner: z.string(),
    repo: z.string(),
    branch: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, branch, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/branches/${branch}/protection/required_status_checks/contexts`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/get-access-restrictions',
  `Get access restrictions`,
  {
    owner: z.string(),
    repo: z.string(),
    branch: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, branch, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/branches/${branch}/protection/restrictions`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/delete-access-restrictions',
  `Delete access restrictions`,
  {
    owner: z.string(),
    repo: z.string(),
    branch: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, branch, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/branches/${branch}/protection/restrictions`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/get-apps-with-access-to-protected-branch',
  `Get apps with access to the protected branch`,
  {
    owner: z.string(),
    repo: z.string(),
    branch: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, branch, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/branches/${branch}/protection/restrictions/apps`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/add-app-access-restrictions',
  `Add app access restrictions`,
  {
    owner: z.string(),
    repo: z.string(),
    branch: z.string(),
    apps: z.any(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, branch, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/branches/${branch}/protection/restrictions/apps`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/set-app-access-restrictions',
  `Set app access restrictions`,
  {
    owner: z.string(),
    repo: z.string(),
    branch: z.string(),
    apps: z.any(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, branch, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/branches/${branch}/protection/restrictions/apps`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/remove-app-access-restrictions',
  `Remove app access restrictions`,
  {
    owner: z.string(),
    repo: z.string(),
    branch: z.string(),
    apps: z.any(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, branch, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/branches/${branch}/protection/restrictions/apps`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/get-teams-with-access-to-protected-branch',
  `Get teams with access to the protected branch`,
  {
    owner: z.string(),
    repo: z.string(),
    branch: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, branch, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/branches/${branch}/protection/restrictions/teams`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/add-team-access-restrictions',
  `Add team access restrictions`,
  {
    owner: z.string(),
    repo: z.string(),
    branch: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, branch, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/branches/${branch}/protection/restrictions/teams`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/set-team-access-restrictions',
  `Set team access restrictions`,
  {
    owner: z.string(),
    repo: z.string(),
    branch: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, branch, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/branches/${branch}/protection/restrictions/teams`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/remove-team-access-restrictions',
  `Remove team access restrictions`,
  {
    owner: z.string(),
    repo: z.string(),
    branch: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, branch, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/branches/${branch}/protection/restrictions/teams`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/get-users-with-access-to-protected-branch',
  `Get users with access to the protected branch`,
  {
    owner: z.string(),
    repo: z.string(),
    branch: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, branch, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/branches/${branch}/protection/restrictions/users`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/add-user-access-restrictions',
  `Add user access restrictions`,
  {
    owner: z.string(),
    repo: z.string(),
    branch: z.string(),
    users: z.any(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, branch, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/branches/${branch}/protection/restrictions/users`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/set-user-access-restrictions',
  `Set user access restrictions`,
  {
    owner: z.string(),
    repo: z.string(),
    branch: z.string(),
    users: z.any(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, branch, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/branches/${branch}/protection/restrictions/users`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/remove-user-access-restrictions',
  `Remove user access restrictions`,
  {
    owner: z.string(),
    repo: z.string(),
    branch: z.string(),
    users: z.any(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, branch, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/branches/${branch}/protection/restrictions/users`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/rename-branch',
  `Rename a branch`,
  {
    owner: z.string(),
    repo: z.string(),
    branch: z.string(),
    newName: z.any(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, branch, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/branches/${branch}/rename`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('newName' in mappedParams) {
        mappedParams['new_name'] = mappedParams['newName']
        delete mappedParams['newName']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'checks/create',
  `Create a check run`,
  {
    owner: z.string(),
    repo: z.string(),
    name: z.any(),
    headSha: z.any(),
    detailsUrl: z.any().optional(),
    externalId: z.any().optional(),
    status: z.any().optional(),
    startedAt: z.any().optional(),
    conclusion: z.any().optional(),
    completedAt: z.any().optional(),
    output: z.any().optional(),
    actions: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/check-runs`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('headSha' in mappedParams) {
        mappedParams['head_sha'] = mappedParams['headSha']
        delete mappedParams['headSha']
      }
      if ('detailsUrl' in mappedParams) {
        mappedParams['details_url'] = mappedParams['detailsUrl']
        delete mappedParams['detailsUrl']
      }
      if ('externalId' in mappedParams) {
        mappedParams['external_id'] = mappedParams['externalId']
        delete mappedParams['externalId']
      }
      if ('startedAt' in mappedParams) {
        mappedParams['started_at'] = mappedParams['startedAt']
        delete mappedParams['startedAt']
      }
      if ('completedAt' in mappedParams) {
        mappedParams['completed_at'] = mappedParams['completedAt']
        delete mappedParams['completedAt']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'checks/get',
  `Get a check run`,
  {
    owner: z.string(),
    repo: z.string(),
    checkRunId: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, checkRunId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/check-runs/${checkRunId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('checkRunId' in mappedParams) {
        mappedParams['check_run_id'] = mappedParams['checkRunId']
        delete mappedParams['checkRunId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'checks/update',
  `Update a check run`,
  {
    owner: z.string(),
    repo: z.string(),
    checkRunId: z.string(),
    name: z.any().optional(),
    detailsUrl: z.any().optional(),
    externalId: z.any().optional(),
    startedAt: z.any().optional(),
    status: z.any().optional(),
    conclusion: z.any().optional(),
    completedAt: z.any().optional(),
    output: z.any().optional(),
    actions: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, checkRunId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/check-runs/${checkRunId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('checkRunId' in mappedParams) {
        mappedParams['check_run_id'] = mappedParams['checkRunId']
        delete mappedParams['checkRunId']
      }
      if ('detailsUrl' in mappedParams) {
        mappedParams['details_url'] = mappedParams['detailsUrl']
        delete mappedParams['detailsUrl']
      }
      if ('externalId' in mappedParams) {
        mappedParams['external_id'] = mappedParams['externalId']
        delete mappedParams['externalId']
      }
      if ('startedAt' in mappedParams) {
        mappedParams['started_at'] = mappedParams['startedAt']
        delete mappedParams['startedAt']
      }
      if ('completedAt' in mappedParams) {
        mappedParams['completed_at'] = mappedParams['completedAt']
        delete mappedParams['completedAt']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PATCH',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'checks/list-annotations',
  `List check run annotations`,
  {
    owner: z.string(),
    repo: z.string(),
    checkRunId: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, checkRunId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/check-runs/${checkRunId}/annotations`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('checkRunId' in mappedParams) {
        mappedParams['check_run_id'] = mappedParams['checkRunId']
        delete mappedParams['checkRunId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'checks/rerequest-run',
  `Rerequest a check run`,
  {
    owner: z.string(),
    repo: z.string(),
    checkRunId: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, checkRunId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/check-runs/${checkRunId}/rerequest`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('checkRunId' in mappedParams) {
        mappedParams['check_run_id'] = mappedParams['checkRunId']
        delete mappedParams['checkRunId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'checks/create-suite',
  `Create a check suite`,
  {
    owner: z.string(),
    repo: z.string(),
    headSha: z.any(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/check-suites`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('headSha' in mappedParams) {
        mappedParams['head_sha'] = mappedParams['headSha']
        delete mappedParams['headSha']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'checks/set-suites-preferences',
  `Update repository preferences for check suites`,
  {
    owner: z.string(),
    repo: z.string(),
    autoTriggerChecks: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/check-suites/preferences`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('autoTriggerChecks' in mappedParams) {
        mappedParams['auto_trigger_checks'] = mappedParams['autoTriggerChecks']
        delete mappedParams['autoTriggerChecks']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PATCH',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'checks/get-suite',
  `Get a check suite`,
  {
    owner: z.string(),
    repo: z.string(),
    checkSuiteId: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, checkSuiteId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/check-suites/${checkSuiteId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('checkSuiteId' in mappedParams) {
        mappedParams['check_suite_id'] = mappedParams['checkSuiteId']
        delete mappedParams['checkSuiteId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'checks/list-for-suite',
  `List check runs in a check suite`,
  {
    owner: z.string(),
    repo: z.string(),
    checkSuiteId: z.string(),
    filter: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, checkSuiteId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/check-suites/${checkSuiteId}/check-runs`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('checkSuiteId' in mappedParams) {
        mappedParams['check_suite_id'] = mappedParams['checkSuiteId']
        delete mappedParams['checkSuiteId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'checks/rerequest-suite',
  `Rerequest a check suite`,
  {
    owner: z.string(),
    repo: z.string(),
    checkSuiteId: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, checkSuiteId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/check-suites/${checkSuiteId}/rerequest`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('checkSuiteId' in mappedParams) {
        mappedParams['check_suite_id'] = mappedParams['checkSuiteId']
        delete mappedParams['checkSuiteId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'code-scanning/list-alerts-for-repo',
  `List code scanning alerts for a repository`,
  {
    owner: z.string(),
    repo: z.string(),
    sort: z.string().optional(),
    state: z.string().optional(),
    severity: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/code-scanning/alerts`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'code-scanning/get-alert',
  `Get a code scanning alert`,
  {
    owner: z.string(),
    repo: z.string(),
    alertNumber: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, alertNumber, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/code-scanning/alerts/${alertNumber}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('alertNumber' in mappedParams) {
        mappedParams['alert_number'] = mappedParams['alertNumber']
        delete mappedParams['alertNumber']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'code-scanning/update-alert',
  `Update a code scanning alert`,
  {
    owner: z.string(),
    repo: z.string(),
    alertNumber: z.string(),
    state: z.any(),
    dismissedReason: z.any().optional(),
    dismissedComment: z.any().optional(),
    createRequest: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, alertNumber, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/code-scanning/alerts/${alertNumber}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('alertNumber' in mappedParams) {
        mappedParams['alert_number'] = mappedParams['alertNumber']
        delete mappedParams['alertNumber']
      }
      if ('dismissedReason' in mappedParams) {
        mappedParams['dismissed_reason'] = mappedParams['dismissedReason']
        delete mappedParams['dismissedReason']
      }
      if ('dismissedComment' in mappedParams) {
        mappedParams['dismissed_comment'] = mappedParams['dismissedComment']
        delete mappedParams['dismissedComment']
      }
      if ('createRequest' in mappedParams) {
        mappedParams['create_request'] = mappedParams['createRequest']
        delete mappedParams['createRequest']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PATCH',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'code-scanning/get-autofix',
  `Get the status of an autofix for a code scanning alert`,
  {
    owner: z.string(),
    repo: z.string(),
    alertNumber: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, alertNumber, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/code-scanning/alerts/${alertNumber}/autofix`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('alertNumber' in mappedParams) {
        mappedParams['alert_number'] = mappedParams['alertNumber']
        delete mappedParams['alertNumber']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'code-scanning/create-autofix',
  `Create an autofix for a code scanning alert`,
  {
    owner: z.string(),
    repo: z.string(),
    alertNumber: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, alertNumber, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/code-scanning/alerts/${alertNumber}/autofix`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('alertNumber' in mappedParams) {
        mappedParams['alert_number'] = mappedParams['alertNumber']
        delete mappedParams['alertNumber']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'code-scanning/commit-autofix',
  `Commit an autofix for a code scanning alert`,
  {
    owner: z.string(),
    repo: z.string(),
    alertNumber: z.string(),
    requestData: z.record(z.any()).optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, alertNumber, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/code-scanning/alerts/${alertNumber}/autofix/commits`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('alertNumber' in mappedParams) {
        mappedParams['alert_number'] = mappedParams['alertNumber']
        delete mappedParams['alertNumber']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'code-scanning/list-alert-instances',
  `List instances of a code scanning alert`,
  {
    owner: z.string(),
    repo: z.string(),
    alertNumber: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, alertNumber, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/code-scanning/alerts/${alertNumber}/instances`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('alertNumber' in mappedParams) {
        mappedParams['alert_number'] = mappedParams['alertNumber']
        delete mappedParams['alertNumber']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'code-scanning/list-recent-analyses',
  `List code scanning analyses for a repository`,
  {
    owner: z.string(),
    repo: z.string(),
    ref: z.string().optional(),
    sarifId: z.string().optional(),
    sort: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/code-scanning/analyses`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('sarifId' in mappedParams) {
        mappedParams['sarif_id'] = mappedParams['sarifId']
        delete mappedParams['sarifId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'code-scanning/get-analysis',
  `Get a code scanning analysis for a repository`,
  {
    owner: z.string(),
    repo: z.string(),
    analysisId: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, analysisId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/code-scanning/analyses/${analysisId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('analysisId' in mappedParams) {
        mappedParams['analysis_id'] = mappedParams['analysisId']
        delete mappedParams['analysisId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'code-scanning/delete-analysis',
  `Delete a code scanning analysis from a repository`,
  {
    owner: z.string(),
    repo: z.string(),
    analysisId: z.string(),
    confirmDelete: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, analysisId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/code-scanning/analyses/${analysisId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('analysisId' in mappedParams) {
        mappedParams['analysis_id'] = mappedParams['analysisId']
        delete mappedParams['analysisId']
      }
      if ('confirmDelete' in mappedParams) {
        mappedParams['confirm_delete'] = mappedParams['confirmDelete']
        delete mappedParams['confirmDelete']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'code-scanning/list-codeql-databases',
  `List CodeQL databases for a repository`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/code-scanning/codeql/databases`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'code-scanning/get-codeql-database',
  `Get a CodeQL database for a repository`,
  {
    owner: z.string(),
    repo: z.string(),
    language: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, language, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/code-scanning/codeql/databases/${language}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'code-scanning/delete-codeql-database',
  `Delete a CodeQL database`,
  {
    owner: z.string(),
    repo: z.string(),
    language: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, language, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/code-scanning/codeql/databases/${language}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'code-scanning/create-variant-analysis',
  `Create a CodeQL variant analysis`,
  {
    owner: z.string(),
    repo: z.string(),
    language: z.any(),
    queryPack: z.any(),
    repositories: z.any().optional(),
    repositoryLists: z.any().optional(),
    repositoryOwners: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/code-scanning/codeql/variant-analyses`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('queryPack' in mappedParams) {
        mappedParams['query_pack'] = mappedParams['queryPack']
        delete mappedParams['queryPack']
      }
      if ('repositoryLists' in mappedParams) {
        mappedParams['repository_lists'] = mappedParams['repositoryLists']
        delete mappedParams['repositoryLists']
      }
      if ('repositoryOwners' in mappedParams) {
        mappedParams['repository_owners'] = mappedParams['repositoryOwners']
        delete mappedParams['repositoryOwners']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'code-scanning/get-variant-analysis',
  `Get the summary of a CodeQL variant analysis`,
  {
    owner: z.string(),
    repo: z.string(),
    codeqlVariantAnalysisId: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, codeqlVariantAnalysisId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/code-scanning/codeql/variant-analyses/${codeqlVariantAnalysisId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('codeqlVariantAnalysisId' in mappedParams) {
        mappedParams['codeql_variant_analysis_id'] = mappedParams['codeqlVariantAnalysisId']
        delete mappedParams['codeqlVariantAnalysisId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'code-scanning/get-variant-analysis-repo-task',
  `Get the analysis status of a repository in a CodeQL variant analysis`,
  {
    owner: z.string(),
    repo: z.string(),
    codeqlVariantAnalysisId: z.string(),
    repoOwner: z.string(),
    repoName: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, codeqlVariantAnalysisId, repoOwner, repoName, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/code-scanning/codeql/variant-analyses/${codeqlVariantAnalysisId}/repos/${repoOwner}/${repoName}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('codeqlVariantAnalysisId' in mappedParams) {
        mappedParams['codeql_variant_analysis_id'] = mappedParams['codeqlVariantAnalysisId']
        delete mappedParams['codeqlVariantAnalysisId']
      }
      if ('repoOwner' in mappedParams) {
        mappedParams['repo_owner'] = mappedParams['repoOwner']
        delete mappedParams['repoOwner']
      }
      if ('repoName' in mappedParams) {
        mappedParams['repo_name'] = mappedParams['repoName']
        delete mappedParams['repoName']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'code-scanning/get-default-setup',
  `Get a code scanning default setup configuration`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/code-scanning/default-setup`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'code-scanning/update-default-setup',
  `Update a code scanning default setup configuration`,
  {
    owner: z.string(),
    repo: z.string(),
    requestData: z.record(z.any()).optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/code-scanning/default-setup`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PATCH',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'code-scanning/upload-sarif',
  `Upload an analysis as SARIF data`,
  {
    owner: z.string(),
    repo: z.string(),
    commitSha: z.any(),
    ref: z.any(),
    sarif: z.any(),
    checkoutUri: z.any().optional(),
    startedAt: z.any().optional(),
    toolName: z.any().optional(),
    validate: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/code-scanning/sarifs`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('commitSha' in mappedParams) {
        mappedParams['commit_sha'] = mappedParams['commitSha']
        delete mappedParams['commitSha']
      }
      if ('checkoutUri' in mappedParams) {
        mappedParams['checkout_uri'] = mappedParams['checkoutUri']
        delete mappedParams['checkoutUri']
      }
      if ('startedAt' in mappedParams) {
        mappedParams['started_at'] = mappedParams['startedAt']
        delete mappedParams['startedAt']
      }
      if ('toolName' in mappedParams) {
        mappedParams['tool_name'] = mappedParams['toolName']
        delete mappedParams['toolName']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'code-scanning/get-sarif',
  `Get information about a SARIF upload`,
  {
    owner: z.string(),
    repo: z.string(),
    sarifId: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, sarifId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/code-scanning/sarifs/${sarifId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('sarifId' in mappedParams) {
        mappedParams['sarif_id'] = mappedParams['sarifId']
        delete mappedParams['sarifId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'code-security/get-configuration-for-repository',
  `Get the code security configuration associated with a repository`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/code-security-configuration`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/codeowners-errors',
  `List CODEOWNERS errors`,
  {
    owner: z.string(),
    repo: z.string(),
    ref: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/codeowners/errors`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'codespaces/list-in-repository-for-authenticated-user',
  `List codespaces in a repository for the authenticated user`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/codespaces`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'codespaces/create-with-repo-for-authenticated-user',
  `Create a codespace in a repository`,
  {
    owner: z.string(),
    repo: z.string(),
    ref: z.any().optional(),
    location: z.any().optional(),
    geo: z.any().optional(),
    clientIp: z.any().optional(),
    machine: z.any().optional(),
    devcontainerPath: z.any().optional(),
    multiRepoPermissionsOptOut: z.any().optional(),
    workingDirectory: z.any().optional(),
    idleTimeoutMinutes: z.any().optional(),
    displayName: z.any().optional(),
    retentionPeriodMinutes: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/codespaces`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('clientIp' in mappedParams) {
        mappedParams['client_ip'] = mappedParams['clientIp']
        delete mappedParams['clientIp']
      }
      if ('devcontainerPath' in mappedParams) {
        mappedParams['devcontainer_path'] = mappedParams['devcontainerPath']
        delete mappedParams['devcontainerPath']
      }
      if ('multiRepoPermissionsOptOut' in mappedParams) {
        mappedParams['multi_repo_permissions_opt_out'] = mappedParams['multiRepoPermissionsOptOut']
        delete mappedParams['multiRepoPermissionsOptOut']
      }
      if ('workingDirectory' in mappedParams) {
        mappedParams['working_directory'] = mappedParams['workingDirectory']
        delete mappedParams['workingDirectory']
      }
      if ('idleTimeoutMinutes' in mappedParams) {
        mappedParams['idle_timeout_minutes'] = mappedParams['idleTimeoutMinutes']
        delete mappedParams['idleTimeoutMinutes']
      }
      if ('displayName' in mappedParams) {
        mappedParams['display_name'] = mappedParams['displayName']
        delete mappedParams['displayName']
      }
      if ('retentionPeriodMinutes' in mappedParams) {
        mappedParams['retention_period_minutes'] = mappedParams['retentionPeriodMinutes']
        delete mappedParams['retentionPeriodMinutes']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'codespaces/list-devcontainers-in-repository-for-authenticated-user',
  `List devcontainer configurations in a repository for the authenticated user`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/codespaces/devcontainers`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'codespaces/repo-machines-for-authenticated-user',
  `List available machine types for a repository`,
  {
    owner: z.string(),
    repo: z.string(),
    location: z.string().optional(),
    clientIp: z.string().optional(),
    ref: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/codespaces/machines`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('clientIp' in mappedParams) {
        mappedParams['client_ip'] = mappedParams['clientIp']
        delete mappedParams['clientIp']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'codespaces/pre-flight-with-repo-for-authenticated-user',
  `Get default attributes for a codespace`,
  {
    owner: z.string(),
    repo: z.string(),
    ref: z.string().optional(),
    clientIp: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/codespaces/new`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('clientIp' in mappedParams) {
        mappedParams['client_ip'] = mappedParams['clientIp']
        delete mappedParams['clientIp']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'codespaces/check-permissions-for-devcontainer',
  `Check if permissions defined by a devcontainer have been accepted by the authenticated user`,
  {
    owner: z.string(),
    repo: z.string(),
    ref: z.string(),
    devcontainerPath: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/codespaces/permissions_check`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('devcontainerPath' in mappedParams) {
        mappedParams['devcontainer_path'] = mappedParams['devcontainerPath']
        delete mappedParams['devcontainerPath']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'codespaces/list-repo-secrets',
  `List repository secrets`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/codespaces/secrets`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'codespaces/get-repo-public-key',
  `Get a repository public key`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/codespaces/secrets/public-key`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'codespaces/get-repo-secret',
  `Get a repository secret`,
  {
    owner: z.string(),
    repo: z.string(),
    secretName: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, secretName, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/codespaces/secrets/${secretName}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('secretName' in mappedParams) {
        mappedParams['secret_name'] = mappedParams['secretName']
        delete mappedParams['secretName']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'codespaces/create-or-update-repo-secret',
  `Create or update a repository secret`,
  {
    owner: z.string(),
    repo: z.string(),
    secretName: z.string(),
    encryptedValue: z.any().optional(),
    keyId: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, secretName, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/codespaces/secrets/${secretName}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('secretName' in mappedParams) {
        mappedParams['secret_name'] = mappedParams['secretName']
        delete mappedParams['secretName']
      }
      if ('encryptedValue' in mappedParams) {
        mappedParams['encrypted_value'] = mappedParams['encryptedValue']
        delete mappedParams['encryptedValue']
      }
      if ('keyId' in mappedParams) {
        mappedParams['key_id'] = mappedParams['keyId']
        delete mappedParams['keyId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'codespaces/delete-repo-secret',
  `Delete a repository secret`,
  {
    owner: z.string(),
    repo: z.string(),
    secretName: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, secretName, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/codespaces/secrets/${secretName}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('secretName' in mappedParams) {
        mappedParams['secret_name'] = mappedParams['secretName']
        delete mappedParams['secretName']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/list-collaborators',
  `List repository collaborators`,
  {
    owner: z.string(),
    repo: z.string(),
    affiliation: z.string().optional(),
    permission: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/collaborators`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/check-collaborator',
  `Check if a user is a repository collaborator`,
  {
    owner: z.string(),
    repo: z.string(),
    username: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, username, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/collaborators/${username}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/add-collaborator',
  `Add a repository collaborator`,
  {
    owner: z.string(),
    repo: z.string(),
    username: z.string(),
    permission: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, username, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/collaborators/${username}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/remove-collaborator',
  `Remove a repository collaborator`,
  {
    owner: z.string(),
    repo: z.string(),
    username: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, username, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/collaborators/${username}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/get-collaborator-permission-level',
  `Get repository permissions for a user`,
  {
    owner: z.string(),
    repo: z.string(),
    username: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, username, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/collaborators/${username}/permission`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/list-commit-comments-for-repo',
  `List commit comments for a repository`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/comments`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/get-commit-comment',
  `Get a commit comment`,
  {
    owner: z.string(),
    repo: z.string(),
    commentId: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, commentId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/comments/${commentId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('commentId' in mappedParams) {
        mappedParams['comment_id'] = mappedParams['commentId']
        delete mappedParams['commentId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/update-commit-comment',
  `Update a commit comment`,
  {
    owner: z.string(),
    repo: z.string(),
    commentId: z.string(),
    body: z.any(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, commentId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/comments/${commentId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('commentId' in mappedParams) {
        mappedParams['comment_id'] = mappedParams['commentId']
        delete mappedParams['commentId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PATCH',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/delete-commit-comment',
  `Delete a commit comment`,
  {
    owner: z.string(),
    repo: z.string(),
    commentId: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, commentId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/comments/${commentId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('commentId' in mappedParams) {
        mappedParams['comment_id'] = mappedParams['commentId']
        delete mappedParams['commentId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'reactions/list-for-commit-comment',
  `List reactions for a commit comment`,
  {
    owner: z.string(),
    repo: z.string(),
    commentId: z.string(),
    content: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, commentId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/comments/${commentId}/reactions`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('commentId' in mappedParams) {
        mappedParams['comment_id'] = mappedParams['commentId']
        delete mappedParams['commentId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'reactions/create-for-commit-comment',
  `Create reaction for a commit comment`,
  {
    owner: z.string(),
    repo: z.string(),
    commentId: z.string(),
    content: z.any(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, commentId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/comments/${commentId}/reactions`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('commentId' in mappedParams) {
        mappedParams['comment_id'] = mappedParams['commentId']
        delete mappedParams['commentId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'reactions/delete-for-commit-comment',
  `Delete a commit comment reaction`,
  {
    owner: z.string(),
    repo: z.string(),
    commentId: z.string(),
    reactionId: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, commentId, reactionId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/comments/${commentId}/reactions/${reactionId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('commentId' in mappedParams) {
        mappedParams['comment_id'] = mappedParams['commentId']
        delete mappedParams['commentId']
      }
      if ('reactionId' in mappedParams) {
        mappedParams['reaction_id'] = mappedParams['reactionId']
        delete mappedParams['reactionId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/list-commits',
  `List commits`,
  {
    owner: z.string(),
    repo: z.string(),
    sha: z.string().optional(),
    path: z.string().optional(),
    author: z.string().optional(),
    committer: z.string().optional(),
    since: z.string().optional(),
    until: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/commits`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/list-branches-for-head-commit',
  `List branches for HEAD commit`,
  {
    owner: z.string(),
    repo: z.string(),
    commitSha: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, commitSha, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/commits/${commitSha}/branches-where-head`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('commitSha' in mappedParams) {
        mappedParams['commit_sha'] = mappedParams['commitSha']
        delete mappedParams['commitSha']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/list-comments-for-commit',
  `List commit comments`,
  {
    owner: z.string(),
    repo: z.string(),
    commitSha: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, commitSha, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/commits/${commitSha}/comments`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('commitSha' in mappedParams) {
        mappedParams['commit_sha'] = mappedParams['commitSha']
        delete mappedParams['commitSha']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/create-commit-comment',
  `Create a commit comment`,
  {
    owner: z.string(),
    repo: z.string(),
    commitSha: z.string(),
    body: z.any(),
    path: z.any().optional(),
    position: z.any().optional(),
    line: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, commitSha, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/commits/${commitSha}/comments`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('commitSha' in mappedParams) {
        mappedParams['commit_sha'] = mappedParams['commitSha']
        delete mappedParams['commitSha']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/list-pull-requests-associated-with-commit',
  `List pull requests associated with a commit`,
  {
    owner: z.string(),
    repo: z.string(),
    commitSha: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, commitSha, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/commits/${commitSha}/pulls`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('commitSha' in mappedParams) {
        mappedParams['commit_sha'] = mappedParams['commitSha']
        delete mappedParams['commitSha']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/get-commit',
  `Get a commit`,
  {
    owner: z.string(),
    repo: z.string(),
    ref: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ref, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/commits/${ref}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'checks/list-for-ref',
  `List check runs for a Git reference`,
  {
    owner: z.string(),
    repo: z.string(),
    ref: z.string(),
    filter: z.string().optional(),
    appId: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ref, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/commits/${ref}/check-runs`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('appId' in mappedParams) {
        mappedParams['app_id'] = mappedParams['appId']
        delete mappedParams['appId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'checks/list-suites-for-ref',
  `List check suites for a Git reference`,
  {
    owner: z.string(),
    repo: z.string(),
    ref: z.string(),
    appId: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ref, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/commits/${ref}/check-suites`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('appId' in mappedParams) {
        mappedParams['app_id'] = mappedParams['appId']
        delete mappedParams['appId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/get-combined-status-for-ref',
  `Get the combined status for a specific reference`,
  {
    owner: z.string(),
    repo: z.string(),
    ref: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ref, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/commits/${ref}/status`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/list-commit-statuses-for-ref',
  `List commit statuses for a reference`,
  {
    owner: z.string(),
    repo: z.string(),
    ref: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ref, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/commits/${ref}/statuses`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/get-community-profile-metrics',
  `Get community profile metrics`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/community/profile`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/compare-commits',
  `Compare two commits`,
  {
    owner: z.string(),
    repo: z.string(),
    basehead: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, basehead, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/compare/${basehead}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/get-content',
  `Get repository content`,
  {
    owner: z.string(),
    repo: z.string(),
    path: z.string(),
    ref: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, path, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/contents/${path}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/create-or-update-file-contents',
  `Create or update file contents`,
  {
    owner: z.string(),
    repo: z.string(),
    path: z.string(),
    message: z.any(),
    content: z.any(),
    sha: z.any().optional(),
    branch: z.any().optional(),
    committer: z.any().optional(),
    author: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, path, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/contents/${path}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/delete-file',
  `Delete a file`,
  {
    owner: z.string(),
    repo: z.string(),
    path: z.string(),
    message: z.any(),
    sha: z.any(),
    branch: z.any().optional(),
    committer: z.any().optional(),
    author: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, path, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/contents/${path}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/list-contributors',
  `List repository contributors`,
  {
    owner: z.string(),
    repo: z.string(),
    anon: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/contributors`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'dependabot/list-alerts-for-repo',
  `List Dependabot alerts for a repository`,
  {
    owner: z.string(),
    repo: z.string(),
    page: z.string().optional(),
    perPage: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/dependabot/alerts`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('perPage' in mappedParams) {
        mappedParams['per_page'] = mappedParams['perPage']
        delete mappedParams['perPage']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'dependabot/get-alert',
  `Get a Dependabot alert`,
  {
    owner: z.string(),
    repo: z.string(),
    alertNumber: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, alertNumber, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/dependabot/alerts/${alertNumber}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('alertNumber' in mappedParams) {
        mappedParams['alert_number'] = mappedParams['alertNumber']
        delete mappedParams['alertNumber']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'dependabot/update-alert',
  `Update a Dependabot alert`,
  {
    owner: z.string(),
    repo: z.string(),
    alertNumber: z.string(),
    state: z.any(),
    dismissedReason: z.any().optional(),
    dismissedComment: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, alertNumber, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/dependabot/alerts/${alertNumber}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('alertNumber' in mappedParams) {
        mappedParams['alert_number'] = mappedParams['alertNumber']
        delete mappedParams['alertNumber']
      }
      if ('dismissedReason' in mappedParams) {
        mappedParams['dismissed_reason'] = mappedParams['dismissedReason']
        delete mappedParams['dismissedReason']
      }
      if ('dismissedComment' in mappedParams) {
        mappedParams['dismissed_comment'] = mappedParams['dismissedComment']
        delete mappedParams['dismissedComment']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PATCH',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'dependabot/list-repo-secrets',
  `List repository secrets`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/dependabot/secrets`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'dependabot/get-repo-public-key',
  `Get a repository public key`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/dependabot/secrets/public-key`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'dependabot/get-repo-secret',
  `Get a repository secret`,
  {
    owner: z.string(),
    repo: z.string(),
    secretName: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, secretName, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/dependabot/secrets/${secretName}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('secretName' in mappedParams) {
        mappedParams['secret_name'] = mappedParams['secretName']
        delete mappedParams['secretName']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'dependabot/create-or-update-repo-secret',
  `Create or update a repository secret`,
  {
    owner: z.string(),
    repo: z.string(),
    secretName: z.string(),
    encryptedValue: z.any().optional(),
    keyId: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, secretName, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/dependabot/secrets/${secretName}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('secretName' in mappedParams) {
        mappedParams['secret_name'] = mappedParams['secretName']
        delete mappedParams['secretName']
      }
      if ('encryptedValue' in mappedParams) {
        mappedParams['encrypted_value'] = mappedParams['encryptedValue']
        delete mappedParams['encryptedValue']
      }
      if ('keyId' in mappedParams) {
        mappedParams['key_id'] = mappedParams['keyId']
        delete mappedParams['keyId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'dependabot/delete-repo-secret',
  `Delete a repository secret`,
  {
    owner: z.string(),
    repo: z.string(),
    secretName: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, secretName, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/dependabot/secrets/${secretName}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('secretName' in mappedParams) {
        mappedParams['secret_name'] = mappedParams['secretName']
        delete mappedParams['secretName']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'dependency-graph/diff-range',
  `Get a diff of the dependencies between commits`,
  {
    owner: z.string(),
    repo: z.string(),
    basehead: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, basehead, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/dependency-graph/compare/${basehead}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'dependency-graph/export-sbom',
  `Export a software bill of materials (SBOM) for a repository.`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/dependency-graph/sbom`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'dependency-graph/create-repository-snapshot',
  `Create a snapshot of dependencies for a repository`,
  {
    owner: z.string(),
    repo: z.string(),
    requestData: z.record(z.any()).optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/dependency-graph/snapshots`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/list-deployments',
  `List deployments`,
  {
    owner: z.string(),
    repo: z.string(),
    sha: z.string().optional(),
    ref: z.string().optional(),
    task: z.string().optional(),
    environment: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/deployments`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/create-deployment',
  `Create a deployment`,
  {
    owner: z.string(),
    repo: z.string(),
    ref: z.any(),
    task: z.any().optional(),
    autoMerge: z.any().optional(),
    requiredContexts: z.any().optional(),
    payload: z.any().optional(),
    environment: z.any().optional(),
    description: z.any().optional(),
    transientEnvironment: z.any().optional(),
    productionEnvironment: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/deployments`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('autoMerge' in mappedParams) {
        mappedParams['auto_merge'] = mappedParams['autoMerge']
        delete mappedParams['autoMerge']
      }
      if ('requiredContexts' in mappedParams) {
        mappedParams['required_contexts'] = mappedParams['requiredContexts']
        delete mappedParams['requiredContexts']
      }
      if ('transientEnvironment' in mappedParams) {
        mappedParams['transient_environment'] = mappedParams['transientEnvironment']
        delete mappedParams['transientEnvironment']
      }
      if ('productionEnvironment' in mappedParams) {
        mappedParams['production_environment'] = mappedParams['productionEnvironment']
        delete mappedParams['productionEnvironment']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/get-deployment',
  `Get a deployment`,
  {
    owner: z.string(),
    repo: z.string(),
    deploymentId: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, deploymentId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/deployments/${deploymentId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('deploymentId' in mappedParams) {
        mappedParams['deployment_id'] = mappedParams['deploymentId']
        delete mappedParams['deploymentId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/delete-deployment',
  `Delete a deployment`,
  {
    owner: z.string(),
    repo: z.string(),
    deploymentId: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, deploymentId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/deployments/${deploymentId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('deploymentId' in mappedParams) {
        mappedParams['deployment_id'] = mappedParams['deploymentId']
        delete mappedParams['deploymentId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/list-deployment-statuses',
  `List deployment statuses`,
  {
    owner: z.string(),
    repo: z.string(),
    deploymentId: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, deploymentId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/deployments/${deploymentId}/statuses`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('deploymentId' in mappedParams) {
        mappedParams['deployment_id'] = mappedParams['deploymentId']
        delete mappedParams['deploymentId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/create-deployment-status',
  `Create a deployment status`,
  {
    owner: z.string(),
    repo: z.string(),
    deploymentId: z.string(),
    state: z.any(),
    targetUrl: z.any().optional(),
    logUrl: z.any().optional(),
    description: z.any().optional(),
    environment: z.any().optional(),
    environmentUrl: z.any().optional(),
    autoInactive: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, deploymentId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/deployments/${deploymentId}/statuses`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('deploymentId' in mappedParams) {
        mappedParams['deployment_id'] = mappedParams['deploymentId']
        delete mappedParams['deploymentId']
      }
      if ('targetUrl' in mappedParams) {
        mappedParams['target_url'] = mappedParams['targetUrl']
        delete mappedParams['targetUrl']
      }
      if ('logUrl' in mappedParams) {
        mappedParams['log_url'] = mappedParams['logUrl']
        delete mappedParams['logUrl']
      }
      if ('environmentUrl' in mappedParams) {
        mappedParams['environment_url'] = mappedParams['environmentUrl']
        delete mappedParams['environmentUrl']
      }
      if ('autoInactive' in mappedParams) {
        mappedParams['auto_inactive'] = mappedParams['autoInactive']
        delete mappedParams['autoInactive']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/get-deployment-status',
  `Get a deployment status`,
  {
    owner: z.string(),
    repo: z.string(),
    deploymentId: z.string(),
    statusId: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, deploymentId, statusId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/deployments/${deploymentId}/statuses/${statusId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('deploymentId' in mappedParams) {
        mappedParams['deployment_id'] = mappedParams['deploymentId']
        delete mappedParams['deploymentId']
      }
      if ('statusId' in mappedParams) {
        mappedParams['status_id'] = mappedParams['statusId']
        delete mappedParams['statusId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/create-dispatch-event',
  `Create a repository dispatch event`,
  {
    owner: z.string(),
    repo: z.string(),
    eventType: z.any(),
    clientPayload: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/dispatches`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('eventType' in mappedParams) {
        mappedParams['event_type'] = mappedParams['eventType']
        delete mappedParams['eventType']
      }
      if ('clientPayload' in mappedParams) {
        mappedParams['client_payload'] = mappedParams['clientPayload']
        delete mappedParams['clientPayload']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/get-all-environments',
  `List environments`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/environments`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/get-environment',
  `Get an environment`,
  {
    owner: z.string(),
    repo: z.string(),
    environmentName: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, environmentName, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/environments/${environmentName}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('environmentName' in mappedParams) {
        mappedParams['environment_name'] = mappedParams['environmentName']
        delete mappedParams['environmentName']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/create-or-update-environment',
  `Create or update an environment`,
  {
    owner: z.string(),
    repo: z.string(),
    environmentName: z.string(),
    waitTimer: z.any().optional(),
    preventSelfReview: z.any().optional(),
    reviewers: z.any().optional(),
    deploymentBranchPolicy: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, environmentName, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/environments/${environmentName}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('environmentName' in mappedParams) {
        mappedParams['environment_name'] = mappedParams['environmentName']
        delete mappedParams['environmentName']
      }
      if ('waitTimer' in mappedParams) {
        mappedParams['wait_timer'] = mappedParams['waitTimer']
        delete mappedParams['waitTimer']
      }
      if ('preventSelfReview' in mappedParams) {
        mappedParams['prevent_self_review'] = mappedParams['preventSelfReview']
        delete mappedParams['preventSelfReview']
      }
      if ('deploymentBranchPolicy' in mappedParams) {
        mappedParams['deployment_branch_policy'] = mappedParams['deploymentBranchPolicy']
        delete mappedParams['deploymentBranchPolicy']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/delete-an-environment',
  `Delete an environment`,
  {
    owner: z.string(),
    repo: z.string(),
    environmentName: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, environmentName, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/environments/${environmentName}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('environmentName' in mappedParams) {
        mappedParams['environment_name'] = mappedParams['environmentName']
        delete mappedParams['environmentName']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/list-deployment-branch-policies',
  `List deployment branch policies`,
  {
    owner: z.string(),
    repo: z.string(),
    environmentName: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, environmentName, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/environments/${environmentName}/deployment-branch-policies`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('environmentName' in mappedParams) {
        mappedParams['environment_name'] = mappedParams['environmentName']
        delete mappedParams['environmentName']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/create-deployment-branch-policy',
  `Create a deployment branch policy`,
  {
    owner: z.string(),
    repo: z.string(),
    environmentName: z.string(),
    requestData: z.record(z.any()).optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, environmentName, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/environments/${environmentName}/deployment-branch-policies`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('environmentName' in mappedParams) {
        mappedParams['environment_name'] = mappedParams['environmentName']
        delete mappedParams['environmentName']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/get-deployment-branch-policy',
  `Get a deployment branch policy`,
  {
    owner: z.string(),
    repo: z.string(),
    environmentName: z.string(),
    branchPolicyId: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, environmentName, branchPolicyId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/environments/${environmentName}/deployment-branch-policies/${branchPolicyId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('environmentName' in mappedParams) {
        mappedParams['environment_name'] = mappedParams['environmentName']
        delete mappedParams['environmentName']
      }
      if ('branchPolicyId' in mappedParams) {
        mappedParams['branch_policy_id'] = mappedParams['branchPolicyId']
        delete mappedParams['branchPolicyId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/update-deployment-branch-policy',
  `Update a deployment branch policy`,
  {
    owner: z.string(),
    repo: z.string(),
    environmentName: z.string(),
    branchPolicyId: z.string(),
    requestData: z.record(z.any()).optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, environmentName, branchPolicyId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/environments/${environmentName}/deployment-branch-policies/${branchPolicyId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('environmentName' in mappedParams) {
        mappedParams['environment_name'] = mappedParams['environmentName']
        delete mappedParams['environmentName']
      }
      if ('branchPolicyId' in mappedParams) {
        mappedParams['branch_policy_id'] = mappedParams['branchPolicyId']
        delete mappedParams['branchPolicyId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/delete-deployment-branch-policy',
  `Delete a deployment branch policy`,
  {
    owner: z.string(),
    repo: z.string(),
    environmentName: z.string(),
    branchPolicyId: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, environmentName, branchPolicyId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/environments/${environmentName}/deployment-branch-policies/${branchPolicyId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('environmentName' in mappedParams) {
        mappedParams['environment_name'] = mappedParams['environmentName']
        delete mappedParams['environmentName']
      }
      if ('branchPolicyId' in mappedParams) {
        mappedParams['branch_policy_id'] = mappedParams['branchPolicyId']
        delete mappedParams['branchPolicyId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/get-all-deployment-protection-rules',
  `Get all deployment protection rules for an environment`,
  {
    owner: z.string(),
    repo: z.string(),
    environmentName: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, environmentName, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/environments/${environmentName}/deployment_protection_rules`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('environmentName' in mappedParams) {
        mappedParams['environment_name'] = mappedParams['environmentName']
        delete mappedParams['environmentName']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/create-deployment-protection-rule',
  `Create a custom deployment protection rule on an environment`,
  {
    owner: z.string(),
    repo: z.string(),
    environmentName: z.string(),
    integrationId: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, environmentName, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/environments/${environmentName}/deployment_protection_rules`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('environmentName' in mappedParams) {
        mappedParams['environment_name'] = mappedParams['environmentName']
        delete mappedParams['environmentName']
      }
      if ('integrationId' in mappedParams) {
        mappedParams['integration_id'] = mappedParams['integrationId']
        delete mappedParams['integrationId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/list-custom-deployment-rule-integrations',
  `List custom deployment rule integrations available for an environment`,
  {
    owner: z.string(),
    repo: z.string(),
    environmentName: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, environmentName, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/environments/${environmentName}/deployment_protection_rules/apps`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('environmentName' in mappedParams) {
        mappedParams['environment_name'] = mappedParams['environmentName']
        delete mappedParams['environmentName']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/get-custom-deployment-protection-rule',
  `Get a custom deployment protection rule`,
  {
    owner: z.string(),
    repo: z.string(),
    environmentName: z.string(),
    protectionRuleId: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, environmentName, protectionRuleId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/environments/${environmentName}/deployment_protection_rules/${protectionRuleId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('environmentName' in mappedParams) {
        mappedParams['environment_name'] = mappedParams['environmentName']
        delete mappedParams['environmentName']
      }
      if ('protectionRuleId' in mappedParams) {
        mappedParams['protection_rule_id'] = mappedParams['protectionRuleId']
        delete mappedParams['protectionRuleId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/disable-deployment-protection-rule',
  `Disable a custom protection rule for an environment`,
  {
    owner: z.string(),
    repo: z.string(),
    environmentName: z.string(),
    protectionRuleId: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, environmentName, protectionRuleId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/environments/${environmentName}/deployment_protection_rules/${protectionRuleId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('environmentName' in mappedParams) {
        mappedParams['environment_name'] = mappedParams['environmentName']
        delete mappedParams['environmentName']
      }
      if ('protectionRuleId' in mappedParams) {
        mappedParams['protection_rule_id'] = mappedParams['protectionRuleId']
        delete mappedParams['protectionRuleId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/list-environment-secrets',
  `List environment secrets`,
  {
    owner: z.string(),
    repo: z.string(),
    environmentName: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, environmentName, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/environments/${environmentName}/secrets`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('environmentName' in mappedParams) {
        mappedParams['environment_name'] = mappedParams['environmentName']
        delete mappedParams['environmentName']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/get-environment-public-key',
  `Get an environment public key`,
  {
    owner: z.string(),
    repo: z.string(),
    environmentName: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, environmentName, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/environments/${environmentName}/secrets/public-key`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('environmentName' in mappedParams) {
        mappedParams['environment_name'] = mappedParams['environmentName']
        delete mappedParams['environmentName']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/get-environment-secret',
  `Get an environment secret`,
  {
    owner: z.string(),
    repo: z.string(),
    environmentName: z.string(),
    secretName: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, environmentName, secretName, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/environments/${environmentName}/secrets/${secretName}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('environmentName' in mappedParams) {
        mappedParams['environment_name'] = mappedParams['environmentName']
        delete mappedParams['environmentName']
      }
      if ('secretName' in mappedParams) {
        mappedParams['secret_name'] = mappedParams['secretName']
        delete mappedParams['secretName']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/create-or-update-environment-secret',
  `Create or update an environment secret`,
  {
    owner: z.string(),
    repo: z.string(),
    environmentName: z.string(),
    secretName: z.string(),
    encryptedValue: z.any(),
    keyId: z.any(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, environmentName, secretName, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/environments/${environmentName}/secrets/${secretName}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('environmentName' in mappedParams) {
        mappedParams['environment_name'] = mappedParams['environmentName']
        delete mappedParams['environmentName']
      }
      if ('secretName' in mappedParams) {
        mappedParams['secret_name'] = mappedParams['secretName']
        delete mappedParams['secretName']
      }
      if ('encryptedValue' in mappedParams) {
        mappedParams['encrypted_value'] = mappedParams['encryptedValue']
        delete mappedParams['encryptedValue']
      }
      if ('keyId' in mappedParams) {
        mappedParams['key_id'] = mappedParams['keyId']
        delete mappedParams['keyId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/delete-environment-secret',
  `Delete an environment secret`,
  {
    owner: z.string(),
    repo: z.string(),
    environmentName: z.string(),
    secretName: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, environmentName, secretName, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/environments/${environmentName}/secrets/${secretName}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('environmentName' in mappedParams) {
        mappedParams['environment_name'] = mappedParams['environmentName']
        delete mappedParams['environmentName']
      }
      if ('secretName' in mappedParams) {
        mappedParams['secret_name'] = mappedParams['secretName']
        delete mappedParams['secretName']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/list-environment-variables',
  `List environment variables`,
  {
    owner: z.string(),
    repo: z.string(),
    environmentName: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, environmentName, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/environments/${environmentName}/variables`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('environmentName' in mappedParams) {
        mappedParams['environment_name'] = mappedParams['environmentName']
        delete mappedParams['environmentName']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/create-environment-variable',
  `Create an environment variable`,
  {
    owner: z.string(),
    repo: z.string(),
    environmentName: z.string(),
    name: z.any(),
    value: z.any(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, environmentName, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/environments/${environmentName}/variables`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('environmentName' in mappedParams) {
        mappedParams['environment_name'] = mappedParams['environmentName']
        delete mappedParams['environmentName']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/get-environment-variable',
  `Get an environment variable`,
  {
    owner: z.string(),
    repo: z.string(),
    environmentName: z.string(),
    name: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, environmentName, name, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/environments/${environmentName}/variables/${name}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('environmentName' in mappedParams) {
        mappedParams['environment_name'] = mappedParams['environmentName']
        delete mappedParams['environmentName']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/update-environment-variable',
  `Update an environment variable`,
  {
    owner: z.string(),
    repo: z.string(),
    environmentName: z.string(),
    name: z.string(),
    value: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, environmentName, name, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/environments/${environmentName}/variables/${name}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('environmentName' in mappedParams) {
        mappedParams['environment_name'] = mappedParams['environmentName']
        delete mappedParams['environmentName']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PATCH',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'actions/delete-environment-variable',
  `Delete an environment variable`,
  {
    owner: z.string(),
    repo: z.string(),
    environmentName: z.string(),
    name: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, environmentName, name, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/environments/${environmentName}/variables/${name}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('environmentName' in mappedParams) {
        mappedParams['environment_name'] = mappedParams['environmentName']
        delete mappedParams['environmentName']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'activity/list-repo-events',
  `List repository events`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/events`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/list-forks',
  `List forks`,
  {
    owner: z.string(),
    repo: z.string(),
    sort: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/forks`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/create-fork',
  `Create a fork`,
  {
    owner: z.string(),
    repo: z.string(),
    organization: z.any().optional(),
    name: z.any().optional(),
    defaultBranchOnly: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/forks`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('defaultBranchOnly' in mappedParams) {
        mappedParams['default_branch_only'] = mappedParams['defaultBranchOnly']
        delete mappedParams['defaultBranchOnly']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'git/create-blob',
  `Create a blob`,
  {
    owner: z.string(),
    repo: z.string(),
    content: z.any(),
    encoding: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/git/blobs`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'git/get-blob',
  `Get a blob`,
  {
    owner: z.string(),
    repo: z.string(),
    fileSha: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, fileSha, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/git/blobs/${fileSha}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('fileSha' in mappedParams) {
        mappedParams['file_sha'] = mappedParams['fileSha']
        delete mappedParams['fileSha']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'git/create-commit',
  `Create a commit`,
  {
    owner: z.string(),
    repo: z.string(),
    message: z.any(),
    tree: z.any(),
    parents: z.any().optional(),
    author: z.any().optional(),
    committer: z.any().optional(),
    signature: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/git/commits`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'git/get-commit',
  `Get a commit object`,
  {
    owner: z.string(),
    repo: z.string(),
    commitSha: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, commitSha, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/git/commits/${commitSha}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('commitSha' in mappedParams) {
        mappedParams['commit_sha'] = mappedParams['commitSha']
        delete mappedParams['commitSha']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'git/list-matching-refs',
  `List matching references`,
  {
    owner: z.string(),
    repo: z.string(),
    ref: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ref, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/git/matching-refs/${ref}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'git/get-ref',
  `Get a reference`,
  {
    owner: z.string(),
    repo: z.string(),
    ref: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ref, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/git/ref/${ref}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'git/create-ref',
  `Create a reference`,
  {
    owner: z.string(),
    repo: z.string(),
    ref: z.any(),
    sha: z.any(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/git/refs`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'git/update-ref',
  `Update a reference`,
  {
    owner: z.string(),
    repo: z.string(),
    ref: z.string(),
    sha: z.any(),
    force: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ref, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/git/refs/${ref}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PATCH',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'git/delete-ref',
  `Delete a reference`,
  {
    owner: z.string(),
    repo: z.string(),
    ref: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ref, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/git/refs/${ref}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'git/create-tag',
  `Create a tag object`,
  {
    owner: z.string(),
    repo: z.string(),
    tag: z.any(),
    message: z.any(),
    object: z.any(),
    type: z.any(),
    tagger: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/git/tags`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'git/get-tag',
  `Get a tag`,
  {
    owner: z.string(),
    repo: z.string(),
    tagSha: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, tagSha, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/git/tags/${tagSha}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('tagSha' in mappedParams) {
        mappedParams['tag_sha'] = mappedParams['tagSha']
        delete mappedParams['tagSha']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'git/create-tree',
  `Create a tree`,
  {
    owner: z.string(),
    repo: z.string(),
    tree: z.any(),
    baseTree: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/git/trees`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('baseTree' in mappedParams) {
        mappedParams['base_tree'] = mappedParams['baseTree']
        delete mappedParams['baseTree']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'git/get-tree',
  `Get a tree`,
  {
    owner: z.string(),
    repo: z.string(),
    treeSha: z.string(),
    recursive: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, treeSha, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/git/trees/${treeSha}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('treeSha' in mappedParams) {
        mappedParams['tree_sha'] = mappedParams['treeSha']
        delete mappedParams['treeSha']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/list-webhooks',
  `List repository webhooks`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/hooks`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/create-webhook',
  `Create a repository webhook`,
  {
    owner: z.string(),
    repo: z.string(),
    name: z.any().optional(),
    config: z.any().optional(),
    events: z.any().optional(),
    active: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/hooks`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/get-webhook',
  `Get a repository webhook`,
  {
    owner: z.string(),
    repo: z.string(),
    hookId: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, hookId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/hooks/${hookId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('hookId' in mappedParams) {
        mappedParams['hook_id'] = mappedParams['hookId']
        delete mappedParams['hookId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/update-webhook',
  `Update a repository webhook`,
  {
    owner: z.string(),
    repo: z.string(),
    hookId: z.string(),
    config: z.any().optional(),
    events: z.any().optional(),
    addEvents: z.any().optional(),
    removeEvents: z.any().optional(),
    active: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, hookId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/hooks/${hookId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('hookId' in mappedParams) {
        mappedParams['hook_id'] = mappedParams['hookId']
        delete mappedParams['hookId']
      }
      if ('addEvents' in mappedParams) {
        mappedParams['add_events'] = mappedParams['addEvents']
        delete mappedParams['addEvents']
      }
      if ('removeEvents' in mappedParams) {
        mappedParams['remove_events'] = mappedParams['removeEvents']
        delete mappedParams['removeEvents']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PATCH',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/delete-webhook',
  `Delete a repository webhook`,
  {
    owner: z.string(),
    repo: z.string(),
    hookId: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, hookId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/hooks/${hookId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('hookId' in mappedParams) {
        mappedParams['hook_id'] = mappedParams['hookId']
        delete mappedParams['hookId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/get-webhook-config-for-repo',
  `Get a webhook configuration for a repository`,
  {
    owner: z.string(),
    repo: z.string(),
    hookId: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, hookId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/hooks/${hookId}/config`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('hookId' in mappedParams) {
        mappedParams['hook_id'] = mappedParams['hookId']
        delete mappedParams['hookId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/update-webhook-config-for-repo',
  `Update a webhook configuration for a repository`,
  {
    owner: z.string(),
    repo: z.string(),
    hookId: z.string(),
    url: z.any().optional(),
    contentType: z.any().optional(),
    secret: z.any().optional(),
    insecureSsl: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, hookId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/hooks/${hookId}/config`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('hookId' in mappedParams) {
        mappedParams['hook_id'] = mappedParams['hookId']
        delete mappedParams['hookId']
      }
      if ('contentType' in mappedParams) {
        mappedParams['content_type'] = mappedParams['contentType']
        delete mappedParams['contentType']
      }
      if ('insecureSsl' in mappedParams) {
        mappedParams['insecure_ssl'] = mappedParams['insecureSsl']
        delete mappedParams['insecureSsl']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PATCH',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/list-webhook-deliveries',
  `List deliveries for a repository webhook`,
  {
    owner: z.string(),
    repo: z.string(),
    hookId: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, hookId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/hooks/${hookId}/deliveries`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('hookId' in mappedParams) {
        mappedParams['hook_id'] = mappedParams['hookId']
        delete mappedParams['hookId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/get-webhook-delivery',
  `Get a delivery for a repository webhook`,
  {
    owner: z.string(),
    repo: z.string(),
    hookId: z.string(),
    deliveryId: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, hookId, deliveryId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/hooks/${hookId}/deliveries/${deliveryId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('hookId' in mappedParams) {
        mappedParams['hook_id'] = mappedParams['hookId']
        delete mappedParams['hookId']
      }
      if ('deliveryId' in mappedParams) {
        mappedParams['delivery_id'] = mappedParams['deliveryId']
        delete mappedParams['deliveryId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/redeliver-webhook-delivery',
  `Redeliver a delivery for a repository webhook`,
  {
    owner: z.string(),
    repo: z.string(),
    hookId: z.string(),
    deliveryId: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, hookId, deliveryId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/hooks/${hookId}/deliveries/${deliveryId}/attempts`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('hookId' in mappedParams) {
        mappedParams['hook_id'] = mappedParams['hookId']
        delete mappedParams['hookId']
      }
      if ('deliveryId' in mappedParams) {
        mappedParams['delivery_id'] = mappedParams['deliveryId']
        delete mappedParams['deliveryId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/ping-webhook',
  `Ping a repository webhook`,
  {
    owner: z.string(),
    repo: z.string(),
    hookId: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, hookId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/hooks/${hookId}/pings`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('hookId' in mappedParams) {
        mappedParams['hook_id'] = mappedParams['hookId']
        delete mappedParams['hookId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/test-push-webhook',
  `Test the push repository webhook`,
  {
    owner: z.string(),
    repo: z.string(),
    hookId: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, hookId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/hooks/${hookId}/tests`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('hookId' in mappedParams) {
        mappedParams['hook_id'] = mappedParams['hookId']
        delete mappedParams['hookId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'migrations/get-import-status',
  `Get an import status`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/import`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'migrations/start-import',
  `Start an import`,
  {
    owner: z.string(),
    repo: z.string(),
    vcsUrl: z.any(),
    vcs: z.any().optional(),
    vcsUsername: z.any().optional(),
    vcsPassword: z.any().optional(),
    tfvcProject: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/import`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('vcsUrl' in mappedParams) {
        mappedParams['vcs_url'] = mappedParams['vcsUrl']
        delete mappedParams['vcsUrl']
      }
      if ('vcsUsername' in mappedParams) {
        mappedParams['vcs_username'] = mappedParams['vcsUsername']
        delete mappedParams['vcsUsername']
      }
      if ('vcsPassword' in mappedParams) {
        mappedParams['vcs_password'] = mappedParams['vcsPassword']
        delete mappedParams['vcsPassword']
      }
      if ('tfvcProject' in mappedParams) {
        mappedParams['tfvc_project'] = mappedParams['tfvcProject']
        delete mappedParams['tfvcProject']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'migrations/update-import',
  `Update an import`,
  {
    owner: z.string(),
    repo: z.string(),
    vcsUsername: z.any().optional(),
    vcsPassword: z.any().optional(),
    vcs: z.any().optional(),
    tfvcProject: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/import`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('vcsUsername' in mappedParams) {
        mappedParams['vcs_username'] = mappedParams['vcsUsername']
        delete mappedParams['vcsUsername']
      }
      if ('vcsPassword' in mappedParams) {
        mappedParams['vcs_password'] = mappedParams['vcsPassword']
        delete mappedParams['vcsPassword']
      }
      if ('tfvcProject' in mappedParams) {
        mappedParams['tfvc_project'] = mappedParams['tfvcProject']
        delete mappedParams['tfvcProject']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PATCH',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'migrations/cancel-import',
  `Cancel an import`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/import`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'migrations/get-commit-authors',
  `Get commit authors`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/import/authors`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'migrations/map-commit-author',
  `Map a commit author`,
  {
    owner: z.string(),
    repo: z.string(),
    authorId: z.string(),
    email: z.any().optional(),
    name: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, authorId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/import/authors/${authorId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('authorId' in mappedParams) {
        mappedParams['author_id'] = mappedParams['authorId']
        delete mappedParams['authorId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PATCH',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'migrations/get-large-files',
  `Get large files`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/import/large_files`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'migrations/set-lfs-preference',
  `Update Git LFS preference`,
  {
    owner: z.string(),
    repo: z.string(),
    useLfs: z.any(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/import/lfs`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('useLfs' in mappedParams) {
        mappedParams['use_lfs'] = mappedParams['useLfs']
        delete mappedParams['useLfs']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PATCH',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'apps/get-repo-installation',
  `Get a repository installation for the authenticated app`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/installation`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'interactions/get-restrictions-for-repo',
  `Get interaction restrictions for a repository`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/interaction-limits`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'interactions/set-restrictions-for-repo',
  `Set interaction restrictions for a repository`,
  {
    owner: z.string(),
    repo: z.string(),
    requestData: z.record(z.any()).optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/interaction-limits`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'interactions/remove-restrictions-for-repo',
  `Remove interaction restrictions for a repository`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/interaction-limits`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/list-invitations',
  `List repository invitations`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/invitations`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/update-invitation',
  `Update a repository invitation`,
  {
    owner: z.string(),
    repo: z.string(),
    invitationId: z.string(),
    permissions: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, invitationId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/invitations/${invitationId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('invitationId' in mappedParams) {
        mappedParams['invitation_id'] = mappedParams['invitationId']
        delete mappedParams['invitationId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PATCH',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/delete-invitation',
  `Delete a repository invitation`,
  {
    owner: z.string(),
    repo: z.string(),
    invitationId: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, invitationId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/invitations/${invitationId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('invitationId' in mappedParams) {
        mappedParams['invitation_id'] = mappedParams['invitationId']
        delete mappedParams['invitationId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'issues/list-for-repo',
  `List repository issues`,
  {
    owner: z.string(),
    repo: z.string(),
    milestone: z.string().optional(),
    state: z.string().optional(),
    assignee: z.string().optional(),
    type: z.string().optional(),
    creator: z.string().optional(),
    mentioned: z.string().optional(),
    sort: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/issues`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'issues/create',
  `Create an issue`,
  {
    owner: z.string(),
    repo: z.string(),
    title: z.any(),
    body: z.any().optional(),
    assignee: z.any().optional(),
    milestone: z.any().optional(),
    labels: z.any().optional(),
    assignees: z.any().optional(),
    type: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/issues`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'issues/list-comments-for-repo',
  `List issue comments for a repository`,
  {
    owner: z.string(),
    repo: z.string(),
    direction: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/issues/comments`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'issues/get-comment',
  `Get an issue comment`,
  {
    owner: z.string(),
    repo: z.string(),
    commentId: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, commentId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/issues/comments/${commentId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('commentId' in mappedParams) {
        mappedParams['comment_id'] = mappedParams['commentId']
        delete mappedParams['commentId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'issues/update-comment',
  `Update an issue comment`,
  {
    owner: z.string(),
    repo: z.string(),
    commentId: z.string(),
    body: z.any(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, commentId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/issues/comments/${commentId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('commentId' in mappedParams) {
        mappedParams['comment_id'] = mappedParams['commentId']
        delete mappedParams['commentId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PATCH',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'issues/delete-comment',
  `Delete an issue comment`,
  {
    owner: z.string(),
    repo: z.string(),
    commentId: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, commentId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/issues/comments/${commentId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('commentId' in mappedParams) {
        mappedParams['comment_id'] = mappedParams['commentId']
        delete mappedParams['commentId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'reactions/list-for-issue-comment',
  `List reactions for an issue comment`,
  {
    owner: z.string(),
    repo: z.string(),
    commentId: z.string(),
    content: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, commentId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/issues/comments/${commentId}/reactions`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('commentId' in mappedParams) {
        mappedParams['comment_id'] = mappedParams['commentId']
        delete mappedParams['commentId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'reactions/create-for-issue-comment',
  `Create reaction for an issue comment`,
  {
    owner: z.string(),
    repo: z.string(),
    commentId: z.string(),
    content: z.any(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, commentId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/issues/comments/${commentId}/reactions`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('commentId' in mappedParams) {
        mappedParams['comment_id'] = mappedParams['commentId']
        delete mappedParams['commentId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'reactions/delete-for-issue-comment',
  `Delete an issue comment reaction`,
  {
    owner: z.string(),
    repo: z.string(),
    commentId: z.string(),
    reactionId: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, commentId, reactionId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/issues/comments/${commentId}/reactions/${reactionId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('commentId' in mappedParams) {
        mappedParams['comment_id'] = mappedParams['commentId']
        delete mappedParams['commentId']
      }
      if ('reactionId' in mappedParams) {
        mappedParams['reaction_id'] = mappedParams['reactionId']
        delete mappedParams['reactionId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'issues/list-events-for-repo',
  `List issue events for a repository`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/issues/events`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'issues/get-event',
  `Get an issue event`,
  {
    owner: z.string(),
    repo: z.string(),
    eventId: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, eventId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/issues/events/${eventId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('eventId' in mappedParams) {
        mappedParams['event_id'] = mappedParams['eventId']
        delete mappedParams['eventId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'issues/get',
  `Get an issue`,
  {
    owner: z.string(),
    repo: z.string(),
    issueNumber: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, issueNumber, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/issues/${issueNumber}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('issueNumber' in mappedParams) {
        mappedParams['issue_number'] = mappedParams['issueNumber']
        delete mappedParams['issueNumber']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'issues/update',
  `Update an issue`,
  {
    owner: z.string(),
    repo: z.string(),
    issueNumber: z.string(),
    title: z.any().optional(),
    body: z.any().optional(),
    assignee: z.any().optional(),
    state: z.any().optional(),
    stateReason: z.any().optional(),
    milestone: z.any().optional(),
    labels: z.any().optional(),
    assignees: z.any().optional(),
    type: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, issueNumber, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/issues/${issueNumber}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('issueNumber' in mappedParams) {
        mappedParams['issue_number'] = mappedParams['issueNumber']
        delete mappedParams['issueNumber']
      }
      if ('stateReason' in mappedParams) {
        mappedParams['state_reason'] = mappedParams['stateReason']
        delete mappedParams['stateReason']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PATCH',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'issues/add-assignees',
  `Add assignees to an issue`,
  {
    owner: z.string(),
    repo: z.string(),
    issueNumber: z.string(),
    assignees: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, issueNumber, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/issues/${issueNumber}/assignees`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('issueNumber' in mappedParams) {
        mappedParams['issue_number'] = mappedParams['issueNumber']
        delete mappedParams['issueNumber']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'issues/remove-assignees',
  `Remove assignees from an issue`,
  {
    owner: z.string(),
    repo: z.string(),
    issueNumber: z.string(),
    assignees: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, issueNumber, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/issues/${issueNumber}/assignees`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('issueNumber' in mappedParams) {
        mappedParams['issue_number'] = mappedParams['issueNumber']
        delete mappedParams['issueNumber']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'issues/check-user-can-be-assigned-to-issue',
  `Check if a user can be assigned to a issue`,
  {
    owner: z.string(),
    repo: z.string(),
    issueNumber: z.string(),
    assignee: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, issueNumber, assignee, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/issues/${issueNumber}/assignees/${assignee}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('issueNumber' in mappedParams) {
        mappedParams['issue_number'] = mappedParams['issueNumber']
        delete mappedParams['issueNumber']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'issues/list-comments',
  `List issue comments`,
  {
    owner: z.string(),
    repo: z.string(),
    issueNumber: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, issueNumber, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/issues/${issueNumber}/comments`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('issueNumber' in mappedParams) {
        mappedParams['issue_number'] = mappedParams['issueNumber']
        delete mappedParams['issueNumber']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'issues/create-comment',
  `Create an issue comment`,
  {
    owner: z.string(),
    repo: z.string(),
    issueNumber: z.string(),
    body: z.any(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, issueNumber, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/issues/${issueNumber}/comments`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('issueNumber' in mappedParams) {
        mappedParams['issue_number'] = mappedParams['issueNumber']
        delete mappedParams['issueNumber']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'issues/list-events',
  `List issue events`,
  {
    owner: z.string(),
    repo: z.string(),
    issueNumber: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, issueNumber, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/issues/${issueNumber}/events`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('issueNumber' in mappedParams) {
        mappedParams['issue_number'] = mappedParams['issueNumber']
        delete mappedParams['issueNumber']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'issues/list-labels-on-issue',
  `List labels for an issue`,
  {
    owner: z.string(),
    repo: z.string(),
    issueNumber: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, issueNumber, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/issues/${issueNumber}/labels`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('issueNumber' in mappedParams) {
        mappedParams['issue_number'] = mappedParams['issueNumber']
        delete mappedParams['issueNumber']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'issues/add-labels',
  `Add labels to an issue`,
  {
    owner: z.string(),
    repo: z.string(),
    issueNumber: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, issueNumber, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/issues/${issueNumber}/labels`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('issueNumber' in mappedParams) {
        mappedParams['issue_number'] = mappedParams['issueNumber']
        delete mappedParams['issueNumber']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'issues/set-labels',
  `Set labels for an issue`,
  {
    owner: z.string(),
    repo: z.string(),
    issueNumber: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, issueNumber, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/issues/${issueNumber}/labels`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('issueNumber' in mappedParams) {
        mappedParams['issue_number'] = mappedParams['issueNumber']
        delete mappedParams['issueNumber']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'issues/remove-all-labels',
  `Remove all labels from an issue`,
  {
    owner: z.string(),
    repo: z.string(),
    issueNumber: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, issueNumber, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/issues/${issueNumber}/labels`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('issueNumber' in mappedParams) {
        mappedParams['issue_number'] = mappedParams['issueNumber']
        delete mappedParams['issueNumber']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'issues/remove-label',
  `Remove a label from an issue`,
  {
    owner: z.string(),
    repo: z.string(),
    issueNumber: z.string(),
    name: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, issueNumber, name, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/issues/${issueNumber}/labels/${name}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('issueNumber' in mappedParams) {
        mappedParams['issue_number'] = mappedParams['issueNumber']
        delete mappedParams['issueNumber']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'issues/lock',
  `Lock an issue`,
  {
    owner: z.string(),
    repo: z.string(),
    issueNumber: z.string(),
    lockReason: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, issueNumber, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/issues/${issueNumber}/lock`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('issueNumber' in mappedParams) {
        mappedParams['issue_number'] = mappedParams['issueNumber']
        delete mappedParams['issueNumber']
      }
      if ('lockReason' in mappedParams) {
        mappedParams['lock_reason'] = mappedParams['lockReason']
        delete mappedParams['lockReason']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'issues/unlock',
  `Unlock an issue`,
  {
    owner: z.string(),
    repo: z.string(),
    issueNumber: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, issueNumber, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/issues/${issueNumber}/lock`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('issueNumber' in mappedParams) {
        mappedParams['issue_number'] = mappedParams['issueNumber']
        delete mappedParams['issueNumber']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'reactions/list-for-issue',
  `List reactions for an issue`,
  {
    owner: z.string(),
    repo: z.string(),
    issueNumber: z.string(),
    content: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, issueNumber, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/issues/${issueNumber}/reactions`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('issueNumber' in mappedParams) {
        mappedParams['issue_number'] = mappedParams['issueNumber']
        delete mappedParams['issueNumber']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'reactions/create-for-issue',
  `Create reaction for an issue`,
  {
    owner: z.string(),
    repo: z.string(),
    issueNumber: z.string(),
    content: z.any(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, issueNumber, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/issues/${issueNumber}/reactions`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('issueNumber' in mappedParams) {
        mappedParams['issue_number'] = mappedParams['issueNumber']
        delete mappedParams['issueNumber']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'reactions/delete-for-issue',
  `Delete an issue reaction`,
  {
    owner: z.string(),
    repo: z.string(),
    issueNumber: z.string(),
    reactionId: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, issueNumber, reactionId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/issues/${issueNumber}/reactions/${reactionId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('issueNumber' in mappedParams) {
        mappedParams['issue_number'] = mappedParams['issueNumber']
        delete mappedParams['issueNumber']
      }
      if ('reactionId' in mappedParams) {
        mappedParams['reaction_id'] = mappedParams['reactionId']
        delete mappedParams['reactionId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'issues/remove-sub-issue',
  `Remove sub-issue`,
  {
    owner: z.string(),
    repo: z.string(),
    issueNumber: z.string(),
    subIssueId: z.any(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, issueNumber, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/issues/${issueNumber}/sub_issue`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('issueNumber' in mappedParams) {
        mappedParams['issue_number'] = mappedParams['issueNumber']
        delete mappedParams['issueNumber']
      }
      if ('subIssueId' in mappedParams) {
        mappedParams['sub_issue_id'] = mappedParams['subIssueId']
        delete mappedParams['subIssueId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'issues/list-sub-issues',
  `List sub-issues`,
  {
    owner: z.string(),
    repo: z.string(),
    issueNumber: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, issueNumber, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/issues/${issueNumber}/sub_issues`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('issueNumber' in mappedParams) {
        mappedParams['issue_number'] = mappedParams['issueNumber']
        delete mappedParams['issueNumber']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'issues/add-sub-issue',
  `Add sub-issue`,
  {
    owner: z.string(),
    repo: z.string(),
    issueNumber: z.string(),
    subIssueId: z.any(),
    replaceParent: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, issueNumber, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/issues/${issueNumber}/sub_issues`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('issueNumber' in mappedParams) {
        mappedParams['issue_number'] = mappedParams['issueNumber']
        delete mappedParams['issueNumber']
      }
      if ('subIssueId' in mappedParams) {
        mappedParams['sub_issue_id'] = mappedParams['subIssueId']
        delete mappedParams['subIssueId']
      }
      if ('replaceParent' in mappedParams) {
        mappedParams['replace_parent'] = mappedParams['replaceParent']
        delete mappedParams['replaceParent']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'issues/reprioritize-sub-issue',
  `Reprioritize sub-issue`,
  {
    owner: z.string(),
    repo: z.string(),
    issueNumber: z.string(),
    subIssueId: z.any(),
    afterId: z.any().optional(),
    beforeId: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, issueNumber, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/issues/${issueNumber}/sub_issues/priority`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('issueNumber' in mappedParams) {
        mappedParams['issue_number'] = mappedParams['issueNumber']
        delete mappedParams['issueNumber']
      }
      if ('subIssueId' in mappedParams) {
        mappedParams['sub_issue_id'] = mappedParams['subIssueId']
        delete mappedParams['subIssueId']
      }
      if ('afterId' in mappedParams) {
        mappedParams['after_id'] = mappedParams['afterId']
        delete mappedParams['afterId']
      }
      if ('beforeId' in mappedParams) {
        mappedParams['before_id'] = mappedParams['beforeId']
        delete mappedParams['beforeId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PATCH',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'issues/list-events-for-timeline',
  `List timeline events for an issue`,
  {
    owner: z.string(),
    repo: z.string(),
    issueNumber: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, issueNumber, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/issues/${issueNumber}/timeline`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('issueNumber' in mappedParams) {
        mappedParams['issue_number'] = mappedParams['issueNumber']
        delete mappedParams['issueNumber']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/list-deploy-keys',
  `List deploy keys`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/keys`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/create-deploy-key',
  `Create a deploy key`,
  {
    owner: z.string(),
    repo: z.string(),
    title: z.any().optional(),
    key: z.any(),
    readOnly: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/keys`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('readOnly' in mappedParams) {
        mappedParams['read_only'] = mappedParams['readOnly']
        delete mappedParams['readOnly']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/get-deploy-key',
  `Get a deploy key`,
  {
    owner: z.string(),
    repo: z.string(),
    keyId: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, keyId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/keys/${keyId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('keyId' in mappedParams) {
        mappedParams['key_id'] = mappedParams['keyId']
        delete mappedParams['keyId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/delete-deploy-key',
  `Delete a deploy key`,
  {
    owner: z.string(),
    repo: z.string(),
    keyId: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, keyId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/keys/${keyId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('keyId' in mappedParams) {
        mappedParams['key_id'] = mappedParams['keyId']
        delete mappedParams['keyId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'issues/list-labels-for-repo',
  `List labels for a repository`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/labels`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'issues/create-label',
  `Create a label`,
  {
    owner: z.string(),
    repo: z.string(),
    name: z.any(),
    color: z.any().optional(),
    description: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/labels`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'issues/get-label',
  `Get a label`,
  {
    owner: z.string(),
    repo: z.string(),
    name: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, name, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/labels/${name}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'issues/update-label',
  `Update a label`,
  {
    owner: z.string(),
    repo: z.string(),
    name: z.string(),
    newName: z.any().optional(),
    color: z.any().optional(),
    description: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, name, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/labels/${name}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('newName' in mappedParams) {
        mappedParams['new_name'] = mappedParams['newName']
        delete mappedParams['newName']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PATCH',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'issues/delete-label',
  `Delete a label`,
  {
    owner: z.string(),
    repo: z.string(),
    name: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, name, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/labels/${name}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/list-languages',
  `List repository languages`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/languages`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'licenses/get-for-repo',
  `Get the license for a repository`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/license`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/merge-upstream',
  `Sync a fork branch with the upstream repository`,
  {
    owner: z.string(),
    repo: z.string(),
    branch: z.any(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/merge-upstream`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/merge',
  `Merge a branch`,
  {
    owner: z.string(),
    repo: z.string(),
    base: z.any(),
    head: z.any(),
    commitMessage: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/merges`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('commitMessage' in mappedParams) {
        mappedParams['commit_message'] = mappedParams['commitMessage']
        delete mappedParams['commitMessage']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'issues/list-milestones',
  `List milestones`,
  {
    owner: z.string(),
    repo: z.string(),
    state: z.string().optional(),
    sort: z.string().optional(),
    direction: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/milestones`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'issues/create-milestone',
  `Create a milestone`,
  {
    owner: z.string(),
    repo: z.string(),
    title: z.any(),
    state: z.any().optional(),
    description: z.any().optional(),
    dueOn: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/milestones`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('dueOn' in mappedParams) {
        mappedParams['due_on'] = mappedParams['dueOn']
        delete mappedParams['dueOn']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'issues/get-milestone',
  `Get a milestone`,
  {
    owner: z.string(),
    repo: z.string(),
    milestoneNumber: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, milestoneNumber, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/milestones/${milestoneNumber}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('milestoneNumber' in mappedParams) {
        mappedParams['milestone_number'] = mappedParams['milestoneNumber']
        delete mappedParams['milestoneNumber']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'issues/update-milestone',
  `Update a milestone`,
  {
    owner: z.string(),
    repo: z.string(),
    milestoneNumber: z.string(),
    title: z.any().optional(),
    state: z.any().optional(),
    description: z.any().optional(),
    dueOn: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, milestoneNumber, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/milestones/${milestoneNumber}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('milestoneNumber' in mappedParams) {
        mappedParams['milestone_number'] = mappedParams['milestoneNumber']
        delete mappedParams['milestoneNumber']
      }
      if ('dueOn' in mappedParams) {
        mappedParams['due_on'] = mappedParams['dueOn']
        delete mappedParams['dueOn']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PATCH',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'issues/delete-milestone',
  `Delete a milestone`,
  {
    owner: z.string(),
    repo: z.string(),
    milestoneNumber: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, milestoneNumber, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/milestones/${milestoneNumber}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('milestoneNumber' in mappedParams) {
        mappedParams['milestone_number'] = mappedParams['milestoneNumber']
        delete mappedParams['milestoneNumber']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'issues/list-labels-for-milestone',
  `List labels for issues in a milestone`,
  {
    owner: z.string(),
    repo: z.string(),
    milestoneNumber: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, milestoneNumber, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/milestones/${milestoneNumber}/labels`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('milestoneNumber' in mappedParams) {
        mappedParams['milestone_number'] = mappedParams['milestoneNumber']
        delete mappedParams['milestoneNumber']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'activity/list-repo-notifications-for-authenticated-user',
  `List repository notifications for the authenticated user`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/notifications`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'activity/mark-repo-notifications-as-read',
  `Mark repository notifications as read`,
  {
    owner: z.string(),
    repo: z.string(),
    lastReadAt: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/notifications`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('lastReadAt' in mappedParams) {
        mappedParams['last_read_at'] = mappedParams['lastReadAt']
        delete mappedParams['lastReadAt']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/get-pages',
  `Get a GitHub Pages site`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/pages`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/create-pages-site',
  `Create a GitHub Pages site`,
  {
    owner: z.string(),
    repo: z.string(),
    buildType: z.any().optional(),
    source: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/pages`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('buildType' in mappedParams) {
        mappedParams['build_type'] = mappedParams['buildType']
        delete mappedParams['buildType']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/update-information-about-pages-site',
  `Update information about a GitHub Pages site`,
  {
    owner: z.string(),
    repo: z.string(),
    cname: z.any().optional(),
    httpsEnforced: z.any().optional(),
    buildType: z.any().optional(),
    source: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/pages`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('httpsEnforced' in mappedParams) {
        mappedParams['https_enforced'] = mappedParams['httpsEnforced']
        delete mappedParams['httpsEnforced']
      }
      if ('buildType' in mappedParams) {
        mappedParams['build_type'] = mappedParams['buildType']
        delete mappedParams['buildType']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/delete-pages-site',
  `Delete a GitHub Pages site`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/pages`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/list-pages-builds',
  `List GitHub Pages builds`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/pages/builds`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/request-pages-build',
  `Request a GitHub Pages build`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/pages/builds`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/get-latest-pages-build',
  `Get latest Pages build`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/pages/builds/latest`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/get-pages-build',
  `Get GitHub Pages build`,
  {
    owner: z.string(),
    repo: z.string(),
    buildId: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, buildId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/pages/builds/${buildId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('buildId' in mappedParams) {
        mappedParams['build_id'] = mappedParams['buildId']
        delete mappedParams['buildId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/create-pages-deployment',
  `Create a GitHub Pages deployment`,
  {
    owner: z.string(),
    repo: z.string(),
    artifactId: z.any().optional(),
    artifactUrl: z.any().optional(),
    environment: z.any().optional(),
    pagesBuildVersion: z.any(),
    oidcToken: z.any(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/pages/deployments`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('artifactId' in mappedParams) {
        mappedParams['artifact_id'] = mappedParams['artifactId']
        delete mappedParams['artifactId']
      }
      if ('artifactUrl' in mappedParams) {
        mappedParams['artifact_url'] = mappedParams['artifactUrl']
        delete mappedParams['artifactUrl']
      }
      if ('pagesBuildVersion' in mappedParams) {
        mappedParams['pages_build_version'] = mappedParams['pagesBuildVersion']
        delete mappedParams['pagesBuildVersion']
      }
      if ('oidcToken' in mappedParams) {
        mappedParams['oidc_token'] = mappedParams['oidcToken']
        delete mappedParams['oidcToken']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/get-pages-deployment',
  `Get the status of a GitHub Pages deployment`,
  {
    owner: z.string(),
    repo: z.string(),
    pagesDeploymentId: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, pagesDeploymentId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/pages/deployments/${pagesDeploymentId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('pagesDeploymentId' in mappedParams) {
        mappedParams['pages_deployment_id'] = mappedParams['pagesDeploymentId']
        delete mappedParams['pagesDeploymentId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/cancel-pages-deployment',
  `Cancel a GitHub Pages deployment`,
  {
    owner: z.string(),
    repo: z.string(),
    pagesDeploymentId: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, pagesDeploymentId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/pages/deployments/${pagesDeploymentId}/cancel`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('pagesDeploymentId' in mappedParams) {
        mappedParams['pages_deployment_id'] = mappedParams['pagesDeploymentId']
        delete mappedParams['pagesDeploymentId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/get-pages-health-check',
  `Get a DNS health check for GitHub Pages`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/pages/health`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/check-private-vulnerability-reporting',
  `Check if private vulnerability reporting is enabled for a repository`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/private-vulnerability-reporting`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/enable-private-vulnerability-reporting',
  `Enable private vulnerability reporting for a repository`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/private-vulnerability-reporting`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/disable-private-vulnerability-reporting',
  `Disable private vulnerability reporting for a repository`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/private-vulnerability-reporting`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'projects-classic/list-for-repo',
  `List repository projects`,
  {
    owner: z.string(),
    repo: z.string(),
    state: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/projects`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'projects-classic/create-for-repo',
  `Create a repository project`,
  {
    owner: z.string(),
    repo: z.string(),
    name: z.any(),
    body: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/projects`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/get-custom-properties-values',
  `Get all custom property values for a repository`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/properties/values`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/create-or-update-custom-properties-values',
  `Create or update custom property values for a repository`,
  {
    owner: z.string(),
    repo: z.string(),
    properties: z.any(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/properties/values`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PATCH',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'pulls/list',
  `List pull requests`,
  {
    owner: z.string(),
    repo: z.string(),
    state: z.string().optional(),
    head: z.string().optional(),
    base: z.string().optional(),
    sort: z.string().optional(),
    direction: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/pulls`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'pulls/create',
  `Create a pull request`,
  {
    owner: z.string(),
    repo: z.string(),
    title: z.any().optional(),
    head: z.any(),
    headRepo: z.any().optional(),
    base: z.any(),
    body: z.any().optional(),
    maintainerCanModify: z.any().optional(),
    draft: z.any().optional(),
    issue: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/pulls`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('headRepo' in mappedParams) {
        mappedParams['head_repo'] = mappedParams['headRepo']
        delete mappedParams['headRepo']
      }
      if ('maintainerCanModify' in mappedParams) {
        mappedParams['maintainer_can_modify'] = mappedParams['maintainerCanModify']
        delete mappedParams['maintainerCanModify']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'pulls/list-review-comments-for-repo',
  `List review comments in a repository`,
  {
    owner: z.string(),
    repo: z.string(),
    sort: z.string().optional(),
    direction: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/pulls/comments`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'pulls/get-review-comment',
  `Get a review comment for a pull request`,
  {
    owner: z.string(),
    repo: z.string(),
    commentId: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, commentId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/pulls/comments/${commentId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('commentId' in mappedParams) {
        mappedParams['comment_id'] = mappedParams['commentId']
        delete mappedParams['commentId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'pulls/update-review-comment',
  `Update a review comment for a pull request`,
  {
    owner: z.string(),
    repo: z.string(),
    commentId: z.string(),
    body: z.any(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, commentId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/pulls/comments/${commentId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('commentId' in mappedParams) {
        mappedParams['comment_id'] = mappedParams['commentId']
        delete mappedParams['commentId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PATCH',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'pulls/delete-review-comment',
  `Delete a review comment for a pull request`,
  {
    owner: z.string(),
    repo: z.string(),
    commentId: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, commentId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/pulls/comments/${commentId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('commentId' in mappedParams) {
        mappedParams['comment_id'] = mappedParams['commentId']
        delete mappedParams['commentId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'reactions/list-for-pull-request-review-comment',
  `List reactions for a pull request review comment`,
  {
    owner: z.string(),
    repo: z.string(),
    commentId: z.string(),
    content: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, commentId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/pulls/comments/${commentId}/reactions`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('commentId' in mappedParams) {
        mappedParams['comment_id'] = mappedParams['commentId']
        delete mappedParams['commentId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'reactions/create-for-pull-request-review-comment',
  `Create reaction for a pull request review comment`,
  {
    owner: z.string(),
    repo: z.string(),
    commentId: z.string(),
    content: z.any(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, commentId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/pulls/comments/${commentId}/reactions`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('commentId' in mappedParams) {
        mappedParams['comment_id'] = mappedParams['commentId']
        delete mappedParams['commentId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'reactions/delete-for-pull-request-comment',
  `Delete a pull request comment reaction`,
  {
    owner: z.string(),
    repo: z.string(),
    commentId: z.string(),
    reactionId: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, commentId, reactionId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/pulls/comments/${commentId}/reactions/${reactionId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('commentId' in mappedParams) {
        mappedParams['comment_id'] = mappedParams['commentId']
        delete mappedParams['commentId']
      }
      if ('reactionId' in mappedParams) {
        mappedParams['reaction_id'] = mappedParams['reactionId']
        delete mappedParams['reactionId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'pulls/get',
  `Get a pull request`,
  {
    owner: z.string(),
    repo: z.string(),
    pullNumber: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, pullNumber, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/pulls/${pullNumber}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('pullNumber' in mappedParams) {
        mappedParams['pull_number'] = mappedParams['pullNumber']
        delete mappedParams['pullNumber']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'pulls/update',
  `Update a pull request`,
  {
    owner: z.string(),
    repo: z.string(),
    pullNumber: z.string(),
    title: z.any().optional(),
    body: z.any().optional(),
    state: z.any().optional(),
    base: z.any().optional(),
    maintainerCanModify: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, pullNumber, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/pulls/${pullNumber}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('pullNumber' in mappedParams) {
        mappedParams['pull_number'] = mappedParams['pullNumber']
        delete mappedParams['pullNumber']
      }
      if ('maintainerCanModify' in mappedParams) {
        mappedParams['maintainer_can_modify'] = mappedParams['maintainerCanModify']
        delete mappedParams['maintainerCanModify']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PATCH',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'codespaces/create-with-pr-for-authenticated-user',
  `Create a codespace from a pull request`,
  {
    owner: z.string(),
    repo: z.string(),
    pullNumber: z.string(),
    location: z.any().optional(),
    geo: z.any().optional(),
    clientIp: z.any().optional(),
    machine: z.any().optional(),
    devcontainerPath: z.any().optional(),
    multiRepoPermissionsOptOut: z.any().optional(),
    workingDirectory: z.any().optional(),
    idleTimeoutMinutes: z.any().optional(),
    displayName: z.any().optional(),
    retentionPeriodMinutes: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, pullNumber, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/pulls/${pullNumber}/codespaces`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('pullNumber' in mappedParams) {
        mappedParams['pull_number'] = mappedParams['pullNumber']
        delete mappedParams['pullNumber']
      }
      if ('clientIp' in mappedParams) {
        mappedParams['client_ip'] = mappedParams['clientIp']
        delete mappedParams['clientIp']
      }
      if ('devcontainerPath' in mappedParams) {
        mappedParams['devcontainer_path'] = mappedParams['devcontainerPath']
        delete mappedParams['devcontainerPath']
      }
      if ('multiRepoPermissionsOptOut' in mappedParams) {
        mappedParams['multi_repo_permissions_opt_out'] = mappedParams['multiRepoPermissionsOptOut']
        delete mappedParams['multiRepoPermissionsOptOut']
      }
      if ('workingDirectory' in mappedParams) {
        mappedParams['working_directory'] = mappedParams['workingDirectory']
        delete mappedParams['workingDirectory']
      }
      if ('idleTimeoutMinutes' in mappedParams) {
        mappedParams['idle_timeout_minutes'] = mappedParams['idleTimeoutMinutes']
        delete mappedParams['idleTimeoutMinutes']
      }
      if ('displayName' in mappedParams) {
        mappedParams['display_name'] = mappedParams['displayName']
        delete mappedParams['displayName']
      }
      if ('retentionPeriodMinutes' in mappedParams) {
        mappedParams['retention_period_minutes'] = mappedParams['retentionPeriodMinutes']
        delete mappedParams['retentionPeriodMinutes']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'pulls/list-review-comments',
  `List review comments on a pull request`,
  {
    owner: z.string(),
    repo: z.string(),
    pullNumber: z.string(),
    direction: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, pullNumber, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/pulls/${pullNumber}/comments`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('pullNumber' in mappedParams) {
        mappedParams['pull_number'] = mappedParams['pullNumber']
        delete mappedParams['pullNumber']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'pulls/create-review-comment',
  `Create a review comment for a pull request`,
  {
    owner: z.string(),
    repo: z.string(),
    pullNumber: z.string(),
    body: z.any(),
    commitId: z.any(),
    path: z.any(),
    position: z.any().optional(),
    side: z.any().optional(),
    line: z.any().optional(),
    startLine: z.any().optional(),
    startSide: z.any().optional(),
    inReplyTo: z.any().optional(),
    subjectType: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, pullNumber, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/pulls/${pullNumber}/comments`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('pullNumber' in mappedParams) {
        mappedParams['pull_number'] = mappedParams['pullNumber']
        delete mappedParams['pullNumber']
      }
      if ('commitId' in mappedParams) {
        mappedParams['commit_id'] = mappedParams['commitId']
        delete mappedParams['commitId']
      }
      if ('startLine' in mappedParams) {
        mappedParams['start_line'] = mappedParams['startLine']
        delete mappedParams['startLine']
      }
      if ('startSide' in mappedParams) {
        mappedParams['start_side'] = mappedParams['startSide']
        delete mappedParams['startSide']
      }
      if ('inReplyTo' in mappedParams) {
        mappedParams['in_reply_to'] = mappedParams['inReplyTo']
        delete mappedParams['inReplyTo']
      }
      if ('subjectType' in mappedParams) {
        mappedParams['subject_type'] = mappedParams['subjectType']
        delete mappedParams['subjectType']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'pulls/create-reply-for-review-comment',
  `Create a reply for a review comment`,
  {
    owner: z.string(),
    repo: z.string(),
    pullNumber: z.string(),
    commentId: z.string(),
    body: z.any(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, pullNumber, commentId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/pulls/${pullNumber}/comments/${commentId}/replies`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('pullNumber' in mappedParams) {
        mappedParams['pull_number'] = mappedParams['pullNumber']
        delete mappedParams['pullNumber']
      }
      if ('commentId' in mappedParams) {
        mappedParams['comment_id'] = mappedParams['commentId']
        delete mappedParams['commentId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'pulls/list-commits',
  `List commits on a pull request`,
  {
    owner: z.string(),
    repo: z.string(),
    pullNumber: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, pullNumber, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/pulls/${pullNumber}/commits`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('pullNumber' in mappedParams) {
        mappedParams['pull_number'] = mappedParams['pullNumber']
        delete mappedParams['pullNumber']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'pulls/list-files',
  `List pull requests files`,
  {
    owner: z.string(),
    repo: z.string(),
    pullNumber: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, pullNumber, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/pulls/${pullNumber}/files`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('pullNumber' in mappedParams) {
        mappedParams['pull_number'] = mappedParams['pullNumber']
        delete mappedParams['pullNumber']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'pulls/check-if-merged',
  `Check if a pull request has been merged`,
  {
    owner: z.string(),
    repo: z.string(),
    pullNumber: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, pullNumber, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/pulls/${pullNumber}/merge`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('pullNumber' in mappedParams) {
        mappedParams['pull_number'] = mappedParams['pullNumber']
        delete mappedParams['pullNumber']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'pulls/merge',
  `Merge a pull request`,
  {
    owner: z.string(),
    repo: z.string(),
    pullNumber: z.string(),
    commitTitle: z.any().optional(),
    commitMessage: z.any().optional(),
    sha: z.any().optional(),
    mergeMethod: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, pullNumber, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/pulls/${pullNumber}/merge`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('pullNumber' in mappedParams) {
        mappedParams['pull_number'] = mappedParams['pullNumber']
        delete mappedParams['pullNumber']
      }
      if ('commitTitle' in mappedParams) {
        mappedParams['commit_title'] = mappedParams['commitTitle']
        delete mappedParams['commitTitle']
      }
      if ('commitMessage' in mappedParams) {
        mappedParams['commit_message'] = mappedParams['commitMessage']
        delete mappedParams['commitMessage']
      }
      if ('mergeMethod' in mappedParams) {
        mappedParams['merge_method'] = mappedParams['mergeMethod']
        delete mappedParams['mergeMethod']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'pulls/list-requested-reviewers',
  `Get all requested reviewers for a pull request`,
  {
    owner: z.string(),
    repo: z.string(),
    pullNumber: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, pullNumber, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/pulls/${pullNumber}/requested_reviewers`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('pullNumber' in mappedParams) {
        mappedParams['pull_number'] = mappedParams['pullNumber']
        delete mappedParams['pullNumber']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'pulls/request-reviewers',
  `Request reviewers for a pull request`,
  {
    owner: z.string(),
    repo: z.string(),
    pullNumber: z.string(),
    reviewers: z.any().optional(),
    teamReviewers: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, pullNumber, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/pulls/${pullNumber}/requested_reviewers`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('pullNumber' in mappedParams) {
        mappedParams['pull_number'] = mappedParams['pullNumber']
        delete mappedParams['pullNumber']
      }
      if ('teamReviewers' in mappedParams) {
        mappedParams['team_reviewers'] = mappedParams['teamReviewers']
        delete mappedParams['teamReviewers']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'pulls/remove-requested-reviewers',
  `Remove requested reviewers from a pull request`,
  {
    owner: z.string(),
    repo: z.string(),
    pullNumber: z.string(),
    reviewers: z.any(),
    teamReviewers: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, pullNumber, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/pulls/${pullNumber}/requested_reviewers`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('pullNumber' in mappedParams) {
        mappedParams['pull_number'] = mappedParams['pullNumber']
        delete mappedParams['pullNumber']
      }
      if ('teamReviewers' in mappedParams) {
        mappedParams['team_reviewers'] = mappedParams['teamReviewers']
        delete mappedParams['teamReviewers']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'pulls/list-reviews',
  `List reviews for a pull request`,
  {
    owner: z.string(),
    repo: z.string(),
    pullNumber: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, pullNumber, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/pulls/${pullNumber}/reviews`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('pullNumber' in mappedParams) {
        mappedParams['pull_number'] = mappedParams['pullNumber']
        delete mappedParams['pullNumber']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'pulls/create-review',
  `Create a review for a pull request`,
  {
    owner: z.string(),
    repo: z.string(),
    pullNumber: z.string(),
    commitId: z.any().optional(),
    body: z.any().optional(),
    event: z.any().optional(),
    comments: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, pullNumber, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/pulls/${pullNumber}/reviews`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('pullNumber' in mappedParams) {
        mappedParams['pull_number'] = mappedParams['pullNumber']
        delete mappedParams['pullNumber']
      }
      if ('commitId' in mappedParams) {
        mappedParams['commit_id'] = mappedParams['commitId']
        delete mappedParams['commitId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'pulls/get-review',
  `Get a review for a pull request`,
  {
    owner: z.string(),
    repo: z.string(),
    pullNumber: z.string(),
    reviewId: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, pullNumber, reviewId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/pulls/${pullNumber}/reviews/${reviewId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('pullNumber' in mappedParams) {
        mappedParams['pull_number'] = mappedParams['pullNumber']
        delete mappedParams['pullNumber']
      }
      if ('reviewId' in mappedParams) {
        mappedParams['review_id'] = mappedParams['reviewId']
        delete mappedParams['reviewId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'pulls/update-review',
  `Update a review for a pull request`,
  {
    owner: z.string(),
    repo: z.string(),
    pullNumber: z.string(),
    reviewId: z.string(),
    body: z.any(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, pullNumber, reviewId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/pulls/${pullNumber}/reviews/${reviewId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('pullNumber' in mappedParams) {
        mappedParams['pull_number'] = mappedParams['pullNumber']
        delete mappedParams['pullNumber']
      }
      if ('reviewId' in mappedParams) {
        mappedParams['review_id'] = mappedParams['reviewId']
        delete mappedParams['reviewId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'pulls/delete-pending-review',
  `Delete a pending review for a pull request`,
  {
    owner: z.string(),
    repo: z.string(),
    pullNumber: z.string(),
    reviewId: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, pullNumber, reviewId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/pulls/${pullNumber}/reviews/${reviewId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('pullNumber' in mappedParams) {
        mappedParams['pull_number'] = mappedParams['pullNumber']
        delete mappedParams['pullNumber']
      }
      if ('reviewId' in mappedParams) {
        mappedParams['review_id'] = mappedParams['reviewId']
        delete mappedParams['reviewId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'pulls/list-comments-for-review',
  `List comments for a pull request review`,
  {
    owner: z.string(),
    repo: z.string(),
    pullNumber: z.string(),
    reviewId: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, pullNumber, reviewId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/pulls/${pullNumber}/reviews/${reviewId}/comments`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('pullNumber' in mappedParams) {
        mappedParams['pull_number'] = mappedParams['pullNumber']
        delete mappedParams['pullNumber']
      }
      if ('reviewId' in mappedParams) {
        mappedParams['review_id'] = mappedParams['reviewId']
        delete mappedParams['reviewId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'pulls/dismiss-review',
  `Dismiss a review for a pull request`,
  {
    owner: z.string(),
    repo: z.string(),
    pullNumber: z.string(),
    reviewId: z.string(),
    message: z.any(),
    event: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, pullNumber, reviewId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/pulls/${pullNumber}/reviews/${reviewId}/dismissals`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('pullNumber' in mappedParams) {
        mappedParams['pull_number'] = mappedParams['pullNumber']
        delete mappedParams['pullNumber']
      }
      if ('reviewId' in mappedParams) {
        mappedParams['review_id'] = mappedParams['reviewId']
        delete mappedParams['reviewId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'pulls/submit-review',
  `Submit a review for a pull request`,
  {
    owner: z.string(),
    repo: z.string(),
    pullNumber: z.string(),
    reviewId: z.string(),
    body: z.any().optional(),
    event: z.any(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, pullNumber, reviewId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/pulls/${pullNumber}/reviews/${reviewId}/events`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('pullNumber' in mappedParams) {
        mappedParams['pull_number'] = mappedParams['pullNumber']
        delete mappedParams['pullNumber']
      }
      if ('reviewId' in mappedParams) {
        mappedParams['review_id'] = mappedParams['reviewId']
        delete mappedParams['reviewId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'pulls/update-branch',
  `Update a pull request branch`,
  {
    owner: z.string(),
    repo: z.string(),
    pullNumber: z.string(),
    expectedHeadSha: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, pullNumber, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/pulls/${pullNumber}/update-branch`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('pullNumber' in mappedParams) {
        mappedParams['pull_number'] = mappedParams['pullNumber']
        delete mappedParams['pullNumber']
      }
      if ('expectedHeadSha' in mappedParams) {
        mappedParams['expected_head_sha'] = mappedParams['expectedHeadSha']
        delete mappedParams['expectedHeadSha']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/get-readme',
  `Get a repository README`,
  {
    owner: z.string(),
    repo: z.string(),
    ref: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/readme`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/get-readme-in-directory',
  `Get a repository README for a directory`,
  {
    owner: z.string(),
    repo: z.string(),
    dir: z.string(),
    ref: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, dir, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/readme/${dir}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/list-releases',
  `List releases`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/releases`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/create-release',
  `Create a release`,
  {
    owner: z.string(),
    repo: z.string(),
    tagName: z.any(),
    targetCommitish: z.any().optional(),
    name: z.any().optional(),
    body: z.any().optional(),
    draft: z.any().optional(),
    prerelease: z.any().optional(),
    discussionCategoryName: z.any().optional(),
    generateReleaseNotes: z.any().optional(),
    makeLatest: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/releases`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('tagName' in mappedParams) {
        mappedParams['tag_name'] = mappedParams['tagName']
        delete mappedParams['tagName']
      }
      if ('targetCommitish' in mappedParams) {
        mappedParams['target_commitish'] = mappedParams['targetCommitish']
        delete mappedParams['targetCommitish']
      }
      if ('discussionCategoryName' in mappedParams) {
        mappedParams['discussion_category_name'] = mappedParams['discussionCategoryName']
        delete mappedParams['discussionCategoryName']
      }
      if ('generateReleaseNotes' in mappedParams) {
        mappedParams['generate_release_notes'] = mappedParams['generateReleaseNotes']
        delete mappedParams['generateReleaseNotes']
      }
      if ('makeLatest' in mappedParams) {
        mappedParams['make_latest'] = mappedParams['makeLatest']
        delete mappedParams['makeLatest']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/get-release-asset',
  `Get a release asset`,
  {
    owner: z.string(),
    repo: z.string(),
    assetId: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, assetId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/releases/assets/${assetId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('assetId' in mappedParams) {
        mappedParams['asset_id'] = mappedParams['assetId']
        delete mappedParams['assetId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/update-release-asset',
  `Update a release asset`,
  {
    owner: z.string(),
    repo: z.string(),
    assetId: z.string(),
    name: z.any().optional(),
    label: z.any().optional(),
    state: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, assetId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/releases/assets/${assetId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('assetId' in mappedParams) {
        mappedParams['asset_id'] = mappedParams['assetId']
        delete mappedParams['assetId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PATCH',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/delete-release-asset',
  `Delete a release asset`,
  {
    owner: z.string(),
    repo: z.string(),
    assetId: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, assetId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/releases/assets/${assetId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('assetId' in mappedParams) {
        mappedParams['asset_id'] = mappedParams['assetId']
        delete mappedParams['assetId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/generate-release-notes',
  `Generate release notes content for a release`,
  {
    owner: z.string(),
    repo: z.string(),
    tagName: z.any(),
    targetCommitish: z.any().optional(),
    previousTagName: z.any().optional(),
    configurationFilePath: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/releases/generate-notes`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('tagName' in mappedParams) {
        mappedParams['tag_name'] = mappedParams['tagName']
        delete mappedParams['tagName']
      }
      if ('targetCommitish' in mappedParams) {
        mappedParams['target_commitish'] = mappedParams['targetCommitish']
        delete mappedParams['targetCommitish']
      }
      if ('previousTagName' in mappedParams) {
        mappedParams['previous_tag_name'] = mappedParams['previousTagName']
        delete mappedParams['previousTagName']
      }
      if ('configurationFilePath' in mappedParams) {
        mappedParams['configuration_file_path'] = mappedParams['configurationFilePath']
        delete mappedParams['configurationFilePath']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/get-latest-release',
  `Get the latest release`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/releases/latest`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/get-release-by-tag',
  `Get a release by tag name`,
  {
    owner: z.string(),
    repo: z.string(),
    tag: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, tag, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/releases/tags/${tag}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/get-release',
  `Get a release`,
  {
    owner: z.string(),
    repo: z.string(),
    releaseId: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, releaseId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/releases/${releaseId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('releaseId' in mappedParams) {
        mappedParams['release_id'] = mappedParams['releaseId']
        delete mappedParams['releaseId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/update-release',
  `Update a release`,
  {
    owner: z.string(),
    repo: z.string(),
    releaseId: z.string(),
    tagName: z.any().optional(),
    targetCommitish: z.any().optional(),
    name: z.any().optional(),
    body: z.any().optional(),
    draft: z.any().optional(),
    prerelease: z.any().optional(),
    makeLatest: z.any().optional(),
    discussionCategoryName: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, releaseId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/releases/${releaseId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('releaseId' in mappedParams) {
        mappedParams['release_id'] = mappedParams['releaseId']
        delete mappedParams['releaseId']
      }
      if ('tagName' in mappedParams) {
        mappedParams['tag_name'] = mappedParams['tagName']
        delete mappedParams['tagName']
      }
      if ('targetCommitish' in mappedParams) {
        mappedParams['target_commitish'] = mappedParams['targetCommitish']
        delete mappedParams['targetCommitish']
      }
      if ('makeLatest' in mappedParams) {
        mappedParams['make_latest'] = mappedParams['makeLatest']
        delete mappedParams['makeLatest']
      }
      if ('discussionCategoryName' in mappedParams) {
        mappedParams['discussion_category_name'] = mappedParams['discussionCategoryName']
        delete mappedParams['discussionCategoryName']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PATCH',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/delete-release',
  `Delete a release`,
  {
    owner: z.string(),
    repo: z.string(),
    releaseId: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, releaseId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/releases/${releaseId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('releaseId' in mappedParams) {
        mappedParams['release_id'] = mappedParams['releaseId']
        delete mappedParams['releaseId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/list-release-assets',
  `List release assets`,
  {
    owner: z.string(),
    repo: z.string(),
    releaseId: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, releaseId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/releases/${releaseId}/assets`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('releaseId' in mappedParams) {
        mappedParams['release_id'] = mappedParams['releaseId']
        delete mappedParams['releaseId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/upload-release-asset',
  `Upload a release asset`,
  {
    owner: z.string(),
    repo: z.string(),
    releaseId: z.string(),
    name: z.string(),
    label: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, releaseId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/releases/${releaseId}/assets`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('releaseId' in mappedParams) {
        mappedParams['release_id'] = mappedParams['releaseId']
        delete mappedParams['releaseId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'reactions/list-for-release',
  `List reactions for a release`,
  {
    owner: z.string(),
    repo: z.string(),
    releaseId: z.string(),
    content: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, releaseId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/releases/${releaseId}/reactions`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('releaseId' in mappedParams) {
        mappedParams['release_id'] = mappedParams['releaseId']
        delete mappedParams['releaseId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'reactions/create-for-release',
  `Create reaction for a release`,
  {
    owner: z.string(),
    repo: z.string(),
    releaseId: z.string(),
    content: z.any(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, releaseId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/releases/${releaseId}/reactions`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('releaseId' in mappedParams) {
        mappedParams['release_id'] = mappedParams['releaseId']
        delete mappedParams['releaseId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'reactions/delete-for-release',
  `Delete a release reaction`,
  {
    owner: z.string(),
    repo: z.string(),
    releaseId: z.string(),
    reactionId: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, releaseId, reactionId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/releases/${releaseId}/reactions/${reactionId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('releaseId' in mappedParams) {
        mappedParams['release_id'] = mappedParams['releaseId']
        delete mappedParams['releaseId']
      }
      if ('reactionId' in mappedParams) {
        mappedParams['reaction_id'] = mappedParams['reactionId']
        delete mappedParams['reactionId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/get-branch-rules',
  `Get rules for a branch`,
  {
    owner: z.string(),
    repo: z.string(),
    branch: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, branch, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/rules/branches/${branch}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/get-repo-rulesets',
  `Get all repository rulesets`,
  {
    owner: z.string(),
    repo: z.string(),
    includesParents: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/rulesets`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('includesParents' in mappedParams) {
        mappedParams['includes_parents'] = mappedParams['includesParents']
        delete mappedParams['includesParents']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/create-repo-ruleset',
  `Create a repository ruleset`,
  {
    owner: z.string(),
    repo: z.string(),
    name: z.any(),
    target: z.any().optional(),
    enforcement: z.any(),
    bypassActors: z.any().optional(),
    conditions: z.any().optional(),
    rules: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/rulesets`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('bypassActors' in mappedParams) {
        mappedParams['bypass_actors'] = mappedParams['bypassActors']
        delete mappedParams['bypassActors']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/get-repo-rule-suites',
  `List repository rule suites`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/rulesets/rule-suites`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/get-repo-rule-suite',
  `Get a repository rule suite`,
  {
    owner: z.string(),
    repo: z.string(),
    ruleSuiteId: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ruleSuiteId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/rulesets/rule-suites/${ruleSuiteId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('ruleSuiteId' in mappedParams) {
        mappedParams['rule_suite_id'] = mappedParams['ruleSuiteId']
        delete mappedParams['ruleSuiteId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/get-repo-ruleset',
  `Get a repository ruleset`,
  {
    owner: z.string(),
    repo: z.string(),
    rulesetId: z.string(),
    includesParents: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, rulesetId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/rulesets/${rulesetId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('rulesetId' in mappedParams) {
        mappedParams['ruleset_id'] = mappedParams['rulesetId']
        delete mappedParams['rulesetId']
      }
      if ('includesParents' in mappedParams) {
        mappedParams['includes_parents'] = mappedParams['includesParents']
        delete mappedParams['includesParents']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/update-repo-ruleset',
  `Update a repository ruleset`,
  {
    owner: z.string(),
    repo: z.string(),
    rulesetId: z.string(),
    name: z.any().optional(),
    target: z.any().optional(),
    enforcement: z.any().optional(),
    bypassActors: z.any().optional(),
    conditions: z.any().optional(),
    rules: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, rulesetId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/rulesets/${rulesetId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('rulesetId' in mappedParams) {
        mappedParams['ruleset_id'] = mappedParams['rulesetId']
        delete mappedParams['rulesetId']
      }
      if ('bypassActors' in mappedParams) {
        mappedParams['bypass_actors'] = mappedParams['bypassActors']
        delete mappedParams['bypassActors']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/delete-repo-ruleset',
  `Delete a repository ruleset`,
  {
    owner: z.string(),
    repo: z.string(),
    rulesetId: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, rulesetId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/rulesets/${rulesetId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('rulesetId' in mappedParams) {
        mappedParams['ruleset_id'] = mappedParams['rulesetId']
        delete mappedParams['rulesetId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/get-repo-ruleset-history',
  `Get repository ruleset history`,
  {
    owner: z.string(),
    repo: z.string(),
    rulesetId: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, rulesetId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/rulesets/${rulesetId}/history`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('rulesetId' in mappedParams) {
        mappedParams['ruleset_id'] = mappedParams['rulesetId']
        delete mappedParams['rulesetId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/get-repo-ruleset-version',
  `Get repository ruleset version`,
  {
    owner: z.string(),
    repo: z.string(),
    rulesetId: z.string(),
    versionId: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, rulesetId, versionId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/rulesets/${rulesetId}/history/${versionId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('rulesetId' in mappedParams) {
        mappedParams['ruleset_id'] = mappedParams['rulesetId']
        delete mappedParams['rulesetId']
      }
      if ('versionId' in mappedParams) {
        mappedParams['version_id'] = mappedParams['versionId']
        delete mappedParams['versionId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'secret-scanning/list-alerts-for-repo',
  `List secret scanning alerts for a repository`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/secret-scanning/alerts`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'secret-scanning/get-alert',
  `Get a secret scanning alert`,
  {
    owner: z.string(),
    repo: z.string(),
    alertNumber: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, alertNumber, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/secret-scanning/alerts/${alertNumber}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('alertNumber' in mappedParams) {
        mappedParams['alert_number'] = mappedParams['alertNumber']
        delete mappedParams['alertNumber']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'secret-scanning/update-alert',
  `Update a secret scanning alert`,
  {
    owner: z.string(),
    repo: z.string(),
    alertNumber: z.string(),
    state: z.any(),
    resolution: z.any().optional(),
    resolutionComment: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, alertNumber, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/secret-scanning/alerts/${alertNumber}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('alertNumber' in mappedParams) {
        mappedParams['alert_number'] = mappedParams['alertNumber']
        delete mappedParams['alertNumber']
      }
      if ('resolutionComment' in mappedParams) {
        mappedParams['resolution_comment'] = mappedParams['resolutionComment']
        delete mappedParams['resolutionComment']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PATCH',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'secret-scanning/list-locations-for-alert',
  `List locations for a secret scanning alert`,
  {
    owner: z.string(),
    repo: z.string(),
    alertNumber: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, alertNumber, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/secret-scanning/alerts/${alertNumber}/locations`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('alertNumber' in mappedParams) {
        mappedParams['alert_number'] = mappedParams['alertNumber']
        delete mappedParams['alertNumber']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'secret-scanning/create-push-protection-bypass',
  `Create a push protection bypass`,
  {
    owner: z.string(),
    repo: z.string(),
    reason: z.any(),
    placeholderId: z.any(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/secret-scanning/push-protection-bypasses`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('placeholderId' in mappedParams) {
        mappedParams['placeholder_id'] = mappedParams['placeholderId']
        delete mappedParams['placeholderId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'secret-scanning/get-scan-history',
  `Get secret scanning scan history for a repository`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/secret-scanning/scan-history`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'security-advisories/list-repository-advisories',
  `List repository security advisories`,
  {
    owner: z.string(),
    repo: z.string(),
    sort: z.string().optional(),
    perPage: z.string().optional(),
    state: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/security-advisories`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('perPage' in mappedParams) {
        mappedParams['per_page'] = mappedParams['perPage']
        delete mappedParams['perPage']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'security-advisories/create-repository-advisory',
  `Create a repository security advisory`,
  {
    owner: z.string(),
    repo: z.string(),
    requestData: z.record(z.any()).optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/security-advisories`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'security-advisories/create-private-vulnerability-report',
  `Privately report a security vulnerability`,
  {
    owner: z.string(),
    repo: z.string(),
    requestData: z.record(z.any()).optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/security-advisories/reports`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'security-advisories/get-repository-advisory',
  `Get a repository security advisory`,
  {
    owner: z.string(),
    repo: z.string(),
    ghsaId: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ghsaId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/security-advisories/${ghsaId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('ghsaId' in mappedParams) {
        mappedParams['ghsa_id'] = mappedParams['ghsaId']
        delete mappedParams['ghsaId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'security-advisories/update-repository-advisory',
  `Update a repository security advisory`,
  {
    owner: z.string(),
    repo: z.string(),
    ghsaId: z.string(),
    requestData: z.record(z.any()).optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ghsaId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/security-advisories/${ghsaId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('ghsaId' in mappedParams) {
        mappedParams['ghsa_id'] = mappedParams['ghsaId']
        delete mappedParams['ghsaId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PATCH',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'security-advisories/create-repository-advisory-cve-request',
  `Request a CVE for a repository security advisory`,
  {
    owner: z.string(),
    repo: z.string(),
    ghsaId: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ghsaId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/security-advisories/${ghsaId}/cve`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('ghsaId' in mappedParams) {
        mappedParams['ghsa_id'] = mappedParams['ghsaId']
        delete mappedParams['ghsaId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'security-advisories/create-fork',
  `Create a temporary private fork`,
  {
    owner: z.string(),
    repo: z.string(),
    ghsaId: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ghsaId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/security-advisories/${ghsaId}/forks`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('ghsaId' in mappedParams) {
        mappedParams['ghsa_id'] = mappedParams['ghsaId']
        delete mappedParams['ghsaId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'activity/list-stargazers-for-repo',
  `List stargazers`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/stargazers`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/get-code-frequency-stats',
  `Get the weekly commit activity`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/stats/code_frequency`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/get-commit-activity-stats',
  `Get the last year of commit activity`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/stats/commit_activity`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/get-contributors-stats',
  `Get all contributor commit activity`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/stats/contributors`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/get-participation-stats',
  `Get the weekly commit count`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/stats/participation`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/get-punch-card-stats',
  `Get the hourly commit count for each day`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/stats/punch_card`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/create-commit-status',
  `Create a commit status`,
  {
    owner: z.string(),
    repo: z.string(),
    sha: z.string(),
    state: z.any(),
    targetUrl: z.any().optional(),
    description: z.any().optional(),
    context: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, sha, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/statuses/${sha}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('targetUrl' in mappedParams) {
        mappedParams['target_url'] = mappedParams['targetUrl']
        delete mappedParams['targetUrl']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'activity/list-watchers-for-repo',
  `List watchers`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/subscribers`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'activity/get-repo-subscription',
  `Get a repository subscription`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/subscription`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'activity/set-repo-subscription',
  `Set a repository subscription`,
  {
    owner: z.string(),
    repo: z.string(),
    subscribed: z.any().optional(),
    ignored: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/subscription`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'activity/delete-repo-subscription',
  `Delete a repository subscription`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/subscription`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/list-tags',
  `List repository tags`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/tags`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/list-tag-protection',
  `Closing down - List tag protection states for a repository`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/tags/protection`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/create-tag-protection',
  `Closing down - Create a tag protection state for a repository`,
  {
    owner: z.string(),
    repo: z.string(),
    pattern: z.any(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/tags/protection`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/delete-tag-protection',
  `Closing down - Delete a tag protection state for a repository`,
  {
    owner: z.string(),
    repo: z.string(),
    tagProtectionId: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, tagProtectionId, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/tags/protection/${tagProtectionId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('tagProtectionId' in mappedParams) {
        mappedParams['tag_protection_id'] = mappedParams['tagProtectionId']
        delete mappedParams['tagProtectionId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/download-tarball-archive',
  `Download a repository archive (tar)`,
  {
    owner: z.string(),
    repo: z.string(),
    ref: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ref, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/tarball/${ref}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/list-teams',
  `List repository teams`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/teams`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/get-all-topics',
  `Get all repository topics`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/topics`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/replace-all-topics',
  `Replace all repository topics`,
  {
    owner: z.string(),
    repo: z.string(),
    names: z.any(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/topics`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/get-clones',
  `Get repository clones`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/traffic/clones`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/get-top-paths',
  `Get top referral paths`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/traffic/popular/paths`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/get-top-referrers',
  `Get top referral sources`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/traffic/popular/referrers`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/get-views',
  `Get page views`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/traffic/views`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/transfer',
  `Transfer a repository`,
  {
    owner: z.string(),
    repo: z.string(),
    newOwner: z.any(),
    newName: z.any().optional(),
    teamIds: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/transfer`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('newOwner' in mappedParams) {
        mappedParams['new_owner'] = mappedParams['newOwner']
        delete mappedParams['newOwner']
      }
      if ('newName' in mappedParams) {
        mappedParams['new_name'] = mappedParams['newName']
        delete mappedParams['newName']
      }
      if ('teamIds' in mappedParams) {
        mappedParams['team_ids'] = mappedParams['teamIds']
        delete mappedParams['teamIds']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/check-vulnerability-alerts',
  `Check if vulnerability alerts are enabled for a repository`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/vulnerability-alerts`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/enable-vulnerability-alerts',
  `Enable vulnerability alerts`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/vulnerability-alerts`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/disable-vulnerability-alerts',
  `Disable vulnerability alerts`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/vulnerability-alerts`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/download-zipball-archive',
  `Download a repository archive (zip)`,
  {
    owner: z.string(),
    repo: z.string(),
    ref: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ref, ...otherParams } = args
      const url = `/repos/${owner}/${repo}/zipball/${ref}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/create-using-template',
  `Create a repository using a template`,
  {
    templateOwner: z.string(),
    templateRepo: z.string(),
    owner: z.any().optional(),
    name: z.any(),
    description: z.any().optional(),
    includeAllBranches: z.any().optional(),
    private: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { templateOwner, templateRepo, ...otherParams } = args
      const url = `/repos/${templateOwner}/${templateRepo}/generate`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('templateOwner' in mappedParams) {
        mappedParams['template_owner'] = mappedParams['templateOwner']
        delete mappedParams['templateOwner']
      }
      if ('templateRepo' in mappedParams) {
        mappedParams['template_repo'] = mappedParams['templateRepo']
        delete mappedParams['templateRepo']
      }
      if ('includeAllBranches' in mappedParams) {
        mappedParams['include_all_branches'] = mappedParams['includeAllBranches']
        delete mappedParams['includeAllBranches']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool('repos/list-public', `List public repositories`, {}, async (args, extra) => {
  try {
    const otherParams = args

    // Map camelCase to original parameter names for API request
    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    const mappedParams: any = { ...otherParams }

    // Extract authorization token from HTTP request headers
    const authorization = extra?.requestInfo?.headers?.authorization as string
    const bearer = authorization?.replace('Bearer ', '')

    const response = await apiClient.request({
      headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
      method: 'GET',
      url: '/repositories',
      params: mappedParams,
    })

    return handleResult(response.data)
  } catch (error) {
    return handleError(error)
  }
})

mcpServer.tool(
  'search/code',
  `Search code`,
  {
    q: z.string(),
    sort: z.string().optional(),
    order: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const otherParams = args

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: '/search/code',
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'search/commits',
  `Search commits`,
  {
    q: z.string(),
    sort: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const otherParams = args

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: '/search/commits',
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'search/issues-and-pull-requests',
  `Search issues and pull requests`,
  {
    q: z.string(),
    sort: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const otherParams = args

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: '/search/issues',
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'search/labels',
  `Search labels`,
  {
    repositoryId: z.string(),
    q: z.string(),
    sort: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const otherParams = args

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('repositoryId' in mappedParams) {
        mappedParams['repository_id'] = mappedParams['repositoryId']
        delete mappedParams['repositoryId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: '/search/labels',
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'search/repos',
  `Search repositories`,
  {
    q: z.string(),
    sort: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const otherParams = args

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: '/search/repositories',
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'search/topics',
  `Search topics`,
  {
    q: z.string(),
  },
  async (args, extra) => {
    try {
      const otherParams = args

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: '/search/topics',
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'search/users',
  `Search users`,
  {
    q: z.string(),
    sort: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const otherParams = args

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: '/search/users',
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'teams/get-legacy',
  `Get a team (Legacy)`,
  {
    teamId: z.string(),
  },
  async (args, extra) => {
    try {
      const { teamId, ...otherParams } = args
      const url = `/teams/${teamId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('teamId' in mappedParams) {
        mappedParams['team_id'] = mappedParams['teamId']
        delete mappedParams['teamId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'teams/update-legacy',
  `Update a team (Legacy)`,
  {
    teamId: z.string(),
    name: z.any(),
    description: z.any().optional(),
    privacy: z.any().optional(),
    notificationSetting: z.any().optional(),
    permission: z.any().optional(),
    parentTeamId: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { teamId, ...otherParams } = args
      const url = `/teams/${teamId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('teamId' in mappedParams) {
        mappedParams['team_id'] = mappedParams['teamId']
        delete mappedParams['teamId']
      }
      if ('notificationSetting' in mappedParams) {
        mappedParams['notification_setting'] = mappedParams['notificationSetting']
        delete mappedParams['notificationSetting']
      }
      if ('parentTeamId' in mappedParams) {
        mappedParams['parent_team_id'] = mappedParams['parentTeamId']
        delete mappedParams['parentTeamId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PATCH',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'teams/delete-legacy',
  `Delete a team (Legacy)`,
  {
    teamId: z.string(),
  },
  async (args, extra) => {
    try {
      const { teamId, ...otherParams } = args
      const url = `/teams/${teamId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('teamId' in mappedParams) {
        mappedParams['team_id'] = mappedParams['teamId']
        delete mappedParams['teamId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'teams/list-discussions-legacy',
  `List discussions (Legacy)`,
  {
    teamId: z.string(),
  },
  async (args, extra) => {
    try {
      const { teamId, ...otherParams } = args
      const url = `/teams/${teamId}/discussions`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('teamId' in mappedParams) {
        mappedParams['team_id'] = mappedParams['teamId']
        delete mappedParams['teamId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'teams/create-discussion-legacy',
  `Create a discussion (Legacy)`,
  {
    teamId: z.string(),
    title: z.any(),
    body: z.any(),
    private: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { teamId, ...otherParams } = args
      const url = `/teams/${teamId}/discussions`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('teamId' in mappedParams) {
        mappedParams['team_id'] = mappedParams['teamId']
        delete mappedParams['teamId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'teams/get-discussion-legacy',
  `Get a discussion (Legacy)`,
  {
    teamId: z.string(),
    discussionNumber: z.string(),
  },
  async (args, extra) => {
    try {
      const { teamId, discussionNumber, ...otherParams } = args
      const url = `/teams/${teamId}/discussions/${discussionNumber}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('teamId' in mappedParams) {
        mappedParams['team_id'] = mappedParams['teamId']
        delete mappedParams['teamId']
      }
      if ('discussionNumber' in mappedParams) {
        mappedParams['discussion_number'] = mappedParams['discussionNumber']
        delete mappedParams['discussionNumber']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'teams/update-discussion-legacy',
  `Update a discussion (Legacy)`,
  {
    teamId: z.string(),
    discussionNumber: z.string(),
    title: z.any().optional(),
    body: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { teamId, discussionNumber, ...otherParams } = args
      const url = `/teams/${teamId}/discussions/${discussionNumber}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('teamId' in mappedParams) {
        mappedParams['team_id'] = mappedParams['teamId']
        delete mappedParams['teamId']
      }
      if ('discussionNumber' in mappedParams) {
        mappedParams['discussion_number'] = mappedParams['discussionNumber']
        delete mappedParams['discussionNumber']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PATCH',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'teams/delete-discussion-legacy',
  `Delete a discussion (Legacy)`,
  {
    teamId: z.string(),
    discussionNumber: z.string(),
  },
  async (args, extra) => {
    try {
      const { teamId, discussionNumber, ...otherParams } = args
      const url = `/teams/${teamId}/discussions/${discussionNumber}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('teamId' in mappedParams) {
        mappedParams['team_id'] = mappedParams['teamId']
        delete mappedParams['teamId']
      }
      if ('discussionNumber' in mappedParams) {
        mappedParams['discussion_number'] = mappedParams['discussionNumber']
        delete mappedParams['discussionNumber']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'teams/list-discussion-comments-legacy',
  `List discussion comments (Legacy)`,
  {
    teamId: z.string(),
    discussionNumber: z.string(),
  },
  async (args, extra) => {
    try {
      const { teamId, discussionNumber, ...otherParams } = args
      const url = `/teams/${teamId}/discussions/${discussionNumber}/comments`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('teamId' in mappedParams) {
        mappedParams['team_id'] = mappedParams['teamId']
        delete mappedParams['teamId']
      }
      if ('discussionNumber' in mappedParams) {
        mappedParams['discussion_number'] = mappedParams['discussionNumber']
        delete mappedParams['discussionNumber']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'teams/create-discussion-comment-legacy',
  `Create a discussion comment (Legacy)`,
  {
    teamId: z.string(),
    discussionNumber: z.string(),
    body: z.any(),
  },
  async (args, extra) => {
    try {
      const { teamId, discussionNumber, ...otherParams } = args
      const url = `/teams/${teamId}/discussions/${discussionNumber}/comments`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('teamId' in mappedParams) {
        mappedParams['team_id'] = mappedParams['teamId']
        delete mappedParams['teamId']
      }
      if ('discussionNumber' in mappedParams) {
        mappedParams['discussion_number'] = mappedParams['discussionNumber']
        delete mappedParams['discussionNumber']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'teams/get-discussion-comment-legacy',
  `Get a discussion comment (Legacy)`,
  {
    teamId: z.string(),
    discussionNumber: z.string(),
    commentNumber: z.string(),
  },
  async (args, extra) => {
    try {
      const { teamId, discussionNumber, commentNumber, ...otherParams } = args
      const url = `/teams/${teamId}/discussions/${discussionNumber}/comments/${commentNumber}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('teamId' in mappedParams) {
        mappedParams['team_id'] = mappedParams['teamId']
        delete mappedParams['teamId']
      }
      if ('discussionNumber' in mappedParams) {
        mappedParams['discussion_number'] = mappedParams['discussionNumber']
        delete mappedParams['discussionNumber']
      }
      if ('commentNumber' in mappedParams) {
        mappedParams['comment_number'] = mappedParams['commentNumber']
        delete mappedParams['commentNumber']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'teams/update-discussion-comment-legacy',
  `Update a discussion comment (Legacy)`,
  {
    teamId: z.string(),
    discussionNumber: z.string(),
    commentNumber: z.string(),
    body: z.any(),
  },
  async (args, extra) => {
    try {
      const { teamId, discussionNumber, commentNumber, ...otherParams } = args
      const url = `/teams/${teamId}/discussions/${discussionNumber}/comments/${commentNumber}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('teamId' in mappedParams) {
        mappedParams['team_id'] = mappedParams['teamId']
        delete mappedParams['teamId']
      }
      if ('discussionNumber' in mappedParams) {
        mappedParams['discussion_number'] = mappedParams['discussionNumber']
        delete mappedParams['discussionNumber']
      }
      if ('commentNumber' in mappedParams) {
        mappedParams['comment_number'] = mappedParams['commentNumber']
        delete mappedParams['commentNumber']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PATCH',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'teams/delete-discussion-comment-legacy',
  `Delete a discussion comment (Legacy)`,
  {
    teamId: z.string(),
    discussionNumber: z.string(),
    commentNumber: z.string(),
  },
  async (args, extra) => {
    try {
      const { teamId, discussionNumber, commentNumber, ...otherParams } = args
      const url = `/teams/${teamId}/discussions/${discussionNumber}/comments/${commentNumber}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('teamId' in mappedParams) {
        mappedParams['team_id'] = mappedParams['teamId']
        delete mappedParams['teamId']
      }
      if ('discussionNumber' in mappedParams) {
        mappedParams['discussion_number'] = mappedParams['discussionNumber']
        delete mappedParams['discussionNumber']
      }
      if ('commentNumber' in mappedParams) {
        mappedParams['comment_number'] = mappedParams['commentNumber']
        delete mappedParams['commentNumber']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'reactions/list-for-team-discussion-comment-legacy',
  `List reactions for a team discussion comment (Legacy)`,
  {
    teamId: z.string(),
    discussionNumber: z.string(),
    commentNumber: z.string(),
    content: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const { teamId, discussionNumber, commentNumber, ...otherParams } = args
      const url = `/teams/${teamId}/discussions/${discussionNumber}/comments/${commentNumber}/reactions`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('teamId' in mappedParams) {
        mappedParams['team_id'] = mappedParams['teamId']
        delete mappedParams['teamId']
      }
      if ('discussionNumber' in mappedParams) {
        mappedParams['discussion_number'] = mappedParams['discussionNumber']
        delete mappedParams['discussionNumber']
      }
      if ('commentNumber' in mappedParams) {
        mappedParams['comment_number'] = mappedParams['commentNumber']
        delete mappedParams['commentNumber']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'reactions/create-for-team-discussion-comment-legacy',
  `Create reaction for a team discussion comment (Legacy)`,
  {
    teamId: z.string(),
    discussionNumber: z.string(),
    commentNumber: z.string(),
    content: z.any(),
  },
  async (args, extra) => {
    try {
      const { teamId, discussionNumber, commentNumber, ...otherParams } = args
      const url = `/teams/${teamId}/discussions/${discussionNumber}/comments/${commentNumber}/reactions`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('teamId' in mappedParams) {
        mappedParams['team_id'] = mappedParams['teamId']
        delete mappedParams['teamId']
      }
      if ('discussionNumber' in mappedParams) {
        mappedParams['discussion_number'] = mappedParams['discussionNumber']
        delete mappedParams['discussionNumber']
      }
      if ('commentNumber' in mappedParams) {
        mappedParams['comment_number'] = mappedParams['commentNumber']
        delete mappedParams['commentNumber']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'reactions/list-for-team-discussion-legacy',
  `List reactions for a team discussion (Legacy)`,
  {
    teamId: z.string(),
    discussionNumber: z.string(),
    content: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const { teamId, discussionNumber, ...otherParams } = args
      const url = `/teams/${teamId}/discussions/${discussionNumber}/reactions`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('teamId' in mappedParams) {
        mappedParams['team_id'] = mappedParams['teamId']
        delete mappedParams['teamId']
      }
      if ('discussionNumber' in mappedParams) {
        mappedParams['discussion_number'] = mappedParams['discussionNumber']
        delete mappedParams['discussionNumber']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'reactions/create-for-team-discussion-legacy',
  `Create reaction for a team discussion (Legacy)`,
  {
    teamId: z.string(),
    discussionNumber: z.string(),
    content: z.any(),
  },
  async (args, extra) => {
    try {
      const { teamId, discussionNumber, ...otherParams } = args
      const url = `/teams/${teamId}/discussions/${discussionNumber}/reactions`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('teamId' in mappedParams) {
        mappedParams['team_id'] = mappedParams['teamId']
        delete mappedParams['teamId']
      }
      if ('discussionNumber' in mappedParams) {
        mappedParams['discussion_number'] = mappedParams['discussionNumber']
        delete mappedParams['discussionNumber']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'teams/list-pending-invitations-legacy',
  `List pending team invitations (Legacy)`,
  {
    teamId: z.string(),
  },
  async (args, extra) => {
    try {
      const { teamId, ...otherParams } = args
      const url = `/teams/${teamId}/invitations`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('teamId' in mappedParams) {
        mappedParams['team_id'] = mappedParams['teamId']
        delete mappedParams['teamId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'teams/list-members-legacy',
  `List team members (Legacy)`,
  {
    teamId: z.string(),
    role: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const { teamId, ...otherParams } = args
      const url = `/teams/${teamId}/members`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('teamId' in mappedParams) {
        mappedParams['team_id'] = mappedParams['teamId']
        delete mappedParams['teamId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'teams/get-member-legacy',
  `Get team member (Legacy)`,
  {
    teamId: z.string(),
    username: z.string(),
  },
  async (args, extra) => {
    try {
      const { teamId, username, ...otherParams } = args
      const url = `/teams/${teamId}/members/${username}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('teamId' in mappedParams) {
        mappedParams['team_id'] = mappedParams['teamId']
        delete mappedParams['teamId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'teams/add-member-legacy',
  `Add team member (Legacy)`,
  {
    teamId: z.string(),
    username: z.string(),
  },
  async (args, extra) => {
    try {
      const { teamId, username, ...otherParams } = args
      const url = `/teams/${teamId}/members/${username}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('teamId' in mappedParams) {
        mappedParams['team_id'] = mappedParams['teamId']
        delete mappedParams['teamId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'teams/remove-member-legacy',
  `Remove team member (Legacy)`,
  {
    teamId: z.string(),
    username: z.string(),
  },
  async (args, extra) => {
    try {
      const { teamId, username, ...otherParams } = args
      const url = `/teams/${teamId}/members/${username}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('teamId' in mappedParams) {
        mappedParams['team_id'] = mappedParams['teamId']
        delete mappedParams['teamId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'teams/get-membership-for-user-legacy',
  `Get team membership for a user (Legacy)`,
  {
    teamId: z.string(),
    username: z.string(),
  },
  async (args, extra) => {
    try {
      const { teamId, username, ...otherParams } = args
      const url = `/teams/${teamId}/memberships/${username}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('teamId' in mappedParams) {
        mappedParams['team_id'] = mappedParams['teamId']
        delete mappedParams['teamId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'teams/add-or-update-membership-for-user-legacy',
  `Add or update team membership for a user (Legacy)`,
  {
    teamId: z.string(),
    username: z.string(),
    role: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { teamId, username, ...otherParams } = args
      const url = `/teams/${teamId}/memberships/${username}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('teamId' in mappedParams) {
        mappedParams['team_id'] = mappedParams['teamId']
        delete mappedParams['teamId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'teams/remove-membership-for-user-legacy',
  `Remove team membership for a user (Legacy)`,
  {
    teamId: z.string(),
    username: z.string(),
  },
  async (args, extra) => {
    try {
      const { teamId, username, ...otherParams } = args
      const url = `/teams/${teamId}/memberships/${username}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('teamId' in mappedParams) {
        mappedParams['team_id'] = mappedParams['teamId']
        delete mappedParams['teamId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'teams/list-projects-legacy',
  `List team projects (Legacy)`,
  {
    teamId: z.string(),
  },
  async (args, extra) => {
    try {
      const { teamId, ...otherParams } = args
      const url = `/teams/${teamId}/projects`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('teamId' in mappedParams) {
        mappedParams['team_id'] = mappedParams['teamId']
        delete mappedParams['teamId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'teams/check-permissions-for-project-legacy',
  `Check team permissions for a project (Legacy)`,
  {
    teamId: z.string(),
    projectId: z.string(),
  },
  async (args, extra) => {
    try {
      const { teamId, projectId, ...otherParams } = args
      const url = `/teams/${teamId}/projects/${projectId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('teamId' in mappedParams) {
        mappedParams['team_id'] = mappedParams['teamId']
        delete mappedParams['teamId']
      }
      if ('projectId' in mappedParams) {
        mappedParams['project_id'] = mappedParams['projectId']
        delete mappedParams['projectId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'teams/add-or-update-project-permissions-legacy',
  `Add or update team project permissions (Legacy)`,
  {
    teamId: z.string(),
    projectId: z.string(),
    permission: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { teamId, projectId, ...otherParams } = args
      const url = `/teams/${teamId}/projects/${projectId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('teamId' in mappedParams) {
        mappedParams['team_id'] = mappedParams['teamId']
        delete mappedParams['teamId']
      }
      if ('projectId' in mappedParams) {
        mappedParams['project_id'] = mappedParams['projectId']
        delete mappedParams['projectId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'teams/remove-project-legacy',
  `Remove a project from a team (Legacy)`,
  {
    teamId: z.string(),
    projectId: z.string(),
  },
  async (args, extra) => {
    try {
      const { teamId, projectId, ...otherParams } = args
      const url = `/teams/${teamId}/projects/${projectId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('teamId' in mappedParams) {
        mappedParams['team_id'] = mappedParams['teamId']
        delete mappedParams['teamId']
      }
      if ('projectId' in mappedParams) {
        mappedParams['project_id'] = mappedParams['projectId']
        delete mappedParams['projectId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'teams/list-repos-legacy',
  `List team repositories (Legacy)`,
  {
    teamId: z.string(),
  },
  async (args, extra) => {
    try {
      const { teamId, ...otherParams } = args
      const url = `/teams/${teamId}/repos`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('teamId' in mappedParams) {
        mappedParams['team_id'] = mappedParams['teamId']
        delete mappedParams['teamId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'teams/check-permissions-for-repo-legacy',
  `Check team permissions for a repository (Legacy)`,
  {
    teamId: z.string(),
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { teamId, owner, repo, ...otherParams } = args
      const url = `/teams/${teamId}/repos/${owner}/${repo}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('teamId' in mappedParams) {
        mappedParams['team_id'] = mappedParams['teamId']
        delete mappedParams['teamId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'teams/add-or-update-repo-permissions-legacy',
  `Add or update team repository permissions (Legacy)`,
  {
    teamId: z.string(),
    owner: z.string(),
    repo: z.string(),
    permission: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { teamId, owner, repo, ...otherParams } = args
      const url = `/teams/${teamId}/repos/${owner}/${repo}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('teamId' in mappedParams) {
        mappedParams['team_id'] = mappedParams['teamId']
        delete mappedParams['teamId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'teams/remove-repo-legacy',
  `Remove a repository from a team (Legacy)`,
  {
    teamId: z.string(),
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { teamId, owner, repo, ...otherParams } = args
      const url = `/teams/${teamId}/repos/${owner}/${repo}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('teamId' in mappedParams) {
        mappedParams['team_id'] = mappedParams['teamId']
        delete mappedParams['teamId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'teams/list-child-legacy',
  `List child teams (Legacy)`,
  {
    teamId: z.string(),
  },
  async (args, extra) => {
    try {
      const { teamId, ...otherParams } = args
      const url = `/teams/${teamId}/teams`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('teamId' in mappedParams) {
        mappedParams['team_id'] = mappedParams['teamId']
        delete mappedParams['teamId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool('users/get-authenticated', `Get the authenticated user`, {}, async (args, extra) => {
  try {
    const otherParams = args

    // Map camelCase to original parameter names for API request
    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    const mappedParams: any = { ...otherParams }

    // Extract authorization token from HTTP request headers
    const authorization = extra?.requestInfo?.headers?.authorization as string
    const bearer = authorization?.replace('Bearer ', '')

    const response = await apiClient.request({
      headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
      method: 'GET',
      url: '/user',
      params: mappedParams,
    })

    return handleResult(response.data)
  } catch (error) {
    return handleError(error)
  }
})

mcpServer.tool(
  'users/update-authenticated',
  `Update the authenticated user`,
  {
    name: z.any().optional(),
    email: z.any().optional(),
    blog: z.any().optional(),
    twitterUsername: z.any().optional(),
    company: z.any().optional(),
    location: z.any().optional(),
    hireable: z.any().optional(),
    bio: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const otherParams = args

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('twitterUsername' in mappedParams) {
        mappedParams['twitter_username'] = mappedParams['twitterUsername']
        delete mappedParams['twitterUsername']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PATCH',
        url: '/user',
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'users/list-blocked-by-authenticated-user',
  `List users blocked by the authenticated user`,
  {},
  async (args, extra) => {
    try {
      const otherParams = args

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: '/user/blocks',
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'users/check-blocked',
  `Check if a user is blocked by the authenticated user`,
  {
    username: z.string(),
  },
  async (args, extra) => {
    try {
      const { username, ...otherParams } = args
      const url = `/user/blocks/${username}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'users/block',
  `Block a user`,
  {
    username: z.string(),
  },
  async (args, extra) => {
    try {
      const { username, ...otherParams } = args
      const url = `/user/blocks/${username}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'users/unblock',
  `Unblock a user`,
  {
    username: z.string(),
  },
  async (args, extra) => {
    try {
      const { username, ...otherParams } = args
      const url = `/user/blocks/${username}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'codespaces/list-for-authenticated-user',
  `List codespaces for the authenticated user`,
  {},
  async (args, extra) => {
    try {
      const otherParams = args

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: '/user/codespaces',
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'codespaces/create-for-authenticated-user',
  `Create a codespace for the authenticated user`,
  {},
  async (args, extra) => {
    try {
      const otherParams = args

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: '/user/codespaces',
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'codespaces/list-secrets-for-authenticated-user',
  `List secrets for the authenticated user`,
  {},
  async (args, extra) => {
    try {
      const otherParams = args

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: '/user/codespaces/secrets',
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'codespaces/get-public-key-for-authenticated-user',
  `Get public key for the authenticated user`,
  {},
  async (args, extra) => {
    try {
      const otherParams = args

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: '/user/codespaces/secrets/public-key',
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'codespaces/get-secret-for-authenticated-user',
  `Get a secret for the authenticated user`,
  {
    secretName: z.string(),
  },
  async (args, extra) => {
    try {
      const { secretName, ...otherParams } = args
      const url = `/user/codespaces/secrets/${secretName}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('secretName' in mappedParams) {
        mappedParams['secret_name'] = mappedParams['secretName']
        delete mappedParams['secretName']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'codespaces/create-or-update-secret-for-authenticated-user',
  `Create or update a secret for the authenticated user`,
  {
    secretName: z.string(),
    encryptedValue: z.any().optional(),
    keyId: z.any(),
    selectedRepositoryIds: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { secretName, ...otherParams } = args
      const url = `/user/codespaces/secrets/${secretName}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('secretName' in mappedParams) {
        mappedParams['secret_name'] = mappedParams['secretName']
        delete mappedParams['secretName']
      }
      if ('encryptedValue' in mappedParams) {
        mappedParams['encrypted_value'] = mappedParams['encryptedValue']
        delete mappedParams['encryptedValue']
      }
      if ('keyId' in mappedParams) {
        mappedParams['key_id'] = mappedParams['keyId']
        delete mappedParams['keyId']
      }
      if ('selectedRepositoryIds' in mappedParams) {
        mappedParams['selected_repository_ids'] = mappedParams['selectedRepositoryIds']
        delete mappedParams['selectedRepositoryIds']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'codespaces/delete-secret-for-authenticated-user',
  `Delete a secret for the authenticated user`,
  {
    secretName: z.string(),
  },
  async (args, extra) => {
    try {
      const { secretName, ...otherParams } = args
      const url = `/user/codespaces/secrets/${secretName}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('secretName' in mappedParams) {
        mappedParams['secret_name'] = mappedParams['secretName']
        delete mappedParams['secretName']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'codespaces/list-repositories-for-secret-for-authenticated-user',
  `List selected repositories for a user secret`,
  {
    secretName: z.string(),
  },
  async (args, extra) => {
    try {
      const { secretName, ...otherParams } = args
      const url = `/user/codespaces/secrets/${secretName}/repositories`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('secretName' in mappedParams) {
        mappedParams['secret_name'] = mappedParams['secretName']
        delete mappedParams['secretName']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'codespaces/set-repositories-for-secret-for-authenticated-user',
  `Set selected repositories for a user secret`,
  {
    secretName: z.string(),
    selectedRepositoryIds: z.any(),
  },
  async (args, extra) => {
    try {
      const { secretName, ...otherParams } = args
      const url = `/user/codespaces/secrets/${secretName}/repositories`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('secretName' in mappedParams) {
        mappedParams['secret_name'] = mappedParams['secretName']
        delete mappedParams['secretName']
      }
      if ('selectedRepositoryIds' in mappedParams) {
        mappedParams['selected_repository_ids'] = mappedParams['selectedRepositoryIds']
        delete mappedParams['selectedRepositoryIds']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'codespaces/add-repository-for-secret-for-authenticated-user',
  `Add a selected repository to a user secret`,
  {
    secretName: z.string(),
    repositoryId: z.string(),
  },
  async (args, extra) => {
    try {
      const { secretName, repositoryId, ...otherParams } = args
      const url = `/user/codespaces/secrets/${secretName}/repositories/${repositoryId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('secretName' in mappedParams) {
        mappedParams['secret_name'] = mappedParams['secretName']
        delete mappedParams['secretName']
      }
      if ('repositoryId' in mappedParams) {
        mappedParams['repository_id'] = mappedParams['repositoryId']
        delete mappedParams['repositoryId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'codespaces/remove-repository-for-secret-for-authenticated-user',
  `Remove a selected repository from a user secret`,
  {
    secretName: z.string(),
    repositoryId: z.string(),
  },
  async (args, extra) => {
    try {
      const { secretName, repositoryId, ...otherParams } = args
      const url = `/user/codespaces/secrets/${secretName}/repositories/${repositoryId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('secretName' in mappedParams) {
        mappedParams['secret_name'] = mappedParams['secretName']
        delete mappedParams['secretName']
      }
      if ('repositoryId' in mappedParams) {
        mappedParams['repository_id'] = mappedParams['repositoryId']
        delete mappedParams['repositoryId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'codespaces/get-for-authenticated-user',
  `Get a codespace for the authenticated user`,
  {
    codespaceName: z.string(),
  },
  async (args, extra) => {
    try {
      const { codespaceName, ...otherParams } = args
      const url = `/user/codespaces/${codespaceName}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('codespaceName' in mappedParams) {
        mappedParams['codespace_name'] = mappedParams['codespaceName']
        delete mappedParams['codespaceName']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'codespaces/update-for-authenticated-user',
  `Update a codespace for the authenticated user`,
  {
    codespaceName: z.string(),
    machine: z.any().optional(),
    displayName: z.any().optional(),
    recentFolders: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { codespaceName, ...otherParams } = args
      const url = `/user/codespaces/${codespaceName}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('codespaceName' in mappedParams) {
        mappedParams['codespace_name'] = mappedParams['codespaceName']
        delete mappedParams['codespaceName']
      }
      if ('displayName' in mappedParams) {
        mappedParams['display_name'] = mappedParams['displayName']
        delete mappedParams['displayName']
      }
      if ('recentFolders' in mappedParams) {
        mappedParams['recent_folders'] = mappedParams['recentFolders']
        delete mappedParams['recentFolders']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PATCH',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'codespaces/delete-for-authenticated-user',
  `Delete a codespace for the authenticated user`,
  {
    codespaceName: z.string(),
  },
  async (args, extra) => {
    try {
      const { codespaceName, ...otherParams } = args
      const url = `/user/codespaces/${codespaceName}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('codespaceName' in mappedParams) {
        mappedParams['codespace_name'] = mappedParams['codespaceName']
        delete mappedParams['codespaceName']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'codespaces/export-for-authenticated-user',
  `Export a codespace for the authenticated user`,
  {
    codespaceName: z.string(),
  },
  async (args, extra) => {
    try {
      const { codespaceName, ...otherParams } = args
      const url = `/user/codespaces/${codespaceName}/exports`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('codespaceName' in mappedParams) {
        mappedParams['codespace_name'] = mappedParams['codespaceName']
        delete mappedParams['codespaceName']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'codespaces/get-export-details-for-authenticated-user',
  `Get details about a codespace export`,
  {
    codespaceName: z.string(),
    exportId: z.string(),
  },
  async (args, extra) => {
    try {
      const { codespaceName, exportId, ...otherParams } = args
      const url = `/user/codespaces/${codespaceName}/exports/${exportId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('codespaceName' in mappedParams) {
        mappedParams['codespace_name'] = mappedParams['codespaceName']
        delete mappedParams['codespaceName']
      }
      if ('exportId' in mappedParams) {
        mappedParams['export_id'] = mappedParams['exportId']
        delete mappedParams['exportId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'codespaces/codespace-machines-for-authenticated-user',
  `List machine types for a codespace`,
  {
    codespaceName: z.string(),
  },
  async (args, extra) => {
    try {
      const { codespaceName, ...otherParams } = args
      const url = `/user/codespaces/${codespaceName}/machines`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('codespaceName' in mappedParams) {
        mappedParams['codespace_name'] = mappedParams['codespaceName']
        delete mappedParams['codespaceName']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'codespaces/publish-for-authenticated-user',
  `Create a repository from an unpublished codespace`,
  {
    codespaceName: z.string(),
    name: z.any().optional(),
    private: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { codespaceName, ...otherParams } = args
      const url = `/user/codespaces/${codespaceName}/publish`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('codespaceName' in mappedParams) {
        mappedParams['codespace_name'] = mappedParams['codespaceName']
        delete mappedParams['codespaceName']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'codespaces/start-for-authenticated-user',
  `Start a codespace for the authenticated user`,
  {
    codespaceName: z.string(),
  },
  async (args, extra) => {
    try {
      const { codespaceName, ...otherParams } = args
      const url = `/user/codespaces/${codespaceName}/start`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('codespaceName' in mappedParams) {
        mappedParams['codespace_name'] = mappedParams['codespaceName']
        delete mappedParams['codespaceName']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'codespaces/stop-for-authenticated-user',
  `Stop a codespace for the authenticated user`,
  {
    codespaceName: z.string(),
  },
  async (args, extra) => {
    try {
      const { codespaceName, ...otherParams } = args
      const url = `/user/codespaces/${codespaceName}/stop`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('codespaceName' in mappedParams) {
        mappedParams['codespace_name'] = mappedParams['codespaceName']
        delete mappedParams['codespaceName']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'packages/list-docker-migration-conflicting-packages-for-authenticated-user',
  `Get list of conflicting packages during Docker migration for authenticated-user`,
  {},
  async (args, extra) => {
    try {
      const otherParams = args

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: '/user/docker/conflicts',
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'users/set-primary-email-visibility-for-authenticated-user',
  `Set primary email visibility for the authenticated user`,
  {
    visibility: z.any(),
  },
  async (args, extra) => {
    try {
      const otherParams = args

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PATCH',
        url: '/user/email/visibility',
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'users/list-emails-for-authenticated-user',
  `List email addresses for the authenticated user`,
  {},
  async (args, extra) => {
    try {
      const otherParams = args

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: '/user/emails',
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'users/add-email-for-authenticated-user',
  `Add an email address for the authenticated user`,
  {},
  async (args, extra) => {
    try {
      const otherParams = args

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: '/user/emails',
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'users/delete-email-for-authenticated-user',
  `Delete an email address for the authenticated user`,
  {},
  async (args, extra) => {
    try {
      const otherParams = args

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: '/user/emails',
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'users/list-followers-for-authenticated-user',
  `List followers of the authenticated user`,
  {},
  async (args, extra) => {
    try {
      const otherParams = args

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: '/user/followers',
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'users/list-followed-by-authenticated-user',
  `List the people the authenticated user follows`,
  {},
  async (args, extra) => {
    try {
      const otherParams = args

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: '/user/following',
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'users/check-person-is-followed-by-authenticated',
  `Check if a person is followed by the authenticated user`,
  {
    username: z.string(),
  },
  async (args, extra) => {
    try {
      const { username, ...otherParams } = args
      const url = `/user/following/${username}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'users/follow',
  `Follow a user`,
  {
    username: z.string(),
  },
  async (args, extra) => {
    try {
      const { username, ...otherParams } = args
      const url = `/user/following/${username}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'users/unfollow',
  `Unfollow a user`,
  {
    username: z.string(),
  },
  async (args, extra) => {
    try {
      const { username, ...otherParams } = args
      const url = `/user/following/${username}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'users/list-gpg-keys-for-authenticated-user',
  `List GPG keys for the authenticated user`,
  {},
  async (args, extra) => {
    try {
      const otherParams = args

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: '/user/gpg_keys',
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'users/create-gpg-key-for-authenticated-user',
  `Create a GPG key for the authenticated user`,
  {
    name: z.any().optional(),
    armoredPublicKey: z.any(),
  },
  async (args, extra) => {
    try {
      const otherParams = args

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('armoredPublicKey' in mappedParams) {
        mappedParams['armored_public_key'] = mappedParams['armoredPublicKey']
        delete mappedParams['armoredPublicKey']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: '/user/gpg_keys',
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'users/get-gpg-key-for-authenticated-user',
  `Get a GPG key for the authenticated user`,
  {
    gpgKeyId: z.string(),
  },
  async (args, extra) => {
    try {
      const { gpgKeyId, ...otherParams } = args
      const url = `/user/gpg_keys/${gpgKeyId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('gpgKeyId' in mappedParams) {
        mappedParams['gpg_key_id'] = mappedParams['gpgKeyId']
        delete mappedParams['gpgKeyId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'users/delete-gpg-key-for-authenticated-user',
  `Delete a GPG key for the authenticated user`,
  {
    gpgKeyId: z.string(),
  },
  async (args, extra) => {
    try {
      const { gpgKeyId, ...otherParams } = args
      const url = `/user/gpg_keys/${gpgKeyId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('gpgKeyId' in mappedParams) {
        mappedParams['gpg_key_id'] = mappedParams['gpgKeyId']
        delete mappedParams['gpgKeyId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'apps/list-installations-for-authenticated-user',
  `List app installations accessible to the user access token`,
  {},
  async (args, extra) => {
    try {
      const otherParams = args

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: '/user/installations',
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'apps/list-installation-repos-for-authenticated-user',
  `List repositories accessible to the user access token`,
  {
    installationId: z.string(),
  },
  async (args, extra) => {
    try {
      const { installationId, ...otherParams } = args
      const url = `/user/installations/${installationId}/repositories`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('installationId' in mappedParams) {
        mappedParams['installation_id'] = mappedParams['installationId']
        delete mappedParams['installationId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'apps/add-repo-to-installation-for-authenticated-user',
  `Add a repository to an app installation`,
  {
    installationId: z.string(),
    repositoryId: z.string(),
  },
  async (args, extra) => {
    try {
      const { installationId, repositoryId, ...otherParams } = args
      const url = `/user/installations/${installationId}/repositories/${repositoryId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('installationId' in mappedParams) {
        mappedParams['installation_id'] = mappedParams['installationId']
        delete mappedParams['installationId']
      }
      if ('repositoryId' in mappedParams) {
        mappedParams['repository_id'] = mappedParams['repositoryId']
        delete mappedParams['repositoryId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'apps/remove-repo-from-installation-for-authenticated-user',
  `Remove a repository from an app installation`,
  {
    installationId: z.string(),
    repositoryId: z.string(),
  },
  async (args, extra) => {
    try {
      const { installationId, repositoryId, ...otherParams } = args
      const url = `/user/installations/${installationId}/repositories/${repositoryId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('installationId' in mappedParams) {
        mappedParams['installation_id'] = mappedParams['installationId']
        delete mappedParams['installationId']
      }
      if ('repositoryId' in mappedParams) {
        mappedParams['repository_id'] = mappedParams['repositoryId']
        delete mappedParams['repositoryId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'interactions/get-restrictions-for-authenticated-user',
  `Get interaction restrictions for your public repositories`,
  {},
  async (args, extra) => {
    try {
      const otherParams = args

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: '/user/interaction-limits',
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'interactions/set-restrictions-for-authenticated-user',
  `Set interaction restrictions for your public repositories`,
  {
    requestData: z.record(z.any()).optional(),
  },
  async (args, extra) => {
    try {
      const otherParams = args

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: '/user/interaction-limits',
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'interactions/remove-restrictions-for-authenticated-user',
  `Remove interaction restrictions from your public repositories`,
  {},
  async (args, extra) => {
    try {
      const otherParams = args

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: '/user/interaction-limits',
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'issues/list-for-authenticated-user',
  `List user account issues assigned to the authenticated user`,
  {
    filter: z.string().optional(),
    state: z.string().optional(),
    sort: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const otherParams = args

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: '/user/issues',
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'users/list-public-ssh-keys-for-authenticated-user',
  `List public SSH keys for the authenticated user`,
  {},
  async (args, extra) => {
    try {
      const otherParams = args

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: '/user/keys',
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'users/create-public-ssh-key-for-authenticated-user',
  `Create a public SSH key for the authenticated user`,
  {
    title: z.any().optional(),
    key: z.any(),
  },
  async (args, extra) => {
    try {
      const otherParams = args

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: '/user/keys',
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'users/get-public-ssh-key-for-authenticated-user',
  `Get a public SSH key for the authenticated user`,
  {
    keyId: z.string(),
  },
  async (args, extra) => {
    try {
      const { keyId, ...otherParams } = args
      const url = `/user/keys/${keyId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('keyId' in mappedParams) {
        mappedParams['key_id'] = mappedParams['keyId']
        delete mappedParams['keyId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'users/delete-public-ssh-key-for-authenticated-user',
  `Delete a public SSH key for the authenticated user`,
  {
    keyId: z.string(),
  },
  async (args, extra) => {
    try {
      const { keyId, ...otherParams } = args
      const url = `/user/keys/${keyId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('keyId' in mappedParams) {
        mappedParams['key_id'] = mappedParams['keyId']
        delete mappedParams['keyId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'apps/list-subscriptions-for-authenticated-user',
  `List subscriptions for the authenticated user`,
  {},
  async (args, extra) => {
    try {
      const otherParams = args

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: '/user/marketplace_purchases',
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'apps/list-subscriptions-for-authenticated-user-stubbed',
  `List subscriptions for the authenticated user (stubbed)`,
  {},
  async (args, extra) => {
    try {
      const otherParams = args

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: '/user/marketplace_purchases/stubbed',
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'orgs/list-memberships-for-authenticated-user',
  `List organization memberships for the authenticated user`,
  {
    state: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const otherParams = args

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: '/user/memberships/orgs',
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'orgs/get-membership-for-authenticated-user',
  `Get an organization membership for the authenticated user`,
  {
    org: z.string(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/user/memberships/orgs/${org}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'orgs/update-membership-for-authenticated-user',
  `Update an organization membership for the authenticated user`,
  {
    org: z.string(),
    state: z.any(),
  },
  async (args, extra) => {
    try {
      const { org, ...otherParams } = args
      const url = `/user/memberships/orgs/${org}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PATCH',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'migrations/list-for-authenticated-user',
  `List user migrations`,
  {},
  async (args, extra) => {
    try {
      const otherParams = args

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: '/user/migrations',
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'migrations/start-for-authenticated-user',
  `Start a user migration`,
  {
    lockRepositories: z.any().optional(),
    excludeMetadata: z.any().optional(),
    excludeGitData: z.any().optional(),
    excludeAttachments: z.any().optional(),
    excludeReleases: z.any().optional(),
    excludeOwnerProjects: z.any().optional(),
    orgMetadataOnly: z.any().optional(),
    exclude: z.any().optional(),
    repositories: z.any(),
  },
  async (args, extra) => {
    try {
      const otherParams = args

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('lockRepositories' in mappedParams) {
        mappedParams['lock_repositories'] = mappedParams['lockRepositories']
        delete mappedParams['lockRepositories']
      }
      if ('excludeMetadata' in mappedParams) {
        mappedParams['exclude_metadata'] = mappedParams['excludeMetadata']
        delete mappedParams['excludeMetadata']
      }
      if ('excludeGitData' in mappedParams) {
        mappedParams['exclude_git_data'] = mappedParams['excludeGitData']
        delete mappedParams['excludeGitData']
      }
      if ('excludeAttachments' in mappedParams) {
        mappedParams['exclude_attachments'] = mappedParams['excludeAttachments']
        delete mappedParams['excludeAttachments']
      }
      if ('excludeReleases' in mappedParams) {
        mappedParams['exclude_releases'] = mappedParams['excludeReleases']
        delete mappedParams['excludeReleases']
      }
      if ('excludeOwnerProjects' in mappedParams) {
        mappedParams['exclude_owner_projects'] = mappedParams['excludeOwnerProjects']
        delete mappedParams['excludeOwnerProjects']
      }
      if ('orgMetadataOnly' in mappedParams) {
        mappedParams['org_metadata_only'] = mappedParams['orgMetadataOnly']
        delete mappedParams['orgMetadataOnly']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: '/user/migrations',
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'migrations/get-status-for-authenticated-user',
  `Get a user migration status`,
  {
    migrationId: z.string(),
    exclude: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const { migrationId, ...otherParams } = args
      const url = `/user/migrations/${migrationId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('migrationId' in mappedParams) {
        mappedParams['migration_id'] = mappedParams['migrationId']
        delete mappedParams['migrationId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'migrations/get-archive-for-authenticated-user',
  `Download a user migration archive`,
  {
    migrationId: z.string(),
  },
  async (args, extra) => {
    try {
      const { migrationId, ...otherParams } = args
      const url = `/user/migrations/${migrationId}/archive`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('migrationId' in mappedParams) {
        mappedParams['migration_id'] = mappedParams['migrationId']
        delete mappedParams['migrationId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'migrations/delete-archive-for-authenticated-user',
  `Delete a user migration archive`,
  {
    migrationId: z.string(),
  },
  async (args, extra) => {
    try {
      const { migrationId, ...otherParams } = args
      const url = `/user/migrations/${migrationId}/archive`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('migrationId' in mappedParams) {
        mappedParams['migration_id'] = mappedParams['migrationId']
        delete mappedParams['migrationId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'migrations/unlock-repo-for-authenticated-user',
  `Unlock a user repository`,
  {
    migrationId: z.string(),
    repoName: z.string(),
  },
  async (args, extra) => {
    try {
      const { migrationId, repoName, ...otherParams } = args
      const url = `/user/migrations/${migrationId}/repos/${repoName}/lock`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('migrationId' in mappedParams) {
        mappedParams['migration_id'] = mappedParams['migrationId']
        delete mappedParams['migrationId']
      }
      if ('repoName' in mappedParams) {
        mappedParams['repo_name'] = mappedParams['repoName']
        delete mappedParams['repoName']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'migrations/list-repos-for-authenticated-user',
  `List repositories for a user migration`,
  {
    migrationId: z.string(),
  },
  async (args, extra) => {
    try {
      const { migrationId, ...otherParams } = args
      const url = `/user/migrations/${migrationId}/repositories`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('migrationId' in mappedParams) {
        mappedParams['migration_id'] = mappedParams['migrationId']
        delete mappedParams['migrationId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'orgs/list-for-authenticated-user',
  `List organizations for the authenticated user`,
  {},
  async (args, extra) => {
    try {
      const otherParams = args

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: '/user/orgs',
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'packages/list-packages-for-authenticated-user',
  `List packages for the authenticated user&#x27;s namespace`,
  {
    packageType: z.string(),
  },
  async (args, extra) => {
    try {
      const otherParams = args

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('packageType' in mappedParams) {
        mappedParams['package_type'] = mappedParams['packageType']
        delete mappedParams['packageType']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: '/user/packages',
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'packages/get-package-for-authenticated-user',
  `Get a package for the authenticated user`,
  {
    packageType: z.string(),
    packageName: z.string(),
  },
  async (args, extra) => {
    try {
      const { packageType, packageName, ...otherParams } = args
      const url = `/user/packages/${packageType}/${packageName}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('packageType' in mappedParams) {
        mappedParams['package_type'] = mappedParams['packageType']
        delete mappedParams['packageType']
      }
      if ('packageName' in mappedParams) {
        mappedParams['package_name'] = mappedParams['packageName']
        delete mappedParams['packageName']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'packages/delete-package-for-authenticated-user',
  `Delete a package for the authenticated user`,
  {
    packageType: z.string(),
    packageName: z.string(),
  },
  async (args, extra) => {
    try {
      const { packageType, packageName, ...otherParams } = args
      const url = `/user/packages/${packageType}/${packageName}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('packageType' in mappedParams) {
        mappedParams['package_type'] = mappedParams['packageType']
        delete mappedParams['packageType']
      }
      if ('packageName' in mappedParams) {
        mappedParams['package_name'] = mappedParams['packageName']
        delete mappedParams['packageName']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'packages/restore-package-for-authenticated-user',
  `Restore a package for the authenticated user`,
  {
    packageType: z.string(),
    packageName: z.string(),
    token: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const { packageType, packageName, ...otherParams } = args
      const url = `/user/packages/${packageType}/${packageName}/restore`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('packageType' in mappedParams) {
        mappedParams['package_type'] = mappedParams['packageType']
        delete mappedParams['packageType']
      }
      if ('packageName' in mappedParams) {
        mappedParams['package_name'] = mappedParams['packageName']
        delete mappedParams['packageName']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'packages/get-all-package-versions-for-package-owned-by-authenticated-user',
  `List package versions for a package owned by the authenticated user`,
  {
    packageType: z.string(),
    packageName: z.string(),
    state: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const { packageType, packageName, ...otherParams } = args
      const url = `/user/packages/${packageType}/${packageName}/versions`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('packageType' in mappedParams) {
        mappedParams['package_type'] = mappedParams['packageType']
        delete mappedParams['packageType']
      }
      if ('packageName' in mappedParams) {
        mappedParams['package_name'] = mappedParams['packageName']
        delete mappedParams['packageName']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'packages/get-package-version-for-authenticated-user',
  `Get a package version for the authenticated user`,
  {
    packageType: z.string(),
    packageName: z.string(),
    packageVersionId: z.string(),
  },
  async (args, extra) => {
    try {
      const { packageType, packageName, packageVersionId, ...otherParams } = args
      const url = `/user/packages/${packageType}/${packageName}/versions/${packageVersionId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('packageType' in mappedParams) {
        mappedParams['package_type'] = mappedParams['packageType']
        delete mappedParams['packageType']
      }
      if ('packageName' in mappedParams) {
        mappedParams['package_name'] = mappedParams['packageName']
        delete mappedParams['packageName']
      }
      if ('packageVersionId' in mappedParams) {
        mappedParams['package_version_id'] = mappedParams['packageVersionId']
        delete mappedParams['packageVersionId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'packages/delete-package-version-for-authenticated-user',
  `Delete a package version for the authenticated user`,
  {
    packageType: z.string(),
    packageName: z.string(),
    packageVersionId: z.string(),
  },
  async (args, extra) => {
    try {
      const { packageType, packageName, packageVersionId, ...otherParams } = args
      const url = `/user/packages/${packageType}/${packageName}/versions/${packageVersionId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('packageType' in mappedParams) {
        mappedParams['package_type'] = mappedParams['packageType']
        delete mappedParams['packageType']
      }
      if ('packageName' in mappedParams) {
        mappedParams['package_name'] = mappedParams['packageName']
        delete mappedParams['packageName']
      }
      if ('packageVersionId' in mappedParams) {
        mappedParams['package_version_id'] = mappedParams['packageVersionId']
        delete mappedParams['packageVersionId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'packages/restore-package-version-for-authenticated-user',
  `Restore a package version for the authenticated user`,
  {
    packageType: z.string(),
    packageName: z.string(),
    packageVersionId: z.string(),
  },
  async (args, extra) => {
    try {
      const { packageType, packageName, packageVersionId, ...otherParams } = args
      const url = `/user/packages/${packageType}/${packageName}/versions/${packageVersionId}/restore`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('packageType' in mappedParams) {
        mappedParams['package_type'] = mappedParams['packageType']
        delete mappedParams['packageType']
      }
      if ('packageName' in mappedParams) {
        mappedParams['package_name'] = mappedParams['packageName']
        delete mappedParams['packageName']
      }
      if ('packageVersionId' in mappedParams) {
        mappedParams['package_version_id'] = mappedParams['packageVersionId']
        delete mappedParams['packageVersionId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'projects-classic/create-for-authenticated-user',
  `Create a user project`,
  {
    name: z.any(),
    body: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const otherParams = args

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: '/user/projects',
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'users/list-public-emails-for-authenticated-user',
  `List public email addresses for the authenticated user`,
  {},
  async (args, extra) => {
    try {
      const otherParams = args

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: '/user/public_emails',
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/list-for-authenticated-user',
  `List repositories for the authenticated user`,
  {
    visibility: z.string().optional(),
    affiliation: z.string().optional(),
    type: z.string().optional(),
    sort: z.string().optional(),
    direction: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const otherParams = args

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: '/user/repos',
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/create-for-authenticated-user',
  `Create a repository for the authenticated user`,
  {
    name: z.any(),
    description: z.any().optional(),
    homepage: z.any().optional(),
    private: z.any().optional(),
    hasIssues: z.any().optional(),
    hasProjects: z.any().optional(),
    hasWiki: z.any().optional(),
    hasDiscussions: z.any().optional(),
    teamId: z.any().optional(),
    autoInit: z.any().optional(),
    gitignoreTemplate: z.any().optional(),
    licenseTemplate: z.any().optional(),
    allowSquashMerge: z.any().optional(),
    allowMergeCommit: z.any().optional(),
    allowRebaseMerge: z.any().optional(),
    allowAutoMerge: z.any().optional(),
    deleteBranchOnMerge: z.any().optional(),
    squashMergeCommitTitle: z.any().optional(),
    squashMergeCommitMessage: z.any().optional(),
    mergeCommitTitle: z.any().optional(),
    mergeCommitMessage: z.any().optional(),
    hasDownloads: z.any().optional(),
    isTemplate: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const otherParams = args

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('hasIssues' in mappedParams) {
        mappedParams['has_issues'] = mappedParams['hasIssues']
        delete mappedParams['hasIssues']
      }
      if ('hasProjects' in mappedParams) {
        mappedParams['has_projects'] = mappedParams['hasProjects']
        delete mappedParams['hasProjects']
      }
      if ('hasWiki' in mappedParams) {
        mappedParams['has_wiki'] = mappedParams['hasWiki']
        delete mappedParams['hasWiki']
      }
      if ('hasDiscussions' in mappedParams) {
        mappedParams['has_discussions'] = mappedParams['hasDiscussions']
        delete mappedParams['hasDiscussions']
      }
      if ('teamId' in mappedParams) {
        mappedParams['team_id'] = mappedParams['teamId']
        delete mappedParams['teamId']
      }
      if ('autoInit' in mappedParams) {
        mappedParams['auto_init'] = mappedParams['autoInit']
        delete mappedParams['autoInit']
      }
      if ('gitignoreTemplate' in mappedParams) {
        mappedParams['gitignore_template'] = mappedParams['gitignoreTemplate']
        delete mappedParams['gitignoreTemplate']
      }
      if ('licenseTemplate' in mappedParams) {
        mappedParams['license_template'] = mappedParams['licenseTemplate']
        delete mappedParams['licenseTemplate']
      }
      if ('allowSquashMerge' in mappedParams) {
        mappedParams['allow_squash_merge'] = mappedParams['allowSquashMerge']
        delete mappedParams['allowSquashMerge']
      }
      if ('allowMergeCommit' in mappedParams) {
        mappedParams['allow_merge_commit'] = mappedParams['allowMergeCommit']
        delete mappedParams['allowMergeCommit']
      }
      if ('allowRebaseMerge' in mappedParams) {
        mappedParams['allow_rebase_merge'] = mappedParams['allowRebaseMerge']
        delete mappedParams['allowRebaseMerge']
      }
      if ('allowAutoMerge' in mappedParams) {
        mappedParams['allow_auto_merge'] = mappedParams['allowAutoMerge']
        delete mappedParams['allowAutoMerge']
      }
      if ('deleteBranchOnMerge' in mappedParams) {
        mappedParams['delete_branch_on_merge'] = mappedParams['deleteBranchOnMerge']
        delete mappedParams['deleteBranchOnMerge']
      }
      if ('squashMergeCommitTitle' in mappedParams) {
        mappedParams['squash_merge_commit_title'] = mappedParams['squashMergeCommitTitle']
        delete mappedParams['squashMergeCommitTitle']
      }
      if ('squashMergeCommitMessage' in mappedParams) {
        mappedParams['squash_merge_commit_message'] = mappedParams['squashMergeCommitMessage']
        delete mappedParams['squashMergeCommitMessage']
      }
      if ('mergeCommitTitle' in mappedParams) {
        mappedParams['merge_commit_title'] = mappedParams['mergeCommitTitle']
        delete mappedParams['mergeCommitTitle']
      }
      if ('mergeCommitMessage' in mappedParams) {
        mappedParams['merge_commit_message'] = mappedParams['mergeCommitMessage']
        delete mappedParams['mergeCommitMessage']
      }
      if ('hasDownloads' in mappedParams) {
        mappedParams['has_downloads'] = mappedParams['hasDownloads']
        delete mappedParams['hasDownloads']
      }
      if ('isTemplate' in mappedParams) {
        mappedParams['is_template'] = mappedParams['isTemplate']
        delete mappedParams['isTemplate']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: '/user/repos',
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/list-invitations-for-authenticated-user',
  `List repository invitations for the authenticated user`,
  {},
  async (args, extra) => {
    try {
      const otherParams = args

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: '/user/repository_invitations',
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/accept-invitation-for-authenticated-user',
  `Accept a repository invitation`,
  {
    invitationId: z.string(),
  },
  async (args, extra) => {
    try {
      const { invitationId, ...otherParams } = args
      const url = `/user/repository_invitations/${invitationId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('invitationId' in mappedParams) {
        mappedParams['invitation_id'] = mappedParams['invitationId']
        delete mappedParams['invitationId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PATCH',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/decline-invitation-for-authenticated-user',
  `Decline a repository invitation`,
  {
    invitationId: z.string(),
  },
  async (args, extra) => {
    try {
      const { invitationId, ...otherParams } = args
      const url = `/user/repository_invitations/${invitationId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('invitationId' in mappedParams) {
        mappedParams['invitation_id'] = mappedParams['invitationId']
        delete mappedParams['invitationId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'users/list-social-accounts-for-authenticated-user',
  `List social accounts for the authenticated user`,
  {},
  async (args, extra) => {
    try {
      const otherParams = args

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: '/user/social_accounts',
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'users/add-social-account-for-authenticated-user',
  `Add social accounts for the authenticated user`,
  {
    accountUrls: z.any(),
  },
  async (args, extra) => {
    try {
      const otherParams = args

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('accountUrls' in mappedParams) {
        mappedParams['account_urls'] = mappedParams['accountUrls']
        delete mappedParams['accountUrls']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: '/user/social_accounts',
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'users/delete-social-account-for-authenticated-user',
  `Delete social accounts for the authenticated user`,
  {
    accountUrls: z.any(),
  },
  async (args, extra) => {
    try {
      const otherParams = args

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('accountUrls' in mappedParams) {
        mappedParams['account_urls'] = mappedParams['accountUrls']
        delete mappedParams['accountUrls']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: '/user/social_accounts',
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'users/list-ssh-signing-keys-for-authenticated-user',
  `List SSH signing keys for the authenticated user`,
  {},
  async (args, extra) => {
    try {
      const otherParams = args

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: '/user/ssh_signing_keys',
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'users/create-ssh-signing-key-for-authenticated-user',
  `Create a SSH signing key for the authenticated user`,
  {
    title: z.any().optional(),
    key: z.any(),
  },
  async (args, extra) => {
    try {
      const otherParams = args

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: '/user/ssh_signing_keys',
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'users/get-ssh-signing-key-for-authenticated-user',
  `Get an SSH signing key for the authenticated user`,
  {
    sshSigningKeyId: z.string(),
  },
  async (args, extra) => {
    try {
      const { sshSigningKeyId, ...otherParams } = args
      const url = `/user/ssh_signing_keys/${sshSigningKeyId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('sshSigningKeyId' in mappedParams) {
        mappedParams['ssh_signing_key_id'] = mappedParams['sshSigningKeyId']
        delete mappedParams['sshSigningKeyId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'users/delete-ssh-signing-key-for-authenticated-user',
  `Delete an SSH signing key for the authenticated user`,
  {
    sshSigningKeyId: z.string(),
  },
  async (args, extra) => {
    try {
      const { sshSigningKeyId, ...otherParams } = args
      const url = `/user/ssh_signing_keys/${sshSigningKeyId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('sshSigningKeyId' in mappedParams) {
        mappedParams['ssh_signing_key_id'] = mappedParams['sshSigningKeyId']
        delete mappedParams['sshSigningKeyId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'activity/list-repos-starred-by-authenticated-user',
  `List repositories starred by the authenticated user`,
  {},
  async (args, extra) => {
    try {
      const otherParams = args

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: '/user/starred',
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'activity/check-repo-is-starred-by-authenticated-user',
  `Check if a repository is starred by the authenticated user`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/user/starred/${owner}/${repo}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'activity/star-repo-for-authenticated-user',
  `Star a repository for the authenticated user`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/user/starred/${owner}/${repo}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'PUT',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'activity/unstar-repo-for-authenticated-user',
  `Unstar a repository for the authenticated user`,
  {
    owner: z.string(),
    repo: z.string(),
  },
  async (args, extra) => {
    try {
      const { owner, repo, ...otherParams } = args
      const url = `/user/starred/${owner}/${repo}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'activity/list-watched-repos-for-authenticated-user',
  `List repositories watched by the authenticated user`,
  {},
  async (args, extra) => {
    try {
      const otherParams = args

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: '/user/subscriptions',
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'teams/list-for-authenticated-user',
  `List teams for the authenticated user`,
  {},
  async (args, extra) => {
    try {
      const otherParams = args

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: '/user/teams',
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'users/get-by-id',
  `Get a user using their ID`,
  {
    accountId: z.string(),
  },
  async (args, extra) => {
    try {
      const { accountId, ...otherParams } = args
      const url = `/user/${accountId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('accountId' in mappedParams) {
        mappedParams['account_id'] = mappedParams['accountId']
        delete mappedParams['accountId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool('users/list', `List users`, {}, async (args, extra) => {
  try {
    const otherParams = args

    // Map camelCase to original parameter names for API request
    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    const mappedParams: any = { ...otherParams }

    // Extract authorization token from HTTP request headers
    const authorization = extra?.requestInfo?.headers?.authorization as string
    const bearer = authorization?.replace('Bearer ', '')

    const response = await apiClient.request({
      headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
      method: 'GET',
      url: '/users',
      params: mappedParams,
    })

    return handleResult(response.data)
  } catch (error) {
    return handleError(error)
  }
})

mcpServer.tool(
  'users/get-by-username',
  `Get a user`,
  {
    username: z.string(),
  },
  async (args, extra) => {
    try {
      const { username, ...otherParams } = args
      const url = `/users/${username}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'users/list-attestations-bulk',
  `List attestations by bulk subject digests`,
  {
    username: z.string(),
    subjectDigests: z.any(),
    predicateType: z.any().optional(),
  },
  async (args, extra) => {
    try {
      const { username, ...otherParams } = args
      const url = `/users/${username}/attestations/bulk-list`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('subjectDigests' in mappedParams) {
        mappedParams['subject_digests'] = mappedParams['subjectDigests']
        delete mappedParams['subjectDigests']
      }
      if ('predicateType' in mappedParams) {
        mappedParams['predicate_type'] = mappedParams['predicateType']
        delete mappedParams['predicateType']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'users/delete-attestations-bulk',
  `Delete attestations in bulk`,
  {
    username: z.string(),
  },
  async (args, extra) => {
    try {
      const { username, ...otherParams } = args
      const url = `/users/${username}/attestations/delete-request`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'users/delete-attestations-by-subject-digest',
  `Delete attestations by subject digest`,
  {
    username: z.string(),
    subjectDigest: z.string(),
  },
  async (args, extra) => {
    try {
      const { username, subjectDigest, ...otherParams } = args
      const url = `/users/${username}/attestations/digest/${subjectDigest}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('subjectDigest' in mappedParams) {
        mappedParams['subject_digest'] = mappedParams['subjectDigest']
        delete mappedParams['subjectDigest']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'users/delete-attestations-by-id',
  `Delete attestations by ID`,
  {
    username: z.string(),
    attestationId: z.string(),
  },
  async (args, extra) => {
    try {
      const { username, attestationId, ...otherParams } = args
      const url = `/users/${username}/attestations/${attestationId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('attestationId' in mappedParams) {
        mappedParams['attestation_id'] = mappedParams['attestationId']
        delete mappedParams['attestationId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'users/list-attestations',
  `List attestations`,
  {
    username: z.string(),
    subjectDigest: z.string(),
    predicateType: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const { username, subjectDigest, ...otherParams } = args
      const url = `/users/${username}/attestations/${subjectDigest}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('subjectDigest' in mappedParams) {
        mappedParams['subject_digest'] = mappedParams['subjectDigest']
        delete mappedParams['subjectDigest']
      }
      if ('predicateType' in mappedParams) {
        mappedParams['predicate_type'] = mappedParams['predicateType']
        delete mappedParams['predicateType']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'packages/list-docker-migration-conflicting-packages-for-user',
  `Get list of conflicting packages during Docker migration for user`,
  {
    username: z.string(),
  },
  async (args, extra) => {
    try {
      const { username, ...otherParams } = args
      const url = `/users/${username}/docker/conflicts`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'activity/list-events-for-authenticated-user',
  `List events for the authenticated user`,
  {
    username: z.string(),
  },
  async (args, extra) => {
    try {
      const { username, ...otherParams } = args
      const url = `/users/${username}/events`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'activity/list-org-events-for-authenticated-user',
  `List organization events for the authenticated user`,
  {
    username: z.string(),
    org: z.string(),
  },
  async (args, extra) => {
    try {
      const { username, org, ...otherParams } = args
      const url = `/users/${username}/events/orgs/${org}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'activity/list-public-events-for-user',
  `List public events for a user`,
  {
    username: z.string(),
  },
  async (args, extra) => {
    try {
      const { username, ...otherParams } = args
      const url = `/users/${username}/events/public`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'users/list-followers-for-user',
  `List followers of a user`,
  {
    username: z.string(),
  },
  async (args, extra) => {
    try {
      const { username, ...otherParams } = args
      const url = `/users/${username}/followers`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'users/list-following-for-user',
  `List the people a user follows`,
  {
    username: z.string(),
  },
  async (args, extra) => {
    try {
      const { username, ...otherParams } = args
      const url = `/users/${username}/following`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'users/check-following-for-user',
  `Check if a user follows another user`,
  {
    username: z.string(),
    targetUser: z.string(),
  },
  async (args, extra) => {
    try {
      const { username, targetUser, ...otherParams } = args
      const url = `/users/${username}/following/${targetUser}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('targetUser' in mappedParams) {
        mappedParams['target_user'] = mappedParams['targetUser']
        delete mappedParams['targetUser']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'gists/list-for-user',
  `List gists for a user`,
  {
    username: z.string(),
  },
  async (args, extra) => {
    try {
      const { username, ...otherParams } = args
      const url = `/users/${username}/gists`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'users/list-gpg-keys-for-user',
  `List GPG keys for a user`,
  {
    username: z.string(),
  },
  async (args, extra) => {
    try {
      const { username, ...otherParams } = args
      const url = `/users/${username}/gpg_keys`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'users/get-context-for-user',
  `Get contextual information for a user`,
  {
    username: z.string(),
    subjectType: z.string().optional(),
    subjectId: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const { username, ...otherParams } = args
      const url = `/users/${username}/hovercard`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('subjectType' in mappedParams) {
        mappedParams['subject_type'] = mappedParams['subjectType']
        delete mappedParams['subjectType']
      }
      if ('subjectId' in mappedParams) {
        mappedParams['subject_id'] = mappedParams['subjectId']
        delete mappedParams['subjectId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'apps/get-user-installation',
  `Get a user installation for the authenticated app`,
  {
    username: z.string(),
  },
  async (args, extra) => {
    try {
      const { username, ...otherParams } = args
      const url = `/users/${username}/installation`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'users/list-public-keys-for-user',
  `List public keys for a user`,
  {
    username: z.string(),
  },
  async (args, extra) => {
    try {
      const { username, ...otherParams } = args
      const url = `/users/${username}/keys`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'orgs/list-for-user',
  `List organizations for a user`,
  {
    username: z.string(),
  },
  async (args, extra) => {
    try {
      const { username, ...otherParams } = args
      const url = `/users/${username}/orgs`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'packages/list-packages-for-user',
  `List packages for a user`,
  {
    username: z.string(),
    packageType: z.string(),
  },
  async (args, extra) => {
    try {
      const { username, ...otherParams } = args
      const url = `/users/${username}/packages`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('packageType' in mappedParams) {
        mappedParams['package_type'] = mappedParams['packageType']
        delete mappedParams['packageType']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'packages/get-package-for-user',
  `Get a package for a user`,
  {
    username: z.string(),
    packageType: z.string(),
    packageName: z.string(),
  },
  async (args, extra) => {
    try {
      const { username, packageType, packageName, ...otherParams } = args
      const url = `/users/${username}/packages/${packageType}/${packageName}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('packageType' in mappedParams) {
        mappedParams['package_type'] = mappedParams['packageType']
        delete mappedParams['packageType']
      }
      if ('packageName' in mappedParams) {
        mappedParams['package_name'] = mappedParams['packageName']
        delete mappedParams['packageName']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'packages/delete-package-for-user',
  `Delete a package for a user`,
  {
    username: z.string(),
    packageType: z.string(),
    packageName: z.string(),
  },
  async (args, extra) => {
    try {
      const { username, packageType, packageName, ...otherParams } = args
      const url = `/users/${username}/packages/${packageType}/${packageName}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('packageType' in mappedParams) {
        mappedParams['package_type'] = mappedParams['packageType']
        delete mappedParams['packageType']
      }
      if ('packageName' in mappedParams) {
        mappedParams['package_name'] = mappedParams['packageName']
        delete mappedParams['packageName']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'packages/restore-package-for-user',
  `Restore a package for a user`,
  {
    username: z.string(),
    packageType: z.string(),
    packageName: z.string(),
    token: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const { username, packageType, packageName, ...otherParams } = args
      const url = `/users/${username}/packages/${packageType}/${packageName}/restore`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('packageType' in mappedParams) {
        mappedParams['package_type'] = mappedParams['packageType']
        delete mappedParams['packageType']
      }
      if ('packageName' in mappedParams) {
        mappedParams['package_name'] = mappedParams['packageName']
        delete mappedParams['packageName']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'packages/get-all-package-versions-for-package-owned-by-user',
  `List package versions for a package owned by a user`,
  {
    username: z.string(),
    packageType: z.string(),
    packageName: z.string(),
  },
  async (args, extra) => {
    try {
      const { username, packageType, packageName, ...otherParams } = args
      const url = `/users/${username}/packages/${packageType}/${packageName}/versions`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('packageType' in mappedParams) {
        mappedParams['package_type'] = mappedParams['packageType']
        delete mappedParams['packageType']
      }
      if ('packageName' in mappedParams) {
        mappedParams['package_name'] = mappedParams['packageName']
        delete mappedParams['packageName']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'packages/get-package-version-for-user',
  `Get a package version for a user`,
  {
    username: z.string(),
    packageType: z.string(),
    packageName: z.string(),
    packageVersionId: z.string(),
  },
  async (args, extra) => {
    try {
      const { username, packageType, packageName, packageVersionId, ...otherParams } = args
      const url = `/users/${username}/packages/${packageType}/${packageName}/versions/${packageVersionId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('packageType' in mappedParams) {
        mappedParams['package_type'] = mappedParams['packageType']
        delete mappedParams['packageType']
      }
      if ('packageName' in mappedParams) {
        mappedParams['package_name'] = mappedParams['packageName']
        delete mappedParams['packageName']
      }
      if ('packageVersionId' in mappedParams) {
        mappedParams['package_version_id'] = mappedParams['packageVersionId']
        delete mappedParams['packageVersionId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'packages/delete-package-version-for-user',
  `Delete package version for a user`,
  {
    username: z.string(),
    packageType: z.string(),
    packageName: z.string(),
    packageVersionId: z.string(),
  },
  async (args, extra) => {
    try {
      const { username, packageType, packageName, packageVersionId, ...otherParams } = args
      const url = `/users/${username}/packages/${packageType}/${packageName}/versions/${packageVersionId}`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }
      if ('packageType' in mappedParams) {
        mappedParams['package_type'] = mappedParams['packageType']
        delete mappedParams['packageType']
      }
      if ('packageName' in mappedParams) {
        mappedParams['package_name'] = mappedParams['packageName']
        delete mappedParams['packageName']
      }
      if ('packageVersionId' in mappedParams) {
        mappedParams['package_version_id'] = mappedParams['packageVersionId']
        delete mappedParams['packageVersionId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'DELETE',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'packages/restore-package-version-for-user',
  `Restore package version for a user`,
  {
    username: z.string(),
    packageType: z.string(),
    packageName: z.string(),
    packageVersionId: z.string(),
  },
  async (args, extra) => {
    try {
      const { username, packageType, packageName, packageVersionId, ...otherParams } = args
      const url = `/users/${username}/packages/${packageType}/${packageName}/versions/${packageVersionId}/restore`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = (args as any).requestData || { ...otherParams }
      if ('packageType' in mappedParams) {
        mappedParams['package_type'] = mappedParams['packageType']
        delete mappedParams['packageType']
      }
      if ('packageName' in mappedParams) {
        mappedParams['package_name'] = mappedParams['packageName']
        delete mappedParams['packageName']
      }
      if ('packageVersionId' in mappedParams) {
        mappedParams['package_version_id'] = mappedParams['packageVersionId']
        delete mappedParams['packageVersionId']
      }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'POST',
        url: url,
        data: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'projects-classic/list-for-user',
  `List user projects`,
  {
    username: z.string(),
    state: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const { username, ...otherParams } = args
      const url = `/users/${username}/projects`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'activity/list-received-events-for-user',
  `List events received by the authenticated user`,
  {
    username: z.string(),
  },
  async (args, extra) => {
    try {
      const { username, ...otherParams } = args
      const url = `/users/${username}/received_events`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'activity/list-received-public-events-for-user',
  `List public events received by a user`,
  {
    username: z.string(),
  },
  async (args, extra) => {
    try {
      const { username, ...otherParams } = args
      const url = `/users/${username}/received_events/public`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'repos/list-for-user',
  `List repositories for a user`,
  {
    username: z.string(),
    type: z.string().optional(),
    sort: z.string().optional(),
    direction: z.string().optional(),
  },
  async (args, extra) => {
    try {
      const { username, ...otherParams } = args
      const url = `/users/${username}/repos`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'billing/get-github-actions-billing-user',
  `Get GitHub Actions billing for a user`,
  {
    username: z.string(),
  },
  async (args, extra) => {
    try {
      const { username, ...otherParams } = args
      const url = `/users/${username}/settings/billing/actions`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'billing/get-github-packages-billing-user',
  `Get GitHub Packages billing for a user`,
  {
    username: z.string(),
  },
  async (args, extra) => {
    try {
      const { username, ...otherParams } = args
      const url = `/users/${username}/settings/billing/packages`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'billing/get-shared-storage-billing-user',
  `Get shared storage billing for a user`,
  {
    username: z.string(),
  },
  async (args, extra) => {
    try {
      const { username, ...otherParams } = args
      const url = `/users/${username}/settings/billing/shared-storage`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'billing/get-github-billing-usage-report-user',
  `Get billing usage report for a user`,
  {
    username: z.string(),
  },
  async (args, extra) => {
    try {
      const { username, ...otherParams } = args
      const url = `/users/${username}/settings/billing/usage`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'users/list-social-accounts-for-user',
  `List social accounts for a user`,
  {
    username: z.string(),
  },
  async (args, extra) => {
    try {
      const { username, ...otherParams } = args
      const url = `/users/${username}/social_accounts`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'users/list-ssh-signing-keys-for-user',
  `List SSH signing keys for a user`,
  {
    username: z.string(),
  },
  async (args, extra) => {
    try {
      const { username, ...otherParams } = args
      const url = `/users/${username}/ssh_signing_keys`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'activity/list-repos-starred-by-user',
  `List repositories starred by a user`,
  {
    username: z.string(),
  },
  async (args, extra) => {
    try {
      const { username, ...otherParams } = args
      const url = `/users/${username}/starred`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool(
  'activity/list-repos-watched-by-user',
  `List repositories watched by a user`,
  {
    username: z.string(),
  },
  async (args, extra) => {
    try {
      const { username, ...otherParams } = args
      const url = `/users/${username}/subscriptions`

      // Map camelCase to original parameter names for API request
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      const mappedParams: any = { ...otherParams }

      // Extract authorization token from HTTP request headers
      const authorization = extra?.requestInfo?.headers?.authorization as string
      const bearer = authorization?.replace('Bearer ', '')

      const response = await apiClient.request({
        headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
        method: 'GET',
        url: url,
        params: mappedParams,
      })

      return handleResult(response.data)
    } catch (error) {
      return handleError(error)
    }
  }
)

mcpServer.tool('meta/get-all-versions', `Get all API versions`, {}, async (args, extra) => {
  try {
    const otherParams = args

    // Map camelCase to original parameter names for API request
    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    const mappedParams: any = { ...otherParams }

    // Extract authorization token from HTTP request headers
    const authorization = extra?.requestInfo?.headers?.authorization as string
    const bearer = authorization?.replace('Bearer ', '')

    const response = await apiClient.request({
      headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
      method: 'GET',
      url: '/versions',
      params: mappedParams,
    })

    return handleResult(response.data)
  } catch (error) {
    return handleError(error)
  }
})

mcpServer.tool('meta/get-zen', `Get the Zen of GitHub`, {}, async (args, extra) => {
  try {
    const otherParams = args

    // Map camelCase to original parameter names for API request
    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    const mappedParams: any = { ...otherParams }

    // Extract authorization token from HTTP request headers
    const authorization = extra?.requestInfo?.headers?.authorization as string
    const bearer = authorization?.replace('Bearer ', '')

    const response = await apiClient.request({
      headers: bearer ? { Authorization: `Bearer ${bearer}` } : undefined,
      method: 'GET',
      url: '/zen',
      params: mappedParams,
    })

    return handleResult(response.data)
  } catch (error) {
    return handleError(error)
  }
})
