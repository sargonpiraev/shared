# pulumi/.env is a symlink to the workspace vault (`npm run link:envs` from meta).
# Key names only — no secrets. CI gates this file, not gitignored `.env`.

PULUMI_BACKEND_URL=file://$PWD
PULUMI_CONFIG_PASSPHRASE=
PULUMI_LOG_LEVEL=
